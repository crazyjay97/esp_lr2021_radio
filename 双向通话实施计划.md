# 双向音频通话实施计划与 V1 实现状态

## 0. 2026-08-18 V1 实现状态

本计划的 V1 代码已完成，等待用户编译、烧录和双板验证：

- CONFIG 的 AUDIO 区域已增加 `Intercom` 开关；开启成功后锁定 Capture、定时、PIR、声音触发、Voice Alarm 和 Low Power，关闭后恢复。
- 开启使用两阶段握手：`Config(APP_CFG_KEY_INTERCOM, session)` / ACK 只表示节点准备完成；网关继续发送带 START 的主时隙，收到节点同 session 首个 reply 后才向 UI 返回成功。
- 关闭使用 TDD 内的 STOP / STOP_ACK；节点对重复 STOP 幂等回复。节点 1.2 秒收不到网关主时隙会自行退出通话。
- 无线采用网关 master、节点 slave 的 20 ms TDD 周期，节点在收到主时隙后等待 2 ms 回复；每个方向每时隙最多聚合 2 个 10 ms Opus 帧。
- 网关改为全双工 I2S 初始化并创建麦克风编码任务，两端均可持续采集、编码、接收、解码和播放。
- 通话占用无线期间屏蔽图像请求、图像发送、自动下一帧、PIR/声音触发、普通 CONFIG、VBAT 和低功耗 CAD；网关开启前 abort 当前图像 RX 并清空待显示帧。
- 两端加入轻量 NLMS 回声消除：far-end 参考取实际送入 I2S 播放的解码 PCM，near-end 在 Opus 编码前处理；双讲期间冻结自适应。参考延迟初值为 40 ms，必须按板上实际声学延迟调整。
- 本地录音诊断在通话期间被双方统一禁用，保证只有 `RadioPing` 的音频发送任务读取 I2S RX。

当前验证边界：已完成 `git diff --check`、UTF-8/BOM/换行、接口引用和状态机静态核对；未构建、未烧录、未板测。视频参数保持 JPEG Q30、LCD 20 MHz、正占空比 64/256，未调整。

首次板测优先保留以下日志：`intercom local start/stop`、`intercom slots tx/rx/missed/queued`、`intercom start handshake timeout`、`intercom stop ACK timeout`、`intercom master sync lost`、Opus 编解码错误和音频读写错误。AEC 需要分别测试单讲、双讲、近距离高音量，并根据残余回声调整 `APP_AEC_REFERENCE_DELAY_MS`。

## 1. 目标与边界

在现有网关与节点之间增加一个可由 CONFIG 页面控制的“双向音频通话”模式：

- CONFIG 页面增加一个双向通话开关。
- 开关请求必须经过网关、节点双方握手；只有握手完成后 UI 才显示为“已开启”。
- 通话模式开启后，暂时停止视频请求、视频接收和节点侧图像触发，只保留低延迟双向语音。
- 无线仍使用 LR2021 单收发器半双工链路，采用时分复用（TDD）轮转收发。
- 节点和网关都要处理本机扬声器到本机麦克风的声学回声，不能只依赖“降低音量”或简单静音。
- 关闭通话后，双方恢复视频和原有 CONFIG/低功耗逻辑。

以下章节保留初版实施路线；V1 已按第 0 节落地业务代码，且没有调整已经平衡的视频参数。

## 2. 当前基线

### 2.1 当前工程（`AM36_Electronic_doorbell_special`）

- CONFIG 页面已有 `PIR Motion`、`Voice Alarm`、`Low Power` 等开关。
- 开关的发送流程是：LVGL 回调 -> `app_main.cpp` 回调 -> `RadioPing::send_config()` -> 节点执行配置 -> `ConfigAck` -> 网关决定是否提交 UI 状态。
- 视频连续流由 `ui_gw_stream_active()`、`on_gw_rx_abort()`、`trigger_image_capture()` 等逻辑控制。
- 当前工程仍保留旧 PTT/音频处理代码，但没有可直接用于本需求的“CONFIG 开启后双向通话会话”。
- 视频当前参数和节拍已经平衡，计划冻结以下链路，不在本功能中顺手改动：
  - JPEG Q30。
  - LCD 像素 SPI 20 MHz、正占空比 64/256。
  - 最近记录中的无线约 16.9 fps、LCD 约 15.4 fps，已接近当前硬件能力匹配点。

### 2.2 参考分支（只读参考，不下载、不切分支）

仓库已有 `AM36_Electronic_doorbell` 分支，可直接通过 Git 查看。该分支已经验证过以下实现方向：

- 16 kHz、单声道、16 bit PCM。
- 10 ms PCM 帧，经 Opus 编码后进入发送队列。
- 网关作为 TDD 主时钟，固定周期发送主时隙。
- 节点收到主时隙后经过保护间隔回复，避免双方自由运行定时器导致碰撞。
- 语音包带方向标志；实时语音不做重传，丢失时依靠抖动缓冲、序号和 PLC/重同步处理。
- 通话期间屏蔽图像、配置、电压广播和低功耗 CAD 对无线时隙的抢占。
- 参考分支的 `AudioProcessor` 主要是降噪、门限、AGC 和限幅，不是完整 AEC；其 `clip/rms` 啸叫检测只能作为兜底，不能当作回声消除方案。

参考代码位置：

- `main/app_config.h`：音频、Opus、TDD 周期和队列参数。
- `main/radio_ping.cpp/.hpp`：`start_live_voice()`、`service_live_voice()`、`send_live_voice_slot()`、语音队列和播放任务。
- `工程日志.md`：双向通话 V1、时隙丢包、输入增益、噪声门和 PA 状态问题的板测记录。

## 3. 总体状态机

```text
通话关闭
  -> 网关发送开启请求
  -> 节点准备音频/停止视频
  -> 节点返回 READY/ACK
  -> 网关发送 TDD START 主时隙
  -> 节点收到 START 并回复
  -> 双方进入通话

通话中
  -> 网关主时隙 TX
  -> 节点保护间隔后 TX
  -> 双方 RX/TX 轮转，持续发送 10 ms 音频帧

通话中关闭
  -> 网关发送 STOP 控制
  -> 节点停止 TDD、关闭 PA、清空队列
  -> 节点返回 STOP ACK
  -> 双方恢复视频/低功耗/普通配置
```

任何阶段超时都必须回到“通话关闭”或“待恢复”状态，不能让 UI 显示已开启但无线仍停留在半初始化状态。

## 4. CONFIG 开关与握手设计

### 4.1 UI 层

1. 在 `AUDIO` 或单独的 `COMMUNICATION` 区域增加 `Intercom`/`双向通话` 开关。
2. 增加 `ui_gw_set_intercom_cb()` 回调，与现有 `ui_gw_set_low_power_cb()` 等回调保持同一模式。
3. 开关事件发生时先进入“请求中”状态，暂时禁止重复点击；只有最终握手成功才提交 NVS 和 UI 的开启状态。
4. 握手失败、超时或节点拒绝时，开关回滚到原状态，并显示失败原因或保留可诊断日志。
5. 通话期间禁止点击 `CAPTURE`；离开通话后再恢复。

### 4.2 协议层

建议复用现有 Config/ConfigAck 的可靠传输框架，但增加明确的通话阶段标志，不把普通配置 ACK 误当成通话已经建立：

- 新增 `APP_CFG_KEY_INTERCOM`，值 `1` 表示请求开启，值 `0` 表示请求关闭。
- 开启请求到达节点后，节点先停止图像触发/图传、准备音频任务和无线状态；准备完成后才发送 ACK。
- 网关收到开启 ACK 后发送带 `START` 标志的 TDD 主时隙；节点收到并成功回复后，双方才把 `intercom_active` 置为真。
- 关闭通话不能依赖普通 `send_config()` 在 TDD 活跃时强行抢占射频；应增加 TDD 控制包或 STOP 标志，在当前时隙边界安全退出。
- STOP ACK 收到前，UI 保持“关闭请求中”；超时则执行双方本地清理并回到关闭态，同时记录“远端 ACK 丢失”。
- 每次会话使用递增的 session/generation，避免旧 ACK、重复包或重启后的残留包重新打开通话。

### 4.3 状态提交原则

- 网关 NVS 保存的是“最后一次确认成功的状态”，不是用户刚点击的意图。
- 节点收到开启请求但资源初始化失败时，必须返回失败 ACK 或不返回 ACK，不能静默进入半通话态。
- 任何一侧重启、TDD 同步超时、连续 TX 超时达到阈值，都清除本地 active 状态并回到普通无线模式。

## 5. TDD 音频链路

### 5.1 第一阶段参数（沿用参考分支，待板测微调）

- 采样率：16 kHz。
- 声道：单声道。
- 采样格式：16 bit PCM。
- 音频帧：10 ms，每帧 160 个采样点。
- 编码：Opus 低延迟模式，固定码率优先，先保持参考分支的低复杂度配置。
- 无线包：按当前 FLRC 负载上限聚合少量 Opus 帧，初版优先保证时隙稳定和低延迟，不追求最大聚合数。
- 主从关系：网关 master，节点 slave。
- 时隙流程：网关 TX -> 网关切 RX -> 节点保护间隔 -> 节点 TX -> 节点切 RX。
- 丢包策略：不重传实时语音；按序号进行丢帧统计、PLC 或跳过本机主动丢弃的旧帧。

### 5.2 任务和数据所有权

- 每个设备只能有一个任务负责 I2S 麦克风读取，避免多个任务抢读造成碎片化。
- 编码前的 PCM 由发送任务独占；编码结果通过有界队列交给无线任务。
- 无线接收任务只负责校验、解析和入播放队列；Opus 解码和 I2S 播放由播放任务负责。
- 队列必须有最大深度和“丢旧保新”策略，禁止因射频短暂阻塞形成无限延迟。
- PA 开关、I2S 写入、AEC 参考缓冲必须有清晰的单一所有者或互斥边界，避免启动提示音、实时播放和告警播放互相覆盖。

### 5.3 视频与普通功能隔离

通话 active 期间：

- 网关停止连续视频下一帧请求，并 abort 当前图像 RX。
- 节点不响应新的图像触发，不启动新的采集/编码/图传。
- 暂停图像发送任务、节点自动抓图、PIR/声音触发和电压广播对射频的占用。
- 低功耗 CAD 不得在 TDD 活跃期间接管射频；通话结束后再按原配置恢复。
- 普通配置请求要么排队到通话结束，要么明确返回“通话中不可配置”，不能直接插入音频时隙。

## 6. 回声消除方案

### 6.1 目标

本机扬声器输出的远端声音不能再次经本机麦克风采集并发回无线链路。节点、网关两端都要实现同一套处理链，不能只在网关做检测。

### 6.2 正确的参考信号路径

AEC 的 far-end reference 必须取“实际送入本机扬声器/I2S TX 的 PCM”，而不是取无线 Opus 包或理论帧时间：

```text
远端 Opus -> 解码 PCM -> AEC far-end reference / I2S 播放
本地麦克风 PCM -> AEC near-end -> 降噪/AGC -> Opus 编码 -> 无线
```

需要测量并补偿 codec、I2S DMA、PA、扬声器、麦克风和任务调度造成的总延迟；参考缓冲必须支持实际延迟范围，而不是假设零延迟。

### 6.3 实施顺序

1. 先确认当前 ESP-IDF/组件环境中可用的 AEC 实现及 API；优先选择能在 ESP32-S3、16 kHz、10 ms 帧上稳定运行的实现。
2. 建立统一的播放参考环形缓冲，保证 AEC 读取到与物理播放时间对齐的 PCM。
3. 将 AEC 插入麦克风唯一读取任务；先关闭旧 `AudioProcessor` 的实时噪声门，避免把低电平语音误判成静音。
4. 完成单端“播放固定测试音 -> 麦克风采集 -> AEC 输出”的延迟、残余能量和稳定性测试。
5. 再加入双讲测试：本地说话、远端说话、两端同时说话，确认 AEC 不会把近端语音一并抹掉。
6. AEC 之后再逐步恢复降噪、AGC、限幅；每次只启用一个处理模块并记录前后电平。
7. 保留参考分支的 clip/rms 啸叫检测作为最后一道保护，但把它标注为故障兜底，不作为 AEC 替代品。

### 6.4 收发门控

TDD 的时隙门控用于降低声学回声源，但不能替代 AEC：

- 本机不需要播放时关闭 PA，避免把残留音频送入声学通道。
- 切换到本机 TX 时清理上一段播放尾音，并在必要时短暂降低/静音麦克风发送，避免 PA 尾响进入当前 TX 帧。
- AEC 在 PA 开启时仍保持运行，覆盖近距离、音量较高、结构件耦合等情况。
- 不能把“无线半双工所以没有 AEC”作为验收结论；本机播放和本机采集仍可能同时发生。

## 7. 分阶段实施与门禁

### 阶段 0：协议和资源盘点

- 输出：确认包格式、状态变量、任务所有权、可复用的 TDD 函数和需要拆分的 PTT 遗留代码。
- 门禁：不改视频链路；完成静态调用链和内存/任务栈风险清单。

### 阶段 1：CONFIG 开关与双方握手

- 实现 UI 回调、NVS 状态、`INTERCOM` 配置键、READY/START/STOP 状态机。
- 先用空音频时隙验证开启、关闭、超时、重启恢复和 UI 回滚。
- 门禁：双方不会出现单边 active；握手失败后视频和普通配置仍可恢复。

### 阶段 2：移植参考分支 TDD 音频

- 迁移 10 ms PCM、Opus 队列、网关主时隙、节点回复时隙、序号和播放超时。
- 删除或隔离旧 PTT 对实时通话的控制路径，保留仍被图像音频使用的公共组件。
- 门禁：双板连续通话，`tx/rx/missed` 计数稳定，队列不持续增长，无明显卡死和单边无声。

### 阶段 3：暂停视频并恢复通话后的功能隔离

- 通话开启时 abort 视频并屏蔽所有图像/低功耗射频入口；关闭后恢复原路径。
- 门禁：通话期间无视频包抢占；关闭后可以重新开始连续视频，视频参数和帧率基线不回退。

### 阶段 4：AEC 与收发门控

- 接入 far-end PCM 参考、延迟校准、AEC、残余回声抑制和故障兜底。
- 门禁：固定音播放、单讲、双讲、近距离高音量、PA 开关切换均无持续啸叫；近端语音仍可辨识。

### 阶段 5：综合板测与参数冻结

- 测试开启/关闭 100 次、通话连续运行、丢包/重启/低电量、视频恢复和不同扬声器音量。
- 固化 TDD 周期、保护间隔、Opus 码率、AEC 延迟、队列深度和超时参数。
- 仅在用户提供编译、烧录和双板日志后判定硬件验证通过；静态检查不等同于板测通过。

## 8. 验收指标（初版）

- 握手：开启和关闭均有明确最终状态；任一 ACK 丢失不会永久卡在半开启状态。
- 音频：双方都能持续听到对方声音，不能只验证单向；无持续 `TX timeout`、队列无限增长或播放永久静音。
- 时隙：网关主时隙和节点回复时隙不发生稳定性碰撞，丢时隙可恢复同步。
- 回声：近距离、高音量、持续播放测试下无持续啸叫；关闭 AEC 与开启 AEC 的残余回声能量要有可量化对比。
- 视频：通话 active 时不传视频；通话关闭后连续视频可恢复，Q30、20 MHz、64/256 等当前基线保持不变。
- 可靠性：重复开关、单边重启、射频短时丢包后能回到可用状态，不需要手动重启另一端。

## 9. 当前风险与待确认项

1. 参考分支的 TDD 代码是可移植基础，不代表当前 `special` 分支可以无冲突直接合并；当前分支的 FLRC 负载、图传状态机和旧 PTT 残留均有差异。
2. 现有 `Config` 包载荷较小，session/generation 和 START/STOP 语义需要决定是扩展现有包还是增加独立控制包。
3. AEC 组件、内部 RAM/PSRAM 占用、实时 CPU 预算和实际声学延迟尚未在当前工程测量。
4. ES8311/I2S 全双工、PA 控制和播放参考取样点必须通过板级日志确认，不能只依据静态代码判断。
5. 旧 `AudioProcessor` 的噪声门曾把当前板上低电平麦克风压成数字静音；实时语音恢复 DSP 前必须重新标定输入电平。
6. 本计划没有决定最终 AEC 库、最终 Opus 码率、最终 TDD 周期和最终 UI 文案，这些应在阶段 0/2/4 的实测数据基础上定版。

## 10. 当前结论

- 方向可行：参考分支已经证明 LR2021 半双工 TDD 双向音频、Opus 队列和 PA/播放链路可以工作。
- 不能直接照搬：参考分支没有完成本需求要求的“CONFIG 双方握手 + 通话期间视频完全停用 + 真正 AEC”。
- 推荐顺序：先握手和空时隙，再音频 TDD，再视频隔离，最后接入 AEC 并做双讲/高音量板测。
- 初版计划阶段仅完成代码与文档静态分析；当前 V1 已按第 0 节实现业务代码，仍未构建、未烧录、未进行硬件验证。
