# 工程日志

> 本日志仅供自己开发记录使用（中文）。工程对外仍是英文开源项目，代码/注释/提交信息保持英文。
> 之前的历史未回溯，从本次优化开始记录。

---

## 2026-08-27 图传融合 · 第3步-E：真 JPEG 前置——相机与 I2S 共存探针（不发不显示，只测）

### 背景 / 5000B 板测
- 5000B(11 片/帧) 板测：`intercom image RX: frames=30 frags=354 14.6 fps`，达标（外推 11–13，实测更高）。
- 用户决策：接下来上真 JPEG，**暂不做重传**（NACK/选择性重传占共享下行、时效差、复杂度高）。
- 点破的关键：现有"循环重发"**不是重传**，是**盲发冗余**，正是它在免费修 ~9% 丢包、让网关集齐完整帧。真 JPEG 不抗丢，**必须保留循环重发**，不能退到"每片只发一次"（否则 ~64% 帧残花屏）。
- 刷新档位（用户选）：**低负载慢刷新**——相机每 ~0.5–1s 抓一张，循环引擎不变，网关解码显示。

### 读码发现的硬约束（动手前必须先验的）
- `on_image_capture_request`：通话中**直接拒抓**。
- camera 模式 `camera_capture_task`：抓帧前 `g_radio.suspend()`+`bsp_audio_suspend()`，**整挂射频+音频**，与活着的通话冲突；每秒 suspend 会把音频撕洞。
- 远程 `image_capture_task` 走 `send_image`，**独占射频**，与 TDD 不兼容。
- 相机是 **SP0A39 DVP**（`esp_cam_ctlr`+DMA，非真 UART），JPEG 是 **core1 软件编码**（几十 ms/帧）。→ **唯一硬未知：DVP 抓帧 + core1 编码能否与活着的 I2S/AEC/Opus 共存**。
- 网关显示消费者在 `intercom_active()` 时也**主动丢帧不显示**。

### 本步只做探针（用户选"先探针验证共存"）
- `app_config.h`：新增 `APP_INTERCOM_IMAGE_CAPTURE_PROBE_MS`(=1000)。>0 时启用；=0 关闭。
- `app_main.cpp`：新增 `intercom_capture_probe_task`（node/camera 模式启动）。通话中每 N ms 用 `prepare_jpeg_blob()`（capture+encode，**不 suspend、不 LCD、不 send**）抓一次，`g_capture_busy` CAS 防与自动抓帧竞争，跑在 `APP_IMAGE_TASK_PRIORITY=3`（低于 voice_tx4/voice_play5，AEC 必抢占），打 `[IMG PROBE] capture+encode=..ms jpeg=..bytes ok=..` 后**丢弃**。合成帧突发照跑，保持射频负载真实。
- 判读：`[IMG PROBE]` 耗时/成败 对照节点 `intercom mic/playback`（missed、aec_max、frames）看有无音频卡顿。
- 探针过关（能抓、编码尖峰不撕音频）→ 下步才接：拓帧-only 路径喂循环引擎 + 网关重组解码显示 + 放开两道 intercom 闸。
- 代码已改；**本地构建未验证**（bash 不可用）。

### 板测判读（用户已烧录，探针每 1s 抓一张）— **过关**
- 节点探针：`[IMG PROBE] capture+encode=198–285ms jpeg≈4780B ok=1 fails=0`，连续 8+ 次全成（抓 ~103–155ms + 编码 ~94–150ms）。
- 节点音频**同期健康**：`intercom mic ... missed=0`，`aec_max=11652us`（比不带探针的 18287us 还稳）、`clip_total=0`，playback frames 100/500/1000 正常推进；intercom slots `missed=0`，`frames_tx` 32/64/95/127/159 稳步推进。
- 网关侧：语音 `voice_rx/masters ≈98%`（`missed=0`、`drops` 恒 5），合成帧突发照收 `intercom image RX ... 14.3–15.5 fps`，探针期间无任何异常闸位（crc=hdr=unknown=rearm=0）。
- **结论（硬件板测已验证）**：DVP 抓帧 + core1 软件编码（~250ms 尖峰、prio3）**能与活着的 I2S/AEC/Opus + TDD 突发共存**，不撕音频、不掉语音槽、不打乱帧率。唯一硬未知已排除。
- 副产物：单帧 capture+encode ≈250ms → 抓帧速率上限 ~4fps，正落在"低负载慢刷新（0.5–1s/帧）"档内，不与射频侧 14.6fps 冲突。

---

## 2026-08-27 图传融合 · 第3步-D：真 JPEG 挂载（通话中带内发帧、网关只重组计帧率）

### 用户决策（本步照此实现，不再问）
- 帧来源：**先用固定内存帧**（合成一段代表大小的 buffer 反复重发），零摄像头风险、不碰刚接受的音频、只改 radio 一个文件；测到干净的射频帧率上限后再考虑上真拍。
- 重发架构：通话开始抓一帧、**反复重发**（跨 slot 滚动游标，末端回绕）。
- 网关：**只重组 + 计完整帧/秒**，不解码、不上屏。

### 改法（代码已改，均英文进仓库）
- `app_config.h`：新增 `APP_INTERCOM_IMAGE_ENABLE`(=1) 与 `APP_INTERCOM_IMAGE_FRAME_BYTES`(=10000)。=0 回落到第3步-A 的 dummy 探针；帧字节数决定分片数 `ceil(bytes/APP_IMAGE_FRAGMENT_DATA_SIZE)`，应改成真实 JPEG 大小。
- `image_transfer`：加 `rx_restart()`——同 session/size 只清收包位图、不 free/alloc。网关 ~10fps 反复重组同一帧，避免每帧一次 `rx_begin` 抖堆。
- 节点 `prepare_intercom_image()`：通话开始在 PSRAM（失败回落内部 RAM）建固定帧、填确定性字节（`i*31+7`，使每片 CRC16 有意义、能暴露错位）、算分片数、游标清零。在 `start_intercom_local` 里对 `!is_gateway_` 调用。
- 节点 `send_intercom_image_burst()`：复刻 `send_intercom_probe_burst` 的 slot 尾纪律（deadline、降到 `APP_INTERCOM_IMAGE_BURST_PRIORITY` 让 AEC 抢占、逐片 `wait_for_tx_done`），但发真 `build_image_fragment`(ImageData) 且用**跨 slot 滚动游标**，末端回绕并 `frames_tx++`。定长 padding 到 `APP_FLRC_BURST_PAYLOAD_LEN`，与既有突发一致。
- `send_intercom_slot`：node voice reply 后，`APP_INTERCOM_IMAGE_ENABLE && 帧已就绪` 走真图突发，否则回落 dummy 探针；探针日志加 `frames_tx=`。
- 网关 `handle_intercom_image_data()`：从包内 session+total **自开** `rx_begin`（不复用 `handle_image_start`——它会发 ready-ACK+`schedule_rx()` 打乱 20ms TDD），校 CRC16，`rx_complete()` 满一遍 → `frames_rx++`、`rx_restart()`、每 ≥2s 打一次 `x.x fps`。
- `dispatch_rx_packet`：在「通话中丢弃一切非 voice 包」那道闸**之前**，加网关 `kPacketTypeImageData → handle_intercom_image_data` 分支（否则真图包会被那道闸直接扔掉）。
- `stop_intercom_local`：释放节点固定帧、复位网关带内开的重组（`!image_rx_pending_` 时），让通话后普通图传从干净状态开始。

### 板测判读（用户已烧录，10000B / 21 片/帧）
- 网关帧率：4.8/5.1/5.2/5.9/5.3/6.8/5.6/6.8 fps，**均值 ≈ 5.7 fps**。
- 发送端：`req=4/slot` 但 `dl_stops` ~45% 槽（撞 guard 少发）→ 实发 **3.55 片/槽 × 50 = ~177 片/s**，`frames_tx` ≈ 8.25 整帧/s 上空口。
- 接收端：每 ~2s 收 284–312 片 → **~145 片/s**；voice_rx/masters ~91%（空口丢 ~9%）。crc=hdr=unknown=rearm=0，连续 RX 干净。
- 5.7 < 8.25：差在**丢片 + 顺序循环集齐开销**，每完成一帧约耗 1.45 轮空口。
- 语音共存健康：missed=0、drops 恒 4、mic/playback 跟上，图传没吃语音槽。
- 结论：链路接收吞吐 ~145 片/s 基本恒定，帧率 ≈ 145 ÷ (片/帧)。
- **下一步**：`APP_INTERCOM_IMAGE_FRAME_BYTES` 10000→**5000**（=11 片/帧），外推 **≈ 11–13 fps**，待烧录读真实值。
- 代码已改；**本地构建未验证**（bash 不可用）。

---

## 2026-08-27 图传融合 · 第3步-C：给 core1 挤余量（Opus 复杂度 3→1、AEC filter 4→2），上 JPEG 前的准备

### 起因
- 网关连续 RX 改动板测成功（见第3步-B），但节点出现 `task_wdt` 警告：IDLE1(CPU1) 被 `voice_tx`(AEC+Opus) 饿死 5s。
- 复位真因已排除固件：`rst:0x15 (USB_UART_CHIP_RESET)` 是 USB/串口线外部复位；`# CONFIG_ESP_TASK_WDT_PANIC is not set`，task_wdt 只打警告不重启，日志报警后一路继续跑。语音帧率实测 100.6 fps（500 帧/4971ms）平均没落后 → core1 是「零余量刚好跟上」，大声段吃满饿死 IDLE1。
- 三次 WDT backtrace 全落在 CELT 编码（exp_rotation/alg_quant/compute_allocation/mdct）→ Opus 也吃了可观 CPU。core1 每 10ms 账：AEC ~4.7ms(峰 20ms) + Opus ~3ms + voice_play ~1-2ms ≈ 10ms，卡满。

### 改动（app_config.h，两个常量，英文进仓库）
- `APP_OPUS_COMPLEXITY` 3→**1**：砍 backtrace 命中的 PVQ 搜索精细度；16kHz/32kbps 语音下与 3 几乎无差。回升到 3 若发糙，0 是 CPU 下限。
- `APP_AFE_AEC_FILTER_LENGTH` 4→**2**：linear filter 每帧 MAC 减半，单个最大省 CPU 杠杆。代价=回声拖尾覆盖 ~128ms→~64ms。门铃外壳小、拖尾短，2 多半够；若起始啸叫/稳态回声回来，回 3-4 或把 `APP_AFE_AEC_NLP_LEVEL_STEADY` 抬到 1。

### 听音验收要点（用户听，我不擅自判定）
- 起始几秒是否啸叫（filter 太短→拖尾没覆盖）。
- 稳态是否有回声漏出（同上）。
- 语音是否发糙/金属味（Opus 复杂度太低）。
- 期望：`aec_max` 与 Opus 段都下降，IDLE1 不再被饿 5s，task_wdt 警告消失。
- 验收通过后即进 Stage-3 真 JPEG（把 dummy 探针换成 build_image_fragment，走 handle_image_data 重组）。
- 代码已改；**本地构建未验证**（bash 不可用）、**硬件板测未验证**。

---

## 2026-08-27 图传融合 · 第3步-B：网关 intercom 改连续 RX、RX_DONE 里只处理不每包重挂

### 真因锁死（unknown 计数板测，用户已烧录）
masters=401 窗口：voice_rx=393、probe_rx=409、**unknown=1118**、rearm=1919、crc=hdr=0。三条决定性观察：
- `voice_rx+probe_rx+unknown = 1920 ≈ rearm 1919` → 每次 RX_DONE 只排空一个包，FIFO 没堆叠。
- `probe_rx+unknown = 1527`，节点每槽发 ~2005，**真正空口丢的只有 ~85**；4 片里 ~3 片其实都触发了 RX_DONE，只是被读**串位**、magic 对不上，当 unknown 扔了。
- crc=hdr=0 → 不是空口坏、不是 FIFO 溢出。纯粹「收到后我方解析错位」。
- 根因：网关每收一包就 `schedule_rx()` 重挂（set_standby+set_rx），与连续 RX 状态机打架，把同一上行突发里后续包的 FLRC 帧同步抖乱 → 读指针落在包中间 → 后续全错位。rearm≈每包一次就是这个抖动的证据。

### 改法（用户方案：一开始就连续 RX，RX_DONE 里直接处理，别每包重挂排空）
- `handle_irq` RX_DONE 分支：把「保持连续 RX、不重挂」的条件从 `image_rx_pending_` 扩成 `image_rx_pending_ || (intercom_active_ && is_gateway_)`（`keep_continuous_rx`）。网关 intercom 与单向图传走同一套。
- 每槽唯一一次重挂仍在 `send_intercom_slot` 末尾（TX 后 set_standby→schedule_rx 恢复连续 RX），不是每包那种。
- 只改网关：节点每槽只收 1 个 master（不存在每包抖动），保持原样，缩小改动面。
- 保留 diag 里 `rearm=` 字段做回归校验：改后应恒为 **0**（intercom_rearm_after_rx_ 已无处自增）；下版日志确认为 0 后可删该计数。
- 代码已改；**本地构建未验证**（bash 不可用）、**硬件板测未验证**。

### 预期
probe_rx 从 ~13% 拉到接近「节点每槽发片数 − 极少空口丢」；unknown 掉到接近 0；rearm=0；voice_rx 不受影响仍 ~98%。若 probe_rx 仍偏低但 unknown≈0，才回头查空口/deadline。

---

## 2026-08-27 图传融合 · 第3步-A 板测判读：真因不是重挂/空口，指向排空读串位

### 板测数据（网关 diag，masters=401 窗口；双方已烧录实测）
| 项 | 数值 | 结论 |
|---|---|---|
| 网关 voice_rx/masters = 391/401 | 97.5% | 语音回复基线满收，稳 |
| 网关 probe_rx / 节点 sent = 209/1594 | 13.1% | 坐实只收 ~15% 捎带片 |
| 网关 crc=0 hdr=0（全程） | 零 | **彻底排除 FIFO 溢出/读半包**（那必出 CRC/HDR 错） |
| 网关 rearm = 1942/401 | 4.84 次/slot | 每槽 5 包几乎每个都触发 RX_DONE+重挂 |

### 假设翻转：`rearm≈4.84≈5` 否定了「重挂把分片冲掉」
- 原猜：重挂在 standby→rx 死区冲掉分片前导码。但若被冲掉，网关就收不到那个 RX_DONE、rearm 会明显 <5。实测 rearm≈节点每槽总包数 → **5 个包几乎每个都被射频收全**（RX_DONE 触发、零 CRC/HDR），却只 13% 认成合法探针。
- 新指向：`handle_rx_packet` 按 FIFO level 排空，一旦某次 `take` 与真实分片边界差几字节 → 后续全部错位 → `magic` 对不上 → 走 `rx_unknown_packets_++` 丢弃。这条路径**同样不产生 CRC/HDR 错**，与实测吻合。

### 补最后一个决定性计数（行为不变）
把 `rx_unknown_packets_`（收到但我方解析丢弃数）加进网关 diag 行：`... crc= hdr= unknown=`，并在 `start_intercom_local` 归零。判读：
- **unknown 高（≈丢片数）** → 排空读串位，改 drain 循环边界/分片带长度对齐，**不动重挂**。
- **unknown 也低** → 才回到重挂/死区方向。
- 代码已改；本地构建未验证（bash 不可用）、硬件板测未验证。烧录看这一个数即可定死。

---

## 2026-08-27 图传融合 · 第3步-A：先诊断网关丢片真因（加分因计数，不盲改）

### 背景
挪核后语音已稳、尾部捎带救活，但网关只收到约 15% 捎带分片（节点发 ~198/s，网关 ~29/s）。这不是回归、不影响语音，是 Stage-3 要解决的融合吞吐问题。用户选 A：一次烧录把真因锁死，别盲改。

### 读码先挖出一个强结构性嫌疑：网关每收一包就重挂 RX，冲掉紧跟语音回复的分片
- `handle_irq` 的 RX_DONE 分支（`radio_ping.cpp` ~989）：只有「专用单向图传流程」`image_rx_pending_==true` 才免重挂、连续 RX；**intercom 探针从不置 `image_rx_pending_`**（探针是 `kPacketTypeIntercomProbe`，不走 `handle_image_start`），于是网关每收完一包（先收到语音回复）就 `schedule_rx()` 重挂一次。
- 代码自己在 979-981 就写明这个坑：*"Re-arming after each fragment would clear bytes of the next fragment already entering the 1024-byte FIFO."* 当初为单向图传加的 `image_stream` 免重挂保护**没覆盖 intercom 捎带通道**。
- 时序上：节点在语音回复后背靠背甩 4 片，网关一收到语音回复的 RX_DONE 就重挂 → 把正进 FIFO 的后续分片冲掉。能解释「语音回复稳收、紧随分片大量丢」。
- 但诚实讲：光算时序，重挂未必能压到 15% 这么低；并存嫌疑还有 **FIFO 溢出**（510B×4 > 1024B）和**排空读半包**（`take=level` 读了没收全的片 → CRC 失败）。到底哪个主导，靠一次实测区分。

### 实现（诊断计数，行为不变；英文进仓库）
网关侧按「原因」分类计数，挂在 20ms 主控心跳上打印（探针全丢也能出数）：
- 新增 `rx_hdr_errors_`（`handle_irq` HDR 错分支，之前只 log 无计数）；`rx_crc_errors_` 已有。
- 新增 `intercom_voice_rx_`（网关收到节点语音回复数，~100% 基线）、`intercom_masters_tx_`（发出的主控数，per-slot 分母）、`intercom_rearm_after_rx_`（**一个会话内网关 RX_DONE 后重挂 RX 的次数**——重挂冲掉假说的直接指纹）。
- `service_intercom` 主控发送处每 100 主控打印一行：`intercom gw diag masters= voice_rx= probe_rx= rearm= crc= hdr=`。
- 上述计数 + 既有 `intercom_probe_*` 全部在 `start_intercom_local` 归零，保证单会话比值自洽。

### 判读（一次板测即可定死真因）
- `voice_rx/masters` ≈ 1.0 且 `probe_rx/masters` ≈ 0.15 → 确认「语音稳、捎带丢」。
- **`rearm` 高 + `crc/hdr` 低** → 重挂冲掉（结构性），改法=把 intercom 捎带窗也纳入 `image_stream` 免重挂保护。
- **`crc/hdr` 高** → FIFO 溢出 / 读半包，改法=缩小分片（494B→~240B 使 ≤2 片进 FIFO）或改排空循环。
- 代码已改；**本地构建未验证**（bash 不可用）、**硬件板测未验证**。烧录看这行日志判读后再定改法。

---

## 2026-08-27 图传融合 · 第3步：AEC 从 core0 挪到 core1（解 core0 双硬实时死结）

### 决策（用户拍板：不要方案，直接挪核 + 抬 AEC 优先级高于 JPEG）
- 病根量化：core0 上「射频 TDD(20ms 硬 deadline)」与「AEC(10ms 节拍、最坏一帧 16ms、跑起不让路)」是**两个硬实时抢一颗核**——抬射频饿死 AEC(啸叫，V1)；抬 AEC 则 16ms 挡住射频重挂 RX、横跨主控 → 节点漏收近半主控、超帧被拖到 **35ms**、双向各丢 ~50%(V2)。调优先级无解，是单核时间不够。
- 用 `drops=` 诊断实锤 V2 双向丢帧同源：节点 `drops`(上行 tx 队列丢) 0→157→312→454 与 `total_lost`(下行漏收) 2→162→317→459 几乎相等同步增长；网关 `tx=101 rx=52`(只收到 51% 节点回复) 佐证漏收一半主控。关键量化：**「语音回复→下一个主控」空档 ≈16ms，恰等于一帧最坏 AEC，塞不下第二件事**。
- 解法：把 AEC 移出 core0。core0 只剩射频(radio_ping+img_tx) → 20ms TDD 稳。core1 变成「硬 AEC vs 弹性 JPEG」，让弹性的图像吃亏，符合「语音刚性、图传弹性」定位。

### 实现（改 `app_config.h` 两常量，英文进仓库）
- `APP_VOICE_TX_TASK_CORE` 0→**1**：voice_tx(=AEC+Opus 编码) 挪 core1。
- `APP_VOICE_TX_TASK_PRIORITY` 3→**4**：core1 优先级排死 voice_play(5) > **AEC(4)** > JPEG=img_cap(3) > camera(0)。AEC 必须高于 JPEG，否则一帧 90ms 的 JPEG 又饿死 AEC（core1 复刻 V1）。
- 两处 task 创建（网关 `radio_ping.cpp:298`、节点 `:471`）都读这两常量，自动生效；注释同步改为新理由。

### 预期与待验证
- 预期：纯语音在挪核后依旧干净、超帧回到 20ms；且 AEC 离开 core0 后，之前捎带式死于「16ms AEC 挡射频重挂 RX」的病根消失 → **简单尾部捎带可能被救活**，或省掉独立图传槽。挪完重测再定。
- 盯：①core1 优先级顺序 AEC>JPEG（否则新 V1）；②PSRAM 单八线总线争用(AEC 缓冲+JPEG+camera DMA)仍是唯一需实测项；③I2S 是 DMA，voice_tx 挪核后与 voice_play 同核，参考 FIFO cache 局部性反而更好。
- 现状 `APP_INTERCOM_IMAGE_PROBE` 仍=4（探针逻辑还在 core0 radio_ping 内）。可直接烧看挪核效果+顺带验证 core0 发分片不再被挡；若想先纯挪核可先设回 0。
- 代码已改；**本地构建未验证**（bash 不可用，未跑 idf.py build）、**硬件板测未验证**。

### 板测结论（挪核后，用户已烧录实测）— 成功，且顺带救活尾部捎带

日志确认 `tx_core=1 tx_prio=4 play_core=1 play_prio=5`。V2 → 挪核版对比：

| 指标 | V2(AEC@core0) | 挪核版(AEC@core1) |
|---|---|---|
| 超帧周期 | 35ms | **20.0~20.2ms**（恢复） |
| 网关收到节点回复 | 52/101=51% | 398/401=**99%** |
| 节点 total_lost | 556 | **3** |
| 节点 drops(队列丢) | 454 | **4~5** |
| missed | 0 | 0 |
| 图传分片 sent/slot | 0.72 | **3.96**（打满 req=4） |
| dl_stops 占比 | ~100% | **~5%**(19/401) |
| AEC max | 16ms（还挡射频） | 节点 20.6ms / 网关 25.5ms（**不再挡射频**） |

- **死结解开**：core0 只剩射频 → 20ms TDD 稳；语音双向 99% 到达、不啸叫、clip=0。网关 `aec_max=25.5ms` 照样不伤射频 = 挪核直接证据（AEC 再慢也不在射频那颗核）。
- **意外收获**：捎带式复活。AEC 离开 core0，之前「16ms AEC 挡 core0 重挂 RX」的病根消失 → 节点每槽稳发 3.96 片、dl_stops 100%→5%；节点侧图传空口 ≈198 片/s×510B ≈ **100KB/s**（V2 ~13KB/s，约 8×）。**不用另开独立图传槽。**
- **敞口（非回归，属 Stage-3）**：网关只接住约 **15%** 图传分片——节点发 ≈198 片/s，网关 `probe rx`（每 200 打一次）1→201 用 6860ms ≈29 片/s。原因：网关端「连收一串突发分片」的 RX 窗口 + 分片重组/流控尚未做（节点甩 4 片，网关目前只在收完语音回复顺手抓零星几片）。语音不受影响（回复照收 99%）。
- 结论：核心目标达成——**全双工语音扎实(20ms/99%/不啸叫) + 图传同通话共存、节点侧吞吐够用**；捎带复活省掉独立槽支线。下一步进 Stage-3：网关突发 RX 窗口，把 15% 拉上来。

---

## 2026-08-27 图传融合 · 第2步板测结论：让路解决啸叫，但捎带式吞吐不可用 + 语音 50% 丢帧（拟转独立图传槽）

### 现象（让路版 V2 板测）
- **主问题已解**：missed=0、无 `task_wdt`、参考 FIFO 不再 over/underflow、mic 不削顶（raw_peak 205–482、aec_peak 8–146）、近场不再啸叫。让路方向对。
- **新问题① 语音丢帧≈50%**：网关侧 `gap≈2` 刷屏，节点 total_lost 从 V1 的 9 涨到 556（约 12s）。
- **新问题② 图传吞吐极低**：`sent/bursts≈0.72`（平均每 20ms 槽只发出 0.7 片），`dl_stops` 几乎每槽触发，折算 ≈13KB/s，一张 JPEG 要数秒，不可用。

### 丢帧根因推断（V1↔V2 对比排除法，未板测实锤）
- **非空口**：V1 每槽发 4 片、丢 9；V2 每槽 0.7 片、丢 556。片更少反而丢更多 → 排除空口干扰。
- **非漏收主帧/RX 重装晚**：V1 突发 ~12ms 对 schedule_rx 推迟不比 V2 小，但 V1 missed/lost 都低 → 排除 RX 时序。
- **剩下就是降优先级本身**：V1 突发期 voice_tx(3) 被 radio_ping(4) 压住、**采集暂停**，tx_queue 只出不进不堆；V2 突发期 voice_tx 高于 radio_ping、**采集照跑**，但出队 `build_voice_packet` 每槽只在开头一次，突发那 ~10ms 队列**只进不出** → 瞬时堆到 4（`queued=4`）→ `enqueue_voice_frame` 静默丢最老帧（该丢弃分支无 ESP_LOGW，日志看不见）。gap 恰=2 = 丢掉一个满包的 2 帧签名。

### 本轮改动（仅诊断，英文进仓库）
- 节点槽日志行追加 `drops=%lu`（`tx_queue_drops_`）：烧录跑一次即可判定 drops 追 rx_lost=节点队列丢 / drops≈0=判断错是空口。代码已改，构建/板测未验证。

### 架构判断（交用户拍板）
- 单射频串行在 core0 radio_ping，AEC(voice_tx) 必须抢在图传前（否则 V1 啸叫）→ **图传空口天然被 AEC 挤掉**，藏在语音 guard 缝里更死。捎带式探针使命完成，结论：**毁语音(V1) 或 自饿+扰队列(V2)，不可兼得**。
- 真实取舍是「图传空口 vs 语音帧率」。建议 Stage-2 转 **网关授权的独立图传槽（交织超帧）**：按比例整槽授予图传（如每 4 语音槽插 1 图传槽），该槽不期待语音回复，语音降 75 帧/s 仍全双工；图传得整槽（扣 AEC 约 12ms 空口）吞吐远高；图传/语音时序彻底解耦，不返工。
- 待用户选：①先烧确认版实锤队列丢帧 → ②转独立图传槽；或直接转 ②。

---

## 2026-08-27 图传融合 · 第2步方案落地：突发「优先级让路 + 本时隙截止线」（重开探针待板测）

### 决策（用户拍板：别将就，选不返工的）
- 否：反转优先级（AEC 提到 radio_ping 上）→ AEC 18ms 尖峰会挤掉 TDD 主控回复，砸语音刚性，原则错。
- 否：拆核/AEC 搬 core1 作第一步 → core1 已有 voice_play(5)+JPEG(3)，争 CPU/PSRAM，工程量大、返工风险高。
- **选：突发放 radio_ping 内，但发突发那段临时把 radio_ping 自身 prio 4→2（低于 voice_tx=3），发完恢复；并给突发一个本时隙截止线。** 让「弹性图传」给「刚性+抖动敏感的 AEC」让路，用调度而非算带宽；单射频仍串行在一个任务，不引第二个碰射频的任务、无射频锁并发。

### 实现（英文进仓库）
- `app_config.h`：`APP_INTERCOM_IMAGE_PROBE` 重开=4；新增 `APP_INTERCOM_IMAGE_BURST_PRIORITY=2`、`APP_INTERCOM_IMAGE_BURST_GUARD_US=6000`（首版取一 AEC 帧~5ms+重挂 RX 余量，先证「不啸叫/低 missed」再调小）。
- `send_intercom_probe_burst()` 重写：①进入先 `uxTaskPriorityGet`存档 + `vTaskPrioritySet(nullptr,2)` 降优先级；②截止线 `deadline_us = (下一个主控预计到达) - BURST_GUARD_US`，其中下一个主控 = `intercom_reply_due_us_ - NODE_GUARD_US + SLOT_PERIOD*1000`；③**改单片一发**（不再 prefill-2 流水线）：每片前查截止线，过则 `intercom_probe_deadline_stops_++` 停发；`wait_for_tx_done` 是唯一阻塞点、锁在其外，voice_tx(AEC) 在这~2.9ms 空口里抢占跑；④发完恢复 `STDBY_XOSC`+idle，**恢复 prio 后**再 `schedule_rx`。
- 触发条件收紧：`ok && intercom_active_ && !is_gateway_ && NodeReply && !StopAck`（挂断 stop-ack 边沿 `intercom_reply_due_us_` 可能 stale，排除）。
- 新增计数 `intercom_probe_sent_`（实发片数）、`intercom_probe_deadline_stops_`（撞线让路次数）。node 日志改印 `bursts / sent / req / dl_stops / missed`。

### 为何不死锁/不返工
- `wait_for_tx_done` 只在读清 IRQ 时短暂持锁，阻塞前已释放；voice_tx 根本不碰射频（只 I2S 读+AEC+Opus 编入队）→ 无射频锁争用、无死锁。
- 第2步换真实 JPEG 片只是把 dummy 换成 `build_image_fragment` 的产物塞进同一「让路+截止线」通道，骨架不变。

### 待板测（用户）
- 重新构建烧录，起通话贴近讲话：①是否**不再啸叫**、无 `task_wdt`、无 `reference FIFO overflow/underflow`；②node 日志 `sent/bursts` 平均每 slot 实发几片、`dl_stops` 占比、`missed` 是否仍≈0；③音质是否与纯语音一致。据此再把 `BURST_GUARD_US` 往下调探 FPS 上限。
- 代码已改，本地构建未验证（bash 不可用），硬件板测未验证。

---

## 2026-08-27 图传融合 · 第1步探针板测结论：N=4 同步塞 radio_ping 会抢垮 AEC（已关探针恢复）

### 现象
近距离直接啸叫、`task_wdt` 连环触发、最后 `master sync lost` 掉线；拉远则不啸叫、音频正常。之前纯语音 OK。

### 根因链（不是空口时序，是 core0 CPU 抢占）
- `intercom probe tx bursts=.. n=4/slot missed=0` **全程 missed=0** → 20ms 时隙一个没丢，探针 4 片确实塞进尾部空口 → **带宽/时序完全够，方案 A 空口层面成立**。
- 但探针在 `radio_ping`(core0,prio4) 里**同步阻塞**发 4 片，每片一次 `ral_set_tx`+一次 `TX_DONE` 中断唤醒。`radio_ping`(prio4) > `voice_tx`(prio3)，**AEC 跑在 voice_tx 里** → 每 slot 4 次 TX_DONE 中断反复抢占正在算 AEC 的 voice_tx。
- AEC 被打断：`aec_max=18815us`（10ms 帧算了 18.8ms，含被抢占墙钟）→ 参考 FIFO `overflow dropped=128`/`underflow missing=128` **失步** → 回声压不住 → mic `raw_peak=32768 aec_peak=32639` 削顶饱和 = 啸叫。core0 被 radio_ping+voice_tx 塞满 → `IDLE0` 喂不上狗 → wdt。
- 近场声耦合强，AEC 不达标即正反馈自激；远场耦合弱，残余回声低于门限还能撑 → 「近啸叫远正常」吻合。

### 处置
- `APP_INTERCOM_IMAGE_PROBE=0` 关探针（该常量为 0 时 `send_intercom_slot` 探针分支整段跳过，等价于加探针前）→ 重新构建烧录即恢复正常通话。探针发送/计数代码保留备用。

### 结论
- 方案 A 浅环全双工**空口成立**；但「在 radio_ping 任务里同步阻塞发 N 片」这个**实现方式不成立**——中断抢占直接搞垮同核 AEC。图传瓶颈确实是 **core0 调度**（radio_ping prio4 抢 voice_tx/AEC prio3），非带宽。**第2步实现方式待重新拍板**（候选：图传 TX 让出/低于 AEC 优先级、拆核、或把 AEC 提到 radio_ping 之上并重评时序精度）。
- 代码已改（关探针），本地构建未验证（bash 不可用），硬件板测未验证。

---

## 2026-08-27 图传融合 · 第1步「时序探针」代码完成（待板测）

### 目的
在写真实图像数据前，先量化「节点上行窗口里，语音回复后追发 N 个填充包」是否会：①拖垮语音 slot（`intercom_missed_slots` 上涨）②语音 glitch ③浅环 RX 采集被饿死。方案 A（浅环全双工，不拆 I2S）落地前的探路。

### 改动（均为英文进仓库）
- `app_config.h`：新增 `APP_INTERCOM_IMAGE_GRANT_MAX=4`、`APP_INTERCOM_IMAGE_PROBE=4`（编译期常量，改这一个数即可调每 slot 追发片数；设 0 关闭探针）。
- `radio_ping.cpp/hpp`：
  - 新增包类型 `kPacketTypeIntercomProbe=13`；计数成员 `intercom_probe_tx_ / intercom_probe_rx_`。
  - 新增 `send_intercom_probe_burst(count)`：完全复刻 `burst_send_fragments` 的 FS-fallback + FIFO 预填2/边发边填流水线；复用成员 `tx_buf_`（此刻 `send_single_packet` 已结束，省 radio 任务栈）；发填充包（magic+type13+session+序号，其余清零）；发完恢复 `STDBY_XOSC`+`idle`。
  - 挂载点：`send_intercom_slot` 里语音回复 `send_single_packet` **成功后、`schedule_rx()` 前**，仅 `!is_gateway_ && (flags & kVoiceFlagNodeReply)` 且 `APP_INTERCOM_IMAGE_PROBE>0` 时触发。→ 与图传实际方向（node→GW，同上行）一致，零额外 TX/RX 掉头。
  - GW 侧：`dispatch_rx_packet` 在「非语音包丢弃」(radio_ping.cpp) **之前**拦截 type13，`intercom_probe_rx_++` 后丢弃；每 200 包 log 一次。node 侧每 100 突发 log 一次 tx 计数+missed。

### 验证方法（待用户板测）
1. 起通话，串口看 node 的 `intercom probe tx bursts=.. n=4/slot missed=..` 与 GW 的 `intercom probe rx=..`。
2. 对比 `intercom slots tx/rx/missed`：missed 是否随 N 明显上涨。
3. 主观听语音是否 glitch；改 `APP_INTERCOM_IMAGE_PROBE`（1→2→4→6…）扫出安全上限 N。
4. rx/tx 比值反映追发片实际到达率（丢多少）。

### 状态
- **代码已改，本地构建未验证（bash 不可用，需用户 `idf.py build`），硬件板测未验证**。这是第1步全部内容；第2/3/4步（真实图像片、GW 重组+授权/ACK、补发+连续帧流）待探针结论后进行。

---

## 2026-08-27 图传融合 · 第1步可行性验证：相机+I2S 无硬件冲突，瓶颈在 CPU/PSRAM 调度

### 结论：可行，无硬件拦路虎
- 相机=LCD_CAM+GDMA，音频=I2S0+独立 DMA，**两套外设、引脚零重叠**。`bsp_audio_suspend()` 释放 I2S 只在本地预览路径（相机独占整机的遗留做法），非硬件必需。
- **活证据**：远程图传路径 `image_capture_task`（app_main.cpp:610）现在就在 I2S 全开、voice_tx 读麦时跑相机 DVP + 软件 JPEG。硬件早已同时跑，只是策略禁止通话中拍照。
- node（门口机）`APP_CAMERA_NODE_LCD_ENABLE=0` 不显示，故连「相机 D4-D7 复用 LCD SPI3 引脚」这个真冲突都不涉及（仅在需显示的机器上有）。

### 芯片调度真相
- Core0（射频/时序核）：radio_ping(4)、voice_tx(3:I2S读+AEC+Opus编)、img_tx(4)。Core1（算力核）：voice_play(5:Opus解+I2S写)、img_cap(3:相机+JPEG编)。
- **射频 20ms TDD 在 core0，相机/JPEG 在 core1，射频=SPI2、相机=LCD_CAM 不同总线 → 加图传不拖累 TDD 时序**（最关键好消息）。
- 软件 JPEG ~90ms/帧纯 CPU 在 core1；voice_play(pri5)>JPEG(pri3) 抢占，语音解码有保障，JPEG 慢是弹性可接受。
- **PSRAM 单八线总线**（Octal/80MHz）是最需板测实测项：相机连续 DVP 写(614KB/帧×5buf)+JPEG 读写+AEC ring 争总线，代码量不出来。

### 被证实的矛盾（决定第2步拆法）
- 注释挑明深环 24desc 是「采集被相机/JPEG 抢 CPU 时不饿死」(bsp_audio.c:212/app_config.h:142-153)；AEC 又要浅环 TX 参考才锁得住（路径B）。**RX 要深、TX 要浅 → 拆 TX/RX 独立通道是图传融合的必要前提，不是可选优化**。待确认 ESP-IDF 能否给 TX/RX 配不同 dma_desc_num。

### 第2步前置调查：拆 TX/RX 独立深度——驱动层硬约束（ESP-IDF v5.5.3 I2S）
- DMA 环深度是 per-channel 存的（浅TX+深RX 在 DMA 层支持），**但全双工 `i2s_new_channel(&cfg,&tx,&rx)` 强制 TX/RX 共用一个 config → 必须同 `dma_desc_num`**（i2s_common.h:116-121）。
- 想拆深度只能分两次 simplex 建 TX/RX。**S3 坑**：BCLK/WS 是按方向分开的信号(soc/esp32s3/i2s_periph.c)，两个 master 各自驱动时钟，后初始化的 RX **静默覆盖**引脚路由+MCLK 源(i2s_std.c gpio/clk)，对 ES8311(ADC/DAC 共用一组时钟)会位滑移/数据损坏且不报错。干净时钟耦合(`sig_loopback`)只在全双工开(i2s_std.c:141-146)。
- **可行三选一**：A 全双工统一浅环6(简单，保 AEC/延迟，RX 采集极端争用下可能丢样)；B TX=master+RX=slave 分离(能拆深度但非预期模式，位滑移风险，须板测)；C 全双工统一深环24(采集稳但 TX 360ms 延迟→通话废)。
- **重新分析**：voice_tx 在 core0、相机/JPEG 在 core1 不同核，I2S DMA 描述符在内部 RAM 不走 PSRAM 总线，浅环 RX 被相机/JPEG 直接饿死的通路比原想的弱；而 TX 深环 360ms 延迟是刚性毁通话。故倾向**方案 A 先跑通**，板测暴露真丢样再上 B。

### 状态
- 纯只读排查，未改代码。第2步方向待用户在 A/B 间拍板（推荐 A：浅环全双工 + 解除图传/通话互斥，板测验证）。

---

## 2026-08-27 确定「单向图传+双向语音」融合架构；纯语音阶段先切浅环治近场 AEC

### 目标与分期（用户拍板）

- 最终目标：**单向图传 + 双向语音**融合，不再是「通话/图传互斥」。分期推进：**先把纯语音全双工做扎实，再叠加图传**。
- 语音延迟指标：端到端 ~100ms 用户无感即可，不必更激进。

### 融合架构方案（已定方向，图传阶段实施）

- 带宽预算：FLRC 2.6Mbps 下，双向语音（每包 ~80B、空口 ~0.3ms）只占约 10% 带宽，90% 空闲。语音与图传不是抢带宽，是抢**单射频半双工的时间片**。
- **20ms 超帧结构**：`[GW→Node 下行语音+流控 ~0.5ms]`+`[Node→GW 上行语音回复 ~0.5ms]`+`[Node→GW 图传突发 N 片 ~15ms]`+`[守卫 ~2ms]`。
- 语音是**刚性骨架**（每 20ms 必有一次双向来回，延迟锁死在一个周期内）；图传是**弹性填缝**（占超帧尾部，N 由网关在主控 slot 授权，防节点盲发打乱语音）。
- 重传：语音**绝不重传**（丢帧走 PLC 隐藏）；图传保留帧末 NACK 补发，补发片也走填缝通道。
- FPS 预估：空口理论 ~25FPS，但天花板是**接收端 LCD ~13FPS**（解码 ~17FPS）。融合后现实目标：**双向语音 <100ms + 单向图传 ~12-13FPS**。想再高需单独优化 LCD SPI。

### 本轮改动（纯语音阶段 · 治近场 AEC 延迟漂移）

- 订正上条日志的误判：`APP_AFE_AEC_FILTER_LENGTH=4` 是 ESP-SR **官方推荐值**（esp_aec.h:61 原文 "recommend to set 4"），非「偏短」。本轮**保持 4 不动**。
- 近场啸叫的物理根因是**节点 TX 深环（24 desc/~360ms）导致喇叭出声延迟大且随环填充浮动** → AEC 参考与真实回声漂移、锁不住。参考在 `radio_ping.cpp:838` decode 后即推入，`bridge_delay` 只补了处理管道固定延迟，没补 DMA 排空这段浮动。
- 纯语音阶段节点**不拍照**（拍照走 `camera_capture_task` 会先 `bsp_audio_suspend()` 整体释放 I2S），深环此刻闲置。故**通话期间临时把节点切浅环 6 desc/~90ms（与网关对齐），挂断恢复 24**。选路径 B（仅通话切换）而非全程浅环，以零影响节点非通话行为、不给图传埋坑。
- **临时措施**：图传融合阶段将用「TX/RX 拆独立通道」（浅 TX 治 AEC、深 RX 扛图传采集）替代本轮改动，届时退役这里。

### 改动文件

- `main/bsp.h`：声明 `bsp_audio_set_dma_desc_num()`。
- `main/bsp_audio.c`：实现运行时重建 DMA 深度（复用 `i2s_deinit()`+`i2s_init()`，与相机 suspend/resume 同一已验证路径；I2S 已释放时只记录深度留给下次 resume）。
- `main/app_config.h`：新增 `APP_INTERCOM_NODE_DMA_DESC_NUM=6`、`APP_AUDIO_NODE_DEFAULT_DMA_DESC_NUM=24`。
- `main/app_main.cpp`：`on_intercom_state()` 节点侧按通话状态在 6↔24 间切换；网关（radio 模式）本就是 6，只刷 UI。

### 竞态与时序核对

- 切换在 radio task 上下文（`start/stop_intercom_local` → cb）执行，重建阻塞 ~40ms。期间：voice_tx 读 I2S 因 disable 失败走 continue（安全）；voice_play 刚 `xQueueReset` 阻塞在队列（不碰 I2S）；首个 TDD slot 晚点触发 `missed_slots` 补偿，但握手窗口 1200ms 可容忍。
- 边界：通话中若被拍照抢占（纯语音阶段理论不发生），I2S 已被相机释放，`bsp_audio_set_dma_desc_num` 走 deferred 分支只记录，resume 时生效。

### 状态

- **代码已改，未构建、未烧录、未板测**（本机 bash 不可用，无法自行 `idf.py build`，需用户构建）。
- 板测重点：①两机贴近全双工时近场啸叫是否消除/减轻；②通话建立瞬间是否有异常（丢 slot 过多导致握手失败、爆音）；③挂断后节点拍照/图传是否恢复正常（确认 24 深环已还原）；④串口关注 `I2S DMA depth changed to 6/24` 日志与 `reference underflow/overflow`、`intercom_missed_slots`。

### 追加（同日）· 治「两头都闷/糊」音质

- 现象：近场啸叫已可接受后，双向语音仍**两头都闷、发糊、不够清晰**。判为**公共链路问题**（两向都过 AEC/Opus/采样率），非单侧音量。
- 节点音量对齐网关：`audio_diagnostics.hpp` 的 `volume_` 90→**80**（对应网关 config level 8，DAC 输出 80%）。90 用户反馈过响。
- 根因锁定 **AEC NLP 档位**：`aec_create()` 不显式设 NLP，吃默认 **AGGR(1, 激进)**；FD 模式下 AGGR 砍残余回声时连带砍语音高频 → 听感发闷。现啸叫已由环路增益(0.50)压住，NLP 有余量下调。
- 改动：新增 `APP_AFE_AEC_NLP_LEVEL=0`(NORMAL)，`echo_canceller.cpp` init 里 `aec_create` 成功后显式 `aec_set_nlp_level(handle, NORMAL)`。类型/函数经 `esp_aec.h`→`esp_aec_nlp.h` 链路可达。既有 ready 日志已打印 `nlp=%d`，板测可直接确认生效为 0。
- 板测重点：①两头是否变清晰、高频回来；②残余回声是否听得到（若明显回声，`APP_AFE_AEC_NLP_LEVEL` 调回 1）；③音量 80 是否合适。
- **代码已改，未构建、未烧录、未板测。**

---

## 2026-08-27 回退到 direct FD AEC 主线后的只读审查与音质/AEC/啸叫三线定位

### 背景

- 判断“完整 AFE + VOIP”路线走偏：它不能根治两机靠近的跨设备啸叫，且是乐鑫正在淘汰的方向（官方推荐 `AEC_MODE_FD_LOW_COST`）。已回退到 `52998a6 双向通话v6`，工作区干净。
- 该版路线为 direct `aec_create(..., AEC_MODE_FD_LOW_COST)` + NLP，方向正确，板测“自身 AEC OK”，但整体音质仍差。本次做只读审查定位原因，未烧录、未监视串口、未推送。

### 关键定位（“板测能过但音质差”的主因）

- `radio_ping.cpp:740` 采集循环末尾有固定 `vTaskDelay(ms_to_ticks_min_1(1))`。`CONFIG_FREERTOS_HZ=1000`（sdkconfig:1619），这 1ms 是真 1ms。
- 该循环已被 `read_mono_frame` 里的 `i2s_channel_read(..., portMAX_DELAY)` 卡节拍并让出 CPU（idle 照常喂 TWDT），这句 delay 对喂狗无必要，却把每帧周期抬到 `10ms(I2S)+处理+1ms`。
- 对上此前实测：400 帧 4.45s ≈ 11.1ms/帧 ≈ 90fps。每帧慢 ~1.1ms → I2S 采集 DMA 环（24 desc×240 frame ≈ 360ms，bsp_audio.c:215）持续积压 → 周期性丢样 = 断续/爆音/底噪，同时让 AEC 参考-麦克风对齐持续漂移。

### 结构确认（现状正确、勿动）

- intercom TX 路径**不**走 `audio_processor.cpp` 的 `process_tx_frame`（NS/AGC/带通/预加重）。这是对的：那套非线性 DSP 会破坏 AEC 需要的线性关系。
- AEC 跑在原始麦克风上（radio_ping.cpp:687），输入增益 ×2 在 AEC 之后（:696）。参考在播放增益 ×0.6 之后推入（:831→835），与真正进喇叭的信号幅度一致。对齐思路正确。

### 三条改进路线

1. 音质：删 `radio_ping.cpp:740` 的 `vTaskDelay(1)` 恢复 100fps（根因）；输入 ×2 有削顶失真（intercom_input_clip_samples_ 在计数），宜改模拟 PGA 或降数字增益。
2. AEC：参考按 decode 时刻推入而非喇叭真正出声时刻，若播放 DMA 积压则参考超前；intercom 下可调浅 TX DMA 深度缩短出声延迟，必要时加大 `APP_AFE_AEC_FILTER_LENGTH`（当前 4，偏短）。先修实时性再评估。
3. 近距离啸叫：本地 AEC 消不掉跨设备环路（对端喇叭→本机麦无参考），核心是压环路增益。当前数字环路增益 = 输入×2 × 播放×0.6 = 1.2（+1.58dB），两机贴近易自激。降到 输入×1 × 播放×0.5 = 0.5（-6dB）。硬要求贴近+大音量+严格全双工则需物理隔离/半双工衰减/专用反馈抑制。

### 本次改动

- 步骤 1：删除 `radio_ping.cpp:740` 的固定 `vTaskDelay(1)`，恢复由 I2S 阻塞读独占 10ms 节拍。
- 步骤 2：`APP_INTERCOM_INPUT_GAIN` 2→1、`APP_INTERCOM_PLAYBACK_PERCENT` 60→50，数字环路增益从 +1.58dB 降到 -6dB，同时消除 ×2 削顶。

### 是否解决

- 两步均为可逆的单行/参数改动，已改代码；未构建、未烧录、未板测。
- 步骤 3（TX DMA 深度 / filter_length）留待前两步复测后再定。

---

## 2026-08-13 双向音频通话初版计划

### 新加入的功能

- 新增《双向通话实施计划.md》，为 CONFIG 双向通话开关、双方握手、TDD 音频、视频暂停和 AEC 制定分阶段路线。

### 改动内容

- 对当前 `AM36_Electronic_doorbell_special` 分支的 CONFIG 回调、ConfigAck、连续视频停止入口、音频任务和旧 PTT 残留进行了静态盘点。
- 只读对照仓库内 `AM36_Electronic_doorbell` 分支的“双向通话 V1”和“时分复用音频传输 v1/v2”实现，确认其可复用的 TDD 主从轮转、10 ms Opus 帧、队列和播放链路。
- 明确参考分支现有 clip/rms 啸叫静音不是完整 AEC；后续必须补充 far-end 播放 PCM 参考、延迟校准和双讲测试。
- 当前已经平衡的视频参数（JPEG Q30、LCD 20 MHz、64/256 正占空比）本轮冻结，不纳入通话计划的顺手调整范围。

### 遇到的问题

- 当前 CONFIG 的普通 ACK 只能证明配置包被处理，不能单独证明 TDD 音频已经建立；开启流程需要 READY/START 二阶段确认，关闭流程需要 TDD 边界上的 STOP/ACK。
- 参考分支与当前 special 分支的 FLRC 负载、图传状态机和 PTT 遗留不同，不能直接假定代码可无冲突移植。
- 当前工程尚未确认可用 AEC 组件、实际声学延迟、AEC 运行内存和双板 CPU 余量。

### 是否解决

- 方案阶段问题已完成整理；业务代码尚未修改。
- 静态分析已完成；未构建、未烧录、未进行硬件验证，后续按计划阶段 0 至阶段 5 实施并由用户板测确认。

---

## 2026-07-22 保留 32KB 指令 cache，修复节点端内存不足

### 想实现的功能 / 目标
- 音频解码和射频共用同一 cache，任务处理不过来。
- 期望通过把指令 cache 从 16KB 提升到 32KB 来缓解性能压力，并保留这一配置（实测确实有用）。
- 目标：在 32KB 指令 cache 下，让节点端也能正常启动，不再报错。

### 改动内容
1. **指令 cache 提升到 32KB**（直接改 `sdkconfig`，不是 `sdkconfig.defaults`）：
   - `# CONFIG_ESP32S3_INSTRUCTION_CACHE_16KB is not set`
   - `CONFIG_ESP32S3_INSTRUCTION_CACHE_32KB=y`
   - `CONFIG_ESP32S3_INSTRUCTION_CACHE_SIZE=0x8000`
   - 数据 cache 原本已是 32KB。
2. **`CMakeLists.txt` 增加 GCC 编译 workaround**：对 `esp_lcd` 组件单独降到 `-O1`，绕开编译器段错误（见下方问题）。
3. **裁剪任务栈**（`main/app_config.h`），把内部 SRAM 省出来：
   - `APP_RADIO_TASK_STACK_BYTES` 32768 → 8192（实测约用 2.2KB）
   - `APP_VOICE_TX_TASK_STACK_BYTES` 32768 → 16384（实测约用 11.9KB，不能再砍）
   - `APP_VOICE_PLAY_TASK_STACK_BYTES` 32768 → 16384（保守值，尚未实测）
   - `APP_IMAGE_TASK_STACK_BYTES` 保持 16384（img_tx / img_cap 共用，img_cap 未实测）
4. 期间加过栈探针日志（`uxTaskGetStackHighWaterMark`）测量各任务栈占用，测完已删除。

### 遇到的问题
1. **改错文件**：一开始改的是 `sdkconfig.defaults`。已存在的 `sdkconfig` 不会被 defaults 覆盖，必须直接改 `sdkconfig`。→ 已解决。
2. **GCC 编译段错误（ICE）**：cache 改 32KB 后，`esp_lcd_panel_rgb.c:700` 报 `internal compiler error: Segmentation fault`。彻底删除重构建也没用。→ 已解决：`CMakeLists.txt` 对 `esp_lcd` 组件降到 `-O1`。
3. **节点端启动报 `ESP_ERR_NO_MEM`**：网关端正常，只有节点端报错。
   - 根因：32KB 指令 cache 是从内部 SRAM 里划出来的，比 16KB 多吃 16KB 内部 RAM；叠加节点端 3 个 32KB 大任务栈 + 摄像头 DMA，内部 SRAM 不够。
   - → 已解决：裁剪任务栈，回收约 56KB 内部 RAM，节点端正常启动。

### 是否解决
- ✅ 保留 32KB 指令 cache 且节点端不再报 `ESP_ERR_NO_MEM`。
- ✅ GCC 段错误已用 `-O1` workaround 绕过。
- ⏳ **待验证**：voice_play 任务栈目前是保守的 16KB，尚未通过实际语音接收触发测量，后续如需继续省 RAM 再测。

### 遗留 / 待办（本次未处理）
- **图传耗时 30 秒问题**：确认是射频链路丢包重传导致（img_tx 只用了 2.6KB 栈，跟内存无关），属于独立问题。
  - 方向：把射频 ISR 回调链（`rp_radio_irq_callback` 等）放进 IRAM。
  - 另注意一个潜在崩溃隐患：ISR 用 `ESP_INTR_FLAG_IRAM` 安装，但回调链目前在 flash 里。

---

## 2026-07-22 图传 prepare 延迟复发（加音频后）

### 问题现象
- 网关端图传日志出现 prepare=110-142ms 的周期性尖峰（正常应 ~35-56ms）。
- 这个问题之前修复过一次（见 `radio_ping.cpp:1797-1804` 注释，把 `vbat_received_cb_` 移出热路径），现在加了音频后又复发。

### 问题根因
- **`prepare_ms`** = 网关发 ImageCmd 到节点开始传第一个包的延迟。
- `radio_task` 收到 ImageStart 包后，**在发 ACK 之前**调用 `image_rx_progress_cb_(0, total_frags, 0)`（`radio_ping.cpp:1840`）。
- 回调进入 `ui_gw_rx_begin` → 用 `portMAX_DELAY` 阻塞抢 LVGL 锁 `s_lock`（`ui_gateway.c:1187`）。
- 如果 LVGL 任务（core 1）正在刷屏（持有 `s_lock`），`radio_task`（core 0）就卡住等锁，最长 **~100ms**（LVGL 刷屏周期）。
- 等拿到锁、更新完 UI、释放锁、发完 ACK，节点才开始传数据，`prepare_ms` 被拉高到 110-140ms。

### 为什么加了音频后复发
- 之前只修了 `vbat_received_cb_`（把电池电压回调移出热路径），但 **`image_rx_progress_cb_` 还在热路径上**。
- 音频播放加入后，LVGL UI 可能增加了音频状态指示、波形、音量条等元素，**LVGL 刷屏频率提高**，锁竞争概率从偶发变成频发（日志中 ~每 2-3 帧出现一次高 prepare）。
- 另一个加重因素：`play_task` 每收一个 opus 包（20ms 音频）就调 `bsp_audio_write` → `i2s_channel_write(..., portMAX_DELAY)` 阻塞写 I2S（`bsp_audio.c:456`）。虽然 `play_task` 在 core 1 不直接卡 `radio_task`（core 0），但可能间接增加了 LVGL 任务的调度延迟或刷屏触发频率。

### 改动内容
1. **调整 ImageStart 处理顺序**(`radio_ping.cpp:1837-1867`):
   - 原本:准备缓冲区 → 更新 UI(阻塞抢锁) → 发 ACK → 进入 RX。
   - 现在:准备缓冲区 → **发 ACK** → **进入 RX** → 更新 UI(非阻塞)。
   - 理由:ACK 发送时刻决定 `prepare_ms`,必须在最前面;RX 紧跟其后,不让首个数据包到达时还没开 RX。
2. **UI 锁改非阻塞**(`ui_gateway.c:1187, 1229`):
   - `ui_gw_rx_begin` / `ui_gw_rx_progress` 的 `xSemaphoreTakeRecursive(s_lock, portMAX_DELAY)` → `xSemaphoreTakeRecursive(s_lock, 0)`。
   - 抢不到锁就跳过本次更新,下一个分片进度或完成回调会补上。
   - 理由:这两个函数从 radio RX 热路径调用,阻塞等锁会延迟 ACK/NACK 发送。

### 是否解决
- ✅ **已修复并验证通过**。
- 板测结果:prepare 稳定在 32-36ms,之前的 110-140ms 尖峰完全消失。
- 帧率从 2.9-3.5 fps 提升到 4.0-4.3 fps(总延迟降低约 60-80ms)。

### 相关代码位置
- `radio_ping.cpp:1840` — 热路径上调用 `image_rx_progress_cb_`
- `radio_ping.cpp:1797-1804` — 之前修复 vbat 的注释（说明了同样的锁竞争问题）
- `ui_gateway.c:1187, 1229` — `ui_gw_rx_begin` / `ui_gw_rx_progress` 用 `portMAX_DELAY` 抢 `s_lock`
- `bsp_audio.c:456` — `bsp_audio_write` 阻塞写 I2S（间接因素）

---

## 2026-07-22 音画同步（AV 配对队列）

### 想实现的功能 / 目标
- 之前图像一到达就立即显示，音频要经过 400ms 预缓冲 + 队列才播放，导致**图像领先音频、音画不同步**。
- 目标：让视频追随音频时间线，实现音画同步播放。

### 改动内容
将独立的 `AudioSegment` 队列改成 **`AVFrame` 配对队列**（`app_main.cpp`）：

1. **结构体重构**（~line 80）：
   - 旧：`struct AudioSegment { opus_packed, len }`
   - 新：`struct AVFrame { rgb565, width, height, opus_packed, opus_len, jpeg_size, transfer_ms, enqueue_ms }`
   - 把图像和音频配成一对，所有权一起移交给队列。

2. **入队逻辑**（`on_image_rx_complete`，~line 1138）：
   - 有音频时：把 JPEG 解码后的 rgb565 拷到 SPIRAM（不立即显示）→ 和 opus 一起打包成 `AVFrame` → 推入 `g_av_queue`。
   - 无音频时：照旧立即显示（保持最低延迟）。
   - SPIRAM 拷贝失败时：回退到立即显示（避免丢帧，但音频会领先）。

3. **播放任务重写**（`gateway_audio_task`，~line 989）：
   - 从队列取出一对 `AVFrame` → **先调 `ui_gw_rx_complete` 显示图像** → 释放 rgb565 → 再解码/I2S 播放对应音频。
   - 图像显示节奏被 **I2S 时钟**（音频排空速度）限速，不再按 RF 到达速度抢跑。

4. **队列创建更新**（~line 1724）：
   - `g_audio_queue`(AudioSegment) → `g_av_queue`(AVFrame)

### 问题根因分析
- 音频受 I2S 时钟驱动、无法跳过或加速，是"刚性"的。
- 视频可延迟、可丢帧，是"柔性"的。
- **方案**：让柔性的视频追随刚性的音频时间线 → 音画自然同步。

### 是否解决
- ✅ **已实现（未板测）**。音画配对入队、由音频任务统一节奏播出，理论上实现同步。
- ⏳ **待板测验证**：确认音画是否真的对齐、I2S 阻塞写期间 LVGL 刷屏是否流畅。

### 相关代码位置
- `app_main.cpp:80` — `AVFrame` 结构体定义
- `app_main.cpp:936` — `av_queue_push` 推入配对帧
- `app_main.cpp:989` — `gateway_audio_task` 播放循环（先显示图像、再播音频）
- `app_main.cpp:1138` — `on_image_rx_complete` 打包 AVFrame 入队

---

## 2026-07-22 降低延迟（移除预缓冲 + 600ms 追赶）

### 想实现的功能 / 目标
- 音画同步搞定后，下一步是**降低延迟**。
- 当前延迟来源：400ms 预缓冲启动门限 + 队列堆积（RF 到帧比播放快时，队列最多堆 20 帧 = 4.4 秒）。
- 目标：有帧就播（不等预缓冲）、队列堆积超过 600ms 就丢旧帧追赶进度。

### 改动内容
1. **移除预缓冲逻辑**（`gateway_audio_task`，~line 978）：
   - 旧：等队列时长 ≥ 400ms 才开始播放（用 `av_queue_ms()` 估算）。
   - 新：`xQueueReceive` 阻塞等帧，取到帧立即开 PA 播放，队列空就关 PA。
   - **有帧就播**，起播延迟降到最低。

2. **加入 600ms 追赶机制**（`gateway_audio_task`，~line 995）：
   - 给每帧打时间戳 `enqueue_ms`（入队时刻）。
   - 每次取出帧后先算 `age = now - enqueue_ms`（这帧在队列里等了多久）。
   - 如果 `age > 600ms` **且队列里还有下一帧**，就丢掉当前帧（图像和音频一起丢、保持同步），继续取下一帧，直到帧足够新或队列空了才播。
   - **丢帧副作用**：画面会从旧帧直接跳到新帧、音频跳过一段，但只在延迟 > 600ms 时才触发，正常抖动不会误触发。

3. **删除时长估算函数**：
   - `av_queue_ms()` 删除（之前用队列帧数 × 220ms 估算，不准且不再需要）。
   - 判断依据改成"队列里有几帧真实的帧"，跟 opus 编码大小彻底脱钩。

4. **常量更新**：
   - `APP_GW_AUDIO_PREBUFFER_MS` / `APP_GW_AV_FRAME_EST_MS` → 删除。
   - 新增 `APP_GW_AV_MAX_LATENCY_MS = 600U`（追赶阈值）。

### 是否解决
- ✅ **已实现并板测验证通过**。
- ✅ 起播延迟降到最低（有帧立即播放，无预缓冲等待）。
- ✅ 600ms 追赶机制运行正常，测试期间全程未触发（队列深度稳定在 0-1 帧，无积压）。
- ✅ 去掉预缓冲后未出现频繁 underrun，PA 开关稳定。

### 板测数据（D:\tmp\网关.txt）
**关键指标：**
- **AV frame latency**: 260 – 401ms（帧从入队到播放的等待时间）
- **queue 深度**: 0 – 1 帧（无积压）
- **FPS**: 2.9 – 4.1 fps
- **无 [CATCHUP] 触发**：全程未超过 600ms 阈值

**延迟分析：**
- 队列 latency 稳定在 300-400ms 是正常现象，因为队列深度 1 表示上一帧音频还在播放（约 220ms），当前帧在等待。
- **总端到端延迟**（节点拍摄 → 网关播出）：RF 传输 ~160ms + 解码 ~35ms + 队列等待 ~350ms ≈ **约 550ms**。
- RF 重传（每帧 2-7 个 missing fragments）占了 40-80ms 额外延迟，是进一步降延迟的主要瓶颈（需射频侧优化，见"遗留/待办"）。

**结论：**
音画同步 + 低延迟目标达成，队列运行健康，无需调整 600ms 阈值。

### 相关代码位置
- `app_main.cpp:80` — `AVFrame` 加入 `enqueue_ms` 字段
- `app_main.cpp:93` — `APP_GW_AV_MAX_LATENCY_MS` 定义
- `app_main.cpp:939` — `av_queue_push` 打时间戳
- `app_main.cpp:978-1007` — `gateway_audio_task` 新播放循环（无预缓冲 + 追赶）

## 2026-07-22 网关端啸叫（声反馈）检测静音

### 想实现的功能 / 目标
- 网关喇叭和节点麦克风离得太近，网关放出的声音被节点麦克风重新采到，经 RF 回到网关再放出，形成声反馈闭环（啸叫）。
- 标准 AEC 不适用：这条回声环往返约 550ms 且时延会变，需要约 8800 抽头的自适应滤波器，性能扛不住。
- 采用「网关端检测 + 临时静音」：解码后的 PCM 里一旦识别出啸叫特征，就把该帧清零。喇叭一静音，闭环被打断，麦克风不再采到该音，电平回落，再自动恢复播放。

### 特征选择（来自板测标定）
- 先只打印特征、不静音，用真实啸叫和正常语音标定。
- 结论：自相关峰值（ac_peak）、过零率方差（zcr_var）在啸叫时并不稳定，不可靠，弃用。
- 真正干净的判据是 **削波率 clip%** 和 **能量 rms**：
  - 正常语音：clip=0%，rms<500
  - 啸叫：clip=22~68%，rms=22000~29000
  - 中间是大片空档、无重叠，所以阈值可以放宽松、不会误伤正常大音量。

### 改动内容
- 删除诊断用的 `[HOWL]` 特征打印函数 `howl_detect_log`。
- 新增静音状态机 `howl_process` + `HowlSuppressor`（只用 clip% + rms）：
  - ACTIVE→MUTED：clip≥5% 且 rms≥6000，连续 3 帧（约 30ms）触发。
  - MUTED→ACTIVE：clip≤2% 且 rms≤4500，连续 50 帧（约 500ms，大于一个回声往返）才恢复。
  - 阈值取折中：首版（clip15%/rms12000/8帧）截止太晚，一度调到（3%/4000/2帧）又太宽松，最终折中到上面这组，既能早掐断又不误伤正常大音量。
  - 阈值刻意放宽：只在明确啸叫时静音，宁可放过轻微情况也不误伤正常声音。
- `gateway_audio_task` 解码循环里：`howl_process` 返回 true 时把该帧写 0 再送 I2S。

### 是否解决
- 待板测验证（本地 bash 环境损坏无法编译，由本人手动编译烧录）。

### 相关代码位置
- `app_main.cpp` — `HowlSuppressor` 结构体 + `HOWL_*` 阈值宏
- `app_main.cpp` — `howl_process` 静音状态机
- `app_main.cpp` — `gateway_audio_task` 解码循环调用 `howl_process` 并按需清零

## 2026-08-07 LR2021 驱动 0.0.7 与 FLRC BURST 射频链路升级

### 日期 / 阶段
- 2026-08-07：完成代码迁移与静态核对，等待 ESP-IDF 编译、双板烧录和实际射频测试。

### 新加入的功能
- LR2021 射频依赖固定升级到 staging registry 的 `esp_lora_driver 0.0.7`。
- 图传数据改为固定 511 字节 FLRC BURST：1024 字节 FIFO 预填两包，TX_DONE 后连续启动下一包并补写一包。
- 图传接收改为连续 RX，并在一次 RX_DONE 处理中按 FIFO level 排空完整的 511 字节数据包。

### 改动内容
- `main/idf_component.yml`：固定驱动版本和 staging registry；本地忽略文件 `dependencies.lock` 已同步到例程中的 0.0.7 hash。
- `sdkconfig`：本地配置补充 LR2021 芯片选择和新版驱动的 Radio Planner 默认项；该文件受 `.gitignore` 管理。
- `main/app_main.cpp`：首次 `ral_reset()` 前将 NRST GPIO38 配成输出高，满足 0.0.7 加载 PRAM 的冷启动要求。
- `main/app_config.h`：适配新版 FLRC 枚举接口；最大包长 255→511，图像有效分片随之从 239→495 字节。
- `main/radio_ping.cpp/.hpp`：适配新版 `ralf_params_flrc_t`，配置 1024 字节 TX/RX FIFO；仅 ImageData 初发和 NACK 补发改走 BURST，ImageStart、EOT、ACK/NACK、语音和配置包继续走原单包逻辑。
- 保留目标工程原有 `tx_done_waiter_ + TaskNotify` 同步机制；BURST 期间 TX_DONE 直接唤醒图传任务，没有改回例程的忙轮询。
- 将射频 511 字节缓冲上限与语音 255 字节构包上限拆开，保持原有语音聚合数量和包内容不变。
- 保留原 NACK 分片索引越界过滤；未改动音画同步、连续图传、UI、摄像、业务回调和业务包内容。

### 遇到的问题
- 专用补丁工具不能直接读取现有源码文件，但严格解码确认文件是 UTF-8 无 BOM。改为先生成统一差异、执行 `git apply --check` 后局部应用，并复核 CRLF/无 BOM，未整文件转码。
- 新版驱动的 `set_rx` 会清 RX FIFO；若每个图传分片后重新进入 RX，会清掉已到达的下一包前半段。已改为图传期间保持连续 RX，只在 CRC/头错误时重新对齐 FIFO。
- 目标工程已有独立的 TX_DONE 任务通知逻辑，不能覆盖成例程的忙轮询。BURST 已接入现有通知所有权，避免主无线任务与图传任务竞争 IRQ。

### 是否解决
- 代码迁移与静态核对已完成：旧 FLRC 字段已清除，发送和接收路径、声明/调用、依赖版本及配置项一致，`git diff --check` 通过。
- 未执行 ESP-IDF 编译和烧录，未进行节点/网关双板测试；最终状态为待验证，不能标记为已解决。
- 双板验证重点：冷启动无 BUSY/WDT、ImageStart 握手正常、511 字节连续图传可完成、NACK 补发有效、语音/配置等短包行为不回退。

## 2026-08-07 纯图片图传优化——第一步：移除图传音频与网关 SAVE

### 目标
- 图传链路只传 JPEG，不改变摄像、声音触发、PIR、告警音等独立业务。
- 去掉图传音频采集准备、Opus 预编码/拼接、网关 AV 解码播放准备。
- 去掉网关逐帧 `image_store_save()` 和 Gallery 服务，为后续 15 FPS 调优减少热路径耗时。

### 改动内容
- 节点端直接发送 JPEG：删除 Opus 环形缓冲初始化、持续预编码、音频 drain、`[jpeg_len][jpeg][opus]` 拼包和音频会话标志。
- 删除图传前后的音频暂停/恢复与 6 秒音频冷却，保留 JPEG 预取流水线。
- 网关端收到完整帧后直接按 JPEG 解码并显示，删除 AV 队列、音频播放任务、啸叫抑制图传分支及逐帧 SAVE。
- 配置界面删除 `Audio Clip` 开关和对应配置同步；WiFi 区域删除 Gallery 地址。
- `image_store.c` 不再加入组件构建，WiFi HTTP 服务不再注册图片存储接口。
- 保留声音触发检测、PIR、Voice Alarm 和原有独立语音业务，未改 FLRC 参数及图片业务内容。

### 验证状态
- 已做静态残留检查和差异检查；图传路径不再引用 Audio Clip、Opus 预编码、AV 队列或 image_store。
- 按协作约定未执行 ESP-IDF 编译、烧录或双板测试。
- 当前结论为“第一步代码完成、待板测”；15 FPS 目标需在第二步图片质量/处理优化和第三步射频调优后联合验证。

## 2026-08-07 纯图片图传优化——网关接收、解码与 LCD 流水线

### 日期 / 阶段
- 2026-08-07：完成网关图传热路径的异步流水线改造和静态核对，等待 ESP-IDF 编译、双板烧录和串口日志验证。

### 新加入的功能
- 新增网关 JPEG 消费任务，固定运行在 Core 1；射频接收回调只重组 JPEG 并交给队列，不再同步执行 JPEG 解码和画面旋转。
- 新增长度为 2 的 JPEG 队列；队列满时丢弃最旧待处理帧，保留最新帧，避免处理速度不足时积累显示延迟。
- RGB565 解码缓冲区改为任务内复用，仅在输出尺寸增大时重新申请，减少逐帧申请和释放。

### 改动内容
- `main/app_main.cpp`：收到完整帧后先保存 session、分片数和传输耗时，重组连续 JPEG，立即 `rx_reset()` 释放 `ImageTransfer`，随后以 1 ms 单次定时器请求下一帧，再由 Core 1 图片任务完成解码、旋转和 LVGL Canvas 提交。
- 停止视频流时清空队列中的待处理 JPEG；图片任务在解码前和提交 UI 前检查视频流状态，避免退出页面后继续显示残帧。
- `[FRAME]` 日志新增 `queue`、`reassemble`、`jpeg` 和 `frags` 字段，可区分排队、重组、解码和 Canvas 更新耗时。
- `main/radio_ping.cpp`：NACK 日志新增首个缺失分片序号 `first`，用于判断“每帧固定缺 1 包”是否为确定性时序问题。
- JPEG Q 值仍为 60，FLRC 2.6 Mbps 参数和 LCD 12 MHz 参数均未调整，业务逻辑未改动。

### 遇到的问题
- 工程部分头文件和 C 文件不能被当前补丁工具按 UTF-8 读取，因此本轮未改动 `app_config.h` 和 LCD 驱动；1 ms 下一帧间隔作为网关流水线内部常量实现，避免整文件转码破坏原始编码。
- LCD 驱动当前记录的 `display` 仍是 LVGL 锁、RGB565 旋转和 Canvas 提交时间，不等于 SPI 面板物理刷新时间；物理刷新瓶颈需结合本轮板测帧率和后续 LCD DMA 完成计时确认。

### 是否解决
- 代码改造和静态所有权核对已完成：JPEG 指针只在队列和图片任务之间转移，队列丢帧、停止视频和初始化失败路径均释放内存。
- 按协作约定未执行 ESP-IDF 编译、烧录或双板测试；当前状态为待验证，不能标记为板测通过。
- 预期当前 51 ms 解码/Canvas 耗时将与下一帧约 124 ms 射频接收重叠，帧周期应由约 187 ms 降到接近射频周期；实际结果以用户串口日志为准。

## 2026-08-07 11/12 号分片静默丢包（69% 帧）定位与修复（三轮）

### 日期 / 阶段
- 2026-08-07：三轮定位完成，修复方向待定，代码将回退重做。当前结论以板测日志为准。

### 现象（回退到 72acc58 后复测）
- 网关每帧缺 1 个分片，固定 12 号（早期 11/12 交替），约 69% 帧。
- 发送端 TX_DIAG：29 包全发、0 超时、72ms 连发（约 2.5ms/包）。
- 接收端无任何痕迹：无 CRC fail、无 short len、无 FIFO near-full、无硬件 RX CRC error。

### 第一轮定位（诊断埋点）
- 新增 RX_DONE 处理间隔诊断（[RX_DIAG] gap）、发送端逐包 TX_DIAG、硬件 CRC 错误每次打印。
- 结论 1：全程无硬件 CRC error，否定"FLRC 长度字节损坏 / 硬件 CRC 错误"假设。
- 结论 2：RX_DONE gap 大量 >3ms（347 次/87 帧），7-13ms 常态、54-131ms 偶发，大 gap 均紧跟 [FRAME]（image 任务 decode+display）之后。
- 结论 3：FIFO 1024B 只能装 2 个 511B 包（约 5ms 余量）；接收端一旦延迟 >5ms，burst 中段（第 12 包附近）溢出静默丢包。丢包机制确认，拖慢源待定。

### 第二轮修复（无效）
- 把 radio_ping 热路径 13 个函数 + ImageTransfer::rx_fragment 放 IRAM；radio 任务优先级 4 -> 24。
- 板测（16:16 日志）：gap 依旧 180 次（5-112ms），first=12 依旧 27/28 帧。flash cache 竞争假设不成立。

### 第三轮修复（无效）
- 驱动层补丁（managed_components，已被 .gitignore 忽略）：
  - lr20xx_radio_fifo.c：read_rx/write_tx/clear_rx/clear_tx/get_rx_level/get_tx_level 加 IRAM_ATTR；
  - smtc_modem_hal.c：protect/unprotect_api_call 加 IRAM_ATTR；
  - radio_ping.cpp：burst 热路径跳过 ral_get_flrc_rx_pkt_status（仅为 RSSI），省一次 SPI 事务。
- 板测（16:27 日志）：gap 依旧 329 次（7-20ms 为主 307 次、>50ms 22 次），first=12 依旧 59/60 帧。
- 结论：应用层与驱动层全部 IRAM 化 + 最高优先级 + 减少 SPI 事务均无效，指令取指/总线竞争假设彻底排除。

### 当前结论与剩余假设
- 已确认：丢包 = 接收端 RX_DONE 响应/处理延迟 > 约 5ms（FIFO 2 包余量）导致 burst 中段溢出；延迟 7-20ms 为常态且与 flash 取指无关。
- 剩余候选（未验证）：
  1. DIO 中断为上升沿触发（GPIO_INTR_POSEDGE），若 LR2021 IRQ 引脚为电平保持型，引脚保持高电平期间到达的包会漏触发 ISR，导致包积压/溢出——可试电平触发或任意沿触发对照；
  2. core0 上的高优先级中断（WiFi/esp_timer/SPI DMA）抢占 radio 任务；
  3. core1 的 PSRAM/flash 共享总线对 core0 数据访问的硬阻塞；
  4. 发送端 TX 节奏在固定相位出现间隙（需节点端 TX_DIAG 逐包时间戳确认）。
- 建议下一步：先做中断触发方式对照实验（改动小、可快速验证），或"显示流水线暂停"对照实验确认是否 core1 干扰。

### 是否解决
- 未解决。代码将回退后按上述候选重新验证；本轮仅记录结论，未标记板测通过。

### 备注 / 风险
- managed_components 不在 git 中，驱动 IRAM 补丁若组件重新下载会丢失。
- 若最终确认是接收端响应延迟问题，兜底方案：发送端 BURST 限速（牺牲吞吐）或接收端改用 FIFO 高水位中断提前排空。
### 第四轮诊断（ISR 计数 vs 任务处理计数）

#### 新增埋点（radio_ping.cpp/.hpp，注释为英文避免编码问题）
- ISR 回调里递增 `irq_isr_count_`（IRAM ISR 内计数，不受任务调度影响）；
- `handle_rx_packet` 每次调用递增 `rx_done_handle_count_`，并统计本帧最大 RX_DONE 间隔 `frame_max_gap_ms_`；
- 每帧 ImageStart 时记录两个计数起点，EOT 完成时打印：
  `[RX_DIAG] frame isr=X handled=Y max_gap=Zms`；
- 每次排空 FIFO 后读硬件 IRQ 状态，若 RX_DONE 位仍置位（说明新包已到但 ISR 未触发），
  打印 `[RX_DIAG] RX_DONE bit pending after drain`。

#### 判读方法（下一轮板测直接区分两种根因）
1. **若 `isr` 增量远小于分片数**（例如 29 包只触发 15 次 ISR）：
   → 上升沿中断漏触发（LR2021 DIO 为电平保持型，引脚保持高电平期间新包不产生上升沿）。
   修复方向：GPIO 中断改电平/任意沿触发，或 ISR 内直接读清 IRQ 状态让引脚回落。
2. **若 `isr` 增量约等于分片数、但 `max_gap` 仍大（7-20ms）**：
   → ISR 正常，是任务被延迟（core0 高优先级中断抢占 / core1 PSRAM-flash 共享总线阻塞）。
   修复方向：查 core0 中断（WiFi/esp_timer/SPI DMA）、显示流水线错峰、或 BURST 限速兜底。
3. **若频繁出现 `RX_DONE bit pending after drain`**：
   → 确认漏触发，与 1 同方向。

#### 是否解决
- 未解决，诊断待板测；下一轮日志按上述三点判读后确定修复方向。
### 16:36 日志判读结果（第四轮诊断）
- ISR 触发正常：每帧 `isr` 增量 29-51（含 ImageStart/EOT/NACK 重传），与分片数相当，无漏触发；
- `RX_DONE bit pending after drain` 出现 0 次，确认新包到达后 ISR 均正常触发；
- **问题确凿在任务侧**：每帧 `max_gap` 均 >=12ms（平均 21ms，峰值 54ms），任务处理 RX_DONE 被周期性延迟；
- first=12 依旧 34/34 帧，丢包与任务延迟直接相关。

### 第五轮诊断（ISR 到处理延迟定位）
- ISR 内记录 `esp_timer_get_time()` 时间戳（IRAM-safe）；
- `handle_rx_packet` 开头计算 ISR 到实际处理的延迟，>3ms 打印 `[RX_DIAG] isr->handle delay=Xms`；
- 判读：若 delay 大 -> 任务被占/等锁（modem_mutex 被其他任务持有、core0 中断抢占）；若 delay 小但包间隔仍大 -> 通知路径问题。
### 16:41 日志判读结果（第五轮诊断：ISR 时间戳）
- `isr->handle delay` 打印 0 次：任务在 ISR 触发后 3ms 内即开始排空 FIFO，任务响应完全正常；
- `RX_DONE bit pending` 依旧 0 次；
- `max_gap` 仍 12-54ms（每帧），first=12 依旧 43/43。
- **结论翻转：网关接收侧彻底排除（ISR 正常 + 任务响应快），12-54ms 的 gap 期间没有包到达，
  根因指向发送端（节点）**：burst 到第 12 号附近漏发/停顿，接收端只是等待。

### 第六轮诊断（节点 TX 增强，待板测）
- burst 循环记录最大发送间隙 `max_gap_ms` 及位置 `max_gap_pos`；
- TX FIFO 写返回值不再忽略，失败时打印 `[TX_DIAG] fifo write fail at next=... wst=...` 并计数；
- 结束汇总扩展为：`[TX_DIAG] init count=.. timeouts=.. first_timeout_pos=.. max_gap=..ms@pos=.. wr_fail=.. duration=..ms`。
- 判读：若 `max_gap` 位于 12 号附近（或 `wr_fail>0`）-> 节点 burst FIFO 写入/时序问题；
  若节点无 gap 且无 wr_fail，但网关仍缺 12 号 -> 需进一步看节点 TX_DONE 与网关 RX 的时间对应。
- 需要节点端串口日志（新固件）配合判读。
### 节点日志判读（16:45）与网关 frag gap 分析（16:52）
- 节点端 TX_DIAG：count=26、timeouts=0、wr_fail=0、max_gap 仅 3ms@pos=2/3、duration 64ms
  -> 发送端 26 包约 2.5ms 均匀连发，完全健康。
- 网关端新增分片到达间隔诊断（frag gap）：
  - `after idx=13`（7 次，间隔 4ms）= 首轮 12 号丢失后，13 号到达时与 11 号的间隔，
    证明 12 号在 11 号与 13 号之间（仅约 4ms）凭空消失；
  - `after idx=12`（14 次，间隔 4ms）= 重传的 12 号到达；
  - `after idx=0`（39 次）= 正常初始握手延迟。
- 结论：节点确认发出 12 号，网关 ISR/任务响应正常、无 CRC 错误、无 IRQ 残留，
  但 12 号未进入应用层 -> 只剩两个静默丢弃路径未打日志：
  handle_image_data 的 session/total 不匹配检查、dispatch 的 magic 检查（原 DEBUG 级）。

### 第七轮诊断（补全静默丢弃路径日志，待板测）
- handle_image_data：session/total 不匹配时打印 `[RX_DIAG] drop sid/tot mismatch idx=...`；
- dispatch_rx_packet：unknown/magic 不符时每 10 次打印 `[RX_DIAG] unknown pkt #... len=... hdr=...`。
- 判读：若 12 号丢帧出现上述日志 -> 12 号包到达但头部错位/内容异常（FIFO 读错位或射频损坏）；
  若仍无任何日志 -> 12 号包确实未到达射频（芯片静默丢弃），需转向射频/驱动层（如 FLRC 长度字节、
  CRC 配置、天线/链路）。
### 17:08 日志判读（unknown 每次打印）：根因实锤
- unknown 36 次 与 丢包 36 次 一一对应（每个 unknown 后紧跟 `sent ACK with 1 missing (first=11/12)`）；
- unknown len 分布：359 出现 17 次（最多），其余 25/27/277/323/331/349/350/353/355/357/361 —— 集中 349-361；
- 结论：12 号包在射频/芯片层被破坏成"349-361 字节的错位乱码伪包"，magic 不符在 dispatch 层被丢弃。
- 特征指向 FLRC 变长模式长度字节损坏（变长模式长度字节不被硬件 CRC 保护）。

### 第八轮修复实验（固定长度 FLRC，待板测）
- configure_flrc：`pld_is_fix = false` -> `true`（固定 511 字节）；
- send_single_packet：内部统一补零到 511（覆盖 ImageStart/EOT/NACK/ImageCmd/Config 等短包）；
- schedule_tx：语音包也补零到 511（tx_buf_ 大小即 511）；
- 图传 ImageData 包原本就是 511（build_padded 已补零），无需改；
- 接收端语音解析按 frame_count 而非 len，补零安全。
- 预期：长度字节不再参与接收，12 号"伪包"机制消失；若仍有异常，会以 RX_CRC_ERROR 形式可见（已有每次打印）。

## 2026-08-10 FLRC 接收半包竞争修复（待板测）

### 日期 / 阶段
- 2026-08-10：根据 349-361 字节 unknown 包日志重新订正根因方向，并完成最小接收侧修复，等待双板串口验证。

### 新加入的功能
- 无业务功能变化；继续使用 FLRC 变长包模式，图片内容、分片大小、发送节奏和业务协议均未调整。

### 改动内容
- `main/radio_ping.cpp`：当 RX FIFO 水位不足 511 字节时，不再直接把水位当作包长读取；改为读取芯片最后完整包长度，且仅当该长度与 FIFO 水位完全一致时才接收短控制包，否则保留 FIFO 中的半包等待真实 RX_DONE。
- 仍允许一次处理过程批量排空多个完整的 511 字节图片分片，保留现有 1024 字节 FIFO 吞吐能力。
- RX_DONE 与 CRC/HDR 错误同时出现时，错误标志优先处理，避免把 CRC/HDR 异常数据按正常包分发。

### 遇到的问题 / 结论订正
- 旧日志把 `unknown len` 当成射频解出的包长；复核源码后确认该值实际来自 FIFO 当前水位，因此 349-361 更可能是固定软件时序下读到的半包长度。
- LR20xx 数据手册说明 FLRC 变长包 Header 参与 CRC 与卷积编码，因此“长度字段不受 CRC 保护”不能作为已确认根因；固定 511 字节方案本轮不采用。
- 本机 Codex 补丁引擎会把 CRLF 文件误报为 UTF-8 错误；文件按 Git 索引原生 LF 归一化后可正常使用逐行补丁，Git 未产生额外全文换行差异。
- 仓库本地 Git 配置设为 `core.autocrlf=false`，仅作用于当前工程，防止后续 Git 操作再次把源码展开成 CRLF。

### 是否解决
- 代码修改与静态差异检查已完成；未执行 ESP-IDF 编译、烧录和硬件测试。
- 当前状态：待验证。重点观察首轮 EOT 是否仍持续缺 11/12 号分片、是否还有 349-361 字节 unknown 包，以及帧传输耗时是否恢复稳定。

## 2026-08-10 连续视频流请求串行化修复（待板测）

### 日期 / 阶段
- 2026-08-10：根据新版网关日志定位连续视频流偶发停住问题，并完成网关拍照请求的任务串行化修复。

### 新加入的功能
- 无业务功能变化；连续视频仍在每帧接收完成后自动请求下一帧。

### 遇到的问题
- 正常帧已经可以在无补包情况下以 24 个分片、约 84-85ms 完成传输，但自动请求下一帧后偶发长期无响应，手动再次触发才恢复。
- 异常帧出现 prepare=429496xxxxms、total=15/18ms 与 transfer=1904/5537ms 相互矛盾的统计，确认请求时间戳和接收会话被另一任务并发覆盖。
- 原实现由 ESP Timer/UI 线程直接执行 trigger_image_capture，同时 radio task 每 2ms 执行 ImageCmd 重发；两条路径会并发修改射频 mode、TX_DONE 等待者、会话和接收状态。

### 改动内容
- main/radio_ping.hpp：增加原子的图片请求和接收中止标志；外部接口不再直接写普通布尔状态。
- main/radio_ping.cpp：trigger_image_capture 只合并并投递请求，然后唤醒 radio task；新增 radio task 内部的请求消费和启动函数。
- 首次 ImageCmd、后续重发、旧会话清理和 session 生成现在全部由 radio task 串行执行，ESP Timer/UI 不再直接操作射频。
- 退出传输页面时会同时取消尚未处理的下一帧请求，并唤醒 radio task 执行接收清理。
- radio task 的处理顺序调整为：接收超时检查 -> 中止请求 -> 新拍照请求 -> ImageCmd 重发，避免退出页面后又启动一帧。

### 是否解决
- 代码修改、调用链核对、换行检查和 git diff --check 已完成；未执行 ESP-IDF 编译、烧录和硬件测试。
- 当前状态：待验证。重点观察连续视频是否仍会停住、RX done 后是否还出现 session=0 的 stale RX、prepare 是否还会出现无符号下溢，以及正常帧是否继续保持无补包。

## 2026-08-10 首次视频请求旧会话串入修复（待板测）

### 日期 / 阶段
- 2026-08-10：上一轮串行化固件板测确认后续连续帧已经稳定，但首次发起请求仍需再次触发才能进入连续流，本轮完成针对性修复。

### 板测现象 / 日志结论
- 首次 UI 请求时间为 3488ms，异常完成日志为 prepare=4294964862ms、transfer=2460ms、total=26ms。
- 由 transfer 反推该帧 ImageStart 约在 1057ms，明显早于首次 UI 请求；说明旧接收会话在首次点击后才借用新请求状态完成。
- 新请求更新了全局 ImageCmd 时间，而旧帧仍使用旧 ImageStart 时间，最终造成 prepare 无符号下溢。
- 后续连续帧维持约 84-85ms 传输且基本无补包，射频半包修复和 radio task 串行化方向继续有效。

### 根因
- 触摸屏 CAPTURE 回调未设置连续流标志，只有硬件 K3 路径设置；因此触摸首次完成后不会自动请求下一帧。
- ImageStart 在网关已有请求/接收会话时未拒绝其他 session，错误会话可能停止当前 ImageCmd 重发。
- ImageEOT 未校验当前重组 session 和总分片数；迟到的旧 EOT 可以借用新请求的 pending 状态处理旧缓冲区。
- RX 完成统计直接使用会被后续请求覆盖的全局 ImageCmd 时间，没有绑定到当前接收会话。

### 改动内容
- main/ui_gateway.c：触摸屏 CAPTURE 与 K3 使用相同的连续流初始化，首次点击即设置 stream mode 并清除 first-shown 状态。
- main/radio_ping.cpp：开始新请求时无条件清理旧重组元数据和 CRC/计时状态。
- 请求等待阶段只接受当前请求 session 的 ImageStart；已有重组时只接受同一接收 session 的重复 ImageStart。
- ImageEOT 必须与当前接收 session、总分片数和有效重组缓冲区完全匹配，否则只记录并忽略。
- 新增每个接收会话独立的请求时间锚点；主动请求按对应 ImageCmd 计时，节点主动推送按 ImageStart 作为起点，完成统计不再被下一请求覆盖。

### 是否解决
- 代码修改、调用链检查、UTF-8/LF 检查和 git diff --check 已完成；未执行 ESP-IDF 编译、烧录和本轮硬件复测。
- 当前状态：待验证。首次触摸 CAPTURE 应直接进入连续流；prepare 不应再出现 429496xxxxms；如收到迟到包，允许出现 ImageStart/ImageEOT ignored 日志，但不能中断当前请求。

## 2026-08-10 首次请求卡在 0% 的节点会话去重修复（待板测）

### 日期 / 阶段
- 2026-08-10：根据网关首次进入 `Receiving 0/25` 后停止、节点后续持续打印 `ImageCmd ignored: capture already busy` 的双端现象，修正节点拍照请求的并发与会话去重状态。

### 新加入的功能
- 无新增业务功能；图片内容、FLRC 包格式、分片参数和连续流业务均未改变。

### 遇到的问题
- 节点先判断 `g_capture_busy`，再判断是否为同一 session 的重发，导致首次 ImageCmdAck 丢失后，同一请求的所有重发都被拒绝且无法再次 ACK；网关继续每 30ms 重发，干扰 ImageStart/Ready-ACK 握手。
- 忙路径调用 `notify_capture_dropped()`，会错误清除当前拍照/传图使用的低功耗唤醒保护。
- 节点永久保存最后处理过的 session；网关重启后 session 计数从 1 重新开始，首次请求若与历史 session 同号，会被误判成旧请求而不再拍照。
- `g_capture_busy` 跨任务、跨核使用普通 volatile 变量，检查和占用不是原子单飞操作，存在并发窗口。

### 改动内容
- `main/app_main.cpp`：将拍照忙状态改为原子变量，并使用 compare-exchange 原子占用唯一拍照槽位。
- 新增当前活动 session 原子状态；仅在该 session 正在拍照/传输时对重发去重并返回成功，使射频层重新发送 ImageCmdAck，但不启动第二个拍照任务。
- 拍照任务的全部退出路径统一清除活动 session 后再发布空闲；任务结束后不保留历史 session，因此允许网关重启后重新使用 session=1。
- 不同 session 在当前拍照忙时仍返回拒绝，但不再调用 `notify_capture_dropped()`，避免破坏当前传输的保持唤醒状态；任务创建失败路径会正确清理状态和唤醒保护。
- `app_main.cpp` 仅按 Git 索引原生格式由 CRLF 机械规范化为 UTF-8 无 BOM + LF，随后使用可见小补丁修改；未生成临时编辑脚本。

### 是否解决
- 代码调用链、原子状态发布顺序、全部远程拍照任务退出路径、UTF-8/LF 和 `git diff --check` 已完成静态检查。
- 未执行 ESP-IDF 编译、烧录或双板硬件验证；当前状态为待验证。
- 板测重点：网关和节点上电后第一次按 CAPTURE 应直接从 0% 开始收包；节点不应再对同一 session 连续打印 busy；若按下新的请求时仍有旧帧在传，busy 日志应同时显示新 session 与 active session，旧传输完成后应自动接收新请求。

## 2026-08-10 重复按键中断首帧接收修复（待板测）

### 日期 / 阶段
- 2026-08-10：根据网关 `3100ms` 首次触发、`4852ms` 第二次触发、随后连续收到 session=1 EOT 的新日志，订正“首帧已经卡死”的判断并修复重复触发行为。

### 新加入的功能
- 无新增业务功能；只调整网关在已有拍照请求或正在接收图片时对重复触发的处理。

### 遇到的问题
- 第二次按下时日志已经显示旧重组 session=1，说明第一帧 ImageStart 已被网关接收，并非请求未到达节点。
- 第二次按下后约 58-124ms 收到 session=1 的 EOT；结合 25 个分片的正常突发耗时，说明第一帧数据正好在第二次按下前后发送。
- 原 `start_image_capture_request()` 会在任意新触发时主动丢弃现有 RX、清空重组缓冲区并生成新 session，导致正在空中的 session=1 数据全部被忽略，最终出现 `ImageEOT ignored: session=1/0 total=25/0`。

### 改动内容
- `main/radio_ping.cpp`：当 `image_rx_pending_` 或 `image_req_active_` 已置位时，重复 CAPTURE 只记录当前 session 和接收进度后返回，不再清空当前重组状态，也不再生成新 session。
- 保留现有 ImageCmd 重发、ImageStart 握手和 10 秒无活动 RX 超时；真实链路无响应时仍会由超时路径释放状态，之后可以重新触发。
- 当前帧正常完成后，连续流定时器仍按原逻辑请求下一帧，不受本次重复触发保护影响。

### 是否解决
- 最小补丁、调用顺序和静态差异检查已完成；未执行 ESP-IDF 编译、烧录和双板复测。
- 当前状态：待验证。第一次按下后即使在 0% 阶段再次按键，也应只看到 `capture trigger ignored: request/RX active`，不能再出现 `trigger: dropping stale RX` 和对应的 `session=1/0` EOT。

## 2026-08-10 首帧停在 1/N 的 RX_DONE 合并修复（待板测）

### 日期 / 阶段
- 2026-08-10：以正常版本 `a8fddd2b7d11a473adcbcac06a3d2b4daf69047c` 为基准复核，针对网关首帧长期停在 `received=1/26`、节点发送完整轮后 8 秒收不到 ACK/NACK 的现象补齐半包修复的 IRQ 消费逻辑。

### 根因分析
- 节点日志表明完整数据轮已经发出，网关也已收到第 1 个分片，因此问题不在 2 秒等待、拍照触发或 ImageStart 会话匹配，而在首个分片之后的接收推进。
- 半包竞争修复会在 RX FIFO 水位不足 511 字节时保留数据，等待下一次真正的 RX_DONE；这是避免把 349-361 字节半包当成完整包的必要保护。
- LR2021 的 RX_DONE 是 IRQ 状态位而不是完成次数计数器。连续图传包很密时多个 RX_DONE 可能合并为一次 DIO 通知；原任务只在 GPIO ISR 设置 `irq_pending_` 后读取芯片 IRQ，可能使已保留的半包完成后没有新的软件通知，接收永久停在首包。
- 之前第二次触发会重启 RX 并清 FIFO，偶然解除该状态，所以表现为需要按两次；重复触发保护生效后不再清状态，底层遗漏随即稳定暴露为卡在 `1/N`。

### 改动内容
- `main/radio_ping.cpp`：图像请求/接收期间且射频处于 `rx_pending` 时，radio task 每轮同时读取并清除芯片 IRQ 状态，不再只依赖 GPIO 的单次 `irq_pending_` 通知。
- 保留现有半包保护、变长 FLRC、511 字节图传分片、session 校验和重复触发保护；未改包长、协议、业务流程或超时参数。
- 未采用 `ral_set_rx()` 重启接收，因为该接口会清 RX FIFO；也未采用固定包长。

### 是否解决
- 已完成与正常提交的差异复核和最小代码修改；未执行 ESP-IDF 编译、烧录或双板硬件验证。
- 当前状态：待板测。预期第一次按下后接收计数应持续从 `1/26` 增长并在首轮 EOT 返回 ACK/NACK，节点不再出现 `no ACK/NACK for 8000ms`。

## 2026-08-10 网关复位后的旧会话 EOT 恢复（待板测）

### 日期 / 阶段
- 2026-08-10：根据 `D:\tmp\网关.txt` 中连续出现的 `ImageEOT ignored: session=13812/0 total=26/0` 定位网关复位后的会话恢复问题。

### 新加入的功能
- 网关在没有活动图片重组缓冲时，可以识别并结束节点仍在重试的旧 EOT 会话，使节点退出旧传输并继续接收新的拍照请求。

### 遇到的问题
- 网关复位会丢失旧图片会话状态，但节点仍在等待旧帧的 ACK/NACK，并约每 33 ms 重发一次 EOT。
- 网关按下拍照后进入等待新 ImageStart 的状态，对旧 EOT 只做忽略；节点在旧 EOT 等待阶段不处理新 ImageCmd，双方无法立即恢复。
- 日志中的 `session=13812/0` 和 `total=26/0` 表明收到的是节点旧会话，而网关当前没有对应的重组 session 和分片总数。

### 改动内容
- `main/radio_ping.cpp`：在 `handle_image_eot()` 中复用空缺失列表 ACK 的构包逻辑。
- 网关无活动重组缓冲时收到未知 EOT，会按该 EOT 自带的 session 返回完成 ACK；若已有新请求等待 ImageStart，则保留新请求和 ImageCmd 重试状态。
- 已有新图片正在重组时，异 session 或异分片数 EOT 仍按原逻辑忽略，避免旧包打断正常传输。

### 是否解决
- 已完成代码修改与静态路径核对；未执行 ESP-IDF 编译、烧录或双板硬件验证。
- 当前状态：待板测。网关复位后应先出现一次 `sending resync ACK`，旧 session 的 EOT 重发应立即停止，随后新 session 应正常收到 ImageStart 和图片分片。

## 2026-08-10 图传 IRQ 合并后的 FIFO 排空补偿（待板测）

### 日期 / 阶段
- 2026-08-10：根据 `D:\tmp\网关.txt` 中网关复位后新会话停在 `received=1/26`，且没有 `RX done`、EOT 或超时日志，继续核对 `E:\MYESP32\esp_lr2021_radio` 参照工程与当前工程的接收路径。

### 新加入的功能
- 图传接收态在没有新的 RX_DONE IRQ 时，也会根据 FIFO 水位主动排空已经完整到达的 511 字节图像分片。

### 遇到的问题
- RX_DONE 是状态位，不是逐包事件计数器；连续分片快速进入 FIFO 时，多个完成事件可能合并为一次通知。
- 当前工程的半包保护会正确保留小于 511 字节的下一包，但原先只在 `handle_irq()` 中调用 `handle_rx_packet()`，因此合并通知后可能把后续完整分片留在 FIFO，接收停在 `1/26`。
- 参照工程并非固定包长或固定长度 FLRC；它同样使用变长 FLRC 和 1024 字节 FIFO，主要差异是没有当前工程这层半包保护，也没有对应的无 IRQ 水位补偿路径。

### 改动内容
- `main/radio_ping.cpp`：在 `poll_once()` 的图传接收分支读取 FIFO 水位；达到 `APP_FLRC_BURST_PAYLOAD_LEN` 时复用 `handle_rx_packet()` 排空完整分片。
- 不调用 `ral_set_rx()`，避免为补通知而清空 FIFO 中已经到达的后续数据；原有小于 511 字节的半包保护保持不变。

### 是否解决
- 已完成静态代码修改和差异检查；未执行 ESP-IDF 编译、烧录或双板硬件验证。
- 当前状态：待板测。重点观察复位后第一次图传是否从 `1/26` 持续增长到 `26/26`，以及是否出现 `RX done`；若仍停滞，应记录新增 FIFO 水位日志或补充更细的收发时序日志。

## 2026-08-10 接收路径按参照工程恢复（前述 FIFO 轮询方案订正，待板测）

### 日期 / 阶段
- 2026-08-10：用户板测反馈前述“无 IRQ 时轮询 FIFO”方案仍停在 `received=1/26`，因此停止在 `3c94745` 的半包保护上继续叠加补偿，重新按 `E:\MYESP32\esp_lr2021_radio` 的稳定实现核对。

### 新加入的功能
- 本阶段没有增加新的协议或补偿机制，而是恢复参照工程已经验证过的 FLRC burst 接收模型。

### 遇到的问题
- 当前工程的 `3c94745` 同时引入了持续轮询并清除 IRQ、`RX_DONE` 与 CRC/HDR 状态互斥判断，以及短包 `GetRxPktLength == FIFO level` 门槛；这些行为均不在参照工程的稳定接收路径中。
- RX_DONE、CRC/HDR 是可合并的状态位，强制互斥可能跳过本应排空的 FIFO；持续主动清除 IRQ 也改变了参照工程“由 DIO 通知后统一排空”的时序。
- 1024 字节 FIFO 是原始字节流，图像 burst 依靠发送端把每个分片补齐到固定 511 字节并由接收端一次排空所有完整分片保持边界，不应再用最后包长寄存器推断 FIFO 头部。

### 改动内容
- `main/radio_ping.cpp`：`poll_once()` 恢复为只在 `irq_pending_` 有效时读取并清除 IRQ，不再在图传期间持续主动轮询 IRQ/FIFO。
- `handle_irq()` 恢复为 RX_DONE 优先处理；即使同次状态还带 CRC/HDR 位，也先按参照工程排空已完成数据。
- `handle_rx_packet()` 恢复参照工程的 FIFO 水位排空方式：首次按 `min(level, 511)` 读取，后续循环遇到不足 511 字节的在途分片才停止；删除 `ral_get_pkt_size()` 与 FIFO 水位必须完全相等的门槛。
- 前一条记录中的“无 IRQ FIFO 水位补偿”代码已经撤销；该记录保留作为排查历史，本条为正式订正。

### 是否解决
- 已完成参照工程只读对比、代码恢复和静态差异检查；参照工程没有任何修改。
- 未执行 ESP-IDF 编译、烧录或双板硬件验证，当前状态仍为待板测。
- 板测重点：先验证冷启动首轮图传，再连续传输后复位网关并发起下一轮；两种场景都应持续从 `1/26` 走到 `26/26` 并出现 `RX done`。
- 为避免叠加未经本次日志验证的猜测性行为，上一阶段加入的“未知旧 EOT 自动回复完成 ACK”代码也已撤销；本阶段最终代码只保留接收核心向参照工程对齐的改动。

## 2026-08-11 网关显示路径提速（待板测）

### 日期 / 阶段
- 2026-08-11：根据 D:\tmp\网关.txt 中 display 从约 20 ms 逐步升到约 100 ms 后回落的锯齿现象，继续优化图像提交和 LCD 刷新路径。

### 新加入的功能
- 网关图像画布增加前台/后台双缓冲；旋转和 RGB565 R/B 换色在图像任务中写入后台缓冲，LVGL 定时器只交换已经完成的缓冲并提交最新画面。
- LCD SPI 颜色传输改为在 on_color_trans_done DMA 完成回调中通知 LVGL，避免传输尚未完成时复用 LVGL 绘图缓冲。

### 改动内容
- main/ui_gateway.c：增加双缓冲、临界区交换、最新帧覆盖旧待提交帧，以及 1 ms LVGL 定时提交；PSRAM 无法分配第二块缓冲时保留单缓冲路径。
- main/bsp_lcd.c：注册 ESP-IDF 5.5.3 的 LCD 颜色传输完成回调；成功提交后不再提前调用 lv_disp_flush_ready。
- main/app_main.cpp：将原 display 统计改名为 compose，明确该时间只表示图像旋转/提交，不表示 LCD 物理刷新完成。
- 未修改 APP_LCD_SPI_PCLK_HZ，由用户后续调整 LCD 时钟。

### 遇到的问题
- 原 display 时间包含等待 LVGL 锁、整帧像素旋转/换色和刷新队列竞争；SPI 颜色传输是异步 DMA，原驱动却在 DMA 完成前通知 LVGL，导致绘图缓冲过早复用并使刷新队列出现周期性阻塞。

### 是否解决
- 已完成代码修改、行级差异、UTF-8/BOM/换行和 LVGL/ESP-IDF 接口静态核对；当前状态：待编译、烧录和板测。
- 预期 compose 稳定在约 20 ms，图像任务不再因 LVGL 刷新锁等待而出现 20→100 ms 锯齿；实际帧率仍由用户最终设置的 LCD SPI 时钟和面板刷新时间决定。

## 2026-08-11 连续图传下一帧请求延时恢复为配置项（待板测）

### 日期 / 阶段
- 2026-08-11：根据降低JPEG质量后的最新网关日志，修复连续图传下一帧请求仍硬编码为1 ms的问题。

### 新加入的功能
- 连续图传下一帧请求延时统一由APP_STREAM_NEXT_FRAME_DELAY_MS控制，当前配置为10 ms，后续无需再修改app_main.cpp即可调参。

### 改动内容
- main/app_main.cpp：kStreamNextFrameDelayMs由固定1U改为APP_STREAM_NEXT_FRAME_DELAY_MS。
- 未修改30 ms ImageCmd重试周期、图像协议、JPEG参数、射频参数和接收流程。

### 遇到的问题
- 最新日志中JPEG已降至约8.08 KB、16～17个分片，无线传输约54.8 ms且没有丢包重传，但帧周期仍分为100～105 ms和134～141 ms两档。
- 两档相差约30～35 ms，与ImageCmd重试周期一致；固定1 ms请求容易落在节点上一帧清理窗口内，使首个ImageCmd未被接收。

### 是否解决
- 已完成最小代码修改、宏定义/使用位置检查、行级差异和UTF-8无BOM/LF静态检查。
- 未执行ESP-IDF编译、烧录或双板板测，当前状态为待验证。
- 板测重点：慢档134～141 ms占比是否下降，prepare是否继续稳定在13～14 ms，以及平均帧率是否从约7.6 FPS提升。

## 2026-08-11 连续图传改为最终确认包链式请求下一帧（待板测）

### 日期 / 阶段
- 2026-08-11：上一阶段的固定10 ms等待仍无法消除约30 ms的快慢两档；为继续提速，改为在上一帧最终确认中直接携带下一帧会话。

### 新加入的功能
- 网关在缺失分片数为0的最终ImageNack确认包中，使用原保留字节12～13携带下一帧session_id。
- 节点收到链式session后先完成旧帧发送和应用层busy状态释放，再通过现有采集回调立即启动预取帧，不新增Ready包，也不增加一次射频往返。
- 网关发送链式确认后直接等待下一帧ImageStart；若30 ms内没有开始，复用原ImageCmd重试流程发送同一个session，兼容丢包和旧节点固件。

### 改动内容
- main/radio_ping.cpp：增加连续流链式会话状态；校验最终确认所属的发送session；解析、保存并延后派发下一session。
- main/radio_ping.cpp：完整帧确认时预分配下一session并写入确认包；网关同步预置接收状态和30 ms兜底；重复EOT重发相同链式session，停止流后不再继续携带下一session。
- main/app_main.cpp：删除固定1/10 ms定时器请求路径以及接收完成后的第二次ImageCmd触发，连续流正常路径完全由最终确认包推进。
- main/app_main.cpp：修复30 s发送超时日志被粘在注释后而实际失效的问题。
- main/app_config.h中的JPEG质量Q值改动属于用户同期调整，本阶段未覆盖该配置；原APP_STREAM_NEXT_FRAME_DELAY_MS已不再被连续流代码引用。

### 遇到的问题
- radio_ping.cpp和radio_ping.hpp均为合法UTF-8无BOM、LF文件，但内置apply_patch和本机Codex补丁入口持续误报非法UTF-8；经用户明确允许后，radio_ping.cpp采用严格UTF-8读取、唯一片段匹配、无BOM/LF写回脚本完成修改。
- app_config.h在修改期间存在用户侧JPEG Q值调整，因此保留当前用户版本，不再对该文件做清理或格式化。
- 增量构建命令刚启动即被停止；用户随后明确要求不由AI构建，因此本阶段没有编译结论。
- 边界限制：若用户恰好在最终确认已经发出后立即停止视频，节点可能已经取得下一session，最多仍可能启动一帧；网关本地abort会清除等待和重试状态，后续需结合板测确认这一极窄竞态是否需要新增取消协议。

### 是否解决
- 已完成协议状态、重复EOT、停止流、session去重和30 ms兜底路径的静态核对；main/app_main.cpp与main/radio_ping.cpp通过行级diff检查。
- 未执行ESP-IDF完整构建、烧录或双板硬件验证，当前状态为待板测，不能将静态检查视为功能已经验证。
- 板测重点：正常连续流中不再出现周期性ImageCmd重试；下一帧prepare应低于原13～14 ms，并消除约30 ms慢档；若链式确认丢失，应在30 ms后自动回退到同session的ImageCmd并继续传输。

## 2026-08-11 更新 ESP32 协作永久约束

### 日期 / 阶段
- 2026-08-11：将用户明确提出的 ESP32 验证边界、脚本修改授权和速度优先级写入桌面全局记忆文件与全局协作 Skill。

### 新加入的功能
- 更新桌面 `AI_COLLAB_GENERAL_RULES_CN.md`，将两项要求固化为全局永久协作约束。
- 同步更新全局 `ai-collab-general-rules-cn` Skill，使后续 ESP32 / ESP-IDF 任务自动遵守相同规则。

### 改动内容
- ESP32 工程只允许静态检查；禁止由 Codex 构建、编译、链接、烧录或启动监视器。
- 明确禁止 `idf.py build`、`ninja`、`cmake --build`、IDE 构建按钮及等价构建命令，除非用户以后明确撤销该规则。
- 用户永久允许使用 PowerShell、Python 或其他精确脚本修改代码，并将处理速度列为关键指标；脚本仍须保持编码、BOM、换行并核对行级差异。
- 静态验证结论必须明确标注“未构建、未烧录、待用户验证”。

### 遇到的问题
- 用户澄清永久记忆源文件是桌面 `AI_COLLAB_GENERAL_RULES_CN.md`，因此没有另建项目级 `project_memory.md`。
- 桌面记忆文件和全局协作 Skill 均位于工程目录之外，已按用户本次明确授权完成同步更新。

### 是否解决
- 已完成桌面全局记忆、全局协作 Skill 和工程日志三处同步。
- 本次没有修改功能源码，没有执行 ESP32 构建、编译、烧录或板测；当前仅完成文档静态核对。

## 2026-08-11 节点预取分段计时与高频日志降噪（待板测）

### 日期 / 阶段
- 2026-08-11：根据最新网关和节点日志，进一步定位约 132 ms 帧周期中的节点预取耗时，并为摄像头 MCLK 提速后的效果增加可量化日志。

### 新加入的功能
- 节点每次预取下一帧成功后输出一条 `[PREFETCH]` 日志，分别记录摄像头采集、JPEG 编码、两者合计、原始帧字节数和 JPEG 字节数。
- 采集或编码失败时输出对应阶段耗时和错误名称，便于区分摄像头与编码链路故障。

### 改动内容
- `main/app_main.cpp`：在 `prepare_jpeg_blob()` 内围绕 `capture_frame()` 和 `encode_frame()` 增加独立毫秒计时，计时不包含无线发送和链式 session 等待。
- `main/app_main.cpp`：将链式下一 session 在旧任务 busy 尾段产生的 `capture busy` 日志由 WARN 降为 DEBUG，默认日志不再每 2 ms 刷屏。
- `main/app_config.h`：用户同期调整了 `APP_SP0A39_MCLK_HZ`；静态检查结束时当前工作区为 18 MHz，本阶段保留且未覆盖。

### 遇到的问题
- 原 `[TIMING] ... tx=` 统计覆盖了 `prefetch_produce()`，不能代表纯无线发送时间；新增 `[PREFETCH]` 后可直接分离节点采集与编码耗时。
- `ImageCmd ignored: capture busy` 同时被内部链式 session 轮询复用，形成大量误导性 WARN；本次只调整日志等级，不改变 session 重试、busy 门控或采集状态机。

### 是否解决
- 已完成最小代码修改和静态差异检查；没有改变图像协议、预取缓存所有权、无线参数或网关 LCD 路径。
- 未执行 ESP32 构建、烧录或板测，当前状态为待用户验证。板测时重点比较 `[PREFETCH] capture/encode/total` 与网关 `[FRAME] period`，确认当前 MCLK 调整实际缩短的是采集阶段还是整体帧周期。

## 2026-08-11 节点采集与 JPEG 编码细分计时（待板测）

### 日期 / 阶段
- 2026-08-11：在已有 `[PREFETCH] capture/encode/total` 总计时基础上，继续拆分摄像头采集和 JPEG 编码内部耗时，用于定位 130 ms 预取周期的具体软件瓶颈。

### 新加入的功能
- 每次成功采集输出 `[CAPTURE]`，以微秒记录 `setup`、`wait`、`alloc`、`cache`、`downsample` 和 `total`。
- 每次成功编码输出 `[ENCODE]`，以微秒记录 `input_alloc`、`swizzle`、`open`、`output_alloc`、`jpeg`、`cleanup` 和 `total`。

### 改动内容
- `main/camera_dvp.cpp`：围绕帧等待、输出缓冲区申请、DMA 缓存同步和 2 倍降采样增加计时；原有单独的 DEBUG 降采样日志合并到新的 INFO 汇总日志中。
- `main/image_transfer.cpp`：围绕编码输入缓冲区申请、YUV422 排列转换、编码器打开、输出缓冲区申请、JPEG 核心处理和资源清理增加计时。
- 未调整采集、降采样、JPEG 编码、图传协议或缓存所有权；用户同期修改的 `APP_SP0A39_MCLK_HZ` 保持不动。

### 遇到的问题
- `camera_dvp.cpp` 和 `image_transfer.cpp` 原工作区为 CRLF，而 Git 索引为 LF，内置补丁工具再次误报非法 UTF-8；按全局规则使用严格单片段匹配脚本修改，并将这两个本次涉及且修改前无用户改动的源文件规范为 UTF-8 无 BOM + LF，最终 Git 差异恢复为局部修改。

### 是否解决
- 已完成代码修改、计时点配对核对、UTF-8/BOM/换行检查和 `git diff --check` 静态检查。
- 未执行 ESP-IDF 构建、编译、烧录或板测；当前状态为待用户验证。板测后根据 `[CAPTURE]` 和 `[ENCODE]` 中占比最大的字段决定下一步优化目标。

## 2026-08-11 DVP 改用驱动原生备用缓冲区（待板测）

### 日期 / 阶段
- 2026-08-11：针对摄像头 MCLK 提高到 24 MHz 后出现绿屏/花屏的问题，修正连续 DVP 采集期间的目标帧缓冲区所有权。

### 新加入的功能
- 启用 ESP-IDF DVP 驱动原生 backup buffer；应用持有目标帧进行缓存同步和降采样时，后续帧由驱动内部备用缓冲区承接。
- 保持 DVP 连续运行，不增加整帧复制或停启控制器，避免牺牲 24 MHz 带来的采集提速收益。

### 改动内容
- `main/camera_dvp.cpp`：将 `bk_buffer_dis` 从 `1` 改为 `0`；`captured_buffer` 非空时，`on_get_new_trans()` 不再提交用户缓冲区，由驱动自动选择内部备用缓冲区。
- `main/camera_dvp.cpp`：删除应用自行申请和释放的 `safe_buffer`，并在 DVP 启动前清空残留的目标帧指针。
- `main/camera_uart.hpp`：删除不再使用的 `safe_buf_` 成员。驱动备用缓冲区替代原应用停车缓冲区，整帧缓冲内存数量不增加。

### 遇到的问题
- DVP 驱动在帧完成中断内先请求并启动下一笔 DMA，再调用应用的完成回调；原逻辑可能在应用拿到目标帧指针前，已经把同一缓冲区再次交给后续 DMA。MCLK 提高后覆盖窗口更紧，更容易表现为绿屏或花屏。
- 该现象更符合 DMA 缓冲区所有权竞争，而不是 640×480×2 缓冲区容量不足；当前帧大小和 64 字节 DMA 对齐关系静态核对无越界缺口。

### 是否解决
- 代码层已消除“应用读取目标帧时 DMA 仍可能复用该缓冲区”的路径，并保留现有 `[CAPTURE]` / `[ENCODE]` 细分计时。
- 未执行 ESP-IDF 构建、编译、烧录或板测；当前状态为待用户验证。建议先以 24 MHz 烧录，重点观察绿屏/花屏是否消失，以及 `[CAPTURE] wait/total` 是否稳定下降。

## 2026-08-11 扩大 DVP 用户缓冲并加入越界保护区（待板测）

### 日期 / 阶段
- 2026-08-11：驱动原生备用缓冲区方案板测后仍出现绿屏/花屏，继续验证 24 MHz 下是否存在单帧 DMA 写入超出 614400 字节的问题。

### 新加入的功能
- 每个 DVP 用户帧缓冲在 614400 字节有效图像区之后增加 256 KiB 保护区，并将扩大后的完整容量提交给 DVP 驱动。
- 保护区预填 `0xA5` 哨兵值；每帧检查紧邻有效图像区的前 4 KiB，并在 `[CAPTURE]` 日志中输出 `received` 和 `guard=ok/changed`。

### 改动内容
- `main/camera_dvp.cpp`：新增 `kCaptureGuardBytes`、`kCaptureDmaBufferBytes` 和保护区检测逻辑；四个用户 DMA 缓冲均扩大到 876544 字节。
- 初始化时将保护区哨兵同步到 PSRAM；采集完成后同步有效帧和 4 KiB 保护区，再执行边界检测与原有降采样。
- 保留驱动原生 backup buffer 和 24 MHz MCLK，不修改 JPEG、无线传输或 LCD 路径。

### 遇到的问题
- 上一轮备用缓冲区修复未消除实际花屏，说明用户缓冲区复用竞争不是唯一根因。
- `D:\tmp\节点.txt` 当前仍是旧日志，其中没有新加入的 `[CAPTURE]` / `[ENCODE]` 字段，因此本阶段只能通过新固件日志继续确认 DMA 实际写入情况。

### 是否解决
- 已完成扩大缓冲区和越界哨兵检测的代码修改；是否能消除绿屏/花屏尚未完成硬件验证。
- 未执行 ESP-IDF 构建、编译、烧录或板测；待用户烧录后检查 `[CAPTURE] received=... guard=...`。若持续为 `received=614400 guard=ok` 但画面仍异常，可排除缓冲容量不足，下一步应转向 24 MHz 连续 DVP 的带宽竞争或采样时序。

## 2026-08-11 24 MHz PCLK 改用相反采样沿（待板测）

### 日期 / 阶段
- 2026-08-11：根据扩大 DVP 缓冲区后的节点实测日志，排除帧缓冲容量不足，转向验证 24 MHz 下的数据建立/保持时间。

### 新加入的功能
- 将 APP_CAMERA_DVP_PCLK_INVERT 真正接入 ESP32-S3 CAM HAL，并将本轮测试值设为 1，改用相反 PCLK 边沿采样。
- DVP 启动日志增加 PCLK=falling/rising，便于确认固件实际使用的采样沿。

### 改动内容
- main/camera_dvp.cpp：在控制器 enable 后调用 cam_ll_enable_invert_pclk()；保留 24 MHz、驱动原生 backup buffer 和保护区检测。
- main/app_config.h：将 APP_CAMERA_DVP_PCLK_INVERT 从 0 调整为 1。
- 核对 ESP-IDF 5.5.3 驱动源码确认：公开 DVP 配置结构没有 PCLK 边沿字段，HAL 初始化固定 cam_clk_inv=0；工程中原有的 PCLK 宏此前没有任何代码引用。

### 遇到的问题
- 实测连续出现 received=614400 guard=ok，说明 DMA 收到的长度精确且没有写入紧邻保护区。
- ESP-IDF 5.5.3 的 esp_cam_ctlr_dvp_start_trans() 固定以 fb_size_in_bytes 启动 DMA，不使用应用缓冲区的扩大容量作为 DMA 长度，因此扩大缓冲不会改变实际 614400 字节传输上限。
- 24 MHz 下预取已从约 130 ms 降至 97～98 ms；当前采集约 34～39 ms，其中降采样约 30 ms，编码约 59 ms。提速有效，但花屏仍未解决。

### 是否解决
- 已排除缓冲容量不足这一方向，并完成不降低 MCLK/帧率的 PCLK 相反沿测试代码。
- 未执行 ESP-IDF 构建、编译、烧录或板测；待用户验证 PCLK=falling 下绿屏/花屏是否消失。若仍异常，下一步验证连续 DVP 与 PSRAM 访问竞争。

## 2026-08-11 连续 DVP 下提升 PSRAM 带宽（待板测）

### 日期 / 阶段
- 2026-08-11：PCLK 相反采样沿板测后仍出现绿屏/花屏；保持 DVP 连续运行，不停止、不重建控制器，转向验证 24 MHz DVP 与图像处理并行访问 PSRAM 时的带宽余量。

### 改动内容
- 仅修改无后缀的 `sdkconfig`：ESP32-S3 Octal PSRAM 从 40 MHz 调整为 80 MHz；当前 Flash 已为 80 MHz，本地 ESP-IDF 5.5.3 的 ESP32-S3 Kconfig 和 F8R8 测试配置均明确支持该组合。
- `sdkconfig.defaults` 和 `main` 下的 C/C++ 源码均不修改；此前加入源码的中文注释及本轮其他实验代码已随回退移除。

### 判断依据
- 板测日志持续为 `received=614400 guard=ok`，已经排除 DMA 写越界。
- ESP-IDF 5.5.3 DVP 驱动实际以控制器的 `fb_size_in_bytes` 启动 DMA，不会使用应用提交的扩大 `buflen` 增加 DMA 长度；扩大保护区无法修复画面，反而会让驱动对 876544 字节执行缓存同步并额外占用约 1 MiB PSRAM。
- 当前 24 MHz 下每帧采集后仍需在 PSRAM 上完成 614400 字节缓存一致性处理、降采样读写以及编码前转换。将 Octal PSRAM 提至 80 MHz 是不破坏连续采集流水线的直接带宽验证。

### 是否解决
- 已完成 `sdkconfig` 单文件配置调整和静态检查；没有加入 DVP 停止或控制器重建路径。
- 未执行 ESP-IDF 构建、编译、烧录或板测；待用户烧录验证启动日志中的 PSRAM 频率、画面完整性及 `[CAPTURE] wait/cache/downsample/total` 变化。

## 2026-08-11 网关 LCD 物理刷新细分计时（待板测）

### 日期 / 阶段
- 2026-08-11：现有 `[FRAME] compose` 只覆盖图像旋转、画布缓冲切换准备等 CPU 工作，没有覆盖 LVGL 后续通过 SPI 向 LCD 发送像素的时间；新增底层刷新统计以确认当前帧率是否受 LCD 限制。

### 新加入的功能
- `main/bsp_lcd.c`：从一轮 LVGL 首次 `flush_cb` 开始计时，到最后一块像素的 `on_color_trans_done` 回调结束，统计一次真实 LCD 刷新的墙钟耗时。
- 新增 `[LCD]` 日志字段：`period` 为相邻物理刷新完成间隔，`total` 为本轮首块提交到末块完成的总耗时，`flushes` 和 `pixels` 为实际分块数与像素数，`swap` 为 RGB565 字节交换 CPU 耗时，`submit` 为面板提交 API 的 CPU 耗时，`xfer` 为去除排队重叠后的 SPI 传输占用时间，`idle` 为本轮刷新中 SPI 未传输的墙钟时间。双缓冲下 CPU 与 SPI 可以重叠，因此 `swap`、`submit` 不再从 `idle` 中重复扣除。
- 统计快照在 LCD 完成回调中发布，在 LVGL 任务中打印；仅输出像素数不少于半屏的刷新，避免状态文字等局部刷新淹没串口日志。

### 改动边界
- 本轮不修改 LCD SPI 时钟、SPI 队列深度、LVGL 缓冲行数、刷新周期和图像接收/解码/合成逻辑，仅增加观测能力。
- LVGL 使用两个 40 行 draw buffer；每个分块的提交时间和 `is_last` 标志按 FIFO 保存，并在 SPI 完成回调中按提交顺序取出，避免异步双缓冲把两轮刷新错误合并。
- 当前节点摄像头 MCLK 仍保持用户回退后的 20 MHz；本轮未改动 `main/app_config.h` 和 `sdkconfig`。

### 静态带宽基线
- 当前 `APP_LCD_SPI_PCLK_HZ` 为 12 MHz，网关照片画布为完整的 240×320 RGB565；仅发送 76800 个像素数据的理论下限已经是 `240×320×16/12 MHz = 102.4 ms`，尚未包含窗口命令、字节交换和分块调度。
- 首轮板测的正常单帧统计为 `flushes=8 pixels=76800 total≈133 ms xfer≈109 ms swap≈3.6 ms idle≈24 ms`，相邻刷新完成周期约 158 ms，即 LCD 实际呈现约 6.3 FPS。
- 同期 `[FRAME] period` 仍为 78～81 ms（约 12.5～12.8 FPS），因此图像接收/解码继续运行在约 12.6 FPS，但 LCD 大致只能显示其中隔帧结果；LCD 已确认是当前实际显示帧率的主要瓶颈。
- 首轮代码偶发 `flushes=16 pixels=153600`，原因是初版统计只保存一个异步分块状态，被双 draw buffer 覆盖后合并了两轮刷新；已改为分块 FIFO，不代表 LCD 实际单帧尺寸发生变化。

### 是否解决
- 已通过首轮板测确认 LCD 刷新是显示侧瓶颈，并修正双缓冲条件下的统计记录竞态；修正版本仍待再次板测确认每轮稳定为 8 段、76800 像素。
- 本轮未执行 ESP-IDF 构建、编译、烧录或串口监视；仅完成静态检查和日志分析。

## 2026-08-11 网关视频整帧直传 LCD（待板测）

### 日期 / 阶段
- 2026-08-11：针对 240×320 视频画面经 LVGL 拆成 8 次 40 行 flush、实测物理刷新约 133 ms 的瓶颈，将连续视频帧改为绕过 LVGL 像素刷新并整帧提交 LCD；普通界面仍保留 LVGL。

### 新加入的功能
- `main/ui_gateway.c`：沿用现有前后台 240×320 RGB565 画布。视频流期间隐藏 LVGL canvas，页面切换只执行一次 LVGL 背景同步，后续帧直接提交当前前台画布；直传失败时恢复 LVGL canvas 作为功能回退。
- `main/bsp_lcd.c`、`main/bsp.h`：新增同步整帧视频提交接口。一次设置完整 LCD 窗口，底层继续按 40 行 DMA 块连续排队，并等待末块传输完成后才允许复用前台画布，避免异步 DMA 与后台写帧发生所有权冲突。
- 新增 `[LCD] direct period=... total=... pixels=76800` 日志，用于直接观察整帧完成周期和物理传输总耗时。

### 性能相关改动
- `main/app_config.h`：将 LVGL 任务延时由 10 ms 降为 1 ms；LCD SPI 传输队列深度由 10 降为 2。队列深度 2 仍可保持相邻 DMA 块连续传输，同时把 PSRAM 视频帧所需的内部 DMA 临时块限制为最多两个 40 行块，约 38.4 KiB。
- ST7789 RGB565 数据端序切换为驱动原生小端模式，并删除 LVGL flush 中逐像素软件字节交换。该修改由 ESP-IDF 5.5.3 ST7789 驱动通过 `RAMCTRL` 配置完成；测试色条常量同步恢复为标准 RGB565 数值，颜色通道 R/B 转换保持原逻辑不变。
- SPI 时钟仍为 12 MHz，理论像素传输下限仍是 102.4 ms；本轮优化目标是去掉 8 次 LVGL 分段之间的调度空隙和约 3.6 ms 软件字节交换，不通过提高 SPI 时钟冒险破坏显示信号完整性。

### 静态检查与验证边界
- 已核对本机 ESP-IDF 5.5.3 的 ST7789 `data_endian`/`RAMCTRL` 实现、SPI 大块分段排队逻辑，以及 PSRAM 缓冲需要内部 DMA 临时块的处理路径。
- 已完成接口引用、关键状态、变更范围和 Git 空白错误静态检查；保留源文件原编码与换行方式。
- 未执行 ESP-IDF 构建、编译、烧录或串口监视，当前待用户板测。重点检查颜色是否正确、是否出现撕裂，以及 `[LCD] direct total/period` 是否由原约 133/158 ms 接近 103～110 ms。

## 2026-08-11 网关视频直传 DMA 内存耗尽修复（待板测）

### 日期 / 阶段
- 2026-08-11：首轮整帧直传板测能够逼近 8～9 FPS，但持续运行后出现 `setup_dma_priv_buffer: Failed to allocate priv TX buffer`，随后直传返回 `ESP_ERR_NO_MEM`。

### 遇到的问题
- 视频前后台画布位于 PSRAM。ESP-IDF 5.5.3 的 SPI LCD IO 没有为颜色事务设置 `SPI_TRANS_DMA_USE_PSRAM`，因此整帧调用虽然被拆成 40 行块，SPI master 仍会为每块动态申请内部 DMA bounce buffer。
- 队列深度降为 2 只能限制同时占用量，不能消除持续申请和释放；内部 DMA 堆碎片化或余量波动后仍可能申请失败。该根因已由本次板测错误栈确认。

### 改动内容
- `main/bsp_lcd.c`：网关 UI 启动时优先永久预留一个 40 行、19200 字节的内部 DMA 缓冲，整帧视频接口保留按需申请回退；每帧从 PSRAM 依次复制 8 个条带到该缓冲，再提交对应 LCD 区域。
- 每个条带由 LCD ISR 完成回调释放二值信号量，LVGL 任务等待该信号后才复用同一 DMA 缓冲。运行期不再为视频像素申请或释放 SPI 临时 DMA 内存，也不会在 DMA 尚未读完时覆盖条带。
- 视频仍绕过 LVGL 像素渲染；新增的成本仅为 8 次 PSRAM 条带复制和窗口命令，不恢复原先 LVGL 分段之间的任务调度空隙。

### 是否解决
- 已静态消除日志中确认的运行期 PSRAM bounce-buffer 动态分配路径；固定 DMA 缓冲在网关 UI 建立前申请一次，申请失败会明确回退 LVGL，而不会运行一段时间后随机失败。
- 未执行 ESP-IDF 构建、编译、烧录或串口监视，当前待用户验证。重点观察是否还出现 `Failed to allocate priv TX buffer`，以及 `[LCD] direct total/period` 能否长期稳定在接近 8～9 FPS 的范围。

## 2026-08-11 网关视频改用 PSRAM 直接 DMA（待板测）

### 日期 / 阶段
- 2026-08-11：固定 40 行内部 DMA 条带方案板测出现 `total≈155 ms`，并发生两帧局部重合、画面最右侧重复最左侧内容，确认该方案既降低帧率又破坏画面完整性，立即弃用。

### 遇到的问题
- 将一帧拆成 8 次独立 `draw_bitmap` 后，每段都重新发送窗口命令并等待 ISR 唤醒 LVGL 任务，累计增加约 53 ms 调度和窗口切换间隙，物理显示周期退回约 158 ms。
- 多次独立设置 ST7789 写入窗口的路径在连续视频下出现列方向重复和帧内容重叠。该异常只在条带方案板测出现，因此不再继续通过缩小条带或调整等待时间修补。

### 改动内容
- 删除固定内部 DMA 条带缓冲及 8 次逐段提交，恢复一次完整 240×320 窗口、一次整帧 `draw_bitmap`，由 LCD SPI IO 在同一窗口内连续排队底层 DMA 块。
- 新增 `main/spi_psram_dma_wrap.c`，通过 ESP-IDF 官方链接包装机制，仅对已经满足外部 DMA 能力的 PSRAM SPI TX 缓冲补充 `SPI_TRANS_DMA_USE_PSRAM`；实际发送仍调用原始 `spi_device_queue_trans`。
- `main/ui_gateway.c`：两个视频画布改为 64 字节对齐的 `MALLOC_CAP_SPIRAM | MALLOC_CAP_DMA` 缓冲，满足 ESP32-S3 外部 GDMA 的 32 字节对齐要求；若申请失败则使用普通内存并回退 LVGL，不进入整帧直传。
- `main/CMakeLists.txt`：加入包装源文件和 `--wrap=spi_device_queue_trans` 链接选项。没有修改本机 ESP-IDF 安装目录。

### 是否解决
- 静态路径上已同时恢复最初较快的一次窗口连续传输，并消除导致 `Failed to allocate priv TX buffer` 的内部 DMA bounce-buffer 分配；PSRAM 缓冲由 SPI master 直接执行外部 DMA 和缓存同步。
- 未执行 ESP-IDF 构建、编译、链接、烧录或串口监视，当前待用户验证。预期画面不再重合或左右重复，`[LCD] direct total` 回到首版约 103～110 ms，并且持续运行不再出现内部 TX DMA 分配失败。

## 2026-08-11 PSRAM SPI DMA 包装符号链接修复（待用户重新构建）

### 日期 / 阶段
- 2026-08-11：用户构建已通过源码编译，但在链接 `lr2021_radio.elf` 时报告 `undefined reference to __wrap_spi_device_queue_trans`。

### 遇到的问题
- `spi_psram_dma_wrap.c` 位于 `main` 静态组件库中。只有 `--wrap=spi_device_queue_trans` 时，链接器不会因其他组件后续产生的包装引用而自动从该静态归档提取独立包装对象，导致包装实现虽然已编译但未进入最终 ELF。

### 改动内容
- `main/CMakeLists.txt`：保留 `-Wl,--wrap=spi_device_queue_trans`，新增 ESP-IDF 自身包装组件采用的 `-u __wrap_spi_device_queue_trans`，强制链接器从 `main` 组件归档提取并保留包装符号。
- 未修改 PSRAM DMA 判断、视频缓冲、LCD 整帧提交或其他运行逻辑。

### 是否解决
- 已按 ESP-IDF 5.5.3 `components/lwip/CMakeLists.txt` 的同类链接写法完成静态修复；用户提供的未定义符号原因已对应消除。
- Codex 未执行 ESP-IDF 构建、编译、链接、烧录或串口监视，当前待用户重新构建验证链接结果。

## 2026-08-11 LCD 整帧直传队列连续化（待板测）

### 日期 / 阶段
- 2026-08-11：分析最新 `D:\tmp\网关.txt`。整帧 LCD 刷新稳定交替出现约 `105 ms` 与 `126 ms` 两组耗时，接收链路仍约 `79 ms / 12.6 FPS`，未发现 DMA 内存、超时或显示提交错误。

### 判断依据
- 12 MHz SPI 发送 240×320 RGB565 的纯像素理论下限为 `102.4 ms`；快组约 `105 ms` 已接近物理极限，慢组多出的约 `21 ms` 不是像素量增加，而是传输过程中 SPI 队列被耗尽后的总线空闲。
- 底层按 40 行把整帧拆为 8 个 DMA 事务。原队列深度 2 只能提前保留两块；网关图像任务与 LVGL 任务同在 CPU1，且图像任务优先级 3 高于 LVGL 的 2。图像任务约 27 ms 的解码/合成会阻止 LVGL 继续补队列，两个事务发送完后 SPI 因无后续事务而停顿。
- 当前画布已是满足外部 DMA 条件的 PSRAM 缓冲，SPI 包装层会设置 `SPI_TRANS_DMA_USE_PSRAM`，不再为每块动态申请内部 TX bounce buffer。因此此前为压低内部 DMA 临时内存占用而把队列深度降到 2 的理由已经不存在。

### 改动内容
- `main/app_config.h`：将 `APP_LCD_SPI_QUEUE_DEPTH` 从 2 恢复为 10，可一次容纳整帧所需的 8 个分块事务；12 MHz SPI、40 行分块、整帧单窗口提交、PSRAM 直接 DMA、1 ms LVGL 延时和现有任务优先级均保持不变。
- 预期 GDMA 在高优先级图像任务运行期间仍有完整后续事务可取，消除约 `126 ms` 的慢组，使刷新主要稳定在约 `105–108 ms`（约 `9.2–9.5 FPS`）。该修改不能突破 12 MHz 下 `9.77 FPS` 的物理上限。

### 是否解决
- 已完成配置修改和静态检查；未执行 ESP-IDF 构建、编译、链接、烧录或串口监视。
- 当前待用户板测。重点观察 `[LCD] direct total/period` 是否不再交替出现约 `126 ms` 慢帧，以及是否持续无 `Failed to allocate priv TX buffer`、`ESP_ERR_NO_MEM` 和画面重叠。

## 2026-08-12 LCD 队列加深板测结论订正与锯齿分析

### 日期 / 阶段
- 2026-08-12：分析更新后的 `D:\tmp\网关.txt`，复核增大 LCD SPI 事务队列后的实际刷新周期，并根据用户反馈的锯齿状画面重新检查 ESP-IDF 分块排队逻辑、任务优先级和 LCD TE 同步条件。

### 新加入的功能
- 本阶段没有新增运行功能，仅补齐队列实验的定量结论、原判断订正和下一轮计时需求。

### 板测数据
- 最新日志包含 97 次整帧 LCD 刷新：平均 `total=116.18 ms`、平均 `period=118.64 ms`，实际显示节奏约 `8.43 FPS`。
- 其中 49 次快组平均 `total=105.55 ms / period=107.97 ms`，48 次慢组平均 `total=127.02 ms / period=129.53 ms`，仍然几乎严格一快一慢交替。
- 与队列深度 2 时的旧数据（平均 `total=116.15 ms / period=118.73 ms`，快组约 `105.62 ms`，慢组约 `126.68 ms`）相比没有有效提升；日志中也没有 `ESP_ERR_NO_MEM`、SPI 发送失败、超时、崩溃或看门狗错误。
- 逐帧关联显示：除日志首个不完整窗口外，所有快组传输区间都只包含 1 次 `[FRAME]` 图像处理完成，所有慢组都包含 2 次。图像解码与合成每次约 27 ms，慢组比快组多约 21.5 ms，二者存在稳定的调度相位关联。

### 原判断订正
- 2026-08-11 记录中“队列深度 2 发送两块后，需要等待下一次 LVGL 循环补队列”的判断不准确。ESP-IDF 的 `panel_io_spi_tx_color()` 会在同一次 `esp_lcd_panel_draw_bitmap()` 调用内部循环拆分整帧；队列满时回收已完成事务，再继续提交剩余事务，不依赖下一轮 `lv_timer_handler()` 才补充数据。
- 当前视频提交运行在 LVGL 定时器回调中。LVGL 任务位于 CPU1、优先级 2，网关图像解码/合成任务同样位于 CPU1、优先级 3。`bsp_lcd_present_video_frame()` 在等待 DMA 完成信号量后，只有等 LVGL 任务重新获得 CPU 才读取 `done_us`，所以现有 `[LCD] direct total/period` 同时包含真实 DMA 时间和 ISR 释放信号量后的任务恢复延迟。
- 因此，增大 SPI 队列即使缩短了部分底层事务等待，也无法消除高优先级图像任务造成的 LCD 提交任务恢复延迟；从最终画面呈现周期看，本次队列加深没有提速收益。后续如需精确区分，应在 LCD 完成 ISR 中记录物理完成时间，并拆分输出 `submit_to_isr`、`isr_to_resume` 和 `total`。

### 锯齿状画面分析
- 当前硬件定义中 `BSP_LCD_TE_GPIO=GPIO_NUM_NC`，LCD 的 TE 同步信号未接入。ESP32 向 ST7789 GRAM 写入新帧时，面板可能仍在扫描显示旧帧；增大队列会改变 8 个 40 行事务的连续程度和写入相位，运动画面可能在同一次面板扫描中混入新旧两帧，表现为移动的斜线、锯齿或撕裂边界。
- 如果锯齿只在人物或摄像头运动时出现、静止后恢复，优先判断为无 TE 同步下的扫描撕裂；如果静态画面也存在固定 40 行错位、列重复或像素偏移，则应转向排查 DMA 分块、缓存一致性或缓冲所有权，不能仅按撕裂处理。
- 队列加深只改变事务在途数量和连续性，不改变 12 MHz SPI 时钟、RGB565 字节数和像素内容。由于实验没有 FPS 收益却出现画面异常，当前 `APP_LCD_SPI_QUEUE_DEPTH` 已恢复为 2；是否恢复原有画面稳定性仍需用户重新构建、烧录和板测确认。

### 是否解决
- 队列加深提速方案已由板测证明无最终 FPS 收益，原根因判断已经订正；锯齿初步指向无 TE 同步下的运动撕裂，但尚缺静态/运动对照画面，当前状态为部分定位、待板测确认。
- Codex 未执行 ESP-IDF 构建、编译、链接、烧录或串口监视；本阶段仅完成日志分析和文档更新。

## 2026-08-12 视频帧差分局部刷新方案准备

### 日期 / 阶段
- 2026-08-12：针对 12 MHz SPI 下整帧 RGB565 理论传输下限 `102.4 ms`、整帧显示上限约 `9.77 FPS` 的约束，检索 GitHub 上 ESP-IDF、LVGL 和 JPEG 分块输出的局部刷新实现，为下一阶段减少实际发送像素数做准备。

### 参考实现与结论
- ESP-IDF 官方 `examples/peripherals/lcd/spi_lcd_touch` 示例使用 `LV_DISPLAY_RENDER_MODE_PARTIAL`，由 LVGL 把无效区域坐标交给 flush 回调，再调用 `esp_lcd_panel_draw_bitmap(x1, y1, x2, y2, pixels)` 只发送该区域；官方示例同样采用小尺寸双 DMA 缓冲和 `trans_queue_depth=10`。
- LVGL 官方 PARTIAL 模式适合文字、按钮和普通 UI 脏区，但摄像头画布每帧整体失效时仍会退化成全屏刷新。视频提速不能只恢复 LVGL PARTIAL，必须在应用层比较前后视频帧并跳过未变化区域。
- JPEGDEC 一类解码器可以按 JPEG MCU 块输出坐标和 RGB565 像素，便于边解码边显示或裁剪解码；但如果所有 MCU 块最终仍全部发送到 LCD，SPI 总字节数不会降低，不能单独解决当前瓶颈。
- 参考链接：ESP-IDF 官方 SPI LCD 示例 `https://github.com/espressif/esp-idf/blob/master/examples/peripherals/lcd/spi_lcd_touch/main/spi_lcd_touch_example_main.c`；LVGL 集成与 PARTIAL 示例 `https://github.com/lvgl/lvgl#integration`；JPEGDEC 分块回调说明 `https://github.com/bitbank2/JPEGDEC/wiki`。

### 推荐的第一版实现
- 保留现有接收、JPEG 解码、旋转换色和 PSRAM DMA 双缓冲主链路，把 240×320 画面划分为 16×16 图块，共 15×20 个图块。
- 对新帧与“LCD 实际已显示参考帧”进行带容差的图块比较。每块可先隔点抽样，只有超过阈值的样本比例达到设定值才标记为变化，避免传感器噪声、JPEG 量化、自动曝光和白平衡微小波动把整屏误判为变化。
- 第一版不提交大量独立小矩形，而是把所有变化图块合并为一个包围矩形，每帧最多调用一次 `esp_lcd_panel_draw_bitmap()`；当包围矩形面积超过全屏约 70% 时，直接回退现有整帧直传，避免矩形打包与窗口命令成本反而更高。
- 每 10～20 帧强制发送一次完整关键帧，用于恢复阈值忽略、偶发失败或显示参考误差造成的残影；后续应根据实测变化率调整关键帧间隔和 70% 回退阈值。
- 建议增加第三个 153600 字节“LCD 已显示参考帧”缓冲。只把实际成功发送的区域复制进参考帧，再用下一帧与该参考帧比较；不能简单用上一张完整解码帧作为参考，否则被容差忽略的小差异会逐帧累积，而软件认为屏幕已经更新。

### 关键内存与提交约束
- 对宽度小于 240 的局部矩形，原全屏缓冲中相邻两行的跨度仍是 240 像素；从矩形左上角取一个指针后直接按“矩形宽×高”提交，会把每行尾部后的错误像素当作下一行数据，造成错行、左右重复、重叠或锯齿。
- 因此任意局部矩形必须逐行打包到一块连续、对齐且满足 `MALLOC_CAP_SPIRAM | MALLOC_CAP_DMA` 的专用缓冲，再将该连续缓冲交给 `draw_bitmap`。该缓冲应在初始化时永久申请，运行期不动态分配；DMA 完成前不得覆盖或复用。
- 如果第一轮需要更低风险的验证，可先只合并为“全宽、局部高度”的水平带。全宽带在原帧中天然连续，不需要重新打包，但节省带宽的能力弱于任意矩形。
- 局部刷新只能减少 MCU 到 ST7789 的写入量，不能提供 TE 同步；它会缩短每次覆盖 GRAM 的持续时间、降低出现撕裂的概率和范围，但不能从原理上保证完全无撕裂。

### 预期收益与观测项
- 要追平当前无线/解码链路约 `12.6 FPS / 79 ms`，平均每帧发送面积必须低于全屏约 77%，并继续扣除差分比较、矩形打包和窗口命令时间；15 FPS 对应约 65%，20 FPS 对应约 49%。运动范围较小的门口固定机位有机会受益，人物占满画面或镜头整体移动时会自动退化为约 9 FPS 的整帧刷新。
- 实施时应新增 `compare_us`、`pack_us`、`dirty_tiles`、`dirty_pixels`、`dirty_ratio`、`rect`、`lcd_dma_us`、`isr_to_resume_us`、`full_fallback` 和 `keyframe` 统计，先用日志确认减少的 SPI 像素量是否大于新增 CPU/PSRAM 成本。

### 是否解决
- 已完成技术路线、参考实现、内存连续性约束、回退策略和必要统计项的准备；尚未修改局部刷新代码，也未进行构建、烧录或板测。
- 当前状态为待用户确认进入实现。后续建议先做“16×16 差分 + 单包围框 + 连续 DMA 打包缓冲 + 70% 整帧回退 + 周期关键帧”，暂不同时引入多矩形队列或 JPEG MCU 流式直刷，以降低再次出现窗口错位和缓冲所有权问题的风险。

## 2026-08-12 LCD 视频完成回调归属与精确计时（待板测）

### 日期 / 阶段
- 2026-08-12：复核用户提供的“队列深度导致视频帧与 LVGL 刷新交错”的分析，并检查 ESP-IDF v5.5.3 的 `esp_lcd_panel_io_spi.c` 实际实现。

### 分析结论
- 图片中“视频直传和 LVGL 共用同一个 SPI panel IO，因此存在共享回调/共享总线竞争”这一点成立。
- “队列深度为 8 会让两次 `draw_bitmap` 的颜色事务直接交错并改写 LCD 窗口，必然造成花屏”这一点不成立。ESP-IDF 的 `panel_io_spi_tx_color()` 会在每次颜色提交前获取 SPI 总线；新的窗口命令会先回收此前在途事务，颜色数据在同一次调用内按块排队，最后一块才设置 `en_trans_done_cb`，不会把一次整帧的中间块当成完成回调。
- 当前工程的真实风险是完成回调原先只检查 `s_video_frame_inflight`。视频提交期间如果仍有 LVGL 事务完成，旧 LVGL 事务的最后回调可能被误判为视频帧完成，导致视频任务提前复用/切换状态，且原 `[LCD] direct total/period` 还混入 ISR 后任务恢复延迟。

### 改动内容
- `main/bsp_lcd.c`：完成回调先按 `s_lcd_pending_count` 消费 LVGL 刷新记录，只有没有待处理 LVGL 事务时才完成视频帧；为视频完成增加单调帧序号、ISR 完成时间戳和序号一致性检查。
- `main/bsp_lcd.c`：视频日志拆分为 `submit`、`submit_to_isr`、`physical`、`isr_to_resume`、`total`、`isr_period`、`resume_period`，其中 `physical` 表示提交函数返回后到 LCD DMA 完成 ISR 的时间，`isr_to_resume` 表示 ISR 释放信号量到视频任务恢复的调度延迟。
- 未修改 LCD SPI 时钟、分辨率、RGB565 格式、队列配置、LVGL 缓冲或图像接收/解码路径；保留用户已有的 `main/app_config.h` 本地改动。

### 是否解决
- 已完成源码级保护和计时埋点，能为下一轮板测区分 LCD 物理传输瓶颈与 CPU/任务恢复延迟。
- 未执行 ESP-IDF 构建、编译、链接、烧录或串口监视，当前待用户板测。重点提供新的 `[LCD] direct seq=... submit=... physical=... isr_to_resume=... total=...` 日志，并确认是否仍有花屏、锯齿或完成序号不匹配。
## 2026-08-12 LCD 完成 ISR 后 LVGL 调度延迟定位（待板测）

### 日期 / 阶段
- 2026-08-12：分析用户更新的 `D:\tmp\网关.txt`，使用新增的 ISR 精确计时字段定位约 8.4 FPS 的剩余瓶颈。

### 板测数据
- `physical` 长期稳定在约 `107.2~107.35 ms`，`submit` 约 `0.6~0.9 ms`，说明 12 MHz SPI 全屏 RGB565 传输已接近物理下限，队列深度不是当前主要瓶颈。
- `isr_to_resume` 呈稳定交替：一组约 `28~31 us`，另一组约 `14.4~17.5 ms`；对应 `total` 约 `108 ms` 与 `122~125 ms`，`resume_period` 约 `110~111 ms` 与 `125~128 ms`。
- 同期 `[FRAME]` 图像接收/解码链路仍约 `77~81 ms / 12.3~12.9 FPS`，无 `completion mismatch`、DMA 错误、超时、崩溃或看门狗记录。

### 根因判断
- `bsp_lcd_present_video_frame()` 在 LVGL 定时器回调中执行；LVGL 任务和网关图像解码/旋转任务都固定在 CPU1。
- 原配置中 `APP_LCD_LVGL_TASK_PRIORITY=2`，网关图像任务优先级为 3。LCD 完成 ISR 释放信号量后，LVGL 任务可能被图像任务继续运行约 15~17 ms，造成每隔一帧的恢复延迟。
- 因此当前平均约 `8.4 FPS` 的主要损失来自 CPU1 调度相位，不是 SPI DMA 物理速度；将 LVGL 提前到 DMA 完成后及时恢复，理论上可把节奏从约 `118~128 ms` 拉近 `108~111 ms`，即约 `9.0~9.2 FPS`。

### 改动内容
- `main/app_config.h`：将 `APP_LCD_LVGL_TASK_PRIORITY` 从 2 调整为 4。该优先级高于网关图像任务 3，低于音频任务 5；保持 `APP_LCD_LVGL_TASK_CORE=1`、SPI 12 MHz、队列深度 10 及其他任务配置不变。

### 是否解决
- 已完成针对性调度修改和静态差异检查；尚未确认实机效果。
- 未执行 ESP-IDF 构建、编译、链接、烧录或串口监视，当前待用户重新构建并板测。重点观察 `isr_to_resume` 是否全部降至几十微秒、`resume_period` 是否稳定在约 `108~111 ms`，以及音频、LVGL UI 和画面完整性是否正常。
## 2026-08-12 网关视频三缓冲消除旋转空窗（待板测）

### 日期 / 阶段
- 2026-08-12：分析更新后的 `D:\tmp\网关.txt`，确认提升 LVGL 任务优先级后的实机结果，并针对剩余约 `125~128 ms` 慢周期修复视频画布缓冲所有权。

### 板测数据与根因
- 最新日志中可完整解析 88 次 LCD 刷新：`physical` 平均约 `107.31 ms`，`total` 平均约 `108.02 ms`；`isr_to_resume` 已稳定在 `26~46 us`，说明此前 CPU1 上约 `15~17 ms` 的任务恢复延迟已经消失。
- 图像源 `[FRAME]` 共 129 帧，周期平均约 `79.15 ms`，范围 `77~83 ms`；无线接收、JPEG 解码和旋转总体快于 LCD，不是当前慢周期来自“下一帧尚未收到”。
- LCD 的 `resume_period` 平均约 `116.02 ms`，其中 29 次大于 `115 ms`，主要交替出现约 `111 ms` 与 `125~128 ms`。旧双缓冲逻辑在开始旋转新帧前会先清空 `s_img_canvas_pending_buf`，并在约 `15~17 ms` 旋转期间设置 `s_img_canvas_writing=true`；LCD 如果恰在该窗口完成，LVGL 定时器没有可提交帧，只能等待旋转结束，慢周期与 `compose` 耗时因此一致。
- 用户已明确此前提高 LCD SPI 速率的板测不可用，本阶段保持 `APP_LCD_SPI_PCLK_HZ=12 MHz`，不再把提高 SPI 时钟作为提速手段。

### 新加入的功能
- `main/ui_gateway.c`：视频画布由前台/后台双缓冲扩展为三缓冲角色：当前 LCD DMA 前台帧、已完成并等待提交的 pending 帧、后台正在旋转的 working 帧；剩余指针组成空闲池。
- 开始旋转时先从空闲池摘下独占缓冲，不再清空或复用 pending；因此第三帧旋转期间，LVGL 仍可立即提交上一张已完成帧。新帧发布时若已有 pending，仅在旋转全部完成后原子替换，并把旧 pending 放回空闲池。
- 初始化日志新增 `image canvas buffers=... bytes_each=153600`，用于确认实机是否成功分配三块 DMA 可用画布。第三块分配失败时自动退化为双缓冲；双缓冲没有空闲块时丢弃新的源帧，优先保护已就绪待显示帧，不再边旋转边制造 pending 空窗。

### 改动边界与预期
- 未修改 LCD SPI 12 MHz、分辨率、RGB565 格式、SPI 队列、LVGL 任务配置、无线协议、JPEG 解码或下一帧请求时序。
- 三缓冲比原双缓冲额外占用约 `153600` 字节 PSRAM。目标是消除约 `125~128 ms` 慢组，使 `resume_period` 主要稳定在约 `110~111 ms`，即约 `9 FPS`；12 MHz 下整帧 RGB565 物理传输仍约 `107 ms`，因此本修改不能突破该物理上限。

### 是否解决
- 已完成缓冲角色、临界区交换、分配降级路径、UTF-8/BOM/换行和 Git 行级差异的静态检查。
- 按 ESP32 协作规则未执行构建、编译、链接、烧录或串口监视，当前待用户板测。重点确认启动日志为 `image canvas buffers=3`，并观察 `[LCD] direct` 的 `resume_period` 是否不再出现约 `125~128 ms` 慢组，同时检查画面完整性和 PSRAM 分配情况。

## 2026-08-12 网关视频三缓冲板测结果

### 日期 / 阶段
- 2026-08-12：分析用户更新的 `D:\tmp\网关.txt`，验证消除旋转空窗后的 LCD 实际刷新周期。

### 板测数据
- 日志包含 LCD `seq=4..42` 的 39 个连续完整帧，序号无缺失。`resume_period` 平均 `111.001 ms`，范围 `110.942~111.056 ms`，标准差约 `0.025 ms`，对应实际显示帧率约 `9.009 FPS`。
- 39 帧中 `resume_period > 112 ms` 和 `> 115 ms` 的数量均为 0；修改前约 `125~128 ms` 的慢周期组已经消失。
- `physical` 平均约 `107.406 ms`，`total` 平均约 `108.015 ms`，`isr_to_resume` 平均 `32 us`、范围 `26~37 us`，说明 LCD 物理传输和任务恢复均保持稳定。
- 图像源共记录 56 帧，平均周期约 `79.143 ms`（约 `12.64 FPS`）；传输平均 `37.036 ms`，解码平均 `11.214 ms`，旋转/换色 `compose` 平均 `16.161 ms`。源帧供给仍快于 LCD 显示。
- 未发现 `direct video present failed`、内存分配失败、传输超时、完成序号不匹配、崩溃或断言。该日志从运行中段开始，未包含启动阶段的 `image canvas buffers=...` 行，因此不能仅凭本文件确认实际分配数量。

### 是否解决
- 已解决。实测证明缓冲修改消除了约 `15~17 ms` 旋转期间的待提交空窗，LCD 周期已稳定在 12 MHz 全屏 RGB565 物理上限附近，并达到约 `9 FPS` 的阶段目标。
- 相比修改前约 `116.02 ms` 的平均显示周期，当前平均周期缩短约 `4.33%`，帧率由约 `8.62 FPS` 提升至约 `9.01 FPS`。继续逼近或达到严格的 `10 FPS` 仍受当前约 `107.4 ms` 物理传输时间限制；在不提高 SPI 速率的约束下，缓冲和调度优化已基本没有可回收的整帧周期空间。
- 本阶段仅分析用户提供的板测日志并更新工程记录；未执行构建、编译、链接、烧录或串口监视。

## 2026-08-12 节点图片准备耗时分析与提速计划

### 日期 / 阶段
- 2026-08-12：在网关局部刷新实现前分析同目录下更新的 `D:\tmp\节点.txt`，先定位节点侧采集与 JPEG 准备耗时，确定源帧供给的优化顺序。本阶段只分析日志和代码，不修改节点运行逻辑。

### 板测数据与耗时大头
- 日志包含 22 组完整 `[CAPTURE] / [ENCODE]` 和 21 组完整 `[PREFETCH]`。图片准备总耗时平均约 `76.714 ms`，其中采集平均 `42.762 ms`、JPEG 编码路径平均 `33.952 ms`。
- 采集内部主要为等待摄像头下一帧 `30.728 ms` 和 640×480 到 320×240 软件降采样 `11.929 ms`。
- 编码内部主要为 JPEG 核心编码 `17.895 ms`、UYVY 到编码器输入顺序重排 `9.535 ms`、约 153600 字节 JPEG 输入缓冲 `calloc`/清零 `5.314 ms`；输出 JPEG 平均约 `4458 bytes`。
- `[TIMING] img_prep=0` 表示发送阶段命中了已经准备好的 JPEG 预取缓存，不代表节点实际准备图片不耗时。当前准备约 `77 ms`，已高于无线接收约 `48~49 ms`，是网关约 `79 ms / 12.6 FPS` 源帧周期的主要限制。

### 根因判断
- 软件降采样、输入缓冲清零和像素重排合计约 `26.78 ms`，连续多次读写同一幅 153600 字节图像，属于第一优先级可回收 CPU/PSRAM 开销。
- `capture_frame()` 每轮主动等待下一次 DVP 帧完成约 `30.7 ms`。摄像头实际连续输出，但当前单一 `safe_buffer` 只在调用采集时等待并取帧，未把下一帧采集与当前 JPEG 编码重叠，因此稳态仍支付完整等帧时间。

### 削减计划
- 第一阶段采用低风险软件融合：保持传感器寄存器、分辨率、JPEG Q10、无线协议和时序不变，新增 JPEG 专用采集路径，把 640×480 UYVY 到 320×240 的隔点降采样与 `Y0,Cb,Y1,Cr` 重排融合为一次循环，直接写入 16 字节对齐的最终 JPEG 输入缓冲，并取消不必要的整块预清零。保留普通 `capture_frame()` 供其他调用点使用。预计准备时间先降至约 `60~63 ms`，源帧能力约 `15~16 FPS`。
- 第二阶段引入原始帧异步预挂起：为采集缓冲建立 `FREE / DMA / READY / PROCESSING` 状态，当前帧完成降采样后、进入 JPEG 核心编码前立即挂起下一帧 DVP 采集；下一轮直接取得冻结的 READY 帧。预计稳态等待接近 0，准备时间降至约 `30~33 ms`，整体瓶颈转回约 `48~52 ms` 的无线链路，理论源帧约 `19~20 FPS`。首帧及低功耗唤醒后的首帧仍允许完整等待。
- 第三阶段仅作独立板测候选：SP0A39 支持 DVP 320×240 子采样，理论上可完全移除软件降采样；但历史传感器输出寄存器和 MCLK 调整曾造成花屏、错行，必须与前两阶段隔离验证。暂不优先启用 JPEG 双任务，以免 Core 0 与无线 TX 争用；也不优先继续降低 JPEG Q10。

### 是否解决
- 已完成节点日志定量拆分、当前代码路径核对和分阶段方案设计；尚未实施提速代码。
- 未执行 ESP-IDF 构建、编译、链接、烧录或串口监视。后续应先实施第一阶段，并由用户板测对比 `[CAPTURE] downsample/total`、`[ENCODE] alloc/reorder/jpeg/total`、`[PREFETCH] prepared` 和网关 `[FRAME] period`，确认收益后再进入异步采集阶段。

## 2026-08-12 节点图片准备第一阶段融合优化（待板测）

### 日期 / 阶段
- 2026-08-12：实施节点图片准备提速第一阶段，在不改变传感器配置、JPEG 参数和无线协议的前提下，消除热路径中的中间图像缓冲清零及第二次完整像素遍历。

### 新加入的功能
- `main/camera_uart.hpp`、`main/camera_dvp.cpp`：新增 `capture_jpeg_input()` JPEG 专用采集接口；仍等待并冻结一张完整 DVP 帧，但将 640×480 UYVY 隔点降采样与编码器要求的 `Y0,Cb,Y1,Cr` 排列融合为一次循环，直接写入 16 字节对齐、无需预清零的 320×240 编码输入缓冲。
- `main/image_transfer.hpp`、`main/image_transfer.cpp`：新增 `encode_prepared_frame()`，验证输入长度、格式和 16 字节地址对齐后直接调用 JPEG 编码器，不再执行 `jpeg_calloc_align()` 和 UYVY 全帧重排。
- `main/app_main.cpp`：JPEG 预取路径和无预取缓存的冷启动发送路径改用上述专用接口；本地相机拍照显示仍使用原 `capture_frame()`，保持原始 UYVY 输出和既有显示行为。
- 新增 `[CAPTURE-JPEG] setup/wait/alloc/cache/fused/total` 和 `[ENCODE-PREPARED] open/output_alloc/jpeg/cleanup/total` 日志，便于板测直接量化融合循环与编码路径耗时。

### 遇到的问题与处理
- Codex `apply_patch` 读取合法 UTF-8、LF 文件时再次出现已知的 `invalid utf-8 sequence` 误报。按永久规则切换为唯一片段 PowerShell 精确替换，并保持所有修改文件为 UTF-8 无 BOM、纯 LF；修改后未发现整文件重写或异常换行。
- JPEG 编码器在 ESP32-S3 上要求输入地址 16 字节对齐。新采集路径使用 `heap_caps_aligned_alloc(16, ...)`，PSRAM 分配失败时退回对齐的普通 8-bit 内存；调用方继续统一使用 `heap_caps_free()` 释放。

### 静态检查
- 已用确定性测试帧逐字节比较原“2 倍降采样后 UYVY 重排”与新融合循环，153600 字节输出完全一致，无差异；输出长度能被 16 整除。
- 已核对预取和冷路径的成功、采集失败、编码失败内存所有权：输入缓冲均在编码调用后释放，JPEG 输出仍交给原发送任务；原本地拍照调用点未切换。
- `git diff --check` 通过；修改文件保持 UTF-8 无 BOM、LF。未修改 LCD SPI 12 MHz、摄像头寄存器、MCLK、分辨率、JPEG Q10、无线分片或任务优先级。

### 是否解决
- 代码阶段已完成，实际提速待板测确认。预期原约 `5.3 ms` 输入缓冲分配/清零和约 `9.5 ms` 独立重排基本消失，融合循环耗时应接近或略高于原约 `11.9 ms` 降采样，图片准备总耗时目标约 `60~63 ms`。
- 按 ESP32 协作规则未执行构建、编译、链接、烧录或串口监视。用户板测时重点观察 `[CAPTURE-JPEG] fused/total`、`[ENCODE-PREPARED] total`、`[PREFETCH] capture/encode/total`、JPEG 输出大小与画面颜色/完整性；确认第一阶段收益后再决定是否进入异步预挂起原始帧。

## 2026-08-12 节点融合优化板测与网关帧节奏分析

### 日期 / 阶段
- 2026-08-12：联合分析更新后的 `D:\tmp\节点.txt` 和 `D:\tmp\网关.txt`，验证节点第一阶段融合优化收益，并排查用户观察到的网关 `[FRAME]` 不再稳定在旧约 `12.6 FPS` 是否由图片队列或画布存储异常造成。

### 节点板测数据
- 节点日志包含 59 组完整 `[CAPTURE-JPEG]`、60 组 `[ENCODE-PREPARED]` 和 60 组 `[PREFETCH]`。融合循环平均 `12.305 ms`、标准差约 `0.200 ms`；编码路径平均 `18.728 ms`，其中 JPEG 核心平均 `18.170 ms`，均保持稳定。
- 对齐输入缓冲分配平均约 `6.7 us`，独立 JPEG 输入 `calloc` 与 UYVY 重排已经从热路径消失。与修改前编码路径平均约 `33.952 ms` 相比，编码阶段直接缩短约 `15.224 ms`。
- 图片预取总耗时平均约 `49.967 ms`，相较修改前约 `76.714 ms` 缩短约 `26.747 ms`，降幅约 `34.9%`；JPEG 输出平均约 `4540 bytes`，范围 `4514~4571 bytes`，未见编码失败。
- 采集等待出现两组相位：29 次短等待平均约 `5.982 ms`、对应采集总耗时约 `18.514 ms`；30 次长等待平均约 `30.895 ms`、对应采集总耗时约 `43.051 ms`。固定融合与编码工作约 `31 ms` 后，下一次采集有时赶上下一个 DVP 帧、有时跨过一个 DVP 周期，造成采集完成间隔主要在约 `40 ms` 与 `78~79 ms` 之间交替。这是新源帧节奏抖动的主要来源。

### 网关接收、存储与显示分析
- 网关日志包含 292 次 `RX done`、290 条带有效周期的 `[FRAME]` 和 LCD `seq=1..153` 的 153 次连续整帧提交。`[FRAME]` 首个成功帧本来不输出周期，文件尾最后一次 `RX done` 尚未来得及输出后续 `[FRAME]`，计数关系没有显示持续丢帧。
- 排除启动阶段后的 285 个稳态处理帧，`[FRAME] period` 平均 `59.351 ms`，即约 `16.85 FPS`；无线传输平均 `40.035 ms`，网关消费平均 `27.498 ms`，其中队列等待平均仅 `0.032 ms`、最大 `1 ms`，重组约 `0.039 ms`、解码约 `11.232 ms`、旋转约 `16.228 ms`。无线完成间隔平均同样为 `59.354 ms`，说明抖动在帧进入网关队列前已经存在，网关处理没有额外拉长节奏。
- 启动首帧发生一次 `decode=113 ms / queue=31 ms`，随后 4 个已排队帧以约 `27~30 ms` 消费速度清空，队列等待短暂达到 `98 ms`；从第 6 个处理帧开始恢复为几乎恒定 0。日志中没有 `image queue full`、丢弃会话、JPEG 解码失败、画布分配失败、`direct video present failed`、超时或完成序号不匹配，因此这属于一次性冷启动开销，不是稳态存储故障。
- LCD `seq=2..153` 的 `resume_period` 平均 `111.005 ms`、标准差约 `0.081 ms`，对应约 `9.009 FPS`；序号无缺失，`physical` 平均约 `107.387 ms`、`total` 平均约 `108.026 ms`。LCD 显示节奏仍然非常稳定。
- 当前每个 LCD 周期平均处理约 `1.875` 张源帧：152 个显示间隔中 133 次处理 2 张、19 次处理 1 张。三缓冲按设计保留最新完成的 pending 帧并回收更旧 pending，因此会主动跳过过时待显示帧，但不会覆盖正在 DMA 的前台画布。源帧约 `16.85 FPS` 高于 LCD 约 `9.01 FPS` 后，这是正常的最新帧策略，不是图片存储损坏。

### 结论订正与下一步
- 修改前约 `79 ms / 12.6 FPS` 是节点准备受限时的源帧供给周期，不是网关图片存储必须维持的固定帧率。第一阶段优化后，源帧处理平均已经提高到约 `16.85 FPS`，因此不再稳定在旧 `12.6 FPS` 是预期变化。
- 用户感觉到的节奏不均匀有数据依据，但根因是节点采集等待的 DVP 相位长短交替，经无线链路后表现为网关约 `48~72 ms` 的源帧周期波动；当前没有证据指向网关队列或三画布所有权错误。
- 目前不建议修改网关存储或画布逻辑。下一性能阶段应在节点侧异步预挂起原始帧，隐藏长短不一的 DVP 等待，使下一张 JPEG 更早且更稳定地准备完成；若要继续精确确认显示选择，可另加源会话号、pending 替换次数和实际显示会话号统计，但这不是修复当前数据链路所必需。
- 本阶段仅分析用户板测日志并更新工程记录；未修改运行代码，未执行 ESP-IDF 构建、编译、链接、烧录或串口监视，也未暂存、提交或推送。

## 2026-08-12 节点原始帧异步预挂起（待板测）

### 日期 / 阶段
- 2026-08-12：实施节点图片准备提速第二阶段，将 DVP 原始帧采集与当前帧的融合降采样、JPEG 编码及无线发送窗口重叠，目标是消除稳态约 `6 ms / 31 ms` 交替的等帧时间。

### 新加入的功能
- `main/camera_uart.hpp`、`main/camera_dvp.cpp`：复用原有 5 块 `640x480` DMA 缓冲，不增加 PSRAM 占用；缓冲角色由原 4 块轮转加 1 块安全缓冲改为 3 块持续停车缓冲和 2 块原始快照槽。
- 快照槽新增 `FREE / DMA / READY / PROCESSING` 所有权状态，ISR 和任务之间使用独立 `portMUX_TYPE` 保护。只有 `FREE` 槽可交给 DVP DMA；DMA 完成后转为 `READY`，任务取得后转为 `PROCESSING`，融合完成或错误退出时归还 `FREE`。
- JPEG 连续路径启用自动预挂起。ESP-IDF 5.5.3 DVP 驱动每帧结束时先索取并启动下一事务、再回调上一事务完成；新状态机利用该顺序在当前快照完成前就让另一空闲快照槽开始接收下一原始帧，因此当前帧执行约 `12 ms` 融合和约 `19 ms` JPEG 编码时，下一帧 DVP 已在后台采集。
- 普通 `capture_frame()` 本地拍照接口保持按需模式，不持续占用快照槽；JPEG 模式下一轮优先直接消费最新 `READY` 帧。超过 `800 ms` 的冻结原始帧会丢弃并重新捕获，避免间歇拍照使用旧画面。
- `[CAPTURE-JPEG]` 日志新增 `prefetched`、`slot`、`raw_age` 和 `prearm` 字段。`prefetched=1` 表示进入采集接口时已经有可直接处理的原始帧；`wait` 用于确认是否仍支付 DVP 等帧时间；`prearm=1` 表示下一原始帧管线仍处于预挂起状态。

### 异常路径与静态检查
- 二值信号量仅作为 ISR 唤醒提示，快照槽状态作为真实所有权；清空残留信号量后会再次检查 `READY`，避免帧恰好在检查窗口完成时丢失唤醒并误等 5 秒。
- 输出缓冲分配失败、短帧和普通失败路径都会释放 `PROCESSING` 槽；采集超时会取消连续请求，并停止、清空状态后重启 DVP 控制器，避免槽永久停在 `DMA`。
- `power_down()` / `low_power_standby()` 在停止控制器后清空所有请求和槽状态，再释放 3+2 块 DMA 缓冲；初始化各个分配、回调注册和控制器启用失败分支均释放已分配内存并清除回调上下文中的旧指针。
- 已核对 ESP-IDF 5.5.3 `esp_cam_ctlr_dvp_cam.c`：DMA 完成 ISR 的准确顺序为 `esp_cam_ctlr_dvp_start_trans()` 调用 `on_get_new_trans`，随后才调用旧事务的 `on_trans_finished`，与双快照状态迁移设计一致。
- `git diff --check` 通过；修改文件保持 UTF-8 无 BOM、LF。未修改传感器寄存器、MCLK、JPEG Q10、无线协议、LCD SPI 12 MHz或网关三缓冲逻辑。

### 是否解决
- 源码阶段已完成，实际帧率与稳定性待用户板测。预期首帧仍需等待一张 DVP 帧，稳态 `[CAPTURE-JPEG] prefetched=1` 且 `wait` 接近 0；融合加 JPEG 准备总耗时目标约 `30~33 ms`，无线链路应重新成为源帧供给瓶颈。
- 板测重点观察：连续 JPEG 路径是否稳定命中 `prefetched=1`，`[PREFETCH] capture/total` 是否不再出现约 `43/62 ms` 长组，是否存在 `capture failed`、DVP 重启、短帧、陈旧画面、花屏或 JPEG 编码异常，并对比网关 `[FRAME] period` 的平均值和抖动范围。
- 按 ESP32 协作规则未执行构建、编译、链接、烧录或串口监视；未暂存、提交或推送。

## 2026-08-12 节点异步预挂起板测通过与局部刷新实施计划

### 日期 / 阶段
- 2026-08-12：用户已完成节点异步预挂起版本的构建、烧录和实机测试；串口日志与代码状态机一致，连续画面无花屏，上一阶段由“待板测”订正为“板测通过”。

### 上一阶段板测结论
- 节点日志中 57/57 次 `[CAPTURE-JPEG]` 均为 `prefetched=1`、`prearm=1`，两个原始快照槽交替工作，DVP 等帧平均约 `2.58 us`。
- 节点图片准备平均约 `32.83 ms`，稳态发送任务平均约 `53.14 ms`；网关稳态源帧周期平均约 `54.47 ms`，即约 `18.36 FPS`，瓶颈已经从采集/JPEG 转移到无线会话。
- 网关 LCD 整帧物理传输平均约 `107.42 ms`，显示周期约 `111.01 ms`，即约 `9.01 FPS`；当前源帧供给平均约为显示能力的 2.04 倍。
- 两端日志未出现采集失败、DVP 重启、JPEG 异常、NACK/CRC/重传、队列满、LCD 完成序号缺失或完成超时；用户确认实际画面无花屏。

### 当前版局部刷新目标
- 保留现有无线接收、JPEG 解码、旋转换色和网关三画布所有权链路，只减少 MCU 到 ST7789 的实际发送像素数。
- 以当前约 `54.47 ms / 18.36 FPS` 的源帧周期为新基线。要跟上全部源帧，平均提交面积理论上需低于全屏约 50.7%；考虑差分比较、矩形打包和窗口命令开销，首阶段目标为平均包围矩形低于全屏约 45%～48%。
- 70% 继续作为局部矩形到整帧直传的回退阈值；接近该阈值时仍可比整帧约 9 FPS 更快，但不能跟满当前源帧。

### 第一版实施方案
1. 将 240×320 LCD 画面划分为 16×16 图块，共 15×20、300 块；对最新完整旋转帧与“LCD 实际显示参考帧”进行带 RGB565 通道容差的隔点抽样比较。
2. 将所有变化图块合并成单个 16 像素对齐包围矩形。无变化时跳过 LCD 提交；包围矩形面积达到或超过全屏 70% 时回退现有整帧直传。
3. 新增一块独立的 153600 字节显示参考帧。当前工程已有三块画布，该参考帧是额外第 4 块全帧状态缓冲，不参与前台、pending、working/空闲池交换，避免比较期间出现引用所有权竞争。
4. 新增一块启动时永久申请的连续 PSRAM DMA 打包缓冲。任意窄矩形必须逐行从完整画布打包为连续数据，禁止直接用带 240 像素行跨度的原画布子指针提交。
5. 在 `bsp_lcd.c/.h` 增加同步局部矩形提交接口，复用现有 DMA 能力检查、`s_video_frame_inflight`、完成 ISR、信号量、超时和完成序号校验；DMA 完成前不得覆盖或复用打包缓冲。
6. 只有局部或整帧 DMA 成功完成后，才把实际发送区域复制进显示参考帧；被容差忽略或提交失败的区域不得提前更新参考帧。
7. 每 15 张候选显示帧强制发送一次完整关键帧；参考帧未初始化、局部缓冲分配失败、局部提交失败或脏区过大时均安全回退整帧。
8. 串口新增 `[DIRTY]` 和局部 `[LCD]` 统计，至少包含 `compare_us`、`pack_us`、`dirty_tiles`、`dirty_pixels`、`dirty_ratio`、`rect`、`rect_pixels`、`rect_ratio`、`lcd_dma_us`、`full_fallback`、`keyframe` 和跳过提交次数。

### 实施与验证边界
- 第一轮只实现“16×16 差分 + 单包围框 + 连续 DMA 打包 + 70% 回退 + 15 帧关键帧”，暂不加入多矩形队列、JPEG MCU 流式直刷或 TE 同步，避免同时扩大变量范围。
- Codex 只实施代码并进行静态检查，不执行 ESP-IDF 构建、编译、烧录或串口监视。代码达到可测状态后，由用户构建烧录并反馈启动分配日志、`[DIRTY]`、`[LCD]` 以及实际画面现象，再根据脏区比例和残影情况调整容差与阈值。
- 当前状态：计划已确认，进入第一版实现。

## 2026-08-12 视频帧差分局部刷新第一版实现（待用户烧录板测）

### 日期 / 阶段
- 2026-08-12：按已确认计划完成第一版局部刷新代码，当前已达到可由用户构建、烧录并观察串口的阶段。

### 新加入的功能
- `main/app_config.h`：新增局部刷新开关和首轮可调参数。图块为 `16×16`，块内每 2 像素抽样一次；RGB565 通道差阈值为 R/B 2、G 3，单块至少 12% 抽样点变化才标记脏块；单包围框达到全屏 70% 时整帧回退，每 15 张候选帧强制一张关键帧。
- `main/bsp.h`、`main/bsp_lcd.c`：把原同步整帧提交内部逻辑扩展为同步区域提交，新增 `bsp_lcd_present_video_region()` 和最近一次同步视频提交总耗时读取接口；继续复用原 DMA 能力检查、在途保护、ISR 完成信号量、1000 ms 超时和完成序号校验。
- `main/ui_gateway.c`：在原三画布之外永久申请一块 153600 字节“LCD 实际显示参考帧”和一块同容量连续 PSRAM DMA 打包缓冲；分配失败时保留整帧显示能力并在启动日志明确报告。
- 新帧与参考帧按 300 个图块进行容差比较，所有脏块合并成一个图块对齐包围矩形。无脏块且非关键帧时跳过 LCD 提交；窄矩形逐行打包为连续数据后提交；脏区过大、资源不足或局部提交失败时回退原整帧路径。
- 参考帧只在同步 LCD DMA 成功完成后更新：整帧成功则复制全帧，局部成功则只复制实际发送矩形。比较、打包、参考更新和同步提交均位于 LVGL 定时提交任务；前台画布在调用返回前不会进入三缓冲空闲池，打包缓冲在 DMA 完成前不会复用。

### 新增串口观测
- 启动时输出 `dirty refresh buffers`，确认 `reference=ok`、`pack_dma=ok`、单块字节数、图块尺寸、70% 回退阈值和 15 帧关键帧间隔。
- 每次实际局部提交由底层输出 `[LCD] region`，包含区域坐标、像素数、提交耗时、物理 DMA 耗时、ISR 恢复延迟、总耗时和相邻完成周期；整帧仍保留原 `[LCD] direct` 格式。
- 每张候选显示帧输出 `[DIRTY]`，包含 `mode=skip/region/full`、`compare_us`、`pack_us`、`ref_us`、脏块/脏像素比例、包围框/包围框比例、`lcd_dma_us`、`full_fallback`、`keyframe`、累计跳过数和提交结果。

### 静态检查
- 参数展开结果：全屏 300 个图块，每块抽样 64 点，达到 8 个变化点即判定脏块；参考帧和打包缓冲各 153600 字节。按当前 `54.47 ms` 源帧与 `107.42 ms` 整帧物理传输基线，跟满源帧的理论面积上限约 50.7%。
- 已核对窄矩形按源帧 240 像素行跨度读取、按矩形宽度连续写入打包缓冲；传给 `draw_bitmap` 的数据长度和窗口宽高一致，不会把原帧行尾误当作下一行。
- 已核对三画布交换：pending 成为 front 后才释放旧 front；同步局部/整帧提交返回前，当前 front 不在 back/spare 空闲池；网关图像任务只能取得空闲画布，不会改写在途 DMA 源。
- 已核对局部失败路径：参考帧不会提前更新；可立即回退时改走整帧，超时导致底层仍在途时整帧保护会拒绝二次提交，不会复用未知状态的 DMA 缓冲。
- `git diff --check` 在 CRLF 文件使用 `cr-at-eol` 规则后通过；修改文件均为合法 UTF-8、无 BOM，`app_config.h` 保持纯 CRLF，其余修改文件保持纯 LF；没有整文件异常重写。

### 是否解决 / 下一步
- 源码第一版已完成，尚未由 Codex 构建、编译、烧录或启动串口监视，当前状态为待用户构建烧录。
- 用户首次测试先确认启动行 `reference=ok pack_dma=ok`，然后观察 `[DIRTY]` 是否出现 `mode=region`，`rect_ratio` 是否经常低于 70%，`lcd_dma_us` 是否随区域像素数下降；同时检查静止画面残影、运动画面撕裂/错行/花屏、15 帧关键帧恢复效果和整帧回退是否正常。
- 请保留首轮网关串口日志。收到板测日志和实际画面反馈后，再决定是否调整颜色容差、12% 块阈值、70% 回退阈值或关键帧间隔；首轮不继续叠加多矩形或 MCU 流式直刷。

## 2026-08-12 网关显示偶发花屏停更记录与 LCD D0/D1 功能连通预检

### 偶发现象记录
- 用户实机观察到一次网关屏幕花屏后停止更新，但串口中的无线接收与图像处理日志仍持续输出；随后继续观察时暂未再次复现。
- 用户当场提供的日志时间为 `285229~285391 ms`：`[FRAME] period=54 ms (18.5 FPS)`、`transfer=40 ms`、`consume=27 ms`、`queue=0`、`reassemble=0`、`decode=11 ms`、`compose=16 ms`，同时 `radio_ping: RX done` 继续保持约 `54 ms` 周期。
- 上述证据说明当时无线接收、重组、JPEG 解码和旋转换色仍在运行，异常范围集中在 LCD/LVGL 显示侧；但用户提供的这一小段没有同期 `[LCD]`、超时、完成序号或显示任务状态，因此目前不能唯一确定是 LCD DMA、完成回调、LVGL 定时器还是画布提交停滞。
- 本轮不根据不完整证据修改显示状态机，先把它登记为“偶发、暂未复现”的已知问题。若再次发生，保留故障前后最后一条 `[LCD] direct`、任何 `video frame transfer timeout` / `completion mismatch` / `direct video present failed` 以及随后仍在刷新的 `[FRAME]`，再按该分界做针对性修复。

### 局部刷新状态订正
- 局部刷新不再作为当前方向；工作区运行代码已经回到原有单数据线 SPI、三画布、整帧直传路径，没有继续加入 `[DIRTY]` 局刷逻辑。本节保留之前的计划和板测记录，仅作为历史过程，不代表当前继续实施局刷。

### D0/D1 功能连通预检
- 原理图网络已核对：LCD_D0 经 0Ω 电阻连接 GPIO48，LCD_D1 经 0Ω 电阻连接 GPIO43，LCD_CLK 为 GPIO44，LCD_CS1 为 GPIO47。当前 4 线单数据线协议中，D0(GPIO48) 用作 D/C，D1(GPIO43) 用作 SDA/MOSI。
- `main/app_main.cpp` 新增一次性启动预检：网关成功初始化 LCD 后、启动 LVGL 网关界面前，先调用现有 `bsp_lcd_show_test_pattern()`，保持 2 秒，再进入正常界面。
- 串口将打印 `[LCD-LINK] single-line precheck: D0/DC=GPIO48 D1/SDA=GPIO43 CLK=GPIO44 CS=GPIO47`，随后提示预期图案。屏幕应从左到右显示红、绿、蓝、黄、青、品红、白、黑 8 条边界整齐、互不重复的纯色竖条。
- 该预检仍使用当前单数据线 SPI，不发送 ST7789V3 双数据线模式命令。若 8 条色条正确，可确认 D0 的命令/数据选择功能和 D1 的串行数据功能在当前链路上均可用，再进入真正的双数据线驱动设计；若出现摄像头残影、重复列、错位或花屏，则预检失败，停止双线实施并先查当前 LCD 链路。

### 当前验证边界
- 本轮只完成源码和工程日志修改，并进行静态差异检查；未执行 ESP-IDF 构建、编译、链接、烧录或串口监视。
- 下一步由用户构建并烧录网关，观察启动最初 2 秒的 8 条纯色竖条及 `[LCD-LINK]` 日志。用户确认预检通过之前，不开始 ST7789V3 双数据线协议实现。

## 2026-08-12 LCD D0/D1 连通板测通过与双数据线协议实证版本

### 连通板测结论
- 用户已烧录上一版并确认启动时 8 条纯色竖条正常显示。由此确认当前单线协议下 GPIO48→LCD_D0/D/C、GPIO43→LCD_D1/SDA、GPIO44→LCD_CLK、GPIO47→LCD_CS 的功能链路正常，排除 D0/D1 未焊接或基础连通故障。

### 双数据线验证依据
- ST7789V3 数据手册 8.3.6 规定：写 `E7h=10h` 进入 2-data-lane；进入后命令仍按 3 线 9-bit 格式在 SDA 上传输，显存数据同时使用 SDA 与 WRX 两条输入线。
- 手册同时标注双线接口对应 `IM[3:0]=0101`，而当前 4 线 8-bit 单线协议能够正常工作，是否允许通过 `E7h` 从当前面板绑定位动态切换仍需实机验证，不能只根据 D0/D1 均连接就直接判定支持。

### 新增纯验证固件
- 新增 `main/lcd_dual_lane_test.c/.h`，测试与正常 `esp_lcd` 驱动隔离：先释放正常 SPI panel IO，再以软件时序完成硬复位、单线初始化和 `E7h=10h`，随后使用 3 线 9-bit 命令设置窗口，并以 D1(GPIO43)+D0(GPIO48) 双线发送 RGB565 静态彩条。
- 曾短暂加入“上下半屏使用相反 lane 位序”的诊断方案；用户实测该方案出现花屏。分析确认它在半帧中途重新发送窗口命令且故意注入一半错误位序，会额外引入窗口回卷/覆盖变量，不适合作为双线可用性的简单判据，现已撤销。
- 当前验证固定为单一全屏窗口和单一位序：`SDA1=D7/D5/D3/D1、SDA2=D6/D4/D2/D0`，整屏只发送一次红、绿、蓝、黄、青、品红、白、黑 8 条竖向纯色条，不再拆分上下半屏，也不尝试反向 lane。
- 当前构建是专用验证版本：双线发送结束后停在测试画面，不恢复单线、不启动 LVGL 网关界面和视频显示。若不支持或画面错误，直接回退该验证改动重新烧录；若整屏 8 色条正确，再按该固定 lane 位序把连续视频路径替换为 ESP32-S3 硬件 DIO DMA。
- 串口关键行包括 `[LCD-DUAL] probe start`、固定 lane 位序说明和 `[LCD-DUAL] probe complete`；完成行记录软件探测发送整帧的耗时，该耗时只验证协议，不代表最终硬件 DIO DMA 帧率。

### 当前验证边界
- 已进行源码差异和静态检查；按 ESP32 协作规则未执行构建、编译、链接、烧录或串口监视。
- 当前下一步是用户构建烧录此专用验证版本，确认是否出现完整、无错位的全屏 8 色条，并提供 `[LCD-DUAL]` 串口行。未取得实机结果前不把双线接入连续视频路径。

## 2026-08-12 双线首轮花屏定位：窗口命令格式不匹配

### 首轮实测
- 用户烧录固定 lane、单一全屏窗口版本后仍观察到花屏。串口完整输出：`probe start`、固定 lane 位序以及 `probe complete`；76,800 像素的软件双线发送耗时 `1,956,740 us`，程序随后按设计停在验证画面。
- 日志证明测试任务没有崩溃或中途停止，软件时序完整跑完；该耗时仅为 GPIO bit-bang 探测速度，不能用于评价最终硬件 DIO DMA 帧率。

### 配置问题判断
- 首轮代码通过当前已验证的 4 线 8-bit 协议写入 `E7h=10h` 后，立即把 `CASET/RASET/RAMWR` 切成 3 线 9-bit 命令格式。
- 当前面板上电后能够使用标准 4 线 8-bit SPI，说明实际接口绑定位仍按 4 线解析命令；`E7h` 的 SPI2EN 位用于使能显存数据双 lane，并不能在软件中改变面板的 IM 硬件绑定位。
- 因此首轮花屏更符合“E7 后窗口命令未被正确解析，双线像素写入旧窗口/错误地址”，不是程序只发送了半帧，也不是串口所示的软件循环卡死。

### 第二轮单变量修正
- 删除 E7 后的 3 线 9-bit 命令发送函数。初始化、`E7h`、`CASET`、`RASET`、`RAMWR` 全部继续使用已验证的 4 线 8-bit命令格式；只在 `RAMWR` 之后把 RGB565 像素改为 D1(GPIO43)+D0(GPIO48) 双 lane。
- 保持单一全屏窗口、固定 lane 位序、12 MHz 以外的正常驱动配置不参与测试；软件探测仍以低速 GPIO 时序发送，完成后停在画面，不恢复网关业务。
- 第二轮预期串口包含 `command=4wire-8bit, RAM data=2lane`。若全屏 8 色条正确，说明该面板采用“4 线命令 + 双 lane RAM 数据”的兼容路径；若仍花屏，再检查 E7 是否在当前 IM 绑定位下生效和双 lane 的控制器位序，而不再改窗口或拆分画面。

## 2026-08-12 双线第二轮板测失败：排除窗口命令格式为唯一根因

### 板测结果
- 用户已烧录“4 线 8-bit 命令 + 双 lane RAM 数据”第二轮验证版本，屏幕仍然花屏，未形成完整全屏 8 色竖条。
- 串口完整输出 `probe start`、`command=4wire-8bit, RAM data=2lane`、`probe complete` 和验证固件停止行；第二轮软件发送耗时为 `1,956,686 us`、约 `0.51 FPS`。
- 第一轮发送耗时为 `1,956,740 us`，两轮只相差 `54 us`、约 `0.0028%`。这说明软件 GPIO 双线循环的执行长度和节奏高度一致，没有少发半帧、任务提前退出或中途卡死。

### 已排除项
- 第二轮的 `CASET`、`RASET`、`RAMWR` 均继续采用当前面板已验证可工作的 4 线 8-bit 命令格式，因此首轮“切换到 3 线 9-bit 后窗口命令无法解析”不是造成花屏的唯一原因。
- 测试保持单一全屏窗口、单次 `RAMWR`、固定 lane 位序，没有上下半屏切换、反向 lane 或连续视频/三缓冲参与；本次花屏与此前无效的上下半屏诊断方案无关。
- 串口日志没有显示 GPIO/I2C 初始化错误，LCD 复位、背光和验证任务均按顺序完成；当前问题集中在 ST7789V3 是否真正接受双 lane RAM 数据以及双 lane 数据编码/位序。

### 当前结论与后续分支
- 当前实机已经证明 D0、D1 分别在原 4 线单数据线协议中功能连通，但尚未证明 `E7h=10h` 在本屏的实际 IM 硬件绑定位下能够把显存写入切为双 lane。
- ST7789V3 手册把 2-data-lane 与 `IM[3:0]=0101` 配套描述；当前屏又能正常运行标准 4 线 8-bit 协议，说明面板的实际 IM 选择/厂商定制行为仍是关键不确定项。软件写 E7 不能改变物理 IM 绑定位。
- 另一待验证项是两条 lane 在每个 SCL 周期上的实际位映射是否与当前 `SDA1=奇数位、SDA2=偶数位` 假设一致。后续若继续双线，不再改窗口或画面结构，应使用逻辑分析仪直接观测 E7、RAMWR 和前若干双线时钟，或设计最小像素签名测试逐项确定 E7 生效状态和 lane 编码。
- 本轮按用户要求只记录板测结果，不修改当前双线验证源码，不回退，也不继续尝试新的协议组合；未执行构建、编译、烧录或串口监视。

## 2026-08-13 双线 SPI 分阶段软件色块诊断版本（待用户烧录板测）

### 日期 / 阶段
- 2026-08-13：用户授权直接修改专用验证固件，通过串口阶段标记和 LCD 色块结果继续定位双线花屏；本轮不接入正常视频路径。

### 新加入的功能
- `main/lcd_dual_lane_test.c` 将原来的单次双线彩条改为约 45 秒的顺序诊断。阶段 1 先用纯软件 4 线单数据线依次刷新全屏红、绿、蓝和 8 条竖向彩条，用于验证本测试代码自身的复位、初始化、窗口范围、RGB565 字节顺序和软件时钟。
- 阶段 2、3 按 ST7789V3 手册描述，在 `E7h=10h` 后用 3 线 9-bit 格式发送 `CASET/RASET/RAMWR`，分别测试 `D1=奇数位、D0=偶数位` 和交换后的 `D1=偶数位、D0=奇数位`。
- 阶段 4、5 保留当前面板已经验证可识别的 4 线 8-bit 窗口命令，同样分别测试上述两种双 lane 位映射。每个双线候选阶段都硬复位并重新做单线初始化，避免前一阶段的 `SPI2EN`、窗口地址或显存写入状态串扰。
- 每个阶段串口均输出 `[LCD-DUAL] stage=... begin/complete`，包括命令格式、lane 映射、RGB565 色值、软件发送耗时、画面保持时间和预期图案。双线候选均固定输出红、绿、蓝、黄、青、品红、白、黑 8 条竖向色条，完成后保持 3 秒供观察。
- 长时间 GPIO 软件刷屏每 16 行主动让出一次调度，避免专用验证循环长期占用 CPU；让出期间保持 CS 有效且不产生额外 SCL 边沿，不改变面板收到的数据位数。

### 遇到的问题与判读方法
- 前两轮只分别验证过“3 线命令 + 固定位序”和“4 线命令 + 固定位序”，两轮都花屏，无法区分 `IM[3:0]`/命令格式不兼容与 lane 位映射相反。新版本把这两个变量拆成四个独立、可复现阶段。
- 若阶段 1.1～1.4 不能依次正确显示红、绿、蓝和 8 色条，说明软件基准本身不成立，阶段 2～5 的结果不能用于判断双线协议。
- 若阶段 1 正常且阶段 2～5 中某一阶段出现边界完整、颜色顺序正确的 8 条竖条，即可确定该面板实际接受的命令格式和 lane 映射。若四个双线阶段全部花屏，则主要证据指向当前面板的 IM 硬件绑位不支持通过 `E7h` 动态进入双数据线，而不是 ESP-IDF 缺少 Kconfig 开关。

### 是否解决 / 验证边界
- 诊断源码已完成，当前状态为待用户构建、烧录和逐阶段观察；双线花屏根因尚未解决，不能把静态核对写成板测通过。
- 按 ESP32 协作规则，Codex 未执行构建、编译、链接、烧录或串口监视；未暂存、提交或推送。用户板测后需提供完整 `[LCD-DUAL]` 串口行，并分别反馈阶段 1.1～1.4、2、3、4、5 的实际画面。

### 2026-08-13 测试流程订正：删除重复单线基准
- 用户指出单线刷新已经完成板测并确认正常，本轮再次执行软件单线红、绿、蓝和彩条没有新增证据，且容易与真正的双线阶段混淆。因此已从 `main/lcd_dual_lane_test.c` 删除原阶段 1 及其专用发送代码。
- 四个双线候选重新编号为阶段 1～4：阶段 1/2 为 3 线 9-bit 命令配两种 lane 映射，阶段 3/4 为 4 线 8-bit 命令配两种 lane 映射。每阶段仍独立硬复位、单线初始化、写 `E7h=10h` 后再发送双线彩条。
- 串口预计总测试时间由约 45 秒缩短为约 22 秒；板测只需分别记录阶段 1、2、3、4 的显示结果。当前仍未构建、未烧录，待用户验证。

## 2026-08-13 双线四阶段全部失败后的 CMD2EN 解锁验证版

### 日期 / 阶段
- 2026-08-13：用户确认上一版阶段 1～4 均不能正常显示，并补充主板原理图只引出 `LCD_D0`、`LCD_D1` 两根数据线；本轮针对 ST7789V3 扩展命令访问条件修改专用板测固件。

### 新加入的功能与改动
- ST7789V3 手册确认 `SPI2EN (E7h)` 位于 Command Table 2。若屏内 `EXTC` 绑低，复位后直接发送 `E7h=10h` 不会执行；必须先发送 `CMD2EN (DFh)` 及参数 `5A 69 02 01`，其中最后一字节 `EN=1` 允许在 `EXTC=Low` 时执行 Command Table 2。
- `main/lcd_dual_lane_test.c` 现于每个阶段独立硬复位和 4 线单线初始化后，先以当前已验证可用的 4 线协议发送 `DF 5A 69 02 01`，再发送 `E7 10`，等待 10 ms 后才切入双数据线测试。
- 串口新增 `send CMD2EN: DF 5A 69 02 01, then SPI2EN: E7 10`，并明确标记 `write-only, no panel readback`；这只能证明软件已发送序列，不能伪称 LCD 已接受或寄存器已读回确认。
- 删除上一版未解锁条件下已经失败的 4 线/3 线四组合枚举。新版本只保留手册明确规定的 3 线 9-bit 命令格式，阶段 1/2 分别测试两种 SDA/WRX 位映射；两阶段均输出同一组 8 色竖条，每阶段保持 4 秒，串口预计总流程约 12 秒。

### 参考工程核对与硬件边界
- 用户提供的 GlassOnTin/z7020 链接中，LCD RTL 实际使用普通 `MOSI + DC + SCK + CS` 的 4 线单数据线 SPI；README 中的 QSPI 内容对应 FPGA 配置 Flash，不能作为 ST7789V3 双数据线协议或 lane 位序的依据。
- 当前主板 `LCD_D1(GPIO43)`、`LCD_D0(GPIO48)`、`LCD_CLK(GPIO44)`、`LCD_CS1(GPIO47)` 与手册双线模式所需 `SDA`、`WRX`、`DCX/SCL`、`CSX` 的外部信号数量和复用关系相符；但手册同时要求屏内 `IM[3:0]=0101`。主板引出 D0/D1 和驱动芯片本身支持双线，不能单独证明 LCD 玻璃/FPC 内部 IM 已按 `0101` 绑位。

### 是否解决 / 验证边界
- 当前最具体的软件遗漏已修正，状态为待用户板测。若阶段 1 或 2 正确显示 8 色条，可确认此前根因是 Command Table 2 未解锁，并确定实际 lane 位序；若两个阶段仍均失败，则证据将主要指向屏内 `IM[3:0]` 不是 `0101`、模组未开放该模式或存在未公开厂商初始化要求。
- 按 ESP32 协作规则，Codex 只进行源码、手册和静态差异核对，未执行构建、编译、链接、烧录或串口监视；未暂存、提交或推送。

## 2026-08-13 参考 Espressif 官方多线 LCD 结构改为 SPI3 硬件 DIO DMA 板测版

### 日期 / 阶段
- 2026-08-13：用户决定停止继续用 GPIO 软件循环判断双线，改为参考 Espressif 官方 QSPI LCD 和 SPI Master 多线事务实现，先制作 ST7789V3 双数据线硬件 DIO 静态色条板测版本。

### 官方参考资料
- Espressif 官方 ESP32-S3 ST77903 QSPI LCD 例程：<https://components.espressif.com/components/espressif/esp_lcd_st77903_qspi/versions/1.0.0/examples/qspi_without_ram?language=en>
- Espressif 官方带 GRAM 的 QSPI LCD 例程（SPD2010/GC9B71/SH8601）：<https://components.espressif.com/components/espressif/esp_lcd_spd2010/versions/2.0.0/examples/qspi_with_ram?language=en>
- ESP-IDF 5.5.3 ESP32-S3 SPI Master 多线事务说明：<https://docs.espressif.com/projects/esp-idf/en/v5.5.3/esp32s3/api-reference/peripherals/spi_master.html#transaction-line-mode>
- ESP-IDF 5.5.3 官方 `esp_lcd` SPI DMA 像素发送实现：<https://github.com/espressif/esp-idf/blob/v5.5.3/components/esp_lcd/spi/esp_lcd_panel_io_spi.c#L383-L391>
- ESP-IDF 5.5.3 官方 DIO 事务标志定义：<https://github.com/espressif/esp-idf/blob/v5.5.3/components/esp_driver_spi/include/driver/spi_master.h#L108-L119>

### 新加入的功能与改动
- `main/lcd_dual_lane_test.c` 保留 ST7789V3 专用命令前导：每个阶段独立硬复位，以已经验证可识别的 4 线单线格式发送初始化、`CMD2EN (DFh)=5A 69 02 01` 和 `SPI2EN (E7h)=10`，再用手册要求的 3 线 9-bit 格式发送 `CASET/RASET/RAMWR`。
- 删除 RGB565 像素的软件 GPIO 双线逐位循环。`RAMWR` 后改用 ESP32-S3 SPI3 Master，事务设置 `SPI_TRANS_MODE_DIO`，总线设置 `SPICOMMON_BUSFLAG_DUAL`，设备设置半双工，通过 DMA 队列发送像素。
- 参考官方 `esp_lcd_panel_io_spi.c` 的大块像素策略，把 240×320 RGB565 色条拆成 20 行、9,600 字节的内部 DMA 块；队列深度为 4，除最后一个块外均设置 `SPI_TRANS_CS_KEEP_ACTIVE`，并先独占 SPI 总线，保证一帧 16 个事务之间 CS 连续有效。
- DIO 时钟直接沿用 `APP_LCD_SPI_PCLK_HZ`，不在测试文件中另设频率。当前工作区该宏已被并行调整为 6 MHz，因此单帧 1,228,800 bit 通过双线需要 614,400 个 SCLK，纯像素时钟时间约 102.4 ms；若以后恢复 12 MHz，理论值才是约 51.2 ms。串口会打印实际 `clock`、`total` 和 `dma` 耗时，板测以串口实值为准。
- 阶段 1：`SPI_IO1=D1(GPIO43)`、`SPI_IO0=D0(GPIO48)`；阶段 2：交换硬件映射，`SPI_IO1=D0(GPIO48)`、`SPI_IO0=D1(GPIO43)`。两阶段发送完全相同的原始 RGB565 字节，不再由软件改变或重排高低位，差异只有 SPI 外设 IO0/IO1 到物理引脚的映射。
- 每阶段结束后释放 SPI 设备和 SPI3 总线，再恢复四个 LCD 引脚为软件 GPIO 空闲电平；下一阶段重新硬复位，避免上一阶段的面板状态和 GPIO Matrix 路由串扰。

### 板测观察与串口记录要求
- 启动行应包含 `pixel_transport=SPI3-HW-DIO-DMA`。每阶段应看到一条 `hardware DIO`，其中包含当前实际时钟（本版预期 6 MHz）、IO1/IO0 引脚、`chunks=16`、`chunk_bytes=9600` 和 `DMA=internal`。
- 阶段完成行应包含 `data=HW-DIO`、`total=...us`、`dma=...us` 和对应 IO 映射。若在 `hardware DIO` 后直接报错，应先按串口错误定位 SPI 总线、DMA 分配或事务参数，不能把它记成面板协议失败。
- 若阶段 1 或阶段 2 正确显示完整的红、绿、蓝、黄、青、品红、白、黑 8 条竖向色条，则该阶段的 IO0/IO1 映射通过；记录实际 `dma` 耗时后即可把同一 DIO DMA 路径接入正常视频刷新。
- 若两阶段均完整执行、耗时接近理论值但画面都不正确，说明 ESP32-S3 已完成硬件双线发送，但不能据此证明面板接受了 `DF/E7` 或当前模组的 `IM[3:0]` 支持双线；需要继续从面板接口模式或厂商初始化条件排查，而不是继续修改 MCU 软件位序。

### 是否解决 / 验证边界
- 硬件 DIO DMA 测试代码已完成，替代了上一版软件双线像素发送；当前状态为未构建、未烧录、待用户板测，不能把静态核对写成双线已通过。
- 本轮没有把 DIO 接入正常网关视频路径，没有改动无线、JPEG、LVGL 或局部刷新链路；未暂存、提交或推送。

## 2026-08-13 SPI3 硬件 DIO 两阶段板测均花屏

### 日期 / 阶段
- 2026-08-13：用户完成参考 Espressif 官方多线 SPI 结构制作的硬件 DIO DMA 专用固件板测，并分别观察阶段 1、阶段 2 的 LCD 画面。

### 实机结果
- 阶段 1（`SPI_IO1=D1/GPIO43`、`SPI_IO0=D0/GPIO48`）没有显示完整八色竖条，实际为花屏。
- 阶段 2（`SPI_IO1=D0/GPIO48`、`SPI_IO0=D1/GPIO43`）同样没有显示完整八色竖条，实际为花屏。
- 本次反馈只包含实机画面结论，尚未附带两阶段的 `hardware DIO`、`stage complete`、`clock`、`dma` 或错误串口行，因此当前不能确认每阶段的 DMA 事务是否完整结束，也不能记录实际硬件 DIO 传输耗时。

### 当前结论
- 已确认 ESP32-S3 官方 `SPI_TRANS_MODE_DIO` 路径下，两种可能的 SPI IO0/IO1 物理映射均不能让当前 LCD 模组正确显示八色竖条。继续只交换两根 lane 或恢复 GPIO 软件拆位没有新增价值。
- 该结果排除了“此前仅因 GPIO 软件双线循环速度慢或软件逐位实现方式错误”作为唯一根因。板测反馈后再次核对，工作区 `APP_LCD_SPI_PCLK_HZ` 已恢复为 12 MHz，`app_config.h` 也不再是修改状态；由于本轮没有串口 `clock` 行，无法确认实际烧录版本使用的是此前短暂出现的 6 MHz 还是当前 12 MHz，因此本次结果不能用于排除或确认高频信号完整性问题。
- 由于没有 LCD 寄存器读回，也缺少本轮完整串口行，仍不能把原因写死为某一项。当前主要未决项仍是：模组内部 `IM[3:0]` 是否确实为手册要求的 `0101`、`DF/E7` 是否被面板接受，以及模组厂商是否还有未公开的双线初始化条件。
- 官方 QSPI LCD 例程证明了 ESP32-S3 多线 SPI DMA 的通用实现结构，但 QSPI 屏的控制器命令协议不能替代 ST7789V3 自身的接口绑位和 `SPI2EN` 条件；本次两阶段失败也说明“套用官方多线传输结构”本身不足以令当前模组进入双线模式。

### 是否解决 / 后续证据
- 双线显示问题仍未解决，本轮硬件 DIO 两种映射板测结论为失败。测试代码暂不继续改动，也不接入正常网关视频链路。
- 若继续定位双线，下一份有效证据应是两阶段完整 `[LCD-DUAL]` 串口输出，或者逻辑分析仪捕获 `DF`、`E7`、`RAMWR` 及开头若干 DIO 时钟；在取得这些证据前不再枚举新的 lane 位序。
- 本轮只根据用户实机反馈追加工程日志；Codex 未执行构建、烧录或串口监视，未修改源码，未暂存、提交或推送。

### 2026-08-13 串口补充：确认两阶段 DIO DMA 均完整执行
- 用户随后补充了完整阶段串口。阶段 1 使用 `clock=12000000Hz`、`IO1=43`、`IO0=48`、`chunks=16`、`chunk_bytes=9600`，完成耗时为 `total=56630us`、`dma=54189us`。
- 阶段 2 同样使用 12 MHz，交换为 `IO1=48`、`IO0=43`，完成耗时为 `total=56437us`、`dma=54185us`。两个阶段的 DMA 时间只相差 4 us，传输长度、分块数量和执行节奏高度一致。
- 12 MHz 双线发送 240×320 RGB565 一帧的纯时钟理论值约为 51.2 ms，实测 DMA 约 54.19 ms，比理论值多约 2.99 ms、约 5.8%，与 16 个排队事务的驱动和分块间开销相符。串口最终正常输出 `diagnostic complete` 和验证固件停止行，期间没有 SPI 总线、DMA 分配、排队或等待结果错误。
- 因此可以确认：ESP32-S3 SPI3 硬件 DIO 已按测试代码完整发送一帧，两种 IO0/IO1 映射都没有少发、中途退出、队列卡死或明显异常降速。此前“缺少串口，无法确认 DMA 是否完成及实际时钟”的不确定项现已消除，实际板测频率为 12 MHz。
- 结合用户实机观察，两阶段均为花屏而非完整八色条，当前故障已不能归因于 GPIO 软件逐位发送，也不能归因于本轮硬件 DIO DMA 没有执行完。主要方向进一步收敛到 LCD 端未按预期解释数据：`DF/E7` 未真正令当前模组进入 2-data-lane、内部 `IM[3:0]` 不满足 `0101`、存在厂商未公开初始化条件，或第二数据线在 12 MHz 下的电气/采样条件不满足。
- 两种物理 lane 交换均已实测失败，不再继续枚举软件位序。若仍要最终区分“面板模式未启用”和“第二数据线电气问题”，最小下一步应是降低 DIO 时钟做一次对照，或用逻辑分析仪捕获 `DF`、`E7`、`RAMWR` 及首批 DIO 时钟；本轮不继续改测试代码。

### 2026-08-13 双线模式寄存器读回验证版（待用户烧录）

- 用户确认 ESP32-S3 SPI3 的多线能力此前已在摄像头链路使用，主板原理图也明确把该接口标为 `QSPI dual LCD + Touch panel`，并将 `LCD_D0`、`LCD_D1`、`LCD_CLK`、`LCD_CS1` 引出。因此后续先按软件协议/配置问题继续定位，不再把“ESP32-S3 SPI3 不支持 DIO”或“主板没有第二数据线”作为主要假设。
- 重新核对 ST7789V3 手册：`E7h` 参数 bit4 是 `SPI2EN`，bit0 是 `SPIRD`；Command Table 2 在串行读取时需要一个 dummy clock。手册同时说明双线模式下普通命令和寄存器读取仍使用 SDA 上的 3-wire 9-bit 协议，WRX 在这些阶段可忽略。
- 当前专用测试此前只打印“已发送 `DF/E7`”，没有 LCD 寄存器读回；因此它能证明 MCU 执行了发送代码，却不能证明面板已接受 `E7` 并进入双线状态。随后直接发送 DIO 彩条，花屏无法区分“模式未切换”和“模式已切换但 DIO 数据格式错误”。
- `main/lcd_dual_lane_test.c` 已把 `E7` 参数从 `0x10` 改为 `0x11`，即同时置位 `SPI2EN` 和 `SPIRD`；写入后按手册时序在 SDA 上发送 3-wire 9-bit `E7` 读命令、插入一个 dummy clock，并读取 8 位寄存器值。最后一个命令位完成上升沿采样后、下降沿前把 SDA 切为输入，满足串行读方向切换要求。
- 新测试只有在读回值的 `SPI2EN`、`SPIRD` 两位均为 1 时才继续 SPI3 DIO DMA 彩条；串口应出现 `mode verified: E7 readback=0x11 SPI2EN=1 SPIRD=1`。若读回不符，将打印 `SPI2EN readback mismatch` 并停止该阶段，不能再把后续花屏归因于像素 lane 编码。
- 这一步只增加双线模式是否真正生效的可观测证据，未改窗口范围、RGB565 数据、12 MHz DIO DMA 分块和两组 IO0/IO1 映射。当前仅完成源码静态修改，未构建、未烧录、待用户验证。

#### 首次 E7 读回板测结果与基准读取补充

- 用户烧录后，阶段 1 正常完成复位、4-wire 初始化以及 `DF 5A 69 02 01`、`E7 11` 的软件发送；随后 `E7` 读回得到 `0x00`，触发 `SPI2EN readback mismatch`，程序按设计在进入 SPI3 DIO DMA 前停止。因此本次没有产生新的双线彩条结果，也没有运行阶段 2。
- 该 `0x00` 不能立即写成“E7 已确认未生效”：此前工程从未验证 LCD 的串行读方向，当前板级 SDA 是否可回读、接口属于使用 SDA 回读的 Interface-I 还是依赖独立 SDO 的 Interface-II，以及软件 dummy 时序是否正确，均没有已知寄存器基准。把未校准的首次读值直接作为硬门槛会产生误判风险。
- 再次核对手册 4-line 串行读图以及 `RDDCOLMOD (0Ch)` 命令表：串行模式先返回一个完整 dummy byte，第二个字节才是有效像素格式；当前初始化已写 `COLMOD=0x55`，因此它可作为切换双线之前的已知读回基准。
- `main/lcd_dual_lane_test.c` 新增 4-wire 双向 SDA 寄存器读取。在每阶段完成单线初始化后，先发送 `RDDCOLMOD(0Ch)`，忽略 8 个 dummy bit，再读取 8 位并要求结果为 `0x55`；串口新增 `baseline read: RDDCOLMOD(0C)=... expected=0x55 dummy_bits=8`。
- 只有 4-wire 基准读取通过，才继续写 `DF/E7` 并执行原有 E7 3-wire 读回。如果基准同样为 `0x00` 或其他值，则结论是当前 SDA 回读链路/软件读时序尚未建立，不能用 E7 读回判断模式；如果基准为 `0x55` 而 E7 仍为 `0x00`，才有充分依据把问题收敛到 `DF/E7` 未被接受或双线切换后的命令格式不符。
- 本轮只完成源码与工程日志修正，未构建、未烧录、待用户再次板测；DIO DMA、lane 映射和像素内容没有改变。

#### RDDCOLMOD 基准读回 `0xFF` 与 dummy 时序订正

- 用户烧录基准读取版本后，阶段 1 的 `RDDCOLMOD(0Ch)` 返回 `0xFF`，随后按上一版硬门槛停止；因此本次同样未执行 `DF/E7` 读回、DIO DMA 和阶段 2。
- 重新对照 ST7789V3 手册 8.3.5 的 4-line serial protocol 图：`RDID1/RDID2/RDID3/0Ah/0Bh/0Ch/0Dh/0Eh/0Fh` 属于 8-bit serial read，发送 8 位命令后紧接着就是 8 位有效返回数据，图中没有额外 dummy clock。命令表/流程中的 dummy read 不能直接套成串行接口的 8 个 dummy bit。
- 上一版 `dual_test_read_4wire_register(0x0C, 8U)` 多跳过了一个完整字节；`0xFF` 更符合有效返回字节已经被跳过后，继续读取 LCD 释放的 SDA，而不能据此判断板级 SDA 不支持回读。该测试设计错误已订正。
- `main/lcd_dual_lane_test.c` 已把 `RDDCOLMOD` 基准读取改为 `dummy_bits=0`，串口预期同步改为 `expected=0x55 dummy_bits=0`。`E7` 属于 Command Table 2，手册对其串行读取另有“one dummy clock”说明，因此 E7 的单个 dummy clock 暂不改变。
- 当前只完成源码和日志订正，未构建、未烧录、待用户再次板测。下一次先看 `RDDCOLMOD` 是否为 `0x55`；通过后程序才继续输出 E7 读回结果。

#### RDDCOLMOD 读回 `0x05`：基准链路实际通过

- 用户烧录 `dummy_bits=0` 版本后，阶段 1 得到 `RDDCOLMOD(0Ch)=0x05`，程序仍按旧门槛 `expected=0x55` 判为失败并停止，尚未继续执行 `DF/E7` 和 DIO 彩条。
- 重新核对 `RDDCOLMOD` 位定义后确认，D2:D0 才是当前串行/控制接口像素格式，`101` 表示 16-bit RGB565；D6:D4 属于独立 RGB 接口像素格式。手册还明确把该寄存器的上电默认读值写为 `0000-0110`，而不是同时包含两个字段的 `0x66`。因此当前读到的 `0x05` 与“控制接口已由默认 18-bit 改为 16-bit”完全一致，不是丢失高四位或采样错位。
- 该结果同时证明：`COLMOD=0x55` 的写操作至少已正确改变控制接口字段，4-wire `0Ch` 命令被面板识别，GPIO43/SDA 由输出切为输入后的 8 位回读链路有效。此前把完整字节固定要求为 `0x55` 的门槛设计错误，现已订正。
- `main/lcd_dual_lane_test.c` 改为只校验 `(RDDCOLMOD & 0x07) == 0x05`，串口提示改为 `expected_control=0x05 mask=0x07 dummy_bits=0`。通过后测试将继续发送 `DF 5A 69 02 01`、`E7 11`，再按 Command Table 2 要求插入一个 dummy clock 读取 E7。
- 下一次板测的关键结果是 E7 读回：若 `(E7 & 0x11) == 0x11`，程序才会继续硬件 DIO DMA 彩条；若不满足，则保留完整 E7 数值继续区分命令未解锁、E7 未写入或切换后的 3-wire 读协议问题。
- 本轮只完成最小源码门槛修正和日志追加，未改变 DF/E7 内容、lane 映射、RGB565 数据或 12 MHz DIO DMA；未构建、未烧录、待用户验证，未暂存、提交或推送。

#### 基准通过后 E7 仍读回 `0x00`：增加切换前 4-wire 分层验证

- 用户烧录修正版后，阶段 1 的 `RDDCOLMOD=0x05` 已通过新门槛，证明普通 4-wire 命令、参数写入和 SDA 回读均有效；程序随后发送 `DF 5A 69 02 01`、`E7 11`，改用双线模式规定的 3-wire 9-bit 读法读取 E7，但结果仍为 `0x00`，因此尚未进入 DIO DMA。
- 当前 `0x00` 仍不能单独证明 E7 没有写入：若 E7 未使面板从当前 4-wire 命令格式切到双线模式，紧随其后的 3-wire 9-bit E7 读命令本身就会被按错误格式解释；反之，若已经切换，也可能是切换后的 3-wire 读时序问题。必须把“DF/E7 是否可在原协议写读”与“E7=0x11 后是否切换协议”拆开。
- `main/lcd_dual_lane_test.c` 现改为两步验证。第一步在 `DF` 解锁后先写 `E7=0x01`，只置位 `SPIRD`、保持 `SPI2EN=0`，然后仍用已验证的 4-wire 8-bit 协议并按 Command Table 2 插入 1 个 dummy clock 读取 E7；串口新增 `pre-switch E7 read: value=... expected=0x01 protocol=4wire-8bit dummy_bits=1`。
- 只有切换前 E7 读回满足 `(value & 0x11) == 0x01`，才继续以 4-wire 写 `E7=0x11`，再按双线模式的 3-wire 9-bit 协议读取并要求 `0x11`。这样下一次结果可以明确区分：切换前就失败，问题在 DF 解锁/E7 写读；切换前成功而切换后失败，问题才收敛到双线模式切换条件、IM 绑定位或切换后的 3-wire 协议。
- 本轮没有修改 GPIO lane、DIO DMA、RGB565 彩条、时钟或正常网关显示路径；仅增加诊断分层和串口打印。当前未构建、未烧录、待用户验证，未暂存、提交或推送。

## 2026-08-13 单线 SPI 超过 12 MHz 花屏的初步分析

### 日期 / 阶段

- 2026-08-13：结合当前正常单线驱动配置、ESP-IDF 5.5.3 SPI3 路由实现、ST7789V3 串行时序和主板 LCD 接口原理图，分析“单线 SPI 在 12 MHz 正常、继续升频即花屏”的原因。本阶段只做静态分析，没有修改正常单线显示代码。

### 已确认事实

- 正常单线链路当前使用 SPI3、mode 0、`SCLK=GPIO44`、`MOSI/SDA=GPIO43`、`DC/WRX=GPIO48`、`CS=GPIO47`，`APP_LCD_SPI_PCLK_HZ` 为 12 MHz。`esp_lcd_panel_io_spi_config_t` 没有显式设置 `cs_ena_pretrans` 或 `cs_ena_posttrans`，两者当前均为默认 0；工程中也没有显式调用 `gpio_set_drive_capability()` 调整 LCD 引脚驱动能力。
- ST7789V3 手册的 4-line 串行写时序给出：最小时钟周期 16 ns、SCL 高/低电平各至少 7 ns、SDA 建立/保持各至少 7 ns、CS 写建立/保持各至少 15 ns、D/C 建立/保持各至少 10 ns，理论写时钟上限约为 62.5 MHz。因此 12 MHz 不是控制器规格上限。
- ESP-IDF 5.5.3 官方文档说明，ESP32-S3 在 40 MHz 及以下通过 GPIO Matrix 路由 SPI 时，其行为应与 IO_MUX 路由相同；本地 ESP-IDF 源码同时确认 SPI3 没有专用 IO_MUX 引脚，所以当前 SPI3 的 SCLK、MOSI 和 CS 都经过 GPIO Matrix。该事实不能把 12 MHz 解释成 SPI3 外设能力上限。
- 主板原理图中 GPIO44、GPIO43、GPIO47、GPIO48 分别通过 R74、R70、R65、R68 的 0 Ω 串联电阻直接进入 0.5 mm FPC LCD 连接器，链路上没有电平转换、缓冲器或源端阻尼电阻。该拓扑在快速 GPIO 边沿下容易受连接器/FPC/走线阻抗不连续、过冲和振铃影响。

### 当前判断

- 最高概率根因是 LCD 物理链路的信号完整性或采样裕量，而不是 ESP32-S3 或 ST7789V3 不支持更高频率。尤其是 SCLK 振铃若在 LCD 输入阈值附近形成二次穿越，控制器会多采一个时钟；后续 RGB565 位流整体错位，外观就是花屏。频率上升还会压缩 SDA 建立/保持裕量。
- 第二种可能是命令边界时序。当前 `esp_lcd` 在每笔命令、参数和颜色事务前通过回调切换 D/C，但 CS 前后延长周期为 0。12 MHz 时尚有裕量，升频后可能首先违反 ST7789V3 的 CS 或 D/C 绝对纳秒要求，导致 `CASET/RASET/RAMWR` 等命令偶发损坏；这与纯像素数据链路失真需要分开验证。
- 这也可能与当前双线花屏存在共同根因：正常单线时 GPIO48 只是低速 D/C，只在命令/数据边界变化；双线时 GPIO48 被改成每个时钟都翻转的第二数据线。单线 12 MHz 正常只验证了 GPIO43 的高速数据能力，并没有验证 GPIO48/FPC 对应通道能作为 12 MHz 连续数据线稳定工作。
- 此前摄像头在 ESP32-S3 SPI3/QSPI 上正常，只能证明 SPI3 多线外设和摄像头那套引脚、走线、连接器及负载可工作，不能替代 LCD FPC 这条物理链路的信号质量验证。

### 建议的最小验证顺序

1. 先保持初始化、窗口命令和 `RAMWR` 为 12 MHz，只把 `RAMWR` 后的整块像素数据提高到 16 MHz。若这样能稳定显示，优先处理 CS/D/C/命令时序；若仍花屏，优先处理 SCLK/SDA 的物理波形。
2. 软件对照测试可按实际可实现分频逐级测试 12、约 13.3、16、20 MHz，并记录请求频率、驱动计算出的实际频率及画面结果；不要只记录宏的请求值。
3. 命令时序方向可对照加入 1 个 SPI bit-cycle 的 `cs_ena_pretrans` 和 `cs_ena_posttrans`，再与原配置进行同频对比。该设置只能验证 CS 边界问题，不能修复 SCLK/SDA 振铃。
4. 电气方向优先观察 LCD 连接器端的 GPIO44/SCLK，而不是只量 ESP32 管脚端；检查上升/下降沿、过冲、振铃和是否重复跨越输入阈值。若确认波形问题，优先把 R74 从 0 Ω 调为约 22~47 Ω，再视结果处理 R70；具体阻值应以实测波形为准。
5. GPIO 驱动能力可作为无改板诊断变量分别对比，不应默认“驱动越强越好”：0 Ω 加 FPC 的链路中，更强边沿可能加重振铃，较弱驱动反而可能扩大有效采样窗口。

### 资料与验证边界

- ST7789V3 数据手册：<https://files.waveshare.com/upload/c/c1/ST7789V3_V0.1.pdf>
- ESP-IDF 5.5.3 ESP32-S3 SPI Master 文档：<https://docs.espressif.com/projects/esp-idf/en/v5.5.3/esp32s3/api-reference/peripherals/spi_master.html>
- 当前结论属于原理图、手册和源码静态分析，尚未用示波器/逻辑分析仪验证，也没有完成上述分频与命令/像素分速对照；问题状态为“未解决，已有高概率方向”。本阶段未构建、未烧录、未启动串口监视，未暂存、提交或推送。

## 2026-08-13 E7 切换前读回仍为 `0x00`：连续 CS 事务对照

### 本次实机结果

- 阶段 1 的基准读取仍为 `RDDCOLMOD(0Ch)=0x05`，与 `expected_control=0x05 mask=0x07` 一致，说明普通 4-wire 命令识别、参数写入、SDA 方向切换和 8 位回读链路均已通过。
- 程序随后按两步验证先发送 `DF 5A 69 02 01`，再在尚未启用双线的状态写入 `E7=0x01`，但 4-wire 读取 E7 得到 `0x00`，没有满足 `(E7 & 0x11) == 0x01`。程序正确地在进入 SPI3 DIO DMA 和阶段 2 之前停止。
- 这次结果已经排除“先切到双线后才因 3-wire 读格式错误而读到 0”的解释。当前故障点位于双线切换之前，收敛到 `DF` 解锁、`E7` 写入或 Command Table 2 的扩展命令事务格式。

### 手册复核与本轮源码调整

- ST7789V3 手册中的 `CMD2EN (DFh)` 参数确为 `5A 69 02 01`；`E7h` 的 bit4 为 `SPI2EN`、bit0 为 `SPIRD`，因此切换前写 `0x01`、切换时写 `0x11` 的位值没有错误。Command Table 2 串行读取使用 1 个 dummy clock，本轮不再修改 dummy 时序。
- 检查测试源码发现，原 `dual_test_write_4wire_cmd()` 会在命令字节以及每个参数字节之后分别拉高 CS。普通寄存器已经证明这种写法在当前面板上可用，但 `DF` 是带 4 个魔术参数的扩展表解锁序列，仍需排除面板/模组对连续事务边界更严格的可能。
- 新增仅供扩展寄存器探测使用的连续事务发送函数：CS 在命令和全部参数期间始终保持低电平，只通过 D/C 区分命令与参数；`DF`、`E7=0x01` 和 `E7=0x11` 均改用该路径。普通初始化、基准读取、lane 映射、RGB565 数据和 12 MHz DIO DMA 均未改变。
- 串口会在阶段说明和 E7 切换前回读行中打印 `DF/E7 write_cs=continuous` / `write_cs=continuous`，用于确认烧录的是本次版本。

### 下一次板测判据

- 若切换前读回变为 `0x01`，说明此前失败来自扩展命令的 CS 事务边界；程序会继续写 `E7=0x11` 并验证切换后的 3-wire 读回。
- 若仍读回 `0x00`，则连续 CS 也不能令 `DF/E7` 生效，下一步不再继续猜 lane 位序，应优先用逻辑分析仪核对 LCD 连接器端的 `CS/SCL/SDA/D-C` 上 `DF`、`E7` 实际位流，并进一步确认模组内部 `IM[3:0]` 绑位或厂商私有初始化条件。
- 本轮只完成源码与工程日志修改；按 ESP32 协作规则，Codex 未构建、未烧录、未启动串口监视，等待用户烧录板测；未暂存、提交或推送。

## 2026-08-13 连续 CS 板测仍为 `E7=0x00`：双线受 IM 绑位限制

### 本次实机结果

- 用户已烧录 `DF/E7 write_cs=continuous` 版本。阶段 1 再次稳定读到 `RDDCOLMOD(0Ch)=0x05`，随后以连续 CS 发送 `DF 5A 69 02 01` 和 `E7=0x01`，切换前仍读回 `E7=0x00`。
- 程序在 `1513~1514 ms` 按门槛报出 `pre-switch E7 readback mismatch` 和 `protocol probe failed`，没有进入 `E7=0x11`、SPI3 DIO DMA 或阶段 2。连续 CS 与此前分字节 CS 得到完全相同的结果，因此“扩展命令需要命令和参数保持连续 CS”已由实机排除。

### 手册页复核与决定性接口条件

- 已对 ST7789V3 数据手册第 55、254、265 页同时进行文字提取和页面渲染核对。第 254 页确认 `DFh` 的四个参数及 `EN=1`；第 265 页确认 `E7h` 的 bit4=`SPI2EN`、bit0=`SPIRD` 及 Command Table 2 串行读取需要 1 个 dummy clock。因此当前命令值和 E7 读取 dummy 数没有发现新的软件错误。
- 第 55 页明确把 2-data-lane 的接口选择写为 `IM[3:0]=0101`，并规定该模式的命令协议与 3-line 串行接口相同，即 D/C 位与 8 位命令/参数一起在 SDA 上传输；WRX 只作为第二数据线。`E7h=10h` 是该接口中的双 lane 使能条件，手册没有说明它能改写 IM 硬件绑位。
- 当前模组上电和硬复位后能够长期使用 WRX 作为独立 D/C、SDA 发送 8-bit 字节的 4-line 协议，并能正确初始化画面、显示色条及读回 `RDDCOLMOD=0x05`。按照同一手册的接口选择表，这一实际行为对应 `IM[3:0]=0110`；若内部绑位为 `0101`，当前不带第 9 个 D/C 位的命令流不会被正确解释，更不可能稳定完成上述显示和精确寄存器读回。

### 当前结论与边界

- 在屏内控制器确为 ST7789V3 的前提下，现有证据表明当前 LCD 玻璃/FPC 模组内部采用 4-line 的 `IM=0110`，而不是双线所需的 `IM=0101`。主板将 `LCD_D0/WRX` 和 `LCD_D1/SDA` 两根信号都连接到 ESP32-S3，只证明外部引脚数量与复用关系具备条件，不能改变或证明屏内 IM 绑位。
- ST7789V3 芯片“支持双线”与当前采购/安装的 LCD 模组“已经按双线模式绑位”是两个不同条件。`E7` 是 `IM=0101` 接口内的使能位，不是从正常工作的 `IM=0110` 4-line 接口动态切换硬件模式的命令。当前 `E7=0x00` 与所有双线彩条花屏结果均和这一限制相符。
- 因此继续修改 ESP32-S3 DIO、lane 位序、RGB565 字节顺序、DF/E7 的 CS 边界或 dummy 时钟，无法补足屏内 IM 绑位。若仍要求使用双线，需要取得当前 LCD 模组/FPC 的明确规格或厂商初始化资料，确认其内部确为 `IM=0101`；否则需更换按 `0101` 绑位的屏模组，或由屏厂调整玻璃/FPC 绑位。只有外部可访问 IM 焊盘时才可能硬件改绑，当前主板原理图没有引出这些控制器裸片 IM 脚。
- 本轮只追加实机结果和结论，没有继续修改 `main/lcd_dual_lane_test.c`，也没有回退现有专用测试固件。Codex 未构建、未烧录、未启动串口监视，未暂存、提交或推送；双线软件验证状态记为“当前模组接口绑位条件不满足，停止继续枚举软件协议组合”。

## 2026-08-13 回到单线 SPI：增加实际时钟与传输路径基线打印

### 日期 / 阶段

- 2026-08-13：用户已自行回退双线验证代码，工程恢复正常 SPI3 单数据线 LCD 和网关视频路径。本轮先建立不改变运行参数的可观测基线，再决定是否升到下一档实际频率。

### 改动内容

- `main/app_config.h` 新增 `APP_LCD_SPI_CS_PRETRANS_CYCLES` 和 `APP_LCD_SPI_CS_POSTTRANS_CYCLES`，本版均显式设为 `0`；LCD 请求时钟仍为 12 MHz，未增加 CS 延时。
- `main/bsp_lcd.c` 使用 ESP-IDF 与 SPI3 相同的 APB 分频计算打印请求频率和实际频率，同时打印 APB 时钟、SPI mode、CS 前后周期、队列深度、单事务最大字节数、整帧字节数和分块数。按当前配置，预期启动行中 `request=12000000Hz`、`actual=11428571Hz`、`max_transfer=19200B`、`frame=153600B`、`chunks=8`。
- LCD panel IO 创建后读取并打印 SCLK、MOSI、CS、D/C 四个 GPIO 的实际驱动等级，取值 `0~3` 从弱到强；本轮只读取，没有调用 `gpio_set_drive_capability()` 修改任何引脚。
- 第一次整帧视频提交时额外打印缓冲位置、DMA 路径、地址、64 字节对齐状态、帧字节数和分块信息。正常三画布路径预期为 `video_buffer=PSRAM dma=PSRAM-direct aligned64=1 bytes=153600 chunk_bytes=19200 chunks=8`；LCD 释放后清除一次性打印标记，以便重新初始化时再次记录。

### 验证边界 / 下一步

- 静态核对确认双线测试源码、构建项和启动入口均已被用户回退；本次没有改变 SPI mode、请求频率、GPIO驱动强度、RGB565、端序、PSRAM DMA、队列或分块行为。
- 下一次由用户构建烧录，先保留两条启动 `[LCD-SPI]`、第一条视频缓冲 `[LCD-SPI]` 以及若干 `[LCD] direct`。确认实际频率、驱动等级和 PSRAM 直接 DMA 与预期一致后，再只修改频率测试 13.333 MHz 档位。
- 按 ESP32 协作规则，Codex 未构建、未烧录、未启动串口监视；未暂存、提交或推送。

## 2026-08-13 单线 SPI 基线板测与 13.333 MHz 单变量测试

### 基线板测结果

- 用户烧录了请求时钟 12 MHz 的诊断版本。启动日志实测 `request=12000000Hz`、`actual=11428571Hz`、`apb=80000000Hz`，确认此前所称的“12 MHz”实际线速为约 11.43 MHz。
- 当前 SPI3 配置为 mode 0，`cs_pre=0`、`cs_post=0`、队列深度 10；单事务 19200 字节，整帧 153600 字节，共 8 个分块。SCLK、MOSI、CS、D/C 四个 GPIO 的驱动能力均为档位 2。
- 启动界面的整帧刷新记录为 `total=109927us`、`xfer=109672us`、`idle=255us`。11.428571 MHz 下发送 153600 字节的理论纯线速耗时约 107.52 ms，实测只多约 2.15 ms，说明当前整帧刷新几乎完全受 SPI 线速限制，队列和分块没有形成明显的软件空洞。
- 本次日志正常完成 LCD 初始化和一次整帧刷新，没有 SPI/LCD API 报错；但日志中没有摄像头视频帧，因而未出现一次性的 `video_buffer=...`，本次尚未确认视频画布实际走 `PSRAM-direct`。用户也尚未在本轮文字反馈中明确屏幕视觉是否正常，因此不能把 11.43 MHz 的画面稳定性写成新的板测结论。

### 下一档单变量测试

- 仅把 `APP_LCD_SPI_PCLK_HZ` 从 12000000 改为 13333333。SPI mode、GPIO 驱动档位、CS 前后周期、DMA、队列、分块、RGB565 和初始化序列全部保持不变。
- 下一次启动日志应打印 `request=13333333Hz`、`actual=13333333Hz`。若画面正常，再继续提高频率；若花屏，则固定在 13.333 MHz，下一轮只调整 SCLK/MOSI 驱动档位，比较档位 2 与档位 3，避免同时改变多个变量。
- 当前只完成源码和工程日志修改，Codex 未构建、未烧录、未启动串口监视；13.333 MHz 结果待用户板测，未暂存、提交或推送。

## 2026-08-13 单线 SPI 13.333 MHz 花屏与驱动档位 3 对照

### 实机结果

- 用户烧录了 13.333 MHz 单变量版本，屏幕实际出现花屏。启动日志确认 `request=13333333Hz`、`actual=13333333Hz`，SCLK、MOSI、CS、D/C 当时均为驱动档位 2，说明烧录版本和预定测试条件一致。
- LCD 初始化、面板命令和 LVGL 提交均未返回软件错误；两次整帧记录分别为 `xfer=94012us` 和 `xfer=93671us`，相较 11.428571 MHz 基线的 `109672us` 明显缩短。传输完成不代表 LCD 端采样正确，本次实物花屏是决定性失败判据。
- 因此当前“驱动档位 2”下的稳定边界已经收敛在实际 11.428571 MHz 与 13.333333 MHz 之间。问题仍未解决，现象更符合高频边沿/采样裕量不足，而不是 SPI 队列饥饿或 DMA 传输中断。

### 下一档单变量对照

- 保持请求及实际频率 13.333333 MHz、SPI mode 0、CS 前后周期 0、队列 10、19200 字节分块和所有显示逻辑不变。
- 只在 panel IO 创建后调用 `gpio_set_drive_capability()`，把 SCLK(GPIO44) 和 MOSI(GPIO43) 从驱动档位 2 改为档位 3；CS(GPIO47) 与 D/C(GPIO48) 保持档位 2。下一次启动日志应显示 `drive_cap SCLK=3 MOSI=3 CS=2 DC=2`。
- 如果档位 3 后画面正常，说明原档位 2 的边沿速度/驱动能力不足；如果仍花屏，不能直接判定需要更强驱动，下一步应在同一频率测试较弱档位或增加 CS 边界周期，以区分振铃与命令边界时序。
- 当前只完成源码和工程日志修改，Codex 未构建、未烧录、未启动串口监视；驱动档位 3 的结果待用户板测，未暂存、提交或推送。

## 2026-08-13 单线 SPI 13.333 MHz 驱动档位 3 板测通过并进入 16 MHz

### 实机结果

- 用户烧录驱动档位 3 对照版本后确认屏幕不花屏。日志实测 `request=13333333Hz`、`actual=13333333Hz`，并读回 `drive_cap SCLK=3 MOSI=3 CS=2 DC=2`，测试条件与源码一致。
- 启动整帧刷新记录为 `total=94440us`、`xfer=94236us`，与上一轮同频档位 2 的约 94 ms 传输时间一致；因此画面改善来自驱动能力变化，不是降频或传输负载变化。
- 同一块硬件、同一实际频率及同一显示软件下，档位 2 花屏、档位 3 不花屏。这组 A/B 板测证明 SCLK/MOSI GPIO 驱动设置会直接影响当前 LCD 链路在 13.333 MHz 的稳定性，档位 2 在该频率下裕量不足。
- 当前附件只包含启动界面整帧提交，没有摄像头视频帧和 `video_buffer=...`，所以本阶段结论限定为启动/静态界面显示通过；连续摄像头视频稳定性仍需在选定最终频率后单独验证。

### 16 MHz 单变量测试

- 保持 SCLK/MOSI 驱动档位 3、CS/D-C 档位 2、SPI mode 0、CS 前后周期 0、队列和分块不变，只把 `APP_LCD_SPI_PCLK_HZ` 从 13333333 改为 16000000。
- 下一次启动日志应打印 `request=16000000Hz actual=16000000Hz` 和 `drive_cap SCLK=3 MOSI=3 CS=2 DC=2`。16 MHz 下整帧 153600 字节的理论纯线速时间为 76.8 ms，预计实测约 79 ms。
- 当前仅完成源码和工程日志修改，Codex 未构建、未烧录、未启动串口监视；16 MHz 画面和持续稳定性待用户板测，未暂存、提交或推送。

## 2026-08-13 单线 SPI 16 MHz 板测通过并进入 20 MHz

### 实机结果

- 用户反馈当前版本画面正常，可以继续提高频率。两次整帧刷新记录分别为 `xfer=78221us`、`xfer=78667us`，平均约 78.44 ms；`idle` 仅为 118 us 和 234 us，传输路径仍接近连续线速。
- 上述耗时与 16 MHz 下整帧 153600 字节的 76.8 ms 理论线速时间相符，说明本轮提速已生效且性能改善符合预期。用户提供的片段没有包含启动 `[LCD-SPI]` 行，因此本条不把实际分频读回单独写成日志已验证事实，但结合当前源码配置和耗时，可作为 16 MHz 当前画面板测通过记录。
- SCLK/MOSI 驱动档位 3 在 13.333 MHz 和本轮 16 MHz 均未出现用户观察到的花屏；当前稳定上限至少已提升到 16 MHz。连续摄像头视频压力仍应在最终选定频率后再做长时间验证。

### 20 MHz 单变量测试

- 保持 SCLK/MOSI 驱动档位 3、CS/D-C 档位 2、SPI mode 0、CS 前后周期 0、DMA、队列和分块不变，只把 `APP_LCD_SPI_PCLK_HZ` 从 16000000 改为 20000000。
- 下一次启动日志应打印 `request=20000000Hz actual=20000000Hz` 和 `drive_cap SCLK=3 MOSI=3 CS=2 DC=2`。20 MHz 下整帧理论纯线速时间为 61.44 ms，预计实测约 63 ms。
- 当前仅完成源码和工程日志修改，Codex 未构建、未烧录、未启动串口监视；20 MHz 画面与稳定性待用户板测，未暂存、提交或推送。

## 2026-08-13 单线 SPI 20 MHz 黑屏与 CS 前后保持周期对照

### 实机结果

- 用户烧录 20 MHz 版本后屏幕黑屏，但串口仍输出完整刷新统计：`total=62949us`、`xfer=62833us`、`idle=116us`。该耗时与 20 MHz 下 153600 字节整帧的 61.44 ms 理论线速时间相符，说明 ESP32 SPI3、DMA、队列和完成回调仍在正常推进。
- “DMA 正常完成但 LCD 黑屏”说明发送端完成不等于面板正确接收。相较 16 MHz 正常，本轮更像初始化命令、窗口命令、RAMWR 或 CS 边界在 20 MHz 下失去时序裕量；不能根据单条刷新日志判断像素数据已被面板写入。
- 用户明确要求保持 20 MHz，不通过降频规避。本阶段状态为“20 MHz 性能已达到约 62.8 ms，但显示功能未通过”。

### CS 时序单变量修正

- 保持 `APP_LCD_SPI_PCLK_HZ=20000000`、SCLK/MOSI 驱动档位 3、CS/D-C 驱动档位 2、SPI mode、DMA、队列、分块和面板初始化内容不变。
- 只把 `APP_LCD_SPI_CS_PRETRANS_CYCLES` 与 `APP_LCD_SPI_CS_POSTTRANS_CYCLES` 从 0 改为 1。20 MHz 下各增加一个 50 ns SPI 周期，用于扩大 CS 到首个时钟以及末个时钟到 CS 释放的建立/保持裕量；每笔事务只增加两个周期，对整帧吞吐影响可忽略。
- 下一次启动日志应显示 `request=20000000Hz actual=20000000Hz cs_pre=1 cs_post=1` 和原有 `drive_cap SCLK=3 MOSI=3 CS=2 DC=2`。如果仍黑屏，下一步继续保持 20 MHz 和 CS 周期不变，只提高 CS/D-C 驱动能力进行控制线对照。
- 当前仅完成源码和工程日志修改，Codex 未构建、未烧录、未启动串口监视；CS 前后各 1 周期的显示结果待用户板测，未暂存、提交或推送。

## 2026-08-13 单线 SPI 20 MHz 增加 CS 周期仍黑屏与控制线驱动对照

### 实机结果

- 用户烧录 `20 MHz + cs_pre=1 + cs_post=1` 版本后仍然黑屏。该结果排除了“只因 CS 前后保持周期为 0 导致黑屏”的单一解释；20 MHz 频率按用户要求继续保持不变。
- 本次用户没有附加新的启动或刷新日志，因此只记录明确的实物黑屏结论，不把未提供的串口数值重复写成新证据。

### 控制线驱动单变量修正

- 保持 20 MHz、CS 前后各 1 周期、SCLK/MOSI 驱动档位 3、SPI mode、DMA、队列、分块和面板初始化内容全部不变。
- 新增 CS 与 D/C 驱动能力配置，并在 panel IO 创建后把 CS(GPIO47)、D/C(GPIO48) 从默认档位 2提升到档位 3。至此 SCLK、MOSI、CS、D/C 四根 LCD 信号线均为档位 3。
- 下一次启动日志应显示 `request=20000000Hz actual=20000000Hz cs_pre=1 cs_post=1` 和 `drive_cap SCLK=3 MOSI=3 CS=3 DC=3`。如果仍黑屏，说明单纯增强四根线驱动和延长 CS 边界都不足，下一阶段需要在保持 20 MHz 像素传输的前提下，把面板初始化/窗口命令与高速像素数据分开处理。
- 当前仅完成源码和工程日志修改，Codex 未构建、未烧录、未启动串口监视；四线驱动档位 3 的显示结果待用户板测，未暂存、提交或推送。

## 2026-08-13 单线 SPI 命令 10 MHz / 像素 20 MHz 分速实现

### 前置板测结论

- 用户已按上一轮给出的 20 MHz 多组合顺序完成测试，所有组合均为黑屏；把 `APP_LCD_SPI_CS_PRETRANS_CYCLES` 与 `APP_LCD_SPI_CS_POSTTRANS_CYCLES` 同时设为 `20U` 后也仍然黑屏。该结果说明继续只调四根信号线驱动档位或 CS 前后周期没有取得有效显示。
- ESP-IDF 5.5.3 的 `esp_lcd_panel_io_spi_config_t` 明确约束 `cs_ena_pretrans` / `cs_ena_posttrans` 的有效范围为 0～16 个 SPI bit-cycle，因此 `20U` 已超出驱动接口约定，不能把这次结果解释为“面板在合法 20-cycle CS 延时下仍失败”。本阶段删除这两个宏，不再依赖硬件 CS 周期参数。
- 16 MHz 已由用户板测确认画面正常，20 MHz 下 SPI3/DMA 又能以约 62.8 ms 完成整帧，但屏幕黑屏；本轮针对“高速控制命令可能失去采样裕量”实施分速，而不是改变 RGB565 内容、窗口尺寸或视频缓冲路径。

### 分速结构

- `main/app_config.h` 新增 `APP_LCD_SPI_CMD_PCLK_HZ=10000000U`，保留 `APP_LCD_SPI_PCLK_HZ=20000000U` 作为纯像素时钟。SCLK、MOSI、CS、D/C 继续使用驱动档位 3。
- `main/bsp_lcd.c` 在同一个 SPI3 总线上创建两个单线 SPI panel IO：低速 command IO 为 10 MHz、队列深度 1；高速 pixel IO 为 20 MHz、队列深度 10。二者只负责不同事务，数据线、时钟线和 D/C 物理引脚不变。
- 上电后的 `SWRESET`、`SLPOUT`、`MADCTL`、`COLMOD`、`RAMCTRL` 初始化序列按 ESP-IDF 5.5.3 ST7789 驱动的既有值逐条以 10 MHz 发送，并在每条命令之间恢复 CS 高电平；`INVON`、`DISPON`、释放时的 `DISPOFF` 同样走低速 command IO。
- 每次刷新先由 10 MHz command IO 发送 `CASET`、`RASET`、`RAMWR`，随后由 20 MHz pixel IO 使用 `lcd_cmd=-1` 的 data-only 事务发送 RGB565 DMA。`lcd_cmd=-1` 是 ESP-IDF SPI panel IO 已实现的无命令像素事务入口，避免在高速 IO 上重复发送 `RAMWR`。
- 两个 panel IO 均关闭硬件 CS，GPIO47 改为软件手动 CS。刷新开始时拉低 CS，在低速窗口命令与高速像素事务切换期间持续保持低电平，直到最后一个像素 DMA 分块的完成 ISR 才拉高；二值信号量保证上一笔像素传输完成后才允许下一次窗口命令，避免两个 SPI 设备交叉访问及测试色条缓冲被提前复用。

### 串口判据与验证边界

- 启动时预期出现 `[LCD-SPI] split host=SPI3 ... command_request=10000000Hz command_actual=10000000Hz pixel_request=20000000Hz pixel_actual=20000000Hz`，以及 `split path: init/CASET/RASET/RAMWR=10000000Hz, RGB565-DMA=20000000Hz, CS=GPIO47 manual hold until pixel ISR`。
- 第一次实际刷新还会打印 `[LCD-SPI] first split draw`，用于确认程序已进入“低速窗口命令 + 高速 data-only 像素”路径；连续视频仍保留 `video_buffer=...` 和 `[LCD] direct` 统计。若画面正常，整帧像素物理传输仍应接近 20 MHz 的约 61.44 ms 线速；若仍黑屏，则说明问题不只在初始化/窗口命令频率，20 MHz 像素采样本身或板级信号完整性仍未通过。
- 本轮已完成源码差异与静态检查，但按 ESP32 协作规则，Codex 未构建、未编译、未烧录、未启动串口监视；分速方案的画面结果等待用户板测。未暂存、提交或推送。

## 2026-08-13 分速方案首轮板测：黑屏消失但颜色异常

### 实机进展

- 用户烧录“初始化/窗口命令 10 MHz、RGB565 像素 20 MHz”的分速版本后，屏幕不再黑屏，连续视频画面能够刷新。该结果证明低速控制命令与高速像素数据拆分方向有效，原 20 MHz 全链路黑屏至少包含控制命令在高速下未被面板可靠接收的问题。
- 连续帧日志 `seq=153/154` 的 `physical=61565us/61245us`、`total=62063us/61922us`，与 20 MHz 发送 240×320×16 bit 的理论 61.44 ms 基本一致；完整像素 DMA、完成 ISR、帧同步信号量和下一帧调度均在持续工作。
- 当前剩余现象是“颜色对不上”。发送耗时和字节总量正常只能证明 ESP32 端完成了事务，不能证明 LCD 在 20 MHz 下逐位采样完全正确。

### 静态对照与待确认分支

- 当前低速初始化仍使用原正常路径的 `MADCTL=0x08 (BGR)`、`COLMOD=0x55 (RGB565)`、`RAMCTRL=00 F8 (little-endian)`；网关 JPEG 解码仍输出 `RGB565_LE`，旋转合成仍保留既有 R/B 交换。与 16 MHz 已通过版本相比，没有发现新增的固定端序或颜色通道配置差异，因此不能在没有画面证据时盲目切换 BGR 或字节序。
- 若画面结构稳定、纯色区域稳定，且仅表现为红蓝互换或所有颜色按固定规则变化，则可通过调整 MADCTL 或去掉/调整网关 R/B 交换修复；若颜色存在颗粒、跳变、条纹或同一区域颜色不固定，则更符合 20 MHz 像素数据采样误码，应继续在保持 20 MHz 的前提下调整 SCLK/MOSI 边沿与驱动组合，而不是修改 RGB565 语义。
- 本阶段先记录板测证据并等待颜色现象的截图或明确映射（尤其是红、绿、蓝的实际显示结果）；Codex 未构建、未烧录、未启动串口监视，未暂存、提交或推送。

## 2026-08-13 分速 20 MHz 颜色噪声：降低 SCLK 驱动边沿

### 现象判定

- 用户确认当前并非稳定的红蓝互换或固定字节序错误，而是颜色带噪声、条纹并且不稳定。因此不修改 `MADCTL`、`RAMCTRL`、RGB565 端序或网关侧 R/B 交换；这些固定颜色解释参数无法修复随时间和位置变化的位误码。
- 分速后窗口完整、画面持续更新且整帧物理传输约 61.4 ms，说明 10 MHz 控制命令路径与 20 MHz DMA 长度正确；剩余问题集中在 20 MHz SCLK/MOSI 的板级信号完整性与采样裕量。

### 单变量修正

- 保持 `APP_LCD_SPI_CMD_PCLK_HZ=10 MHz`、`APP_LCD_SPI_PCLK_HZ=20 MHz`、SPI mode 0、软件 CS 连续保持、DMA队列、RGB565和所有显示逻辑不变。
- 仅把 `APP_LCD_SPI_SCLK_DRIVE_CAP` 从档位3降到档位2；MOSI、CS、D/C仍为档位3。目的不是降低频率，而是减缓GPIO44时钟边沿，降低走线/连接器反射导致的过冲、振铃和二次阈值穿越，同时保持GPIO43数据线的建立速度。
- 下一次启动日志应显示 `command_request=10000000Hz`、`pixel_request=20000000Hz`以及 `drive_cap SCLK=2 MOSI=3 CS=3 DC=3`。若噪声和条纹消失，说明根因是最大SCLK驱动造成的时钟信号完整性问题；若仍异常，下一批对照依次为 `SCLK=1/MOSI=3`、`SCLK=2/MOSI=2`、`SCLK=3/MOSI=2`，无需再改命令频率或颜色端序。
- 本轮仅完成源码和工程日志修改；Codex未构建、未烧录、未启动串口监视，结果待用户板测。未暂存、提交或推送。

## 2026-08-13 分速 20 MHz 像素驱动组合全部失败：稳定边界结论

### 实机结果

- 用户在保持“命令10 MHz、像素20 MHz”的分速结构下，依次完成 `SCLK/MOSI=2/3`、`1/3`、`2/2`、`3/2` 四组驱动档位板测；所有组合均仍表现为颜色噪声、条纹或不稳定，没有得到可靠画面。
- 此前 `SCLK/MOSI=3/3` 同样颜色异常，因此当前已覆盖时钟档位1～3与数据档位2～3的主要组合。改变GPIO驱动强弱只能改变边沿速度和振铃幅度，全部失败说明问题不能再归结为某一个简单的驱动档位选错。

### 结论

- 当前硬件在20 MHz下存在两个可分离的问题：控制命令在20 MHz下可能无法可靠接收，表现为整屏黑屏；命令降至10 MHz后显示恢复，但20 MHz RGB565像素仍发生动态位误码，表现为颜色噪声和不稳定条纹。分速方案只解决了前者。
- 16 MHz已由同一硬件板测确认正常，20 MHz像素在所有主要驱动组合下均失败，因此现有主板、连接器/FPC和LCD模组组成的单线SPI链路可靠上限位于16 MHz与20 MHz之间。ESP32端DMA完成、字节数正确和约61.4 ms耗时不代表LCD端采样正确。
- 软件中的BGR、RGB565端序或JPEG颜色转换只会造成固定、可重复的颜色映射，不会造成随位置/时间变化的噪声和条纹；继续修改这些颜色参数没有依据。

### 后续路线

- 纯软件稳定方案：保留命令10 MHz分速结构，把像素频率降到16 MHz，或继续测试16～20 MHz之间的实际分频档位以寻找最高稳定点。
- 必须保持20 MHz时，需要进入硬件信号完整性处理：优先用示波器/逻辑分析仪观察LCD端SCLK是否有过冲、振铃或二次阈值穿越，并在源端给SCLK串联约22～47Ω阻尼电阻；必要时MOSI也串联约22～33Ω，同时检查地回流、FPC/连接线长度、支路和电源去耦。仅继续枚举软件驱动档位的收益已经很低。
- 本轮只记录用户实机结果并形成边界结论，没有继续修改颜色、端序、SPI频率或业务代码；Codex未构建、未烧录、未启动串口监视，未暂存、提交或推送。

## 2026-08-13 ESP32-S3 SPI3 中间频率分频限制复核

- 当前 `esp_lcd` SPI3设备未指定其他时钟源，ESP32-S3默认使用80 MHz APB作为GPSPI源时钟。ESP-IDF 5.5.3的 `spi_ll_master_cal_clock()` 通过整数 `pre × n` 分频选择最接近请求值的实际SCLK，不具备17～20 MHz区间的连续小数分频。
- 按本机ESP-IDF 5.5.3源码中的同一算法静态计算：请求17 MHz实际为16 MHz（80/5）；请求18 MHz实际仍为16 MHz（80/5）；请求19 MHz实际跳到20 MHz（80/4）。因此只修改 `APP_LCD_SPI_PCLK_HZ` 依次填写17/18/19 MHz，不会得到三个有效中间档位，也不能用于寻找16～20 MHz之间的稳定边界。
- ESP32-S3 SPI可选的另一时钟源是40 MHz XTAL，在该区间同样只能得到20 MHz等整数分频结果，不能提供17～19 MHz连续档位。无需也不应修改ESP-IDF SDK源码；若保持现有 `esp_lcd`/SPI3硬件时钟结构，实际可用相邻档位就是16 MHz与20 MHz。
- 本轮只进行ESP-IDF源码和分频算法静态核对，没有修改工程频率或SDK；Codex未构建、未烧录、未启动串口监视。未暂存、提交或推送。

## 2026-08-13 修改SDK能否生成18 MHz的结论

- 复核ESP32-S3本机ESP-IDF 5.5.3时钟树：GPSPI只支持 `SOC_MOD_CLK_APB` 与 `SOC_MOD_CLK_XTAL` 两种源，当前分别为80 MHz APB和40 MHz晶振。GPSPI硬件时钟寄存器使用整数 `pre × n` 分频，ESP-IDF的 `spi_ll_master_cal_clock()` 只是枚举这些硬件整数分频组合并选择最接近值。
- 因此修改ESP-IDF SDK的频率选择算法、去掉范围检查或强制返回18 MHz，都不能令SPI3物理输出真正18 MHz；80 MHz和40 MHz均不存在得到18 MHz的整数分频组合。实际寄存器只能落到16 MHz或20 MHz，若只改返回值则会造成日志与硬件频率不一致。
- 理论上只有更换可提供36/72 MHz等合适源频率的硬件时钟体系，或放弃GPSPI并用另一套可编程波形外设重新实现串行数据与时钟同步，才能生成18 MHz；这已经不是修改SDK常量或小补丁，且会显著增加DMA、同步和CPU负担，不适合作为当前LCD稳定提速方案。
- 当前工程未修改SDK或工程源码；建议保持命令10 MHz、像素16 MHz，若必须达到20 MHz则处理板级SCLK/MOSI信号完整性。本轮未构建、未烧录、未启动串口监视，未暂存、提交或推送。

## 2026-08-13 SPI1/SPI2能否提供16～20 MHz中间档位复核

- ESP32-S3的SPI2与SPI3同属GPSPI，使用相同的APB/XTAL时钟源和整数 `pre × n` 分频结构；把LCD从SPI3迁移到SPI2不会增加16～20 MHz之间的实际频率，仍只有80/5=16 MHz和80/4=20 MHz。
- 当前工程的SPI2已经由模组内部LR2021无线芯片占用：NSS=GPIO39、SCLK=GPIO40、MOSI=GPIO41、MISO=GPIO42。LCD使用GPIO44/43/48/47，是另一组物理线路；迁移会与无线链路冲突，并且没有频率收益。
- 本机ESP-IDF 5.5.3源码明确规定ESP32-S3通用SPI Master的有效主机仅为SPI2和SPI3；SPI1只在经典ESP32上可作为GPSPI。ESP32-S3的SPI1属于MSPI存储接口，当前工程又使用80 MHz DIO Flash和80 MHz Octal PSRAM，不能作为普通LCD SPI主机迁移目标。
- 因此SPI1、SPI2、SPI3中都不存在可直接用于本项目的18 MHz方案；当前稳定选择仍是命令10 MHz、像素16 MHz。若要求高于16 MHz，只能处理20 MHz板级信号完整性，或改用另一套可编程串行波形实现，后者不再是现有 `esp_lcd` SPI DMA路径。
- 本轮只完成工程与ESP-IDF源码静态核对，没有修改SPI主机、频率、SDK或业务代码；Codex未构建、未烧录、未启动串口监视，未暂存、提交或推送。

## 2026-08-13 软件提速路线重新评估：不局限于GPSPI整数频率

### 订正

- 此前“当前只能选择16 MHz或20 MHz”的结论只适用于ESP32-S3 GPSPI自身的整数分频档位；若把目标扩大为“通过软件提高LCD有效像素吞吐”，仍有改变时钟占空比、减少每像素传输位数或改用另一外设生成串行时钟等路线。

### 路线一：20 MHz像素时钟占空比整形

- ESP32-S3 GPSPI在20 MHz时使用80 MHz/4，默认正占空比128/256，对应高、低各约25 ns。SPI Master底层支持 `duty_cycle_pos`；设置为64/256时可形成约12.5 ns高电平、37.5 ns低电平。在mode 0下LCD于上升沿采样，而主机在此前下降沿更新MOSI，因此可把数据建立窗口由约25 ns扩大到约37.5 ns，同时仍满足ST7789V3手册高/低电平至少约7 ns的要求。
- 当前 `esp_lcd_panel_io_spi_config_t` 没有暴露 `duty_cycle_pos`，因此不能只改工程宏。实现可选择给本机ESP-IDF增加该字段，或在工程内仅为20 MHz像素通道改用原生 `spi_device_interface_config_t`；后者不污染SDK，但需要接管像素事务分块和完成回调。建议先测试25%正占空比，失败后再测试75%，用于区分数据建立与保持裕量。

### 路线二：稳定16 MHz下使用RGB444

- 已重新提取并渲染ST7789V3手册第69页。手册明确规定4-line串行接口支持 `COLMOD(3Ah)=03h` 的12-bit RGB444，两个像素按 `R1G1 / B1R2 / G2B2` 打包为3字节。该功能不是推测，而是控制器正式支持的像素格式。
- 240×320整帧RGB444仅需115200字节；在已验证稳定的16 MHz下，理论像素传输为57.6 ms，比20 MHz RGB565的61.44 ms还快约6.25%，比16 MHz RGB565的76.8 ms快25%。代价是颜色由65K降为4096色，摄像画面可能出现一定色阶带状。
- 可只在连续视频直传时切到RGB444并打包，普通LVGL界面继续RGB565；退出视频或发生LVGL刷新时恢复COLMOD和RAMCTRL。这样无需修改ESP-IDF SDK，也不依赖不稳定的20 MHz电气链路。

### 路线三：I2S1生成约18 MHz串行DMA

- ESP32-S3有两个I2S控制器，当前音频仅使用I2S0，I2S1为空闲。I2S时钟支持160/240 MHz源和分数MCLK分频，理论上可将I2S1的BCLK输出到LCD SCLK、DOUT输出到LCD MOSI，以约18 MHz连续发送像素位流，从而绕过GPSPI的16/20 MHz整数档位。
- 该路线需要自定义串行传输：处理I2S槽位/位序、DMA、GPIO矩阵切换、低速命令与高速像素之间的CS连续性，并验证与I2S0音频并行工作。它能保留RGB565，但改动面和板测风险明显高于前两条，应作为第三阶段实验方案。

### 当前建议

- 第一优先级是20 MHz、25%正占空比试验，因为不降低颜色深度且理论帧时不变；第二优先级是16 MHz RGB444，因为它完全避开20 MHz采样失真，并能获得约57.6 ms的更高有效吞吐；I2S1 18 MHz作为前两者不满足要求时的后备。
- 本轮使用PDF阅读规范核对了276页ST7789V3手册，并视觉检查第69页RGB444串行时序；仅完成源码、手册和驱动静态分析，没有修改工程代码或SDK。临时渲染页已清理。Codex未构建、未烧录、未启动串口监视，未暂存、提交或推送。

## 2026-08-13 单线 SPI 20 MHz、25% 正占空比板测版本

### 阶段目标

- 保持已经板测证明可消除黑屏的分速结构：初始化、CASET、RASET、RAMWR 继续使用 10 MHz；仅 RGB565 像素数据使用 20 MHz。
- 本轮只把 20 MHz 像素时钟的正占空比从默认 128/256 改为 64/256。在 ESP32-S3 SPI3 的 80 MHz/4 时钟下，目标波形约为高电平 12.5 ns、低电平 37.5 ns，用于扩大 mode 0 下 MOSI 从下降沿更新到下一上升沿采样之间的数据建立时间。
- 当前工作区的像素频率宏在实施前为 24 MHz；为避免测试版本混淆，本轮已按用户指定固定回 20 MHz。

### 新加入的功能与改动内容

- main/app_config.h：设置 APP_LCD_SPI_PCLK_HZ=20000000U，新增 APP_LCD_SPI_PIXEL_DUTY_CYCLE_POS=64U；10 MHz 命令时钟、四根 LCD 信号线驱动档位 3、队列深度 10 和 RGB565 均保持不变。
- main/bsp_lcd.c：低速命令通道继续使用 esp_lcd panel IO；纯像素通道改为工程内原生 spi_device_interface_config_t，显式填写 duty_cycle_pos=64。没有修改本机 ESP-IDF 5.5.3 SDK。
- 原生像素通道仍按 SPI 总线最大事务长度拆分整帧；当前 240×40×2=19200 字节一块，240×320 RGB565 整帧为 8 块。最后一块的 SPI 完成回调继续负责拉高软件 CS、释放颜色信号量、通知 LVGL 或视频帧等待者，并保留原有传输统计语义。
- 启动和首次刷新日志新增 pixel_duty=64/256。预期关键行应同时出现 pixel_request=20000000Hz、pixel_actual=20000000Hz 和 pixel_duty=64/256，用于确认烧录的确是本轮波形版本。

### 遇到的问题与处理

- esp_lcd_panel_io_spi_config_t 没有暴露 duty_cycle_pos，因此不能只增加一个宏而继续沿用其高速度 panel IO。当前通过工程内原生 SPI 像素设备接管分块和完成回调，避免污染全局 SDK，也不改变 LCD 控制命令协议。
- 内置补丁工具再次误判 Windows 文件编码，已按协作规则改用单片段精确替换；main/app_config.h 保持 UTF-8 无 BOM、CRLF，main/bsp_lcd.c 与本日志保持 UTF-8 无 BOM、LF。
- 普通 git diff --check 会把该仓库 CRLF 文件的新行尾 CR 误报为尾随空白；使用 core.whitespace=cr-at-eol 的只读检查后无空白错误，未修改仓库 Git 配置。

### 是否解决与待验证项

- 当前状态：代码实施完成，静态差异检查通过；20 MHz、25% 正占空比能否消除颜色噪声、条纹和不稳定仍待实机板测。
- 板测先确认串口明确打印 pixel_duty=64/256，再观察启动界面和连续摄像画面。若画面稳定且颜色固定正确，说明增加数据建立裕量有效；若仍为动态噪声或条纹，则本轮失败，后续可单独对照 75% 正占空比以判断保持时间方向。
- Codex 未构建、未编译、未烧录、未启动串口监视；未暂存、提交或推送。

## 2026-08-13 单线 SPI 20 MHz、25% 正占空比实机通过

### 实机板测结果

- 用户烧录 20 MHz 像素时钟、64/256 正占空比版本后确认：画面没有问题，一切正常；此前默认 50% 占空比下的颜色噪声、条纹和不稳定均未再出现。
- 连续视频日志中，seq=437 的 physical=61467 us、total=61951 us，seq=438 的 physical=61269 us、total=61978 us。两帧像素物理传输平均约 61.37 ms，与 20 MHz 发送 240×320×16 bit 的理论值 61.44 ms 基本一致，说明占空比整形没有降低像素时钟或牺牲整帧吞吐。
- 两次 LCD ISR 周期分别为 64973 us 和 65035 us，约对应 15.38 次/秒的完整屏幕更新；同时无线图像接收日志为 48～49 ms 一帧，即约 20.4～20.8 fps。当前接收链路已经快于整帧 LCD 输出，后续若继续提升实际显示帧率，主要约束重新回到 153600 字节全帧像素传输时间。

### 结论与根因收敛

- 本阶段问题已解决。相同的 20 MHz、RGB565、10 MHz 控制命令、软件 CS、DMA 分块和 GPIO 驱动档位下，仅把像素 SCLK 正占空比从默认 128/256 改为 64/256，画面便由动态噪声/条纹恢复为完全正常。该 A/B 结果直接排除了固定 RGB/BGR、字节序或像素长度错误作为此前异常根因。
- 结果强烈支持“20 MHz 默认 50% 波形下，LCD 采样上升沿之前的 MOSI 数据建立裕量不足”。64/256 使 mode 0 下下降沿到下一上升沿的低电平阶段由约 25 ns 扩大到约 37.5 ns，因此当前板级链路能够在保持 20 MHz 位率的同时可靠采样。
- 当前推荐保留 APP_LCD_SPI_PCLK_HZ=20000000U 与 APP_LCD_SPI_PIXEL_DUTY_CYCLE_POS=64U，不再测试 75% 占空比，也无需修改 ESP-IDF SDK、RGB565 格式或 GPIO 驱动档位。
- 本条依据用户已经完成的构建、烧录、串口日志和画面观察记录为实机验证通过；Codex 本轮未构建、未烧录、未启动串口监视，未暂存、提交或推送。
## 2026-08-13 20 MHz 视频首帧切换概率黑屏修复

### 现象修订与代码判定

- 用户进一步板测确认：20 MHz、64/256 正占空比在连续视频稳态下画面正常，但启动界面按键进入图像传输时仍有概率黑屏；一旦黑屏，后续持续接收日志仍在输出，屏幕却不会自行恢复。因此上一条“实机通过”的结论应收窄为“连续视频稳态链路通过”，不能视为按键到首帧的状态切换已经通过。
- 首帧呈现会先由 LVGL 隐藏画布并刷新页面背景，随后同一显示驱动立即提交整帧视频 DMA。原完成 ISR 没有记录当前像素事务的真实来源，而是用全局 `s_lcd_pending_count > 0` 和 `s_video_frame_inflight` 临时推断完成事件属于 LVGL 还是视频；这在两条显示路径交接时存在错误归类窗口。
- 原 `bsp_lcd_present_video_frame()` 的 1 秒超时分支只返回 `ESP_ERR_TIMEOUT`，没有清除 `s_video_frame_inflight`。因此首帧完成通知只要偶发丢失一次，后续所有视频帧就会持续命中“video frame still in flight”，形成“黑屏一次后永久黑屏”的软件锁死；该状态链与实机现象一致。

### 本轮修复

- `main/bsp_lcd.c` 为每次像素 DMA 增加显式事务归属：`LVGL`、`VIDEO`、`OTHER`。LVGL 刷新、直接视频帧和测试图案分别在提交时携带固定归属，不再由 ISR 根据两个可并发变化的全局状态猜测。
- 最后一块像素 DMA 的 ISR 现在先读取并清除事务归属，再更新 LVGL 或视频的完成状态，最后才释放 `s_lcd_color_idle`。这保证另一 CPU 核上的下一提交者不能在 ISR 尚未完成归类时覆盖归属状态。
- 即使 LVGL 统计队列异常，显式 `LVGL` 事务仍会调用 `lv_disp_flush_ready()`，避免把 LVGL 永久挂住；视频事务则按提交时的序号写入完成序号并唤醒对应等待者。
- 视频超时路径新增恢复判断：若 `s_lcd_color_idle` 表明像素 DMA 实际已经结束，则清除本帧遗留的 `s_video_frame_inflight`/归属状态，使 UI 回退刷新和下一帧仍可继续；若 DMA 确实尚未结束，则保持互斥状态，避免在活动事务上强行叠加新命令。
- 保持已通过的链路参数不变：初始化及窗口命令 10 MHz，RGB565 像素 20 MHz，像素 SCLK 正占空比 64/256，GPIO 驱动档位和 DMA 分块均未调整。

### 串口判据与板测要求

- 开始传输后的前三帧会输出 `[LCD-TRANSITION] submit video seq=1/2/3 lvgl_pending=... owner_mismatch=...`；正常完成仍紧跟原有 `[LCD] direct seq=...`。正常首帧预期 `owner_mismatch=0`，之后连续出现 direct 完成日志且画面正常。
- 若再次异常，重点保留完整的 `[LCD-TRANSITION]` 行：`reject video` 表示进入调用时仍残留活动视频状态；`video timeout ... pixel_idle=1 ... recovered=1` 表示 DMA 已完成但通知链异常，软件已解除锁死；`pixel_idle=0` 表示像素事务本身在 1 秒内没有完成，需要继续查 SPI/DMA 完成路径。
- 建议本轮板测不要只看一次：上电后重复执行“启动界面 → 按键开始传输 → 连续显示数帧 → 退出/重启”至少 20 次，统计是否仍出现黑屏，并在任何一次异常时从第一条 `[LCD-TRANSITION]` 起截取日志。
- 本轮仅完成源码修改、编码检查和静态差异检查；按 ESP32 协作规则，Codex 未构建、未编译、未烧录、未启动串口监视。修复是否消除概率黑屏等待用户烧录板测确认，未暂存、提交或推送。

## 2026-08-13 当前无线与 LCD 实际帧率统计

### 26.67 MHz占空比板测结果

- 用户已在 26.67 MHz 像素频率下测试约 33% 高电平（宏值 `64U`）和约 67% 高电平（宏值 `192U`）两种有意义的占空比，两者均不能得到正常画面。26.67 MHz 的周期只有 3 个 APB 时钟拍，其他宏值只会量化到上述两种有效波形，因此该频率不能继续依靠占空比细调解决。

### `D:\tmp\网关.txt`统计结果

- 日志更新时间为 2026-08-13 16:45:28，共统计到 114 条无线 `[FRAME]`/`RX done` 和 95 条 LCD `[LCD] direct`。日志没有包含启动频率打印，但 LCD 整帧物理传输平均 `61337 us`，与 20 MHz RGB565 理论 `61440 us`吻合，因此可判定该段是 20 MHz 显示链路，不是 26.67 MHz。
- 无线完整帧周期平均 `54.123 ms`，中位数 `54 ms`，范围 `51~60 ms`，对应实际 `18.476 fps`。其中空口 transfer 平均 `40.105 ms`，单看空口阶段约可达 `24.93 fps`；prepare 等阶段把端到端周期增加到约 54.1 ms。
- LCD 像素 DMA 物理时间平均 `61337 us`，范围 `61096~61658 us`；单看纯像素线速对应 `16.303 fps`。包含提交及等待的单帧调用平均 `61964 us`，对应 `16.138 fps`。
- LCD 相邻完成 ISR 周期平均 `65010 us`，序号 `855~949` 在时间戳 `58871~64982 ms` 之间的实测结果同样为 `15.382 fps`。约 `3.05 ms` 的额外周期来自帧切换、调度及窗口命令等间隔，但即使完全消除这部分，20 MHz RGB565的61.44 ms物理下限仍无法达到20 fps。
- 当前实际显示帧率与无线新帧率之比约为 `15.382 / 18.476 = 83.25%`。现有三缓冲/待显示帧替换机制会保留最新帧，因此约有16.75%的已接收画面不会实际送到LCD，约等于每6个无线帧跳过1个旧帧；这不是无线丢包，而是显示输出慢于输入后的主动追新。

### 达到20帧的约束

- LCD要达到20次完整新画面刷新，整帧完成周期必须不高于50 ms；当前20 MHz RGB565仅像素数据就需要61.44 ms，因此必须提高有效像素位率或减少每帧发送位数，不能只靠队列、信号量或LVGL调度优化实现。
- 无线端当前平均54.123 ms，即使LCD达到20 fps，也只能提供约18.48 fps的新画面。若目标是端到端真实20帧，无线完整周期也必须降至50 ms以内；当前约40 ms空口传输加约14 ms prepare，需再缩短约4.1 ms。此前较小JPEG、8个分片的日志曾达到约48~49 ms，说明可从JPEG大小、分片数或prepare阶段继续优化。
- 本轮仅分析用户提供的日志并记录统计结果，没有修改显示或无线代码。Codex未构建、未编译、未烧录、未启动串口监视，未暂存、提交或推送。

## 2026-08-13 提高 JPEG 画质后的无线/LCD帧率再平衡

### 最新日志统计

- 最新 `D:\tmp\网关.txt` 更新时间为 2026-08-13 17:44:00。本次连续区间统计到37条无线完整帧和34条LCD整帧完成记录。
- 无线 `[FRAME]` 周期平均 `59.297 ms`，范围 `56~62 ms`，对应 `16.864 fps`；`RX done`总周期平均 `59.351 ms`，对应 `16.849 fps`。其中空口传输平均 `44.162 ms`，prepare平均 `15.216 ms`。
- LCD像素物理传输平均 `61357.8 us`，对应纯线速 `16.298 fps`；单次直接呈现调用平均 `61950.9 us`，相邻LCD ISR完成周期平均 `64940.1 us`。序号27~60在2143 ms内完成，实际刷新率为 `15.399 fps`，两种统计口径一致。
- 无线与LCD实际周期差为 `5.643 ms`，帧率差为 `1.465 fps`；LCD输出能力约为无线输入的 `91.31%`。因此当前已经从上一轮的明显失衡进入基本平衡，但仍不是严格的一进一出：长期运行时大约每11~12个接收帧仍会由最新帧覆盖1个尚未显示的旧帧。

### 画质与吞吐变化

- 本轮JPEG平均 `5727.7 bytes`，范围 `5674~5763 bytes`，全部为12个分片。上一轮平均画面约4760 bytes、10个分片；当前每帧JPEG数据量约增加20%，与用户观察到的画质改善一致。
- 画质提升使空口传输由上一轮约40.1 ms增加到44.2 ms，无线完整周期由54.1 ms增加到59.3 ms，实际无线帧率从18.48 fps降到16.86 fps；LCD仍保持约15.4 fps，因此两端匹配率反而从83.25%提高到91.31%，无效排队/旧帧覆盖显著减少。
- 当前参数是较合理的“画质优先且吞吐接近LCD上限”的平衡点。若目标是充分利用现有20 MHz RGB565 LCD而不刻意追求20 fps，可继续保留；若要求严格减少旧帧覆盖，可把无线目标周期调到约65 ms，但帧率会降到约15.4 fps；若目标仍是端到端20 fps，则LCD和无线两边都必须继续提速，当前配置尚未达到。
- 本轮仅分析用户更新的日志并记录结论，没有修改显示、无线或JPEG参数。Codex未构建、未编译、未烧录、未启动串口监视，未暂存、提交或推送。

## 2026-08-13 当前图传端到端延时拆解

### 当前网关日志可直接证明的延时

- 本次继续使用更新时间为 2026-08-13 17:44:00 的 `D:\tmp\网关.txt`。`RX done` 中 `prepare` 是网关发出本帧请求到收到 `ImageStart` 的时间，平均 `15.216 ms`；`transfer` 是 `ImageStart` 到完整收到 EOT 且无缺片的时间，平均 `44.162 ms`；请求到无线接收完整合计平均 `59.351 ms`。
- `[FRAME] consume` 覆盖接收完成后的 JPEG 重组、解码和旋转合成，平均 `28.838 ms`；当前 `queue` 均为 `0 ms`，重组基本为 `0~1 ms`，解码约 `11~14 ms`，合成约 `16~19 ms`。因此“本帧请求发出→完成可显示 RGB565 帧”的平均延时约为 `59.351 + 28.838 = 88.189 ms`。
- 用每条 `[LCD] direct` 的日志时刻减去该条 `total`，反推出 LCD 调用开始时间，再与此前最近且尚未映射的 `[FRAME]` 完成时刻关联；在日志内可完整关联 32 帧。RGB565 帧准备完成到 LCD 开始提交平均等待 `28.80 ms`，中位数 `27.07 ms`，范围约 `0.04~56.06 ms`。这是无线约 `59.3 ms/帧` 与 LCD 约 `64.94 ms/帧` 不完全同相导致的待显示相位等待；待显示缓冲会保留最新帧，所以不会无限累积。
- 当前 LCD 整屏像素按从上到下顺序传输，物理像素阶段平均 `61.358 ms`，整个阻塞调用平均 `61.951 ms`。以屏幕中部作为一幅画面“实际可见”的代表位置，LCD 开始提交后约再需 `30.68 ms`；屏幕底部/整帧发送完约需 `61.95 ms`。
- 据此，当前网关侧从“发出本帧请求”计时：到 LCD 开始提交平均约 `116.99 ms`，到屏幕中部约 `147.67 ms`，到整屏发送完成约 `178.94 ms`。因为物体在画面中的纵向位置不同，单帧肉眼可见时刻会落在 LCD 扫描窗口内；32 帧关联样本的“RGB565 准备好→屏幕中部”约 `30.7~86.8 ms`，“RGB565 准备好→整屏完成”约 `62~118 ms`。

### 节点编码与现实物理世界延时估算

- `D:\tmp\节点.txt` 的更新时间为 2026-08-12 16:24:02，不是本轮提高 JPEG 画质后的同一次测试，不能与当前网关数据当作严格同步测量。它只能用于估算节点流水线结构和量级：57 个样本中，已完成原始帧的 `raw_age` 平均 `35.56 ms`（`12~55 ms`），采集/缩放平均 `12.83 ms`，JPEG 编码平均 `19.24 ms`，合计预取平均 `32.82 ms`。
- 节点采用预取流水线：采集和编码下一帧与当前帧无线发送重叠，因此这约 `32.82 ms` 通常不会直接加到同一轮网关 `prepare` 后面；编码完成的预取帧还会等待当前无线帧结束，日志中该等待平均约 `20.32 ms`，随后才作为下一帧发送。
- 从“一帧开始进入节点采集/缩放”算到当前屏幕中部，参考值约为 `12.83 + 19.24 + 20.32 + 59.35 + 28.84 + 28.80 + 30.68 = 200.06 ms`；到整屏完成约 `231.33 ms`。若从摄像头已经完成、但在原始帧槽中等待处理的内容时刻算，还应加入旧日志平均 `raw_age=35.56 ms`，得到屏幕中部约 `235.62 ms`、整屏完成约 `266.89 ms`。
- 因此当前现实场景的典型“物体变化→画面中部可见”可暂估为约 `0.23~0.27 s`，整幅画面全部刷完约 `0.27~0.31 s`。这个范围还包含一个无法由现有软件日志精确量出的摄像头曝光、传感器逐行读出及物体变化落入哪一帧的相位误差；严格的 motion-to-photon 结果应使用毫秒计时器/闪烁 LED 与屏幕同框高速录像测量。

### 当前主要延时来源

- 当前可直接优化的最大串行段是：无线请求与接收约 `59.35 ms`、网关解码/合成约 `28.84 ms`、等待下一次 LCD 提交平均约 `28.80 ms`、LCD 扫到画面中部约 `30.68 ms`。预取已经把节点采集和编码的大部分时间藏在上一帧无线传输期间，单纯继续缩短 JPEG 编码不会等比例降低端到端延时。
- 若后续要降低体感延时，优先级应是减少 LCD 整帧扫描时间/避免整帧发送、降低无线完整周期，以及让显示提交节拍更贴近新帧完成节拍；仅把无线与 LCD 帧率调得更接近主要减少旧帧覆盖和抖动，不会自动消除约 `61 ms` 的整屏扫描时间。
- 本轮只读取代码和日志、进行静态时序关联并更新工程日志，没有修改业务代码。Codex未构建、未编译、未烧录、未启动串口监视，未暂存、提交或推送。
## 2026-08-18 双向音频通话 V1

## 新加入的功能

- CONFIG 的 AUDIO 区域新增 `Intercom` 开关；双方完成 CONFIG ACK 和 START/首 reply 两阶段握手后，UI 才提交开启状态。
- 新增网关主时隙、节点回复时隙的双向 Opus 通话；20 ms TDD 周期、2 ms 节点保护间隔，每时隙最多 2 个 10 ms 音频帧。
- 新增 TDD 内 STOP/STOP_ACK 关闭握手、重复 STOP 幂等 ACK、会话号校验和节点主同步丢失自动退出。
- 新增轻量 NLMS 回声消除，两端均以实际送入 I2S 的播放 PCM 作为 far-end reference；双讲期间冻结自适应。

## 改动内容

- `main/app_config.h`：增加 Intercom 配置键、TDD、超时、队列、输入增益和 AEC 初始参数；保留 JPEG Q30、LCD 20 MHz、占空比 64/256。
- `main/radio_ping.cpp/.hpp`：增加会话状态机、START/STOP 标志、双向音频时隙、网关编码任务、普通包隔离、图像入口保护及低功耗恢复逻辑。
- `main/echo_canceller.cpp/.hpp`、`main/CMakeLists.txt`：新增 256 tap NLMS AEC 组件并纳入 main component。
- `main/ui_gateway.c/.h`、`main/app_main.cpp`：增加 Intercom 开关和回调；通话期间锁定冲突控件、停止视频并把网关音频改为全双工初始化。
- `main/audio_diagnostics.cpp/.hpp`：两端进入通话时禁用本地录音诊断，避免两个任务同时读取 I2S RX。

## 遇到的问题

- 多个工作树源码通过普通文件 API 读取时出现共同异常字节前缀，但 Git 内容层哈希与 HEAD 一致。已先用隔离探针确认 Git blob 回写工程目录保持明文，再只对本次涉及且无正文差异的文件恢复可编辑文本；没有覆盖用户业务改动。
- 当前依赖中没有 ESP-SR/AFE 等现成 AEC 组件，不能把旧 `AudioProcessor` 的噪声门或啸叫静音当成 AEC，因此 V1 增加了工程内 NLMS 实现。其 40 ms 参考延迟是静态初值，不是板测结论。
- 网关原先只初始化播放 I2S，无法采集麦克风；本轮改为全双工，并创建与节点相同的音频 TX task。

## 是否解决

- 代码实现和静态检查：已完成。`git diff --check` 通过；本次源码均为合法 UTF-8、无 BOM、无双 CRLF；新增接口引用已核对。
- 构建、烧录、双板通话、无线稳定性和 AEC 效果：未完成，待用户验证。
- 未执行 `idf.py build`、烧录或串口监视；未暂存、提交或推送。

## 2026-08-18 双向通话首次构建错误修复

## 新加入的功能

- 本次没有新增功能，仅修复双向通话 V1 的 C++ 编译声明顺序问题。

## 改动内容

- `main/app_main.cpp`：在文件前部已有的函数声明区补充 `on_gw_rx_abort()` 前置声明，使 `on_gw_intercom_change()` 可以在该函数实体定义之前调用它。

## 遇到的问题

- 用户构建时报错：`on_gw_rx_abort was not declared in this scope`。根因是调用点位于函数定义之前，且此前缺少前置声明。
- 同次构建的 `on_lcd_capture()` 和 `prefetch_discard()` 未使用提示属于警告，不是本次构建中止原因，本轮未作无关清理。

## 是否解决

- 代码层面的声明缺失已修复，并完成静态引用检查。
- 修改后未构建、未烧录，待用户重新构建验证；未暂存、提交或推送。

## 2026-08-24 图片流卡死、Intercom 无法取消与双向通话首次运行诊断

## 问题复核

- 当前分支为 `AM36_Electronic_doorbell_special`，双向通话 V1 位于 `d7b26c0`；上一版图片基线为 `08fd8fb`。用户当前把 `APP_LCD_SPI_PCLK_HZ` 从 20 MHz 调为 16 MHz，本轮保留该本地改动，也保留精心设定的 `APP_LCD_SPI_PIXEL_DUTY_CYCLE_POS=64U`。
- V1 文档写明 AEC 的 CPU 预算、内存和实际延迟尚未测量，但实现却默认在每个 10 ms 采音帧运行 256-tap 浮点 NLMS。参考分支的首次 TDD 板测明确把实时 DSP 关闭，因此当前实现跳过了“先验证基础 TDD/Opus、再接 AEC”的阶段边界。
- 当前 `service_intercom()` 缺少参考例程中的低功耗 CAD 到 FLRC 显式恢复。节点可能已完成 CONFIG ACK，却仍不能可靠接收第一个 START 主时隙。
- V1 给网关新增优先级 5 的 `voice_tx` 后，即使未开启 Intercom 也每 10 ms 持续读取麦克风；同在 core 1 的 LVGL 为优先级 4、JPEG 消费任务为优先级 3。当前分支没有网关事件音频预编码消费者，因此该空转会无功能收益地抢占基础图片链。
- 关闭流程不论 STOP_ACK 是否收到都会完成本地清理，但原函数在 ACK 超时时返回 `false`；CONFIG UI 因而把已经关闭的开关重新拨回 ON，形成“只能设置、不能取消”的直接表现。
- 图片 abort 原位于 radio task 的 intercom `continue` 之后；一旦通话状态先置 active，abort 请求会在整个通话期间得不到消费，留下旧的 ImageCmd/RX 重组状态。

## 改动内容

- `main/radio_ping.cpp`：首次板测临时设置 `APP_INTERCOM_AEC_ENABLE=0`，只隔离尚未测量的 NLMS，保留 TDD、Opus 编解码和全双工 I2S；没有修改 `app_config.h` 的 LCD 频率或占空比。
- `main/radio_ping.cpp`：网关未进入 Intercom 时暂停麦克风读取任务，恢复双向通话引入前的图片/LVGL CPU 边界；节点侧采音、声音触发和 PIR 逻辑不变，网关进入通话后自动恢复采音。
- `main/radio_ping.cpp/.hpp`：把图片 abort 提到 intercom 分支之前处理；开启通话前最多等待 100 ms 确认 abort 已被 radio task 消费，并输出请求状态快照。
- `main/radio_ping.cpp`：按参考 TDD 例程补回低功耗 CAD 到 FLRC 的恢复；增加 START、首个 node reply、STOP_ACK、TX 失败、麦克风输入电平和播放输出电平日志。
- `main/radio_ping.cpp/.hpp`：增加图片请求入队、ImageCmd 等待、节点接受状态、ImageCmdAck、首个 ImageStart、10 秒 RX 超时和 abort 清理日志；高频 ImageStart 重发只记录每个会话的第一次，避免刷屏。
- `main/radio_ping.cpp`：STOP_ACK 超时仍保留告警，但本地清理完成后返回成功，让 UI 正确落到关闭态；远端仍由重复 STOP 和 1.2 秒链路失联保护退出。
- `main/app_main.cpp`：增加 CONFIG Intercom 请求结果、耗时和最终 active 状态日志。

## 板测判据

- 基础图片流：正常顺序应出现 `image capture queued` → `image request start` → `RX ImageCmdAck` → `RX ImageStart` → `RX done` → `[FRAME]` → `[LCD] direct`。若每秒只有 `image request waiting`，问题在 ImageCmd/节点响应之前；若已有 `RX done/[FRAME]` 但无后续 `[LCD] direct`，问题在显示提交链。
- 双向通话：开启时两端应有 `intercom local start`，节点出现 `intercom START received`，网关出现 `intercom first node reply` 和 `intercom START confirmed`。约 1 秒后两端都应出现 `intercom mic` 与 `intercom playback`，可分别判断麦克风是否为近零以及无线下行是否真正进入解码播放。
- 关闭时网关应出现 `intercom STOP_ACK received`；即使 ACK 丢失，也应出现 `intercom stop ACK timeout`、`intercom local close complete remote_ack=0`，CONFIG 开关保持关闭，随后图片请求不再被 intercom 门禁拦截。

## 是否解决

- 已完成源码修改和静态差异检查；未修改用户的 16 MHz LCD 配置。
- 按 ESP32 协作规则，本轮未执行构建、编译、链接、烧录或串口监视；实际图片流、CONFIG 开关、TDD 双向语音和 AEC 后续启用仍待用户双板验证。
- 未暂存、未提交、未推送。

## 2026-08-24 网关 SoftAP 启动崩溃与内部 DMA 内存收紧

## 问题复核

- 用户提供的网关日志明确出现 `wifi:alloc eb len=752 type=4 fail`，随后 Core 0 在 `ieee80211_hostap_attach -> wifi_softap_start` 中触发 `LoadProhibited`；`A2=0`、`EXCVADDR=0x2c` 表明 Wi-Fi SoftAP 启动的失败路径发生了空指针访问。`RTC_SW_CPU_RST` 是 panic 后的软件重启结果，不是看门狗或欠压复位。
- `08fd8fb` 网关使用 TX-only I2S，默认 6 个 DMA descriptor；双向通话版本改为全双工后复用了节点侧 `dma_desc_num=24`，该深度同时作用于 TX 和 RX。按 16 kHz、每 descriptor 240 帧计算，网关全双工环由约 11.5 KB 扩大到约 46 KB 内部 DMA RAM。
- Wi-Fi 位于音频、无线任务、LCD/LVGL 和图片流水线之后启动；结合 beacon 分配失败，当前最高可能根因是双向通话增加的内部 DMA/任务资源使 SoftAP 启动时内部内存不足或碎片化。最终仍需新增堆日志板测确认。

## 新加入的功能与改动内容

- `main/bsp_audio.c/.h`：新增 `bsp_audio_init_gateway_duplex()`。网关全双工固定使用默认 6 个 descriptor（约 90 ms 环深度），节点原 `bsp_audio_init()` 继续保留 24 个 descriptor，避免影响节点拍照/JPEG 竞争场景；相对当前网关配置预计释放约 34.5 KB 内部 DMA RAM。BSP 保存当前 descriptor 数，低功耗/拍照后的 I2S 恢复继续沿用网关 6 或节点 24，不会恢复成错误深度。
- `main/bsp_audio.c`：I2S 启动日志增加 `desc` 和估算 `ring` 毫秒数，便于确认两种角色实际使用的 DMA 深度。
- `main/app_main.cpp`：在启动、音频完成、无线任务完成、Wi-Fi 启动前后记录内部堆和 DMA 堆的 `free/largest/minimum`；网关调用新的 6-descriptor 全双工入口，并检查 `wifi_mgr_init()` 返回值。
- `main/wifi_manager.c`：在 `esp_wifi_init()` 和 `esp_wifi_start()` 前后增加同一组堆指标；补充检查 `esp_wifi_set_config()` 返回值，避免配置失败被静默忽略。
- 保留 `APP_LCD_SPI_PCLK_HZ=16 MHz` 和 `APP_LCD_SPI_PIXEL_DUTY_CYCLE_POS=64U`，本轮没有调整 LCD 时序、JPEG、射频或 CONFIG/TDD 协议参数。

## 是否解决

- 已完成源码修改、唯一片段匹配、Git 行级差异和 `git diff --check` 静态检查；没有整文件换行噪声，没有临时脚本残留。
- 未构建、未烧录，SoftAP 是否不再崩溃以及图片流/双向通话是否稳定均待用户板测。重点保留从 `[HEAP] startup` 到 `[HEAP] after esp_wifi_start` 的完整启动日志；若仍崩溃，最后一条 `[HEAP]` 可直接给出失败前的内部空闲量和最大连续块。
- 未暂存、未提交、未推送。


## 2026-08-24 双向通话改为设置启用后由拍照键触发

## 新加入的功能

- CONFIG 的 Intercom 开关改为“启用/待触发”设置；打开开关不再立即发送 CONFIG 或建立无线通话。
- 设置启用后，网关 K3 拍照键和设置页 CAPTURE 按钮统一触发双向通话 START 握手；未启用时仍保持原连续图片流功能。
- 新增独立 Intercom 状态页，显示准备、连接、通话中和连接失败四种状态。

## 改动内容

- main/ui_gateway.c/.h：拆分 Intercom 设置状态和实际会话状态；保留 CAPTURE 控件可用，增加统一触发入口、Intercom 页面及无线状态同步接口。Intercom 设置现在可从 NVS 恢复，但重启后不会自动恢复实际通话。
- main/app_main.cpp：无线层进入或退出 Intercom 时同步更新网关 UI；真正的 set_intercom(true) 只会由启用后的拍照入口调用。
- 未修改 LCD 16 MHz、像素占空比 64/256、JPEG、I2S descriptor 或 TDD/Opus 无线协议参数。

## 遇到的问题

- 受保护源码仍不能由内置补丁工具直接按 UTF-8 打开；本轮按工程既有规则改用 Python 唯一片段脚本，所有目标片段在写入前均要求只匹配一次。

## 是否解决

- 代码修改与静态检查已完成：接口引用、页面枚举、K3/CAPTURE 统一入口、设置关闭路径、NVS 状态、LF 换行和 Git 行级差异均已核对，git diff --check 通过。
- 按 ESP32 协作规则未构建、未烧录、未板测；设置开关、K3/CAPTURE 触发、Intercom 页面和实际双板通话仍待用户验证。
- 未暂存、未提交、未推送。


## 2026-08-24 启用双向通话 AEC 与强化 Intercom NVS 持久化

## 问题复核

- 用户双板相距约 5 米仍由轻微声音迅速触发啸叫。代码确认 APP_INTERCOM_AEC_ENABLE 被首次联调临时宏固定为 0，因此 NLMS AEC 实际完全没有运行，不是参数效果不佳。
- 普通解码帧会进入 far-end reference，但丢包后实际播放的 Opus PLC 帧此前没有进入参考环；启用 AEC 后，这会让参考时间线在每次丢帧后永久错位。
- 网关 UI 已使用 ui_gw/intercom 保存启用状态，但原保存函数忽略 nvs_open、nvs_set_u8 和 nvs_commit 的返回值，写入失败时开关仍会显示成功，无法从日志定位。
- 节点收到的 APP_CFG_KEY_INTERCOM 值是本次通话 session，不是持久化布尔参数，因此不把该临时 session 写入节点 NVS。

## 改动内容

- main/app_config.h：新增 APP_INTERCOM_AEC_ENABLE=1，正式启用两端 256-tap NLMS AEC；输入增益、40 ms 参考延时和 NLMS 参数暂不改动。
- main/radio_ping.cpp：删除首次联调的本地 AEC=0 覆盖；普通播放和 PLC 补偿播放都按实际送入 I2S 的 PCM 推入 AEC reference，保持丢包后的参考连续性。
- main/ui_gateway.c：NVS u8 保存改为检查 open/set/commit 全部返回值；Intercom 开关只有保存成功才提交 UI 状态，关闭失败时恢复原持久化值，并增加 persisted=1/0 日志。
- 按用户要求未加入削波/能量啸叫检测，也未加入自动静音保险；未修改 LCD 16 MHz、像素占空比 64/256、I2S descriptor 或 TDD 时序。

## 是否解决

- 代码修改与静态检查已完成：AEC唯一配置、普通/PLC参考路径、NVS错误回退、文件换行和Git行级差异均已核对；按CRLF规则执行的git diff --check通过。
- 未构建、未烧录、未板测；AEC CPU 占用、40 ms参考延时、实际回声抑制和NVS掉电恢复仍待用户验证。
- 未暂存、未提交、未推送。


## 2026-08-24 AEC 启用后 CPU1 任务看门狗修复

## 问题复核

- 网关日志连续触发任务看门狗，超时用户为 IDLE1，CPU1 当前任务始终是 voice_tx；两次回溯分别落在 EchoCanceller::process_capture 的256-tap估计循环和系数更新循环。
- sdkconfig 的任务看门狗超时为5秒、FreeRTOS tick为1000 Hz。原NLMS每个10 ms/160采样帧对每个采样执行一次256-tap估计、一次256-tap归一化能量和一次256-tap系数更新，并在tap内反复做环形取模；voice_tx保持永久ready，导致CPU1的IDLE任务无法运行。
- 无线TDD仍保持missed=0，因此这不是射频时隙错误，而是AEC计算路径的CPU调度问题。

## 改动内容

- main/echo_canceller.cpp：保留256 taps和40 ms参考延时；每帧一次性把far-end窗口复制为连续float数组，移除tap热循环中的环形取模；归一化能量改为滑动更新；系数每4个采样更新一次并按stride补偿步长，输出估计仍逐采样执行。
- main/app_config.h：新增APP_AEC_ADAPT_STRIDE=4，作为可调的AEC计算/收敛折中参数。
- main/radio_ping.cpp：每10个采音帧阻塞voice_tx 1 ms，为CPU1 IDLE提供确定调度窗口；麦克风诊断新增aec=...us单帧耗时。
- 按用户要求继续启用AEC，不加入削波/能量啸叫检测或自动静音；NVS持久化修复和PLC参考连续性修复保留。

## 是否解决

- 源码修改与静态差异检查已完成：连续参考窗口在160采样帧下使用415个float、索引范围0～414，未发现越界；新增局部窗口约1.7 KB，voice_tx任务栈为16 KB；git diff --check通过，相关文件换行格式保持不变。
- 未构建、未烧录、未板测；需确认两端不再出现IDLE1 watchdog，并通过aec耗时、TDD missed和实际回声效果判断优化结果。
- 未暂存、未提交、未推送。

## 2026-08-24 双向通话改用官方 ESP-SR AFE AEC

## 问题复盘

- 板测确认此前自研 256-tap NLMS 虽已解决 CPU1 看门狗占满问题，但说话音质仍明显不清晰；它不是乐鑫官方 AEC，并且固定 40 ms 参考偏移没有对应两端实际 I2S 播放队列。
- `bsp_audio.c` 当前网关使用 6 个、节点使用 24 个 DMA descriptor，每个 descriptor 为 15 ms，因此播放参考相对实际扬声器输出分别可能提前约 90 ms 和 360 ms；两端统一使用 40 ms 会使节点参考严重错位，AEC 无法稳定收敛。

## 本次工程改动

- `main/idf_component.yml`：增加官方 `espressif/esp-sr ^2.5.1` 组件；`main/CMakeLists.txt` 增加 `espressif__esp-sr` 依赖。`dependencies.lock` 留给下一次 IDF Component Manager 配置阶段自动解析，不手工伪造锁文件。
- `main/echo_canceller.cpp/.hpp`：删除自研浮点 NLMS，改为 ESP-SR AFE 的 `MR`（单麦克风 + 单播放参考）输入，采用 `AFE_TYPE_FD + AFE_MODE_LOW_COST + AEC_MODE_FD_LOW_COST`，NLP 使用 `AEC_NLP_LEVEL_NORMAL`；仅打开 AEC，明确关闭 SE、NS、VAD、WakeNet 和 AGC，不引入识别模型分区。
- 项目 Opus/I2S 仍以 16 kHz、单声道、10 ms/160 samples 工作；封装层按 AFE 运行时返回的 feed/fetch chunk size 累积并交织输入，再把输出重新切回 160-sample Opus 帧。首次攒够 AFE 帧前输出静音，避免把未处理的近端原声直接送回远端形成启动回声。
- AFE 和桥接缓冲优先放入 PSRAM；AFE 后台任务固定 CPU1、优先级 4。AFE 在用户按键真正发起通话时才创建，结束通话立即释放，避免在 SoftAP 启动前长期占用内部 RAM；AEC 创建失败时，网关拒绝开始通话，节点不回 Intercom CONFIG ACK，避免 UI 显示已通话但实际没有消回声。
- `main/app_config.h`：根据当前 I2S DMA 深度分别配置网关 90 ms、节点 360 ms 参考偏移，滤波长度 4，NLP 取 NORMAL；保留后续板上校准入口。
- `main/radio_ping.cpp/.hpp`：初始化并检查 AFE；删除为自研 NLMS 临时加入的每 100 ms 强制延时；Intercom CONFIG ACK 等待按启动超时扩展到 1200 ms，以容纳节点首次创建 AFE；日志增加 AFE ready、输出预热、参考生效帧数、累计平均/最大耗时和输入增益削顶累计值。
- 按用户要求没有加入削波/能量啸叫检测、紧急静音或自动静音保险。没有修改 LCD：`APP_LCD_SPI_PCLK_HZ=16000000U`、`APP_LCD_SPI_PIXEL_DUTY_CYCLE_POS=64U` 保持不变。

## 验证状态

- 已完成唯一片段写入、官方 2.5.1 API/字段静态核对、依赖和调用链检查；`git -c core.whitespace=cr-at-eol diff --check` 通过，所有相关文件保持原换行和无 BOM，运行代码中无自研 NLMS/啸叫静音残留。
- 按 ESP32 协作规则，本次未构建、未烧录、未监视串口；AFE 实际 feed/fetch 帧长、PSRAM 占用、CPU1 看门狗、双讲清晰度和 90/360 ms 参考偏移仍待用户双板实测。
- 未暂存、未提交、未推送。

## 2026-08-24 ESP-SR AFE 模式与生命周期纠正

## 问题纠正

- 上一版把门铃双向语音错误配置为 `AFE_TYPE_FD + AFE_MODE_LOW_COST + AEC_MODE_FD_LOW_COST`，不符合本项目普通语音通话场景；同时在停止通话和网关 CONFIG 失败时销毁 AFE，也不符合后续音视频同传需要常驻音频前端的设计。
- 本次按要求纠正为 `AFE_TYPE_VC + AFE_MODE_HIGH_PERF + AEC_MODE_VOIP_HIGH_PERF`。算法使用 ESP-SR 官方 AFE/AEC 实现，没有保留自研 NLMS、削波/能量啸叫检测或自动静音。

## 本次工程改动

- `main/echo_canceller.cpp`：AFE 类型改为 VC，高性能模式，AEC 明确改为 VOIP_HIGH_PERF；保留官方建议值 `aec_filter_length=4`。删除 `aec_nlp_level` 覆盖，因为 ESP-SR 文档说明该 NLP 配置只对 FD AEC 生效。
- `main/echo_canceller.hpp`、`main/radio_ping.cpp`：删除运行期 `deinit()` 接口；AFE 第一次真正开始通话时创建并保持常驻，停止通话、CONFIG 失败和下一次开始通话只调用官方 `reset_buffer` 并清空桥接状态。对象析构和初始化失败清理仍保留 `destroy`，正常运行期不销毁。
- 项目现有 Opus/I2S 帧为 10 ms，而 VOIP AEC 官方处理帧为 16 ms；保留按 AFE 运行时 chunk size 查询结果执行的 10 ms/16 ms 累积与切片适配。该桥接、两端 I2S DMA 参考延迟补偿、互斥和诊断属于项目接入层，AEC 算法本身未修改。
- LCD 继续保持 `APP_LCD_SPI_PCLK_HZ=16000000U` 和 `APP_LCD_SPI_PIXEL_DUTY_CYCLE_POS=64U`，未改射频 TDD、Opus 帧长和 I2S DMA 深度。

## 验证状态

- 已完成模式、生命周期和运行期调用链的静态核对；未构建、未烧录、未板测。VOIP_HIGH_PERF 在 ESP32-S3 上的 CPU/内存占用、AEC 实际耗时、双讲音质、无线 missed 和未来音视频并发余量待双板验证。
- 未暂存、未提交、未推送。

## 2026-08-24 ESP-SR 与 esp_new_jpeg 依赖冲突修复

## 遇到的问题

- 用户执行 `idf.py reconfigure` 时版本解析失败：项目直接依赖 `espressif/esp_new_jpeg ^0.6.0`，而新增的 `espressif/esp-sr ^2.5.1` 依赖 `esp-dl 3.3.9`，后者要求 `espressif/esp_new_jpeg ^1`；两个主版本约束没有交集，因此 CMake 配置阶段终止。

## 改动内容

- `main/idf_component.yml`：将 `espressif/esp_new_jpeg` 约束由 `^0.6.0` 调整为 `^1.0.0`，允许 Component Manager 解析 ESP-SR 所需的 1.x 版本。
- 静态核对工程使用的 `jpeg_enc_open/process/close`、`jpeg_dec_open/parse_header/get_outbuf_len/process/close`、对齐内存接口及配置字段；这些入口在 1.x 仍保留，本次不修改 JPEG 编解码业务代码。
- 未手工填写 `dependencies.lock` 的组件哈希和传递依赖。静态复查时，本地依赖解析已在该文件中选出 `esp-sr 2.5.1`、`esp-dl 3.3.9` 和 `esp_new_jpeg 1.0.2`；但该文件当前被 `.gitignore` 忽略，且 `managed_components` 中 JPEG 仍是 0.6.1、ESP-SR 目录尚未生成，说明组件下载和配置尚未完成。

## 是否解决

- 依赖约束冲突的根因和 manifest 修改已完成，本地 lock 已得到兼容版本组合；尚未确认重新配置成功、未构建、未烧录，待用户重新执行 `idf.py reconfigure`，确认组件下载完成和 CMake 配置通过，再验证后续编译结果。
- 未暂存、未提交、未推送。

## 2026-08-24 Component Manager 反复 reconfigure 才能完成下载的问题复核

## 遇到的问题

- 用户澄清：并非只遇到一次 `esp_new_jpeg corrupted`，而是新增传递依赖较多时，每次 `idf.py reconfigure` 只能下载一部分，随后因某个组件下载或完整性校验失败而终止；需要人工重复运行多次才能全部完成。
- 本次 ESP-SR 依赖实际按时间依次落盘：`cjson`、`dl_fft`、`esp-dl`、`esp-dsp`、`esp-sr`、`esp_new_jpeg`。Component Manager 在一次调用中遇到首个损坏组件即退出，下一次调用会复用此前已成功安装的组件，因此形成“一次推进一个或几个组件”的现象。

## 当前结论与处理建议

- 当前 `managed_components` 已存在上述全部组件；静态读取版本和 `.component_hash`，确认 `esp-sr 2.5.1`、`esp-dl 3.3.9`、`esp-dsp 1.8.0`、`dl_fft 0.6.0`、`cjson 1.7.19~2`、`esp_new_jpeg 1.0.2` 已按 lock 组合落盘。尚未由 Codex 执行 CMake 配置或构建。
- 本机 `idf-component-manager` 为 2.4.10，属于支持 ESP-IDF 5.x 的当前 2.x 版本；不建议为此升级到仅面向 ESP-IDF 6.x 的 3.x。
- 高频损坏更符合组件存储下载链路不稳定或安全软件/代理干预，不能仅凭现有日志断言具体外部原因。乐鑫官方对中国网络推荐将 Component Manager 的 `storage_url` 配置为 `https://components-file.espressif.cn`；建议先在当前 PowerShell 会话设置该存储源后重新配置，再判断是否仍出现校验失败。

## 是否解决

- 依赖组件目前已全部落盘，重复下载的工作机制和推荐下载源已确认；下载链路是否稳定、CMake 是否配置通过仍待用户下一次 `idf.py reconfigure` 验证。未构建、未烧录。
- 本阶段未修改功能源码、未暂存、未提交、未推送。

## 2026-08-25 AFE 常驻后 LR20xx FIFO SPI DMA 分配崩溃修复

## 遇到的问题

- 网关开启双向通话时复位。回溯显示 `lr20xx_hal_direct_read_fifo()` 调用 `spi_device_polling_transmit()` 后，ESP-IDF 5.5.3 报 `Failed to allocate priv RX buffer`，随后在 `uninstall_priv_desc()` 中以空源地址执行 `memcpy`，触发 `LoadProhibited`。
- 当前 LR20xx HAL 使用栈上 VLA 拼接 FIFO 命令和数据。原固定图传负载为 511 字节，加上 2 字节 FIFO 命令后单次全双工传输为 513 字节；地址和长度不满足 ESP32-S3 当前 32 字节数据缓存行对齐要求，因此 SPI 驱动每包临时申请 TX、RX 两块内部 DMA 缓冲。
- ESP-SR AFE VOIP_HIGH_PERF 创建后内部 DMA 堆余量下降。该次日志表明 TX 临时块已经申请、RX 临时块申请失败；ESP-IDF 5.5.3 的失败清理路径又复制了空 RX 临时指针，因此由一次可返回的 `ESP_ERR_NO_MEM` 放大为系统崩溃。

## 本次工程改动

- `main/app_config.h`：固定 FLRC 图传负载由 511 调整为 510 字节，使 2 字节 FIFO 命令加负载正好为 512 字节；图像分片数据由 495 调整为 494 字节，两端使用同一固件配置，协议字段和图像内容不变。
- `main/spi_psram_dma_wrap.c`：在项目现有 SPI 包装层增加 `spi_device_polling_transmit` 包装；预留一对 1024 字节、按当前 32 字节缓存行对齐的内部 DMA 缓冲，并使用静态互斥量串行保护。满足对齐的轮询传输先复制到预留缓冲，再调用真实 SPI API，不再在运行期申请临时内部 DMA 块。
- `main/CMakeLists.txt`：增加 `spi_device_polling_transmit` 的链接包装参数。没有修改 `managed_components` 和本机 ESP-IDF，避免重新配置组件或更换 IDF 后丢失修复。
- `main/radio_ping.cpp`：网关和节点创建 AFE 前后输出 internal/DMA free 与 largest block，便于板测确认 AFE 对内部 DMA 堆的实际占用。

## 是否解决

- 根因、IDF 失败路径和项目侧规避方案已完成静态核对；大图传 FIFO 的 512 字节 TX/RX 轮询路径将使用预留 DMA 缓冲，不再依赖临时 DMA 堆。
- 未构建、未烧录、未板测；待用户验证开启双向通话时不再出现 `Failed to allocate priv RX buffer`/`LoadProhibited`，并提供 AFE 前后两条 `intercom heap` 日志。图传分片少 1 字节对吞吐的实际影响也待板测确认。
- 未暂存、未提交、未推送。

## 2026-08-25 ESP-SR AFE feed/fetch 流水线修复

## 遇到的问题

- 双向通话握手成功后，节点和网关都持续输出 `AFE: Ringbuffer of AFE is empty, Please use feed() to write data`；项目诊断同时出现 `AFE fetch failed/result=-1`。
- 当前接入层在 `voice_tx` 中凑够一帧后执行同步 `feed -> fetch_with_delay(20 ms)`。VOIP HIGH_PERF 首帧未在 20 ms 内产生时，`voice_tx` 被超时等待拖慢，后续 feed 速率低于 16 kHz 实时输入，导致 AFE 输入流水线持续饥饿。
- ESP-SR 2.5.1 官方 AFE 文档和例程明确使用相互独立的 feed 任务与 fetch 任务：feed 连续写入输入，fetch 在另一任务中阻塞取得处理结果。当前同步调用结构不符合官方例程。

## 本次工程改动

- `main/echo_canceller.cpp/.hpp`：保留现有 `voice_tx` 作为唯一 feed 端，新增常驻 `afe_fetch` 任务作为唯一 fetch 端；fetch 结果复制到项目输出环形缓冲，`voice_tx` 只读取已完成结果，不再等待 AFE 算法输出。
- 新增 fetch 启停、执行中和退出原子状态。开始收到首个 feed 后唤醒 fetch；停止通话时关闭 fetch 门、等待当前阻塞 fetch 返回，再调用官方 `reset_buffer` 并清理项目桥接缓冲；正常运行期不销毁 AFE 和 fetch 任务。
- `main/app_config.h`：fetch 等待由 20 ms 调整为官方示例使用的 100 ms 量级，新增 4096 字节 fetch 任务栈；AFE 类型继续为 VC、模式继续为 HIGH_PERF、AEC 继续为 VOIP_HIGH_PERF，未更换算法模式。

## 是否解决

- 官方调用结构和项目并发状态已完成静态修正；feed 不再被 fetch 超时阻塞，持续输入条件下 AFE 应能建立稳定输出流水线。
- 未构建、未烧录、未板测；待用户验证两端不再持续打印 AFE 空 Ringbuffer，并确认出现 `AFE output started`、语音帧持续发送、AEC 耗时和实际双向音质正常。
- 未暂存、未提交、未推送。

## 2026-08-25 AFE fetch 任务创建失败修复

## 遇到的问题

- 网关创建 `VOIP HIGH_PERF` AFE 后，新增的 `afe_fetch` 任务申请 4096 字节任务栈失败，随后初始化清理 AFE，日志表现为 `AFE fetch task creation failed`、`AFE: exit` 和 `intercom start rejected`。
- AFE 实例本身已经创建成功；失败点是普通 `xTaskCreatePinnedToCore()` 默认从内部 RAM 申请任务栈。HIGH_PERF AFE、无线和音视频缓冲同时存在时，内部连续空闲块不足以满足该任务栈，而项目 PSRAM 仍可用且 `CONFIG_SPIRAM_ALLOW_STACK_EXTERNAL_MEMORY=y` 已开启。

## 本次工程改动

- `main/echo_canceller.cpp`：按 ESP-IDF 5.5.3 接口改用 `xTaskCreatePinnedToCoreWithCaps()`，将 `afe_fetch` 的 4096 字节栈定向分配到 `MALLOC_CAP_SPIRAM | MALLOC_CAP_8BIT`；任务 TCB 仍由 IDF 保留在内部 RAM。AFE 继续使用 `AFE_TYPE_VC + AFE_MODE_HIGH_PERF + AEC_MODE_VOIP_HIGH_PERF`，没有降级算法模式，也没有改变正常运行期常驻策略。
- WithCaps 任务按官方约束改由其他任务调用 `vTaskDeleteWithCaps()` 回收；fetch 任务收到停止请求后只置退出完成标志并自挂起，避免自删时额外创建内部 RAM 清理任务。
- 若任务创建仍失败，错误日志增加栈大小、内部 RAM 和 PSRAM 的总空闲量及最大连续块，便于继续确认是否为 PSRAM 初始化或内存碎片问题。

## 是否解决

- 已完成 IDF 5.5.3 API、静态分配配置、PSRAM 外部任务栈配置和任务退出配对接口核对；源码静态修改完成。
- 未构建、未烧录、未板测；待网关和节点更新固件后确认不再出现 `AFE fetch task creation failed`，并能继续打印 `ESP-SR AFE ready`、`AFE output warmup/underflow` 和 `AFE output started`。
- 未暂存、未提交、未推送。

## 2026-08-25 AFE fetch 动态 TCB 分配失败订正

## 板测结果与根因订正

- 上一阶段把 fetch 栈改到 PSRAM 后，网关仍报告任务创建失败；新增诊断给出 `internal_free=687`、`internal_largest=288`，同时 `psram_free=7529972`、`psram_largest=7471104`。
- 该结果证明 PSRAM 栈空间充足。复核 ESP-IDF 5.5.3 源码确认：`xTaskCreatePinnedToCoreWithCaps()` 虽按指定 capability 分配任务栈，但仍先通过 `pvPortMalloc(sizeof(StaticTask_t))` 从内部堆动态申请 TCB；当前 288 字节最大连续内部块无法容纳 TCB，因此上一阶段只迁移任务栈并未解决完整创建路径。

## 本次工程改动

- `main/echo_canceller.hpp`：在 `EchoCanceller` 内增加 `StaticTask_t` 成员。当前唯一 `EchoCanceller` 属于 `app_main.cpp` 的全局 `g_radio` 对象，因此 TCB 在链接阶段进入内部 `.bss`，不再于开启通话时占用或依赖内部堆连续块。
- `main/echo_canceller.cpp`：fetch 栈继续从 PSRAM 分配，任务改用 `xTaskCreateStaticPinnedToCore()` 将静态内部 TCB 与 PSRAM 栈组合创建；退出后使用普通 `vTaskDelete()` 删除静态任务，再由接入层释放自有 PSRAM 栈。
- AFE 仍为 `AFE_TYPE_VC + AFE_MODE_HIGH_PERF + AEC_MODE_VOIP_HIGH_PERF`，正常运行期仍保持常驻；没有调整 AEC 模式、音频帧长、无线时隙或 LCD 参数。

## 是否解决

- 已完成 ESP-IDF 5.5.3 WithCaps 实现、静态任务接口、外部栈许可配置以及 `g_radio` 存储期的静态核对；该路径在 AFE 创建完成后不再动态申请 TCB 内存。
- 未构建、未烧录、待用户双板验证；预期不再出现 `AFE fetch task creation failed`，并继续进入 `ESP-SR AFE ready` 和 AFE 输出流水线日志。
- 未暂存、未提交、未推送。

## 2026-08-25 节点 AFE FEED 满与输出持续欠载调度修复

## 板测现象

- 节点双向通话已成功创建 AFE，且无线日志保持 `intercom slots tx=201 rx=201 missed=0`，说明前一阶段静态 TCB 修复已越过任务创建失败，TDD 时隙也没有卡死。
- AFE 输出仍持续报告 `warmup/underflow`，项目输出环仅剩 32 samples，发送前麦克风诊断为 `avg_abs=0 peak=0`；约 7.5 秒后 ESP-SR 连续报告 `Ringbuffer of AFE(FEED) is full`。
- 节点实测 `aec_avg=6789us`、`aec_max=17683us`。当前 CPU1 上 `voice_tx`、`voice_play` 为优先级 5，而 AFE 内部任务和项目 `afe_fetch` 均为优先级 4；I2S DMA 积压使高优先级语音任务持续 ready 后，低优先级 AFE 流水线无法及时排空输入。

## 本次工程改动

- `main/app_config.h`：将 `APP_AFE_TASK_PRIORITY` 从 4 提高到 6；该值同时用于 ESP-SR 内部任务的 `afe_perferred_priority` 和项目 `afe_fetch`，使二者高于优先级 5 的 `voice_tx/voice_play`。CPU 核心仍为 CPU1，未调整无线任务。
- `main/echo_canceller.cpp`：AFE ready 日志增加实际任务优先级；fetch 首帧及每 1000 帧输出一次累计帧数、返回字节数、换算 samples、`ringbuff_free_pct` 的千分比值、项目输出排队量和 fetch 栈最低剩余量。
- 没有全局屏蔽 ESP-SR 的 `AFE` warning。若调度修复有效，FEED 满警告应自然停止；若仍失败，保留官方警告并结合新增低频诊断继续区分 CPU 不足、fetch 返回尺寸异常和栈余量问题。
- AFE 继续使用 `AFE_TYPE_VC + AFE_MODE_HIGH_PERF + AEC_MODE_VOIP_HIGH_PERF`；未修改 10 ms Opus 帧、I2S DMA、无线 TDD、LCD 参数或 AEC 参考延迟。

## 是否解决

- 调度优先级关系、feed/fetch 运行核心、返回 `data_size` 字节单位和日志访问时的互斥范围已完成静态核对；源码调整完成。
- 未构建、未烧录、待用户双板验证。重点确认出现 `AFE output started`、麦克风 `avg_abs/peak` 恢复非零、FEED 满警告消失，并提供首条 `AFE fetch:` 日志以及 `aec_avg/aec_max`。
- 未暂存、未提交、未推送。

## 2026-08-25 完整 AFE 流水线改为官方直接 AEC

## 改动前的缺点

- 项目实际只需要 AEC，却通过完整 AFE 的 `feed/fetch` 管线接入。除 `voice_tx` 外，运行期还会创建 ESP-SR 内部处理任务和项目 `afe_fetch` 任务，并维护 AFE 输入环、AFE 输出环、项目输出环及 10 ms/AFE chunk 转接缓存。
- AEC 结果异步返回，存在输出预热、欠载、FEED 满和 Ringbuffer 空等额外状态；任何一个生产者、处理任务或消费者没有及时调度，都会把本来单帧可完成的 AEC 变成持续静音或缓存堵塞。
- 节点板测中 CPU1 的 `voice_tx` 每 10 ms 调用一次 AEC，实测 `aec_avg=6787us`、`aec_max=15044us`，同时还要与 ESP-SR 内部处理任务、`afe_fetch` 和播放任务竞争。看门狗回溯持续落在 `voice_tx -> afe_feed -> VOIP AEC`，说明 CPU1 音频流水线长期保持可运行，`IDLE1` 得不到调度。
- 完整 AFE 和 fetch 任务还额外占用任务栈、TCB、内部 RAM/PSRAM 与调度槽位；此前已经先后出现 fetch 任务创建失败、AFE FEED 满、输出持续欠载等问题，不利于后续音视频同传继续增加负载。

## 改动后的优点

- `EchoCanceller` 改用 ESP-SR 官方 `afe_aec_create()` / `afe_aec_process()` 直接接口，仍明确使用 `AFE_TYPE_VC + AFE_MODE_HIGH_PERF`，并在初始化时校验实际算法模式为 `AEC_MODE_VOIP_HIGH_PERF`；滤波长度仍为官方建议的 4，NLP 使用该模式的官方默认配置。
- 每个 10 ms、160 samples 的 `MR` 帧同步得到 AEC 输出，不再创建 ESP-SR 完整 AFE 工作任务和项目 `afe_fetch` 任务，也不再需要 feed/fetch/output 环形缓冲、预热静音和异步欠载处理。
- 减少 CPU1 上两个高优先级常驻任务及多次唤醒、互斥和数据搬运，让 `voice_tx` 的 AEC 计算成本更直接、调度链更短，为 `IDLE1`、播放以及未来图像并发保留调度机会。
- AEC 实例第一次启用后继续常驻。停止或再次开始双向通话时只清空项目拥有的参考延迟环和临时帧，不销毁、重建官方 AEC；对象析构或初始化失败时才释放实例。
- 保留网关 90 ms、节点 360 ms 的 I2S 播放参考延迟补偿；未改 Opus 帧长、I2S DMA、射频 TDD、图像链路、LCD 时钟或像素占空比。
- `main/CMakeLists.txt` 中被空白行替代的 `echo_canceller.cpp` 源文件项已恢复，否则无论使用完整 AFE 还是直接 AEC，当前实现都不会进入组件编译并会在链接阶段缺少符号。

## 为什么要这样改

- 当前需要的是普通双向语音通话场景下的高性能回声消除，而不是唤醒词、VAD、NS、AGC 等完整音频前端功能。既然这些模块都被关闭，继续承担完整 AFE 的任务和 ringbuffer 调度成本没有收益。
- 当前故障不是“任务数量多”这一抽象问题，而是 CPU1 上完整 AFE 异步管线的多个高优先级环节在音频持续输入后长期处于 ready 状态。改成官方同步直接 AEC 能从结构上去掉不必要的并发环节，比取消任务看门狗更接近根因；看门狗仍保留，用于发现真实的 CPU 饥饿。
- 该方案没有降低到 `FD_LOW_COST`，也没有替换或修改乐鑫 AEC 算法，只改变官方算法的接入层，符合当前音质要求和后续音视频同传对 CPU/内存余量的要求。

## 验证状态

- 已完成直接 AEC API、输入格式、实际模式、采样率、帧长、参考环容量、实例生命周期和调用引用的静态核对。
- 未构建、未烧录、未板测；待用户双板验证启动日志出现 `ESP-SR direct AEC ready` 且显示 `mode=VOIP_HIGH_PERF`、`worker_tasks=0`，持续通话不再出现 AFE Ringbuffer/FEED/fetch 日志和 `IDLE1` 看门狗，并记录 `aec_avg/aec_max`、无线 `missed/queued` 及实际双讲音质。
- 未暂存、未提交、未推送。

## 2026-08-25 直接 AEC 256/160 帧桥接与固定参考延迟订正

## 板测问题

- 网关创建官方直接 AEC 后报告 `mode=4 core_mode=4 rate=16000 frame=256 channels=2 mic=1 ref=1`，说明 `VOIP_HIGH_PERF` 模式和 MR 通道配置正确，但 ESP-SR 2.5.1 在 ESP32-S3 上的直接 AEC chunk 为 256 samples（16 ms）。项目此前错误要求其必须等于 Opus/I2S 的 160 samples（10 ms），因此主动拒绝了本来创建成功的 AEC。
- 上一阶段保留的网关 90 ms、节点 360 ms 不是 `VOIP_HIGH_PERF` 参数，而是项目接入层根据 DMA descriptor 总容量推算的固定偏移。descriptor 数表示队列容量，不等于运行期始终存在的播放延迟；设备之间的物理距离也不是本端 AEC 的配置参数，因此该固定角色偏移依据不足。

## 本次工程改动

- `main/echo_canceller.cpp/.hpp`：接受 AEC 运行时返回的 256-sample chunk，新增纯同步的流式帧桥接。麦克风和播放参考仍以项目 160-sample 帧进入，累计到 256 samples 后调用一次官方 `afe_aec_process()`，AEC 输出再通过小型环形缓冲按 160 samples 返回给 Opus。
- 桥接启动时预填 320 samples 静音，即 20 ms 固定算法接入延迟，保证 256/160 两种 chunk 周期在后续运行中不会输出欠载；该延迟只影响本地发送音频的启动，不是声学距离参数，也没有创建任何新任务。
- 删除网关 90 ms、节点 360 ms 固定参考偏移和对应初始化参数。播放参考改为 FIFO 顺序消费，维持解码/PLC 后写入扬声器的时间线，不再按 DMA 容量从历史环中任意回看。
- AEC 仍为官方 `AFE_TYPE_VC + AFE_MODE_HIGH_PERF`，初始化继续校验实际 `AEC_MODE_VOIP_HIGH_PERF`；实例保持常驻，未恢复完整 AFE、`afe_fetch` 或其他工作任务。

## 为什么这样处理

- `VOIP_HIGH_PERF` 会自适应本机扬声器到本机麦克风的声学路径，两个门铃设备相距多远不会改变这一条本地回声路径。它仍要求 mic/ref 属于同一连续时间线，但 ESP-SR 直接 API没有 90/360 ms 角色参数，不能把设备间距离或 DMA 最大容量作为固定补偿值。
- 256 samples 是官方算法的处理边界，160 samples 是项目无线与 Opus 的传输边界；两者无需强行相等，只需在接入层做样本守恒的流式切片。这样既遵守官方每次处理完整 chunk 的调用要求，也不修改现有无线帧长。

## 验证状态

- 已完成 256/160 累积周期、启动预填量、输出环容量、参考 FIFO、重置/释放路径和废弃固定延迟引用的静态核对。
- 未构建、未烧录、未板测；待用户确认启动日志显示 `aec_frame=256 io_frame=160 bridge_delay=20ms reference=fifo worker_tasks=0`，不再出现 `unexpected direct AEC format`，并继续验证 AEC 耗时、看门狗、回声与双讲音质。
- 未暂存、未提交、未推送。

## 2026-08-25 直接 AEC CPU1 空闲任务看门狗负载重平衡

## 板测问题

- 节点双向通话运行到 `tx=201 rx=201 missed=0 queued=0` 后触发 `IDLE1` 任务看门狗。无线收发槽位连续且没有 missed，说明 TDD 和语音包传输没有卡死。
- 看门狗打印瞬间的 CPU1 当前任务显示为 `voice_play`，但回溯明确落在 `voice_tx -> EchoCanceller::process_capture() -> afe_aec_process() -> esp_voip_process_api()`；这是同步 `VOIP_HIGH_PERF` AEC 的重采样/处理路径。两条优先级 5 的语音任务此前都固定在 CPU1，使该核在连续通话期间长期没有机会运行 `IDLE1`。
- 同一条日志中 CPU0 处于 `IDLE0`，表明当前并非双核总体算力耗尽，而是任务亲和性造成 CPU1 局部饱和。直接 AEC 的 256/160 帧桥已正常工作，本次问题属于调度负载分配，不是 AEC 格式或无线协议错误。

## 本次工程改动

- `main/app_config.h`：将 `voice_tx` 从 CPU1/优先级5 调整到 CPU0/优先级3。无线任务继续保持 CPU0/优先级4，因此每 2 ms 唤醒时仍可抢占采集、AEC 和 Opus 编码，优先保障 TDD 时序。
- `voice_play` 保持 CPU1/优先级5；它大部分时间阻塞于接收队列或 I2S 写入。移走同步 AEC 计算后，CPU1 不再同时承担两条高优先级语音执行链。
- 每个双向通话采集帧完成 AEC、增益、Opus 编码和入队后，`voice_tx` 主动阻塞 1 tick，为所在 CPU 的 Idle 任务提供确定的调度窗口。没有关闭任务看门狗，也没有修改其超时或 Idle 检查配置。
- 双向通话诊断日志增加 `tx_core/tx_prio/play_core/play_prio`，便于从实际固件日志确认任务布局。

## 为什么这样处理

- 日志已经证明 CPU0 有空闲，而 CPU1 被同步高性能 AEC 链持续占用；把计算迁移到空闲核比取消看门狗更直接地解决局部饱和，并保留看门狗发现真正死循环或长期饥饿的能力。
- `voice_tx` 必须低于无线任务，否则一次最高约 15 ms 的 AEC 计算可能推迟 2 ms 的射频状态机。优先级3允许无线任务及时抢占，同时 I2S DMA 和音频队列仍为语音处理提供缓冲余量。
- 双向通话期间图像传输互斥，因此 CPU0 不会同时承担图像批量发送；当前重平衡不会把已知的图像发送负载叠加到 AEC 上。

## 验证状态

- 已完成任务创建宏、核亲和性、优先级关系、双向通话单帧让出点、跨核参考 FIFO 互斥保护和诊断日志的静态核对。
- 未构建、未烧录、未板测；待用户确认启动双向通话时日志为 `tx_core=0 tx_prio=3 play_core=1 play_prio=5`，持续通话至少 30 秒不再出现 `IDLE0/IDLE1` 看门狗，同时 `missed` 保持 0，并记录 `aec_avg/aec_max` 和实际音质。
- 未暂存、未提交、未推送。

## 2026-08-25 双向通话清晰度与近距离啸叫调参

## 板测结果

- 搬核版本已在网关和节点进入 `tx_core=0 tx_prio=3 play_core=1 play_prio=5`。网关连续记录到 `tx=601 rx=600 missed=0`，节点记录到 `tx=401 rx=401 missed=0`；观察片段内未再触发 Idle 任务看门狗，无线队列保持 `queued=0~1`。
- 网关直接 AEC 为 `aec_avg=6359~6392us`、`aec_max=18048us`，节点为 `aec_avg=5998~6067us`、`aec_max=12493us`。同步高性能 AEC 保持运行，CPU 调度修复没有破坏 TDD 时隙。
- 网关累计出现 79 个发送增益削顶样本，节点为 0。用户实听反馈为语音略不清晰，两台设备靠近时仍会啸叫。
- 当前现象不等同于本地 AEC 完全失效。两台全双工设备靠近时，一端扬声器会被另一端麦克风当作近端声音再次发送，形成跨设备声学闭环；该路径不属于本机播放参考，不能只依靠单端 AEC 完全消除，必须同时降低闭环增益。

## 本次工程改动

- `main/app_config.h`：Opus 保持 `OPUS_APPLICATION_RESTRICTED_LOWDELAY`，CBR 从 24 kbps 提高到 32 kbps、编码复杂度从 0 提高到 3，改善低延迟 CELT 语音清晰度；10 ms 帧长、双帧聚合和无线时隙不变。
- `main/app_config.h`：AEC 后发送增益从 4 倍降到 2 倍，并新增通话播放 PCM 60% 限幅，降低削顶和两端闭环总增益。
- `main/radio_ping.cpp`：正常解码帧和 Opus PLC 帧都在写入 AEC 参考 FIFO 前应用相同的 60% 播放增益，因此 AEC 参考与实际送入 I2S 的数字样本保持一致；非双向通话播放路径不受影响。
- 双向通话诊断日志增加 `tx_gain` 和 `play_gain`，便于确认两端实际运行的是本次调参版本。
- AEC 继续使用 `VOIP_HIGH_PERF` 和库默认 aggressive NLP。本轮没有切换 very-aggressive，避免在尚未验证增益与编码质量前引入更强非线性抑制造成近端语音断续。

## 验证状态

- 已完成配置边界、正常帧/PLC 帧播放增益位置、AEC 参考一致性、Opus 最大帧缓冲和无线包容量的静态核对。
- 未构建、未烧录、待用户双板验证。重点确认启动日志为 `32000 bps complexity=3`，通话日志含 `tx_gain=2 play_gain=60%`；先在正常距离比较清晰度，再逐步靠近确认啸叫阈值，并继续观察 `missed`、`queued`、`clip_total`、`aec_avg/aec_max` 和 Idle 看门狗。
- 未暂存、未提交、未推送。

## 2026-08-25 通话播放 60% 缩放有符号运算订正

## 板测问题与根因

- 调参版本启动参数正确，无线保持 `missed=0`，但节点第 100 个播放帧突然达到 `avg_abs=10469 peak=23592`，实听表现为立即乱叫；这不是正常的近距离啸叫阈值变化。
- `APP_INTERCOM_PLAYBACK_PERCENT` 定义为无符号常量 `60U`。原表达式将有符号 PCM 样本与该无符号值直接相乘，按 C++ 常规算术转换规则，负采样会先转换为无符号数；除法及截回 `int16_t` 后负半波被严重破坏，直接造成全幅失真。
- 这是上一阶段新增播放缩放代码引入的确定性回归，与 `VOIP_HIGH_PERF`、参考 FIFO、无线丢包或搬核无关。

## 本次工程改动

- `main/radio_ping.cpp`：在逐样本乘法前把播放百分比显式转换为 `int32_t`，乘积和除法全程保持有符号 32 位运算，再安全转换回 `int16_t`。按当前 60% 配置，输入 `-32768~32767` 对应输出约 `-19660~19660`，不会溢出或改变符号。
- 正常解码帧和 PLC 帧继续共用同一个缩放函数；Opus 32 kbps/复杂度3、发送增益2、播放60%、AEC 模式、任务绑核和看门狗配置均未再次调整。

## 验证状态

- 已完成正负边界、整数提升、正常帧/PLC 调用点和差异范围的静态核对。
- 未构建、未烧录、待用户双板验证。修复版应不再出现无声输入下播放幅度突然上万或立即乱叫；随后再评估 32 kbps/复杂度3、发送增益2和播放60%的真实清晰度与近距离啸叫阈值。
- 未暂存、未提交、未推送。

## 2026-08-25 本机 AEC 播放参考时间线校准

## 新加入的功能

- 双向通话日志同时输出 AEC 前的原始麦克风电平 `raw_avg/raw_peak` 和 AEC 后的 `aec_avg_abs/aec_peak`，便于在远端静音、本机播放条件下直接评估本机回声抑制量。
- AEC 参考 FIFO 新增起始对齐延迟和运行期欠载告警；启动诊断显示 `aec_ref_delay=20ms`，参考时间线不足时输出 `reference FIFO underflow`。

## 改动内容

- `main/app_config.h`：新增 `APP_INTERCOM_AEC_REFERENCE_DELAY_MS=20`。初值依据当前约 15 ms 的单个 I2S TX DMA descriptor，再给 Codec、功放和本机声学传播保留少量余量；该值是待板上校准参数，不写成已验证结论。
- `main/radio_ping.cpp/.hpp`：`play_mono_frame()` 改为返回完整 I2S 写入结果；正常 Opus 帧和 PLC 帧只在 I2S 接受完整 PCM 后才推入 AEC 参考 FIFO。参考入队从阻塞写之前移动到写成功之后，避免 TX DMA 堵塞时参考先于实际播放持续前跑。
- `main/echo_canceller.cpp/.hpp`：第一次收到本轮播放参考时，在 FIFO 前插入 20 ms 静音作为本机播放链路初始补偿；通话重置时重新建立该时间线。保留 `VOIP_HIGH_PERF`、滤波长度4和库默认 aggressive NLP。
- 未修改 Opus 32 kbps/复杂度3、发送增益2、播放60%、无线 TDD、任务绑核/优先级、I2S DMA 深度或看门狗设置。

## 遇到的问题

- 当前实现原先在 `bsp_audio_write()` 之前就推送参考，而 I2S 写入可能因 DMA 满而阻塞；参考 FIFO 因此可能领先真实扬声器播放，AEC 收到的 `mic/ref` 不属于同一时刻。
- 直接 AEC 改造时曾删除无依据的 90/360 ms DMA 总容量偏移，但随后没有保留一个基于实际最小播放链路的可调校准量，也没有原始/处理后电平对比，导致板测只能凭听感判断。
- 编辑 `app_config.h` 后补丁链路一度形成混合换行，已只做格式恢复，文件保持 UTF-8 无 BOM、纯 CRLF，正文未被额外重写。

## 是否解决

- 参考入队时机、可调起始延迟、PLC 连续性、重置路径和诊断日志已完成源码修改与静态核对。
- 未构建、未烧录、未板测；本机回声抑制是否改善及 20 ms 是否为最优值待用户验证。测试时应让远端麦克风静音或物理隔离，只播放稳定人声，比较同一条日志中的 `raw_avg` 与 `aec_avg_abs`，并观察是否出现 `reference FIFO underflow`。
- 未暂存、未提交、未推送。

## 2026-08-26 AEC 对齐 ESP-SR 2.5.1 官方全双工直接接口

## 新加入的功能

- AEC 改为 ESP-SR 2.5.1 官方 `esp_aec.h` 直接接口，并按官方全双工建议使用 `AEC_MODE_FD_LOW_COST`；该模式包含线性滤波和非线性处理，NLP 使用库默认等级。
- AEC 实例改为每次通话创建、通话结束或启动失败时销毁，避免跨会话保留上一轮滤波器内部状态。
- 保留 AEC 前后麦克风电平和参考 FIFO 欠载诊断，用于板上确认回声抑制量及参考流是否连续；这些日志不改变算法输入。

## 改动内容

- `main/echo_canceller.cpp/.hpp`：删除 `afe_aec_create()`、`afe_aec_process()`、`MR` 交织缓存和 `AFE_TYPE_VC + AFE_MODE_HIGH_PERF` 包装层，改为官方 `aec_create(16000, 4, 1, AEC_MODE_FD_LOW_COST)`、`aec_get_chunksize()`、分离的 `mic/ref/out` 16 字节对齐缓冲、`aec_process()` 和 `aec_destroy()`。
- `main/echo_canceller.cpp/.hpp`：完全删除上一阶段新增的 20 ms 播放参考静音预填、配置宏和相关状态；算法只接收连续的麦克风与播放参考样本，不再由应用猜测声学延迟。
- `main/radio_ping.cpp`：播放参考恢复为在阻塞 I2S 写入前进入参考时间线，正常解码帧和 PLC 帧采用同一顺序；采集固定使用 I2S 第一通道，不再逐样本比较左右绝对值后择大。
- `main/radio_ping.cpp`：启动诊断改为 `esp-sr-direct-fd-low-cost`；通话停止和启动发送失败均释放 AEC 实例，下次通话重新按官方参数创建。
- `main/app_config.h`：保留官方建议滤波长度 4，删除无官方依据的参考延迟参数，并把任务注释同步为 `FD_LOW_COST`。
- 项目 10 ms Opus/TDD 帧与 ESP-SR 运行时 chunk 不同，因此仅保留样本守恒的流式切片与输出桥接；该接入层不改变官方 AEC 的采样率、处理帧长、模式、NLP 或 `mic/ref/out` 调用次序。

## 遇到的问题

- 上一阶段将 I2S DMA 单描述符时长推导成固定 20 ms 参考延迟，并在参考 FIFO 前插入静音；ESP-SR 官方示例和测试音频均按相同样本索引提供麦克风与参考，滤波器负责学习实际声学路径，固定应用延迟没有官方依据。
- 上一阶段又把参考入队移到阻塞 I2S 写入完成之后，采集任务可能先消耗对应时段，造成参考相对麦克风后移；这与官方“送往扬声器的参考帧与麦克风帧同步处理”的数据流不一致。
- 原采集代码逐样本选择左右声道绝对值较大者，形成非线性拼接信号，不符合官方固定麦克风通道输入前提。

## 是否解决

- 官方全双工直接 API、模式、采样率、滤波长度、单麦克风、分离缓冲、处理调用、销毁路径、无固定参考延迟和固定采集通道均已完成源码修改与静态引用核对。
- `git diff --check` 已通过；`main/app_config.h` 保持 UTF-8 无 BOM、纯 CRLF，其余本次源码保持 LF。
- 未构建、未烧录、待用户双板验证。板测应确认启动日志为 `api=aec mode=FD_LOW_COST` 和 `aec=esp-sr-direct-fd-low-cost`，再观察 `raw_avg/raw_peak` 与 `aec_avg_abs/aec_peak`、`reference FIFO underflow`、AEC 耗时、无线 `missed/queued`、看门狗以及近距离回声和啸叫现象。
- 未暂存、未提交、未推送。
