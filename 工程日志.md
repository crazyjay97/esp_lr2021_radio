# 工程日志

> 本日志仅供自己开发记录使用（中文）。工程对外仍是英文开源项目，代码/注释/提交信息保持英文。
> 之前的历史未回溯，从本次优化开始记录。

---

## 2026-07-22 保留 32KB 指令 cache，修复节点端内存不足

### 想实现的功能 / 目标
- 音频解码和射频共用同一 cache，任务处理不过来。
- 期望通过把指令 cache 从 16KB 提升到 32KB 来缓解性能压力，并保留这一配置（实测确实有用）。
- 目标：在 32KB 指令 cache 下，让节点端也能正常启动，不再报错。

### 改动内容
1. **指令 cache 提升到 32KB**（直接改 `sdkconfig`，不是 `sdkconfig.defaults`）：
   - `# CONFIG_ESP32S3_INSTRUCTION_CACHE_16KB is not set`
   - `CONFIG_ESP32S3_INSTRUCTION_CACHE_32KB=y`
   - `CONFIG_ESP32S3_INSTRUCTION_CACHE_SIZE=0x8000`
   - 数据 cache 原本已是 32KB。
2. **`CMakeLists.txt` 增加 GCC 编译 workaround**：对 `esp_lcd` 组件单独降到 `-O1`，绕开编译器段错误（见下方问题）。
3. **裁剪任务栈**（`main/app_config.h`），把内部 SRAM 省出来：
   - `APP_RADIO_TASK_STACK_BYTES` 32768 → 8192（实测约用 2.2KB）
   - `APP_VOICE_TX_TASK_STACK_BYTES` 32768 → 16384（实测约用 11.9KB，不能再砍）
   - `APP_VOICE_PLAY_TASK_STACK_BYTES` 32768 → 16384（保守值，尚未实测）
   - `APP_IMAGE_TASK_STACK_BYTES` 保持 16384（img_tx / img_cap 共用，img_cap 未实测）
4. 期间加过栈探针日志（`uxTaskGetStackHighWaterMark`）测量各任务栈占用，测完已删除。

### 遇到的问题
1. **改错文件**：一开始改的是 `sdkconfig.defaults`。已存在的 `sdkconfig` 不会被 defaults 覆盖，必须直接改 `sdkconfig`。→ 已解决。
2. **GCC 编译段错误（ICE）**：cache 改 32KB 后，`esp_lcd_panel_rgb.c:700` 报 `internal compiler error: Segmentation fault`。彻底删除重构建也没用。→ 已解决：`CMakeLists.txt` 对 `esp_lcd` 组件降到 `-O1`。
3. **节点端启动报 `ESP_ERR_NO_MEM`**：网关端正常，只有节点端报错。
   - 根因：32KB 指令 cache 是从内部 SRAM 里划出来的，比 16KB 多吃 16KB 内部 RAM；叠加节点端 3 个 32KB 大任务栈 + 摄像头 DMA，内部 SRAM 不够。
   - → 已解决：裁剪任务栈，回收约 56KB 内部 RAM，节点端正常启动。

### 是否解决
- ✅ 保留 32KB 指令 cache 且节点端不再报 `ESP_ERR_NO_MEM`。
- ✅ GCC 段错误已用 `-O1` workaround 绕过。
- ⏳ **待验证**：voice_play 任务栈目前是保守的 16KB，尚未通过实际语音接收触发测量，后续如需继续省 RAM 再测。

### 遗留 / 待办（本次未处理）
- **图传耗时 30 秒问题**：确认是射频链路丢包重传导致（img_tx 只用了 2.6KB 栈，跟内存无关），属于独立问题。
  - 方向：把射频 ISR 回调链（`rp_radio_irq_callback` 等）放进 IRAM。
  - 另注意一个潜在崩溃隐患：ISR 用 `ESP_INTR_FLAG_IRAM` 安装，但回调链目前在 flash 里。

---

## 2026-07-22 图传 prepare 延迟复发（加音频后）

### 问题现象
- 网关端图传日志出现 prepare=110-142ms 的周期性尖峰（正常应 ~35-56ms）。
- 这个问题之前修复过一次（见 `radio_ping.cpp:1797-1804` 注释，把 `vbat_received_cb_` 移出热路径），现在加了音频后又复发。

### 问题根因
- **`prepare_ms`** = 网关发 ImageCmd 到节点开始传第一个包的延迟。
- `radio_task` 收到 ImageStart 包后，**在发 ACK 之前**调用 `image_rx_progress_cb_(0, total_frags, 0)`（`radio_ping.cpp:1840`）。
- 回调进入 `ui_gw_rx_begin` → 用 `portMAX_DELAY` 阻塞抢 LVGL 锁 `s_lock`（`ui_gateway.c:1187`）。
- 如果 LVGL 任务（core 1）正在刷屏（持有 `s_lock`），`radio_task`（core 0）就卡住等锁，最长 **~100ms**（LVGL 刷屏周期）。
- 等拿到锁、更新完 UI、释放锁、发完 ACK，节点才开始传数据，`prepare_ms` 被拉高到 110-140ms。

### 为什么加了音频后复发
- 之前只修了 `vbat_received_cb_`（把电池电压回调移出热路径），但 **`image_rx_progress_cb_` 还在热路径上**。
- 音频播放加入后，LVGL UI 可能增加了音频状态指示、波形、音量条等元素，**LVGL 刷屏频率提高**，锁竞争概率从偶发变成频发（日志中 ~每 2-3 帧出现一次高 prepare）。
- 另一个加重因素：`play_task` 每收一个 opus 包（20ms 音频）就调 `bsp_audio_write` → `i2s_channel_write(..., portMAX_DELAY)` 阻塞写 I2S（`bsp_audio.c:456`）。虽然 `play_task` 在 core 1 不直接卡 `radio_task`（core 0），但可能间接增加了 LVGL 任务的调度延迟或刷屏触发频率。

### 改动内容
1. **调整 ImageStart 处理顺序**(`radio_ping.cpp:1837-1867`):
   - 原本:准备缓冲区 → 更新 UI(阻塞抢锁) → 发 ACK → 进入 RX。
   - 现在:准备缓冲区 → **发 ACK** → **进入 RX** → 更新 UI(非阻塞)。
   - 理由:ACK 发送时刻决定 `prepare_ms`,必须在最前面;RX 紧跟其后,不让首个数据包到达时还没开 RX。
2. **UI 锁改非阻塞**(`ui_gateway.c:1187, 1229`):
   - `ui_gw_rx_begin` / `ui_gw_rx_progress` 的 `xSemaphoreTakeRecursive(s_lock, portMAX_DELAY)` → `xSemaphoreTakeRecursive(s_lock, 0)`。
   - 抢不到锁就跳过本次更新,下一个分片进度或完成回调会补上。
   - 理由:这两个函数从 radio RX 热路径调用,阻塞等锁会延迟 ACK/NACK 发送。

### 是否解决
- ✅ **已修复并验证通过**。
- 板测结果:prepare 稳定在 32-36ms,之前的 110-140ms 尖峰完全消失。
- 帧率从 2.9-3.5 fps 提升到 4.0-4.3 fps(总延迟降低约 60-80ms)。

### 相关代码位置
- `radio_ping.cpp:1840` — 热路径上调用 `image_rx_progress_cb_`
- `radio_ping.cpp:1797-1804` — 之前修复 vbat 的注释（说明了同样的锁竞争问题）
- `ui_gateway.c:1187, 1229` — `ui_gw_rx_begin` / `ui_gw_rx_progress` 用 `portMAX_DELAY` 抢 `s_lock`
- `bsp_audio.c:456` — `bsp_audio_write` 阻塞写 I2S（间接因素）

---

## 2026-07-22 音画同步（AV 配对队列）

### 想实现的功能 / 目标
- 之前图像一到达就立即显示，音频要经过 400ms 预缓冲 + 队列才播放，导致**图像领先音频、音画不同步**。
- 目标：让视频追随音频时间线，实现音画同步播放。

### 改动内容
将独立的 `AudioSegment` 队列改成 **`AVFrame` 配对队列**（`app_main.cpp`）：

1. **结构体重构**（~line 80）：
   - 旧：`struct AudioSegment { opus_packed, len }`
   - 新：`struct AVFrame { rgb565, width, height, opus_packed, opus_len, jpeg_size, transfer_ms, enqueue_ms }`
   - 把图像和音频配成一对，所有权一起移交给队列。

2. **入队逻辑**（`on_image_rx_complete`，~line 1138）：
   - 有音频时：把 JPEG 解码后的 rgb565 拷到 SPIRAM（不立即显示）→ 和 opus 一起打包成 `AVFrame` → 推入 `g_av_queue`。
   - 无音频时：照旧立即显示（保持最低延迟）。
   - SPIRAM 拷贝失败时：回退到立即显示（避免丢帧，但音频会领先）。

3. **播放任务重写**（`gateway_audio_task`，~line 989）：
   - 从队列取出一对 `AVFrame` → **先调 `ui_gw_rx_complete` 显示图像** → 释放 rgb565 → 再解码/I2S 播放对应音频。
   - 图像显示节奏被 **I2S 时钟**（音频排空速度）限速，不再按 RF 到达速度抢跑。

4. **队列创建更新**（~line 1724）：
   - `g_audio_queue`(AudioSegment) → `g_av_queue`(AVFrame)

### 问题根因分析
- 音频受 I2S 时钟驱动、无法跳过或加速，是"刚性"的。
- 视频可延迟、可丢帧，是"柔性"的。
- **方案**：让柔性的视频追随刚性的音频时间线 → 音画自然同步。

### 是否解决
- ✅ **已实现（未板测）**。音画配对入队、由音频任务统一节奏播出，理论上实现同步。
- ⏳ **待板测验证**：确认音画是否真的对齐、I2S 阻塞写期间 LVGL 刷屏是否流畅。

### 相关代码位置
- `app_main.cpp:80` — `AVFrame` 结构体定义
- `app_main.cpp:936` — `av_queue_push` 推入配对帧
- `app_main.cpp:989` — `gateway_audio_task` 播放循环（先显示图像、再播音频）
- `app_main.cpp:1138` — `on_image_rx_complete` 打包 AVFrame 入队

---

## 2026-07-22 降低延迟（移除预缓冲 + 600ms 追赶）

### 想实现的功能 / 目标
- 音画同步搞定后，下一步是**降低延迟**。
- 当前延迟来源：400ms 预缓冲启动门限 + 队列堆积（RF 到帧比播放快时，队列最多堆 20 帧 = 4.4 秒）。
- 目标：有帧就播（不等预缓冲）、队列堆积超过 600ms 就丢旧帧追赶进度。

### 改动内容
1. **移除预缓冲逻辑**（`gateway_audio_task`，~line 978）：
   - 旧：等队列时长 ≥ 400ms 才开始播放（用 `av_queue_ms()` 估算）。
   - 新：`xQueueReceive` 阻塞等帧，取到帧立即开 PA 播放，队列空就关 PA。
   - **有帧就播**，起播延迟降到最低。

2. **加入 600ms 追赶机制**（`gateway_audio_task`，~line 995）：
   - 给每帧打时间戳 `enqueue_ms`（入队时刻）。
   - 每次取出帧后先算 `age = now - enqueue_ms`（这帧在队列里等了多久）。
   - 如果 `age > 600ms` **且队列里还有下一帧**，就丢掉当前帧（图像和音频一起丢、保持同步），继续取下一帧，直到帧足够新或队列空了才播。
   - **丢帧副作用**：画面会从旧帧直接跳到新帧、音频跳过一段，但只在延迟 > 600ms 时才触发，正常抖动不会误触发。

3. **删除时长估算函数**：
   - `av_queue_ms()` 删除（之前用队列帧数 × 220ms 估算，不准且不再需要）。
   - 判断依据改成"队列里有几帧真实的帧"，跟 opus 编码大小彻底脱钩。

4. **常量更新**：
   - `APP_GW_AUDIO_PREBUFFER_MS` / `APP_GW_AV_FRAME_EST_MS` → 删除。
   - 新增 `APP_GW_AV_MAX_LATENCY_MS = 600U`（追赶阈值）。

### 是否解决
- ✅ **已实现并板测验证通过**。
- ✅ 起播延迟降到最低（有帧立即播放，无预缓冲等待）。
- ✅ 600ms 追赶机制运行正常，测试期间全程未触发（队列深度稳定在 0-1 帧，无积压）。
- ✅ 去掉预缓冲后未出现频繁 underrun，PA 开关稳定。

### 板测数据（D:\tmp\网关.txt）
**关键指标：**
- **AV frame latency**: 260 – 401ms（帧从入队到播放的等待时间）
- **queue 深度**: 0 – 1 帧（无积压）
- **FPS**: 2.9 – 4.1 fps
- **无 [CATCHUP] 触发**：全程未超过 600ms 阈值

**延迟分析：**
- 队列 latency 稳定在 300-400ms 是正常现象，因为队列深度 1 表示上一帧音频还在播放（约 220ms），当前帧在等待。
- **总端到端延迟**（节点拍摄 → 网关播出）：RF 传输 ~160ms + 解码 ~35ms + 队列等待 ~350ms ≈ **约 550ms**。
- RF 重传（每帧 2-7 个 missing fragments）占了 40-80ms 额外延迟，是进一步降延迟的主要瓶颈（需射频侧优化，见"遗留/待办"）。

**结论：**
音画同步 + 低延迟目标达成，队列运行健康，无需调整 600ms 阈值。

### 相关代码位置
- `app_main.cpp:80` — `AVFrame` 加入 `enqueue_ms` 字段
- `app_main.cpp:93` — `APP_GW_AV_MAX_LATENCY_MS` 定义
- `app_main.cpp:939` — `av_queue_push` 打时间戳
- `app_main.cpp:978-1007` — `gateway_audio_task` 新播放循环（无预缓冲 + 追赶）

## 2026-07-22 网关端啸叫（声反馈）检测静音

### 想实现的功能 / 目标
- 网关喇叭和节点麦克风离得太近，网关放出的声音被节点麦克风重新采到，经 RF 回到网关再放出，形成声反馈闭环（啸叫）。
- 标准 AEC 不适用：这条回声环往返约 550ms 且时延会变，需要约 8800 抽头的自适应滤波器，性能扛不住。
- 采用「网关端检测 + 临时静音」：解码后的 PCM 里一旦识别出啸叫特征，就把该帧清零。喇叭一静音，闭环被打断，麦克风不再采到该音，电平回落，再自动恢复播放。

### 特征选择（来自板测标定）
- 先只打印特征、不静音，用真实啸叫和正常语音标定。
- 结论：自相关峰值（ac_peak）、过零率方差（zcr_var）在啸叫时并不稳定，不可靠，弃用。
- 真正干净的判据是 **削波率 clip%** 和 **能量 rms**：
  - 正常语音：clip=0%，rms<500
  - 啸叫：clip=22~68%，rms=22000~29000
  - 中间是大片空档、无重叠，所以阈值可以放宽松、不会误伤正常大音量。

### 改动内容
- 删除诊断用的 `[HOWL]` 特征打印函数 `howl_detect_log`。
- 新增静音状态机 `howl_process` + `HowlSuppressor`（只用 clip% + rms）：
  - ACTIVE→MUTED：clip≥5% 且 rms≥6000，连续 3 帧（约 30ms）触发。
  - MUTED→ACTIVE：clip≤2% 且 rms≤4500，连续 50 帧（约 500ms，大于一个回声往返）才恢复。
  - 阈值取折中：首版（clip15%/rms12000/8帧）截止太晚，一度调到（3%/4000/2帧）又太宽松，最终折中到上面这组，既能早掐断又不误伤正常大音量。
  - 阈值刻意放宽：只在明确啸叫时静音，宁可放过轻微情况也不误伤正常声音。
- `gateway_audio_task` 解码循环里：`howl_process` 返回 true 时把该帧写 0 再送 I2S。

### 是否解决
- 待板测验证（本地 bash 环境损坏无法编译，由本人手动编译烧录）。

### 相关代码位置
- `app_main.cpp` — `HowlSuppressor` 结构体 + `HOWL_*` 阈值宏
- `app_main.cpp` — `howl_process` 静音状态机
- `app_main.cpp` — `gateway_audio_task` 解码循环调用 `howl_process` 并按需清零

## 2026-07-22 双向对讲实现规划（乒乓轮转，沿用现有图传逻辑）

### 想实现的功能 / 目标
- 在现有「节点传图+语音、网关放音」基础上，增加 **网关→节点 的语音下行**，做成双向对讲。
- **硬约束**：LR2021 单收发器半双工，任何时刻只能收或发其一。
- **设计原则（本次定的）**：大逻辑沿用现在的，不另起 TDMA 超帧、不搞全局时钟同步。节点每次传输仍和现在一样 = `[jpeg_len][jpeg][opus]` 一个 blob，走现有图传通道。只在「传输完成的应答帧」上做文章，把方向切换点利用起来。

### 核心思路：把应答帧升级成「反向传输的首帧」
半双工收发器的收发方向切换点，本来就在 ACK 那一刻。把 ACK 帧升级成「反向传输的启动首帧」，让方向切换和下一轮握手合二为一，省掉独立握手往返。事件驱动的乒乓，不需要 beacon 时钟同步。

```
节点: 发[jpeg+opus] blob ──> 发 ImageEOT
网关: 收完校验 OK ──> 应答帧不再只是"完成确认"
      而是"网关下行首帧"，捎带: 网关本轮要发的 OPUS 包总数 + CRC
网关: 发[opus] 下行数据 ──完成──>
节点: 收完 ──> 应答帧变成"下一轮上行首帧"
      节点随即开始下一轮 [jpeg+opus] blob 发送
... 乒乓循环
```

### 令牌（发送权）状态机
- 任何时刻「发送权」只在一方手里，天然无争用。
- 网关是 master：**方向切换超时后，由网关强制夺回主动权**（重发下行首帧或回到图像请求态），节点始终以网关为准。避免"两边互相干等"死锁。
- 沿用现有 30/50ms 超时重试，额外补一条规则：切换超时 → 令牌归 master。

### 节点传输：保持现状不动
- blob 布局不变：`[4B jpeg_len][jpeg][opus]`，session 打 `APP_AUDIO_SESSION_FLAG`。
- 图传的握手/数据突发/EOT/重传（ImageStart/EOT/ImageNack）整套不动。
- 网关侧拆 blob → AVFrame → g_av_queue → gateway_audio_task 播放，也不动。

### 新增：网关下行语音链
- 网关补 **麦克风采集**（现在是 `bsp_audio_init_playback_only`，只有播放）+ **Opus 编码器**（现在 `init_decoder_only()`，只有解码）。
- 网关侧新增 opus 预编码环 + 采集任务（可参照节点 `voice_tx`）。
- 下行数据段用已有的 `kPacketTypeVoice` + `build_voice_packet`（5 帧/包=50ms）+ `schedule_tx` 那套现成通道发出（当前是没绑按键的死代码，正好复用）。
- 节点侧激活接收播放：`queue_voice_packet` → `play_task`（voice_play 任务在，确保图传/CAD 睡眠之间能被调度到）。

### 回声 / AEC 结论：本方案天然半双工，不用跑 AEC
- 这个乒乓结构本身就是严格半双工：节点发时网关在收，网关发时节点在收，任何时刻只有一方在"说"。
- 配上 **收发门控**：收数据的一端把本地麦克风静音（发送方开麦、接收方关麦），两条声反馈环从物理上断掉，不给啸叫形成机会。比自适应 AEC 省太多算力。
- 已有的网关 clip/rms 啸叫静音（见上一条日志）退化为"门控没配好时的兜底"，两端各留一份。

### 已知软肋 + 缓解
1. **语音延迟被图传时长绑架**：串行乒乓，语音延迟 ≈ 一整轮时长。图大/重传多时会飙升、抖动。缓解：必要时给每轮图传数据量设上界（分块，每轮只发 N 片），保证轮转周期上界 → 语音延迟上界。本次先按"节点整帧图+opus"实现，延迟不达标再引入分块限流。
2. **接力棒丢失死锁**：靠上面的"令牌归 master"规则化解。

### 待办 / 实现顺序
1. 网关麦克风采集 + Opus 编码器 + 预编码环 + 采集任务。
2. 应答帧格式扩展：ImageNack 完成确认帧 → 捎带下行 OPUS 总数/CRC 的"下行首帧"。
3. 令牌状态机：发送权归属 + 方向切换 + 超时归 master。
4. 节点侧激活 voice_play 收网关下行语音。
5. 收发门控：收数据端静音本地麦克风。
6. 板测：先验证双向能通，再看语音延迟是否需要图传分块限流。

### 相关代码位置（规划阶段，尚未改动）
- `radio_ping.cpp` — 主 radio 任务 / `Mode` 状态机 / `poll_once` / `schedule_rx|tx`（加令牌层）
- `radio_ping.cpp` — `handle_image_eot` / `ImageNack` 应答（升级成下行首帧）
- `radio_ping.cpp` — `build_voice_packet` / `schedule_tx` / `play_task`（复用为下行通道）
- `app_main.cpp` — `init_gateway` / `bsp_audio_init_playback_only`（补采集+编码）

## 2026-07-22 双向对讲 步骤1：网关麦克风采集 + Opus 编码器

### 目的
- 双向对讲第 1 步：给网关补上「麦克风采集 + Opus 编码器 + 预编码环」这条链。
- 本步只让内存真实分配出来，先烧录测 RAM 是否够，不接发送逻辑。

### 改动内容
- `radio_ping.cpp` `init_gateway()`：
  - codec 从 `init_decoder_only()` 改成 `init()`（同时建编码器+解码器）。
  - 新增 opus 预编码环初始化（`opus_ringbuf_.init`，与节点同样 `APP_OPUS_RING_SECONDS` 尺寸）。
- `radio_ping.cpp` `start_gateway()`：
  - 复用 `tx_task` 起 `voice_tx` 采集任务。网关模式下该任务的 PIR / 低功耗CAD / 声音触发分支天然惰性（`is_gateway_==true` 且 `sound_trigger_level_==0`），只剩「读麦克风→编码→写环」的干净循环。
- `app_main.cpp`：
  - 网关音频从 `bsp_audio_init_playback_only` 改成全双工 `bsp_audio_init`（麦克风采集需要输入通道）。
  - `start_gateway` 成功后 `enable_opus_preenc(true)` 打开预编码。

### 当前状态 / 注意
- 网关采集出的 opus 现在只写环、无消费者，环满会丢最老帧（`dropped_count` 增加）——这是本步的预期，仅为测内存。下一步接下行发送后才有消费者。
- 待烧录确认 RAM 够用后再继续步骤 2（应答帧升级成下行首帧）。
- 本地 bash 环境损坏无法编译，由本人手动编译烧录。

### 相关代码位置
- `radio_ping.cpp` — `init_gateway`（codec init + opus 环）
- `radio_ping.cpp` — `start_gateway`（新增 voice_tx 采集任务）
- `app_main.cpp` — 网关音频全双工初始化 + `enable_opus_preenc`

### 步骤1 补丁：网关掉帧修复（采集降优先级）
- 现象：网关烧录后接收帧数明显变低。
- 根因：复用节点 `tx_task` 时，网关采集任务用了 `APP_VOICE_TX_TASK_PRIORITY=5`，比 core 1 上的 `gw_audio` 播放任务（优先级 4）还高。播放是拉流命脉（放完一帧才请求下一帧图），被采集周期性抢占 → 帧率掉。节点没这问题（节点 core 1 只采集、不放音）。
- 方案定调：**支持插话**（接收时也连续采集，不静音麦克风），接受啸叫靠 clip/rms 兜底。所以采集必须连续、不能按需，否则接收期间用户说的话会丢。
- 改动：
  - `app_config.h` 新增 `APP_GW_VOICE_TX_TASK_PRIORITY=3`（专给网关，不动节点的 5）。
  - `start_gateway` 的 `voice_tx` 改用优先级 3，低于 `gw_audio`(4)。
  - 采集仍连续写环、不丢音，只在播放阻塞等 I2S DMA 的空隙里编码。
- 待验证：接收帧数是否回升；采集是否饿死（RX DMA 溢出）。若帧数仍偏低，再查全双工 `bsp_audio_init` 是否动了 I2S 时钟/DMA 配置。

### 步骤1 补丁2：图传突发丢包修复（I2S 中断挪到 core 1）
- 现象：优先级补丁后 core 1 的抢占问题解决，但图传突发期仍丢包（日志 `image RX: sent ACK with 31/6/1 missing`，帧率掉到 2.8fps）。
- 根因：这是 **core 0** 的问题，跟 core 1 优先级无关。I2S 的 DMA 中断绑定在**调用 `i2s_channel_enable()` 的那个核**上。原来 `bsp_audio_init` 在 `app_main`（core 0）里跑，所以 RX DMA 中断落在 core 0，图传突发时和射频 GPIO 中断抢 core 0 → 丢包。之前是 `playback_only`（只 TX，没常开 RX DMA）所以没暴露；网关改全双工后 RX DMA 常开，问题才出现。节点本来就全双工，同样受益。
- 改动（`bsp_audio.c`）：
  - 新增 `i2s_init_on_core1()`：在 core 1 上起一个**一次性 worker 任务**跑整个 `i2s_init`（含 `i2s_channel_enable`），DMA 中断随之注册在 core 1；用二值信号量阻塞等它做完，worker 自删。任务/信号量创建失败则回退到调用核本地初始化。
  - `bsp_audio_init` 全双工路径把 `i2s_init(...)` 换成 `i2s_init_on_core1(...)`。`i2s_channel_read/write` 只是队列操作，不关心 ISR 在哪个核，两核调用者都不受影响。
  - `bsp_audio_init_playback_only`（TX-only）未动。
- 坑：第一版用 `esp_ipc_call_blocking(1, ...)` 触发 `ipc1` 任务**栈溢出**（`A stack overflow in task ipc1`）。原因是 IPC 任务栈只有约 1KB，而 `i2s_init` 在栈上放了 `i2s_chan_config_t`+`i2s_std_config_t` 两个大结构体又调进 I2S 驱动，直接冲爆。改用自带 4096 栈的一次性任务后不再用 IPC，`#include` 也从 `esp_ipc.h` 换成 `freertos/semphr.h`。
- 风险点：本地无法编译（bash 坏），需烧录验证。观察点：`image RX ... missing` 计数是否明显下降、帧率是否回到接近满速；同时确认音频采集/播放仍正常（I2S 时钟先于 codec 编程这个时序被信号量阻塞等待天然保留）。

## 2026-07-22 双向对讲 详细实现规划（下行语音：网关→节点，照搬图传发送链）

### 目标
在现有「节点上行图+语音、网关放音」之上，加「网关→节点 的下行 OPUS 语音」，做成双向对讲。下行数据链**整体照搬图传发送链**（ImageStart 握手 → 数据突发 → EOT → 带缺失表 NACK → 重传 → missing==0 完成），仅把分片载荷从 JPEG 字节改成「长度前缀的 OPUS 帧序列」。

### 为什么能照搬（调研结论）
- 收发两套代码本来就都编进了同一个 `RadioPing` 类。现在节点是图像**发送方**（`image_tx_task`）、网关是**接收方**（`handle_image_start/data/eot`）。下行只是把角色对调：网关发、节点收。
- 所有 radio 状态都串行在单个 radio task，跨任务只有 `send_single_packet`/`wait_for_tx_done` 用 `tx_done_waiter_` 交接，机制通用。
- 就绪/完成 ACK 本来就是复用 ImageNack（`missing_count==0`），天然就是「反向握手信号」，和乒乓设计吻合。

### 与图传的唯一区别：分片载荷
- 图传分片 = 按 `idx*239` 切 JPEG 的定长字节块（`APP_IMAGE_FRAGMENT_DATA_SIZE=239`）。
- 下行语音分片 = 一串「`[1B opus_len][opus 数据]...`」的变长帧序列，参考 `build_voice_packet`（828-869）的组帧、`queue_voice_packet`（1013-1060）的解包。
- 接收端重组按「帧」而非「idx*239 偏移」。为降低改动量，第一步可先复用图传定长分片容器（把整段 opus blob 当字节流切 239 一片传，接收端拼回完整 blob 后再按 `[len][data]` 拆帧），语义最省事、和图传收发几乎一模一样。

### 分步实现顺序（每步可独立烧录验证）
1. **步骤A：射频交互 + 网关端 opus 采集打包发送 + 节点收到只串口打印**（先做这个）
   - 网关：新增一个「下行发送」入口，把 `opus_ringbuf_` 里 drain 出的 opus blob 走图传发送链的镜像（新增 `kPacketType` 或复用 Image* + 一个 session flag 区分下行语音）发出。
   - 节点：新增下行接收处理，收齐后**只打印**：session、总分片数、总字节数、opus 帧数，**不解码不播放**。
   - 目的：验证下行链路能通、重传能修补丢片。
2. **步骤B：节点解码播放**：收齐 blob → 按 `[len][data]` 拆 opus 帧 → 复用 `play_task`/`play_mono_frame` 播放机制放出来。
3. **步骤C：令牌/方向状态机**：把「上一段完成 ACK」升级成「反向传输首帧」，方向切换超时 → 令牌归网关（master），避免死锁。
4. **步骤D：收发门控 + 板测**：收数据端静音本地麦克风；先验证双向能通，再看语音延迟是否要给图传分块限流。

### 步骤A 要新增/改动的位置（规划，未改动）
- 包类型：新增下行语音类型，或复用 Image* 系列 + session flag（`APP_AUDIO_SESSION_FLAG=0x8000` 已有先例）区分「这是下行语音 blob，不是图」。倾向复用 Image* 系列最省代码。
- `radio_ping.cpp`：网关侧新增「下行 blob 发送」触发（类似 `send_image`/`image_tx_task`，载荷来自 `drain_opus`）。
- `radio_ping.cpp`：节点侧 `task()` 循环要能跑 `handle_image_start/data/eot` 的等价接收（节点当前不接收图，需要打开这条接收路径 + 超时检查）。
- 打印点：节点收齐后 `ESP_LOGI` 打印 session/分片数/字节数/opus 帧数。

### 步骤A 已实现：下行 opus 射频交互 + 节点串口打印（未解码播放）
- 决策：复用 Image* 系列包 + session flag 区分下行语音，不新增包类型（用户拍板「直接复用，反正没影响」）。
- 触发时机（用户拍板）：网关收完节点上行图（`handle_image_eot` 的 `missing_count==0` 完成分支）后自动发一轮下行 opus。这就是乒乓的自然切换点。
- 打印端（用户拍板）：节点端打印收到的下行 opus 信息。
- 改动清单：
  - `app_config.h`：新增 `APP_DOWNLINK_VOICE_SESSION_FLAG=0x4000`（区别于上行图 blob 的 `APP_AUDIO_SESSION_FLAG=0x8000`）、`APP_DOWNLINK_OPUS_MAX_BYTES=3000`。
  - `radio_ping.hpp`：声明 `send_downlink_voice()`；新增成员 `downlink_voice_session_`（下行 session 滚动计数，恒带 0x4000 标志位）。
  - `radio_ping.cpp` `init_gateway`：网关补建 `image_tx_queue_`（原来只有节点有）。
  - `radio_ping.cpp` `start_gateway`：网关补建 `img_tx` 任务（原样复用节点的 `image_tx_task`/协议）。
  - `radio_ping.cpp` 新增 `send_downlink_voice()`：`drain_opus` 取一段 opus blob（heap_caps 分配，PSRAM 优先）→ 打 `0x4000` flag 的 session → 走 `send_image` 入队。空 blob（麦克风静音/环空）则跳过本轮。运行在 radio task 上下文，只入队不碰射频，真正发送由 `img_tx` 异步做（设 `suspended_=true` 接管射频，和节点发图一模一样）。
  - `radio_ping.cpp` `handle_image_eot` 完成分支：`session_id & 0x4000` → 节点侧下行语音路径：`rx_reassemble` 重组 → 数 opus 帧 → `ESP_LOGI` 打印 session/分片数/字节数/帧数 → `rx_reset`，**不解码不播放不走图像 complete_cb**；否则走原图像完成逻辑，且末尾 `if (is_gateway_) send_downlink_voice()` 触发下行。
- 数据源确认：网关 `tx_task`（voice_tx）在 `opus_preenc_enabled_==true`（start_gateway 后已置）时持续把麦克风编码写入 `opus_ringbuf_`（501-508 行），`drain_opus` 取增量。`rx_jpeg_size_` 收满时按分片长度累加（与内容无关），下行 opus blob 能正确重组。
- 风险点 / 待烧录验证：
  1. 本地不能编译（bash 坏），需烧录看。观察：节点串口是否出现 `downlink voice RX: session=0x4xxx frags=.. bytes=.. opus_frames=..`；网关是否出现 `downlink voice TX: session=0x4xxx .. bytes`。
  2. 乒乓时序未加令牌状态机（步骤C 才做）。当前是「网关收图→发下行→恢复请求下一帧」的粗糙轮转，靠 ImageStart 的 600×50ms 重试和现有超时自愈。若出现网关发下行与节点 PIR 自推图相撞，可能短暂错乱——先观察是否发生。
  3. 网关发下行期间 `suspended_=true`，主 radio task 被挡、暂停 ImageCmd flood，属预期半双工行为。
  4. 语音延迟被整轮图传时长绑架（已知软肋），步骤A 不管，先验证链路通。

