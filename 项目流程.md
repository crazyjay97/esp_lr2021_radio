---
name: esp-lr2021-radio-flow
description: ESP32-S3 + LR2021 FLRC 电台固件的项目流程参考。涉及摄像头节点/电台网关双模式、语音链路、图传协议、配置与触发处理、低功耗 CAD 待机时使用。涵盖编译烧录、任务架构、数据包类型和请求/传输握手流程。
---

# ESP LR2021 Radio 项目流程

Lierda L-LRMAM36-FANN4-DK01 开发板（ESP32-S3 + Semtech LR2021）固件。

核心一句话：**同一份固件跑在两端**，靠运行时模式（存在 NVS 里）决定这台是
**摄像头节点**还是**电台网关**。所有电台访问都串行在一个 radio 任务里。

> 本仓库为开源项目，代码/注释/提交信息一律用英文；本流程文档用中文说明，仅供
> 开发者理解整体流程，与仓库内英文代码规范不冲突。

## 编译与烧录

- ESP-IDF 工程，应用代码都在 `main/`。`managed_components/` 是第三方库（lvgl、
  cmake_utilities、esp_new_jpeg），不要改。
- 电台驱动依赖 `lierda-iot__esp_lora_driver`，由组件管理器从
  `main/idf_component.yml` 自动下载。
- 目标芯片 `esp32s3`，标准流程：
  - `idf.py set-target esp32s3`（仅切换目标时用，先删 `build` 和 `sdkconfig`）
  - `idf.py build`，再 `idf.py -p <端口> flash monitor`
- 板子内部把 ESP32-S3 <-> LR2021 用 SPI2 预连线，这些引脚归 `esp_lora_driver`
  管（Kconfig `CONFIG_LIERDA_BOARD_LRMAM36_FANN4`），不在 BSP 里。

## 双模式

模式在启动时从 NVS 读取（`app` 命名空间，键 `mode`），默认摄像头模式。

- **摄像头节点**（`AppMode::camera`）：驱动 SP0A39 DVP 摄像头 + ST7789V3 LCD，
  拍照、JPEG 编码，收到请求后通过 FLRC 发送。还负责定时抓拍、声音/PIR 触发，
  以及可选的低功耗 CAD 待机。
- **电台网关**（`AppMode::radio`）：驱动 LVGL 网关界面，向节点发起拍照请求，
  接收并重组 JPEG 分片，显示进度。收到的图片通过 AP 模式 HTTP 图库对外提供。

## 配置开关（`app_config.h`）

- `APP_*_ENABLE` 系列开关控制音频、电台、摄像头、Wi-Fi（`APP_WIFI_STA_ENABLE`
  为 0 表示纯 AP 图库模式）、DSP 等功能。
- 所有可调的音频/编解码/电台/PTT/触发参数都集中在 `app_config.h`，改行为先动
  这里，别直接改任务逻辑。

## 任务架构（RadioPing）

电台核心是 `RadioPing`（`radio_ping.cpp/.hpp`），所有电台硬件访问都收敛到一个
radio 任务里，避免 IRQ/模式竞争。关键任务分核：

- **radio 任务**（CPU0，优先级 4）：`poll_once` 循环，处理 TX done / RX done、
  ImageCmd 重发、图传超时、CAD 待机。所有 RAL 电台操作只在这个任务里发生。
- **语音发送任务**（CPU1）：I2S 读麦克风 → DSP → Opus 编码 → 入 TX 队列。
- **语音播放任务**（CPU1）：从 RX 队列取 → Opus 解码 → I2S 写扬声器。
- **图传任务**（CPU1，优先级 3）：节点侧发送 JPEG 分片。
- **LVGL 任务**（CPU1，优先级 2）：网关界面刷新。

UI 线程要碰电台状态时，只设置请求标志（如 `abort_image_rx()` 置
`image_rx_abort_req_`），实际拆除在 radio 任务里做（`check_image_rx_abort`），
保证电台访问串行。

## 数据包类型（FLRC）

包头 14 字节，魔数 `"LRP1"` + 1 字节类型 + 会话/序号等字段。类型定义在
`radio_ping.cpp` 顶部：

| 值 | 类型 | 方向 | 说明 |
| --- | --- | --- | --- |
| 1 | Ping | 双向 | 链路测试 |
| 2 | Voice | 双向 | Opus 语音帧（每包聚合多帧） |
| 3 | ImageCmd | 网关→节点 | 请求拍照 |
| 11 | ImageCmdAck | 节点→网关 | 已收到请求 |
| 8 | ImageStart | 节点→网关 | 会话号/总分片数/crc32 |
| 4 | ImageData | 节点→网关 | 图片分片 |
| 7 | ImageEOT | 节点→网关 | 分片发完，问缺哪些 |
| 5 | ImageNack | 网关→节点 | 缺失分片列表（0=就绪/完成） |
| 6 | ImageDone | 网关→节点 | 传输完成 |
| 9 / 10 | Config / ConfigAck | 双向 | 参数下发与确认 |
| 12 | Vbat | 节点→网关 | 电池电压广播 |

FLRC 半双工，单包最大 255 字节；分片 = 14 字节头 + 最多 239 字节数据 +
2 字节 CRC16。

## 图传握手流程

网关按拍照键（K3）后：

1. 网关发 ImageCmd → 进入 RX
2. 节点收到 ImageCmd → 拍照 → JPEG 编码
3. 节点发 ImageStart（会话号、总分片数、crc32）→ 等 ACK
4. 网关回 Nack(missing_count=0) 表示"就绪"
5. 节点连续猛发所有分片（239 字节/片，中间不等待）
6. 节点发 EOT → "我发完了，你看还缺啥"
7. 网关回 Nack(缺失列表) → 告诉节点丢了哪些分片
8. 节点只补发缺失分片 → 再发 EOT → 重复直到 Nack(0)

**关键约束（防死锁）**：传输进行中不能再次发起新请求。半双工电台上，旧会话的
补包 NACK 和新请求的 ImageCmd 洪泛会互相抢占，两边都完不成，导致卡死。判据是
radio 的真实状态 `image_busy()`（`image_tx_active_ || image_rx_pending_ ||
image_req_active_`），覆盖"按下拍照→节点应答→收分片→完成/超时"整个窗口。
在途状态会在 RX 完成 / 10 秒无活动超时 / 用户离开传输页主动中止 这三处可靠清除，
所以拦截不会永久卡住，当前传输结束后下一次触发正常放行。

拦截分两层：

1. **UI 按键层（主拦截，网关）**：拍照键（触摸 case 0 / 物理键 K3）进入处理
   最前面先查 `s_busy_cb()`（指向 `g_radio.image_busy()`）。忙则**当场丢弃这次
   按键**——不切 RX 页、不调 capture 回调、不进入发起流程，UI 无反应。回调经
   `ui_gw_set_busy_cb()` 注册。这是正确的做法：源头忽略按键，而不是发起后再忽略。
2. **底层兜底**：`trigger_image_capture()` 入口和 `on_gw_capture()` 开头同样查
   `image_busy()`，忙则返回 `false` 不发起。用于万一有其他路径（声音/PIR 触发、
   冷却重试定时器）绕过 UI 时兜底。

以上是**网关（发起方）**的行为：传输中按键不发起新请求。

**节点（响应方）的并发处理（原有逻辑，非本次改动）**：节点侧不是"忙时不响应"，
而是"**照常回 ACK，但靠去重保证一次请求只拍一次**"。`handle_image_cmd()`
(radio_ping.cpp) 收到任何 ImageCmd 都**立刻回 ImageCmdAck**——哪怕正在忙。这是
故意的：网关收不到 ACK 会一直重发 ImageCmd，回 ACK 让它停止重发，丢包能自愈。
是否真的拍照由 `on_image_capture_request()` (app_main.cpp) 的几道拦截决定：

- `g_capture_busy` 为真（正在拍/正在传）→ 忽略，不拍第二张
- `session_id` 与上次派发相同（网关重发的同一请求）→ 忽略，不重复拍
- 音频冷却期内 → 忽略
- 只有带**新 session_id** 的请求才会真正触发拍照

也就是说：网关端在源头就不会发起并发请求；即便节点端收到重复/并发 ImageCmd，
也只回 ACK + 去重，不会重复拍照。两端配合保证半双工电台上不会出现会话互相抢占。

## 重传与超时

- EOT 无回应：重发 EOT，最多若干次
- 多次 EOT 仍无回应：盲发全部分片，再发 EOT
- RX 侧：10 秒无活动 → 放弃
- 低功耗：每轮请求先发 ~520ms LoRa 唤醒前导码唤醒节点 CAD 扫描，再在节点 8 秒
  唤醒窗口内每 30ms 洪泛 ImageCmd；一轮结束无 ImageStart 就开新一轮。

## 触发路径（摄像头节点）

- **定时抓拍**：`APP_AUTO_CAPTURE_DEFAULT_SEC`（默认 5 分钟）
- **声音触发**：RMS 过阈值后延迟 `APP_SOUND_TRIGGER_DELAY_MS`（500ms）再抓拍，
  让 Opus 预编码环形缓冲能录到触发时刻及其后的声音
- **PIR 触发**：GPIO12，触发后 15 秒冷却期，冷却期内不重新武装唤醒
- 各触发共用 `image_capture_cb_`，带去重，避免重复拍照

## 低功耗 CAD 待机（节点）

- `poll_once` 空闲时进入 CAD 光睡眠（停整片，每次 ~500ms）省电。
- 抓拍/图传/PIR 期间必须保持"醒着但空闲"，靠 `pir_push_wake_` 守卫（8 秒安全
  超时）；同步音频播放（如报警语音）用 `audio_playing_` 守卫（无超时，必须成对
  begin/end），否则光睡眠会打断 I2S 输出。

## 相关文件速查

- `app_main.cpp`：模式切换、NVS、UI 回调、定时器、按键长短按
- `radio_ping.cpp/.hpp`：FLRC 收发引擎、语音链路、图传协议、低功耗
- `image_transfer.cpp/.hpp`：RX 分片重组、JPEG 编解码
- `image_store.c`：收到图片的存储 + HTTP 图库
- `ui_gateway.c`：LVGL 网关界面（拍照/配置/链路/图片页）
- `audio_processor.cpp`：降噪/AGC/噪声门 DSP
- `opus_codec.cpp`、`audio_ringbuf.hpp`、`opus_ringbuf.hpp`：编解码与预录缓冲
- `bsp_*.c`：板级驱动（音频/LCD/按键/I2C/电池/CON6）
- `board_config.h`：引脚映射

---

# FLRC → FLRC BURST 传输路径替换方案（进行中）

> 目标：把 `radio_ping.cpp` 现有「普通 FLRC 收发」改成「FLRC BURST 流式收发」，
> **并承载现有语音/图传业务数据**（不是纯吞吐测速）。样板工程：
> `E:\MYESP32\lr2021_rf_test_comp\lr2021_rf_test_comp\main\LiotRfCmd.c`（BURST 实现在 260~547 行）。
> 驱动已升到 `esp_lora_driver` v2.0.2；编译已通过；启动 WDT 崩溃是串口刷屏慢的假象，已排除。

## 普通 FLRC vs FLRC BURST

| 维度 | 现有普通 FLRC | 目标 FLRC BURST |
|---|---|---|
| 单包长度 | 变长 ≤255B（`APP_FLRC_MAX_PAYLOAD_BYTES`） | 定长 **511B** |
| FIFO | 默认 256B | 扩 **1024B**（放 2×511） |
| 发送 | 一包一次 `set_tx`，`TX_DONE` 回 idle 再排下一包 | 背靠背：FALLBACK=**FS**，预填 2 包，`TX_DONE` 里先 `set_tx` 再补 1 包 |
| 接收 | 每次 `RX_DONE` 后重新 `set_rx`（有盲区） | `set_rx_with_timeout(0xFFFFFF)` 持续接收，`RX_DONE` 只读 FIFO |
| 数据接口 | `ral_set_pkt_payload` / ral 层 RX | 直接 `lr20xx_radio_fifo_write_tx/read_rx` |

## 样板代码地图（LiotRfCmd.c）

- `rf_setup_flrc(is_tx,len)` @244 —— 设 is_tx/pld_len → `ralf_setup_flrc`
- BURST 参数宏 @266~275：`PAYLOAD_LEN 511`、`RAW_BIT_RATE 2_600_MBPS`、`CR 3_4`、
  `PULSE_SHAPE BT_05`、`PREAMBLE 32_BITS`、`CRC 2_BYTES`、`CRC_SEED 0xFFFFFFFF`、**`CRC_POLY 0x0000755B`**
- `flrc_burst_setup(is_tx)` @348 —— 覆盖参数 → setup → 配 1024B FIFO → 重设 DIO 中断 → set_fallback（TX=FS/RX=STDBY_XOSC）
- `flrc_burst_start_tx()` @419 —— 清 TX FIFO → 预填 2 包 → `set_tx(0)`
- `flrc_burst_start_rx()` @455 —— 清 RX FIFO → `set_rx_with_timeout_in_rtc_step(0xFFFFFF)`
- `flrc_burst_on_tx_done()` @504 —— 先 `set_tx(0)` 发已缓存包，再 `fifo_write_tx` 补 1 包
- `flrc_burst_on_rx_done()` @522 —— `get_rx_level` → `read_rx`，不再 set_rx

底层 API（已确认 v2.0.2 驱动存在，头文件 `lr20xx_radio_common.h` / `lr20xx_radio_fifo.h`）：
`lr20xx_radio_fifo_configure_1024_byte_tx_fifo/_rx_fifo`、`_clear_tx/_clear_rx`、
`_write_tx`/`_read_rx`/`_get_rx_level`、`lr20xx_radio_common_set_tx(ctx,0)`、
`_set_rx_with_timeout_in_rtc_step(ctx,0xFFFFFF)`、`_set_rx_tx_fallback_mode(ctx, FS/STDBY_XOSC)`。
ctx 取法：本工程用 `radio_.ral.context`（样板用 `smtc_rac_radio.ral.context`）。

## 现有 radio_ping.cpp 要改的点

- `configure_flrc()` @760 —— 普通 FLRC 初始化（保留，作 BURST setup 基础）
- `schedule_tx()` @715 —— 现走 `build_voice_packet` → `ral_set_pkt_payload` → `ral_set_tx`
- `schedule_rx()` @683 —— 现走 `ral_set_dio_irq_params` → `ral_set_rx`
- `handle_irq()` @642 —— 处理 TX_DONE / RX_DONE / CRC_ERROR / HDR_ERROR / TIMEOUT
- `build_voice_packet()` @790 —— 组业务包（magic `LRP1`+头+多 opus 帧），**保持不动**，只改怎么塞进射频

## 改造设计（BURST 是传输层，业务包是载荷）

- **载荷适配**：业务包 ≤255B，BURST 定长 511B。
  - 方案 A（先做）：业务包放进 511B 缓冲，多余部分填充；接收端按业务头（magic/长度字段）解析真实长度，忽略填充。改动最小。
  - 方案 B（二期）：一个 511B 里拼多个业务包，接收端按业务头逐个拆，省带宽但复杂。
- **发送**：新增 `configure_flrc_burst(true)` → 清 TX FIFO → `build_voice_packet` 填 511B → 预填 2 包 → `set_tx(0)`；`TX_DONE` 里先 `set_tx(0)` 再取队列填 511B 补包。
- **接收**：新增 `configure_flrc_burst(false)`（fallback=STDBY_XOSC，配 rx fifo）→ continuous `set_rx`；`RX_DONE` 里 `read_rx` 读 511B → 按业务头解析 → 复用现有 `handle_rx_packet` 处理 → 不再 set_rx。
- **保留** `smtc_modem_hal_protect_api_call/start_radio_tcxo/set_ant_switch` 包裹；SPI 操作仍放 radio 任务，ISR 只 notify。

## ⚠️ 关键风险

1. **CRC 多项式不一致 → 全丢包**：样板 BURST 用 `0x0000755B`，现有 `configure_flrc()` 用 `0x04C11DB7`。改 BURST 时收发两端 + 两个 configure 必须统一为 `0x0000755B`。
2. **队列空 TX 空转**：无语音/图传数据时不能一直补空包空占信道，需加「队列空停发、有数据再 start_tx」门控。
3. **511B 定长 ≠ 真实长度**：接收端严格按业务头长度取，别拿 511 当有效长度。

## 落地步骤（重启后按序）

1. `app_config.h` 加 BURST 宏（`APP_FLRC_BURST_PAYLOAD_LEN 511U`、`APP_FLRC_BURST_CRC_POLY 0x0000755BUL`），保留现有普通 FLRC 宏
2. radio_ping.cpp 新增 `configure_flrc_burst(bool is_tx)`（照抄样板 `flrc_burst_setup`，ctx 用 `radio_.ral.context`）
3. 改 `schedule_tx()`：BURST 预填 2 包 + set_tx(0)，载荷来自 `build_voice_packet`（方案 A）
4. 改 `handle_irq()` TX_DONE 分支：先 set_tx(0) 再 fifo_write_tx 补包 + 队列空门控
5. 改 `schedule_rx()`：BURST continuous set_rx
6. 改 `handle_irq()` RX_DONE 分支：fifo read_rx 511B → 按业务头解析 → 不再 set_rx
7. 统一 CRC 多项式 0x0000755B（收发 + configure_flrc/burst 一致）
8. 编译（用户自行）→ 烧录双板（一发一收）→ 验证语音/图传通、看 pkt/s 与丢包
9. 更新下方工程日志

## 进度

- [x] 修编译错误（FLRC 结构体迁移新驱动 API）
- [x] 排除启动 WDT 崩溃（串口慢假象）
- [x] 写本方案到流程文档
- [ ] app_config.h 加 BURST 宏
- [ ] configure_flrc_burst() 新增
- [ ] schedule_tx / TX_DONE 改 BURST
- [ ] schedule_rx / RX_DONE 改 BURST
- [ ] 统一 CRC 多项式 0x0000755B
- [ ] 编译烧录双板验证语音/图传
- [ ] 更新工程日志

---

# 工程日志

> 开发期中文记录，仅供内部。开源发布前删除本节（仓库对外内容一律英文）。

## 2026-08-10 异步显示 busy 生命周期与误触中止修复（代码完成，待编译/板测）

### 新加入的功能
- 图片完成事件增加 session ID 和显示回执；应用层新增 `g_image_ui_pending`，从 RGB565 投递前
  一直保持到 LVGL 确认该 session 已显示或已丢弃，避免下一帧早于上一帧实际显示启动。
- 图传、JPEG 处理或 UI 显示待完成期间，统一屏蔽 K3/K4/K5/K6 和左右滑动；短按页面键不再
  离开 RX 页，也不会调用接收中止回调。K6 长按切换模式仍保留为独立逃生入口。

### 改动内容
- `main/app_main.cpp`：busy 条件扩展为射频状态、图片任务状态和 UI 待显示状态三者并集；图片
  显示/丢弃回调按 session 清除 pending；冷却定时器也复用同一 busy 条件。
- `main/radio_ping.cpp`、`main/app_main.cpp`：射频完成时保持 `image_rx_pending_` 到完成回调返回，
  回调入口立即置 `g_image_rx_processing`；消除“射频 busy 已清、图片 busy 尚未置位”的跨核
  交接空窗。
- `main/app_main.cpp`：`on_gw_capture()` 返回 `trigger_image_capture()` 的真实结果，不再把底层拒绝
  固定报告为启动成功。
- `main/ui_gateway.c/.h`：图片完成事件携带 session ID；新 ImageStart 发布时记录最新 session，
  LVGL 丢弃不匹配的迟到图片并释放 RGB565，再向应用层回执 `displayed=false`。
- `main/ui_gateway.c`：队列满淘汰图片完成事件、显示参数错误和正常显示三条路径都会释放所有权
  并回执，避免 UI pending 永久不清；按键日志带实际 key ID，便于继续排查 ADC 键值误识别。

### 遇到的问题与根因
- 网关日志显示 session 43 在 `32861 ms` 仅完成 UI 入队，`32866 ms` 就启动了 session 44，
  而 session 43 到 `32886 ms` 才由 LVGL 显示；说明图片任务结束后 busy 提前清零，上一帧完成
  事件能够跨进下一帧并覆盖 RX 页面。
- session 44 期间 `33066/33311 ms` 的 K3 均被原 busy 判断拒绝；真正中断传输的是紧随其后的
  `33312 ms UI: left transfer page`。原代码只屏蔽 K3，K4/K5/K6 仍会在 RX 页主动 abort。
- abort 后网关连续创建 session 45～59，但节点仍在 session 44 发送任务中，所以持续返回
  `ImageCmdAck status=1 (busy)`；这不是射频锁再次卡死，而是主动中止后的请求风暴。

### 是否解决
- 代码状态：busy 生命周期、全页面键门禁、迟到 session 丢弃和异常回执已完成静态修改。
- 本地编译：按 ESP32 协作规范未执行。
- 硬件验证：未完成；需连续快速按 K3/K4/K5/K6，确认只出现
  `key N ignored: transfer/display in progress`，且每帧 `image UI complete` 后才能启动下一帧。

## 2026-08-10 网关图片完成链彻底异步化（代码完成，待编译/板测）

### 新加入的功能
- 新增网关 `image_rx` 图片处理任务，固定在 CORE1，优先级 1、栈 16384 字节；其优先级低于
  CORE1 上优先级 2 的 LVGL 任务，避免 JPEG 解码持续占用 CORE1 时饿死界面刷新。
- 图片接收完成后采用三级所有权传递：CORE0 射频任务重组独立原始帧并投递；CORE1 图片任务
  解码 JPEG；LVGL 队列接管 RGB565，复制到画布后统一用 `jpeg_free_align()` 释放。

### 改动内容
- `main/app_main.cpp`：`on_image_rx_complete()` 不再解码 JPEG、保存图库、播放音频或调用任何
  LVGL/显示函数；重组成功后立即 `rx_reset()`，用零等待队列投递给图片任务并返回。
- `main/app_main.cpp`：新增单元素图片工作队列和忙状态；上一帧仍在解码、入图库或播放随帧音频时，
  网关拒绝启动下一帧，避免两帧处理缓冲区重叠。
- `main/ui_gateway.c/.h`：`ui_gw_rx_complete()` 改为非阻塞事件投递；投递成功即转移 RGB565
  所有权，LVGL timer 在自身上下文中更新链路数据、复制画布、切换图片页并释放缓冲区。
- `main/ui_gateway.c`：关键事件挤掉旧图片完成事件时会先释放旧 RGB565；投递失败时所有权仍归
  图片任务，由调用者释放，补齐队列满和显示参数错误路径的内存清理。
- `main/app_config.h`：新增网关图片接收任务的核、优先级和栈配置。

### 遇到的问题与当前结论
- 现象为无线传输已经到 100%，但图片很久后才出现；节点日志显示 33.5 KB 数据约 302 ms 发完，
  因此延迟位于网关接收完成后的重组/解码/等待 LVGL 锁/画布复制链，而不是空中分片传输。
- 旧版 `ui_gw_rx_complete()` 在 CORE0 使用 `portMAX_DELAY` 等待 LVGL 递归锁，且 JPEG 解码、
  图库保存和音频播放也都串行压在射频回调内；本次已移除该同步等待链。
- 静态检查已通过：接口声明/调用一致、所有新增队列投递均为零等待、同步 LVGL 锁已从图片完成路径移除，
  `git diff --check` 无空白错误；5 个改动文件均为严格 UTF-8 无 BOM、纯 CRLF、无 `CRCRLF`。
- ESP-IDF 编译和硬件板测未执行；需用户编译烧录后观察 `image processing queued`、
  `image display queued`、`image displayed` 三段日志的时间间隔。
- 是否解决：代码层解耦已完成；实际 100% 到图片显示的延迟改善程度仍待硬件验证。


## 2026-08-10 图传 CRC/接收异常状态闭环（代码完成，待编译/板测）

### 新加入的功能

- CRC32 校验错误时，RX 页标题改为 `CRC ERROR`，状态标记为 `RETRY`，百分比、进度条、
  分片计数、速率、耗时和 RSSI 全部清零；随后继续显示整图重传进度。
- 接收超时、接收缓冲区内存不足、分片重组失败、JPEG 解码失败、图像尺寸或显示错误等
  异常统一在原 `Receiving #SESSION` 标题位置显示，状态标记为 `FAIL`。

### 改动内容
- `main/ui_gateway.c/.h`：增加 CRC 重试和通用失败 UI 事件；错误字符串复制进队列，队列满时
  为关键错误事件腾出一个位置，所有标题和进度对象仍只由 LVGL 任务更新。
- `main/radio_ping.hpp/.cpp`：增加图传错误回调；CRC 错误、10 秒无活动超时和接收内存不足
  都会通知应用层；重传过程中每 25 ms 合并投递一次进度，避免射频任务阻塞或 UI 队列洪泛。
- `main/radio_ping.cpp`：增加 EOT session/总分片校验，丢弃上一帧迟到的 EOT，避免其重置
  当前帧接收状态。
- `main/app_main.cpp`：把射频错误映射为 RX 页标题，并补齐重组失败的 UI 错误通知。

### 遇到的问题
- 旧逻辑在 EOT 处理时先显示 `100%`，之后才校验整图 CRC；CRC 错误、重组失败或最终超时
  只清内部状态，没有清 UI，因此屏幕会永久保留旧的 `71/71、100%`。

### 是否解决
- 代码状态闭环：已完成静态修改。
- 本地编译：按项目协作规范未执行。
- 硬件验证：未完成；需用户编译烧录后验证 CRC 错误提示、进度清零、重传进度和最终失败提示。

## 2026-08-10 网关图传进度 UI 与射频任务第一版解耦（代码完成，待编译/板测）

### 新加入的功能
- 新增一个长度为 8 的 FreeRTOS UI 值事件队列；接收开始、进度和节点电压事件由 radio 回调
  非阻塞投递，LVGL timer 在 `lv_timer_handler()` 上下文中取出并刷新页面。

### 改动内容
- `main/ui_gateway.c/.h`：把 `ui_gw_rx_begin()`、`ui_gw_rx_progress()` 改成仅复制
  session、分片数、RSSI、节点电压等小型值数据到队列，不再获取 LVGL 锁或调用 `lv_refr_now()`；
  实际对象更新收敛到 LVGL timer 回调。
- 新帧开始时同时清零百分比、进度条、速率、耗时和 RSSI，避免沿用上一帧的 `100%`。
- `main/radio_ping.hpp/.cpp`、`main/app_main.cpp`：进度回调增加真实 session ID，UI 标题
  不再固定显示 `Receiving #000`。

### 遇到的问题
- 原逻辑在 radio 任务处理 `ImageStart` 时同步进入 LVGL 并强制刷新 LCD，可能延迟随后
  的 ready-ACK；充电宝小电流模式下的射频供电/ACK 失败风险仍是独立待排查项。
- Codex 内置 `apply_patch` 在当前 Windows CRLF 工作区读取失败，本次改用等价差异补丁
  应用，并在完成后单独核对 UTF-8、BOM、CRLF 和 `git diff`。

### 是否解决
- 代码解耦：已完成静态修改。
- 本地编译：按项目协作规范未执行。
- 硬件验证：未完成；需用户编译烧录后重点复测充电宝小电流模式的连续多帧传输。

## 2026-07-27 ImageCmdAck 状态化，修复狂按拍照后约 10 秒无响应（代码完成，待编译/板测）

### 新加入的功能

- 复用现有 14 字节 `ImageCmdAck`，使用原保留字节 `byte[8]` 返回 `ACCEPTED(0)`、`BUSY(1)`、`COOLDOWN(2)`、`DUPLICATE(3)` 或 `REJECTED(4)`。
- 网关收到 `BUSY`、`COOLDOWN` 或 `REJECTED` 后立即结束本次图片等待并恢复按键，不再等待 10 秒无数据超时。

### 改动内容

- `main/radio_ping.hpp`：增加 ACK 状态枚举、拍照回调返回值和网关拒绝通知回调。
- `main/radio_ping.cpp`：节点先执行拍照准入判断，再携带状态发送 ACK；网关解析 ACK 状态，只有 `ACCEPTED` / `DUPLICATE` 保持等待图片，其余状态立即清除 `image_req_active_`、`image_rx_pending_` 和接收缓冲。
- `main/app_main.cpp`：同会话重发优先识别为 `DUPLICATE`；新会话撞上当前拍照返回 `BUSY`；音频冷却返回 `COOLDOWN`；只有拍照任务创建成功才返回 `ACCEPTED`。网关拒绝回调显示可重试原因。
- `BUSY` 路径不再调用 `notify_capture_dropped()`，避免误清除当前正在执行拍照的低功耗保活标志。
- ACK 包类型、包长度和其他无线图传协议保持不变；旧节点发送的保留字节 0 会被新网关解释为 `ACCEPTED`。

### 遇到的问题

- 原节点在判断 `g_capture_busy` 前已无条件发送成功 ACK；网关因此停止重发并等待一张实际未启动的图片，直到约 10 秒接收超时后才恢复。
- 原代码先判断 busy、后判断同会话去重，无法区分正常的同会话重发和真正的新会话冲突。

### 是否解决

- ACK 状态和网关立即退出逻辑已完成静态核对，理论上可消除狂按拍照触发的约 10 秒假等待。
- 尚未进行 ESP-IDF 编译、烧录和双板测试；需连续狂按拍照键，确认 `BUSY` 后按键立即恢复、正常重复包返回 `DUPLICATE` 且当前图片不中断。
- 当前修改未暂存、未提交，验证通过后是否提交由用户决定。

## 2026-07-27 摄像采集改为取第二帧（代码完成，待编译/板测）

### 新加入的功能

- 每次拍照改为取启动采集后的第 2 帧，仅跳过第 1 帧，缩短拍照准备时间。

### 改动内容

- `main/camera_dvp.cpp`：原逻辑 `capture_target = frame_count + 3` 会取第 3 帧、跳过前 2 帧；新增 `kCaptureFrameNumber = 2`，统一控制目标帧序号。
- 修正采集日志，固定输出本次目标帧和实际跳帧数，避免原来的累计 `frame_count - 1` 被误认为单次跳帧数。

### 遇到的问题

- 工程内没有已有的目标帧配置变量，原值为函数内写死的 `+3`，因此提取为文件内常量。

### 是否解决

- 代码和静态核对已完成，理论上相机采集阶段减少一个帧周期。
- 尚未进行 ESP-IDF 编译、烧录和板测；需确认第二帧的 AE/AWB、亮度和色彩已经稳定，并对比 `capture done` 与总耗时。
- 当前修改未暂存、未提交，验证通过后是否提交由用户决定。

## 2026-07-27 发送端 240×320 JPEG 预处理（代码完成，待编译/板测）

### 新加入的功能

- 摄像节点继续完整采集 `640×480 UYVY`，发送前使用双线性插值缩小到 `320×240`，再由 JPEG 编码器顺时针旋转 90°，直接生成 `240×320` 竖屏 JPEG。
- JPEG 固定使用 Q90、4:2:0；LCD 与网页图库统一使用同一份 `240×320` JPEG。
- 网关仅接受 `240×320` JPEG，不保留旧 `640×480` 节点兼容路径。

### 改动内容

- `main/app_config.h`：增加发送尺寸和旋转前预处理尺寸常量，保留 400 KB JPEG 输出上限。
- `main/image_transfer.cpp`：增加 64 字节对齐 PSRAM 缩放缓冲；使用 `esp_image_effects` 双线性缩放；将缩放后的 UYVY 原地整理为 JPEG 编码器需要的 YCbYCr；配置 Q90、4:2:0 和顺时针 90° 旋转；增加缩放耗时、编码耗时和 JPEG 字节数日志。分配、缩放或编码失败时释放临时资源并终止本次发送，不回退旧分辨率。
- `main/app_main.cpp`：解析 JPEG 头后严格检查 `240×320`；尺寸、内存或解码异常时终止本次接收，不写入网页图库。
- `main/ui_gateway.c`：删除网关端 `640×480 → 320×240 → 240×320` 的缩放和旋转，解码后直接写入 LCD 画布，仅保留当前屏幕需要的 R/B 通道交换。
- `main/image_store.c`：图库缩略图比例改为竖屏 3:4，并使用 `contain`，避免 `240×320` 图片在网页中被裁剪或拉伸。
- 图传分片协议、FLRC BURST、音频拼接格式和 NACK 重传机制未修改；现有发送日志继续记录分片数和总传输时间。

### 遇到的问题

- Windows 工作区中补丁工具读取部分 UTF-8 文件时报误，改用精确文本替换并在修改后复核 UTF-8、无 BOM 和 CRLF 格式。
- 按协作规则，本次未运行 ESP-IDF 编译、烧录或板端测试。
- 当前修改未暂存、未提交；只有用户完成适用验证并确认通过，且明确决定提交后，才允许执行暂存和提交。

### 是否解决

- 代码修改和静态核对已完成：组件依赖存在，UYVY 双线性缩放和 JPEG 顺时针旋转接口匹配，旧网关缩放路径已移除，差异格式检查通过。
- 仍需用户完成双固件编译、烧录和双板实测，重点确认 JPEG 头 `240×320`、方向、清晰度/色彩、传输耗时、连续 10 次稳定性，以及远程/定时/PIR/声音触发和带音频图片链路。
## 2026-07-27 LoRa 长前导唤醒 TX 超时（拍照路径 IRQ 双消费竞争）修复（待板测）

### 背景 / 现象
- 低功耗下网关拍照唤醒（508 符号长前导）间歇性 `LoRa wakeup TX timeout` → `LoRa wakeup failed (round retry)`。
- 网关.txt 日志：拍照路径唤醒耗时**渐进恶化** 545ms → 913ms → 超时（>1500ms）；
  而配置路径（`send_config`）唤醒每次都干净 545ms；重试轮（radio 任务里）唤醒也是 545ms。
- 长前导空口时间是确定的 ~545ms（SF7/BW125，508 符号 ≈ 520ms + 头/载荷），不是空口问题。

### 根因（已对代码核对）
- `trigger_image_capture()` 跑在 **UI/定时器任务**（`on_gw_capture` / `cooldown_retry_cb`），
  不是 radio 任务。它首轮调 `start_image_req_round` → `send_lora_wakeup` → `wait_for_tx_done`，
  后者忙轮询 `irq_pending_` 等 TX_DONE。
- 与此同时 radio 任务的 `poll_once()` **没被挂起**，也在消费 `irq_pending_`
  （`ral_get_and_clear_irq_status` 会清掉**全部** IRQ 位）。
- TX_DONE 触发时两个任务抢：若 radio 任务先抢到并清位，`wait_for_tx_done` 永远看不到
  TX_DONE → 空转到 1500ms 超时。是调度抖动导致的间歇 + 渐进。
- `send_config` 之所以每次干净：它开唤醒前先 `suspended_ = true` 停掉 radio 轮询，
  自己是唯一 IRQ 消费者。拍照路径漏了这层保护。

### 改动
- `radio_ping.cpp` `trigger_image_capture`：首轮 `start_image_req_round()` 前后加
  `suspended_ = true` / 还原（沿用 `send_config` 同款保护），让唤醒成为唯一 IRQ 消费者；
  首轮结束立即释放，好让 `check_image_req_retry`（radio 任务）继续 flood。
- 只动了唤醒时序保护，未碰协议/握手/队列/重传/状态机/BURST 图传原语。

### 是否解决
- 代码已改，Bash 本机无法编译，待人工 `idf.py build` + 网关上板验证
  （低功耗下连续拍照，唤醒应稳定 545ms，不再 TX timeout）。

## 2026-07-27 网关低功耗未关 audio clip（会话内状态劈叉）修复（待板测）

### 背景 / 现象
- FLRC BURST 图传已板测正常。新问题：网关**先开 AUDIO 再进低功耗**，低功耗下按拍照
  仍走 audio cooldown 逻辑，刷 `UI capture: audio cooldown, auto-retry in ...ms`。

### 根因
- audio 开关状态存三份，进低功耗时只清了两份：
  1. UI 层 `ui_gateway.c:cfg_apply_low_power_lock`：置 `s_audio_clip_on=false` + 存
     `ui_gw` NVS `audio=0` + 灰掉开关。✓
  2. 节点侧 app 层：收 `APP_CFG_KEY_LOW_POWER` 时强制 `g_audio_clip_enabled=false`（app_main.cpp:363-377）。✓
  3. **网关侧 app 层 `g_audio_clip_enabled`：没人清。✗**
- `on_gw_low_power_change`（app_main.cpp:1096）只 `send_config` + 置 `g_low_power_enabled`，
  没碰网关自己的 `g_audio_clip_enabled`；而拍照回调 `on_gw_capture:1011` 判的正是它。
- UI 层 lock 直接改 `s_audio_clip_on` 而**没调 `s_audio_clip_cb`**，app 层收不到通知 → 状态劈叉。
- 因 UI 已把 `ui_gw` NVS `audio` 写 0，重启后从 NVS 读回 false 自愈 → **仅当前会话内的 bug**。

### 改动
- `app_main.cpp` `on_gw_low_power_change`：开低功耗成功后同步 `g_audio_clip_enabled=false`，
  与节点侧 `APP_CFG_KEY_LOW_POWER` 路径、UI 侧 lock 对齐（NVS 由 UI 层写，不重复）。

### 是否解决
- 代码已改，Bash 本机无法编译，待人工 `idf.py build` + 网关上板验证
  （开 AUDIO → 开低功耗 → 按拍照，应不再刷 audio cooldown）。

## 2026-07-27 FLRC BURST 图传全丢包根因修复：连续 RX 中途 set_rx 清 FIFO（待板测）

### 背景 / 现象
- 511B 定长 BURST + TX 补零 + RX 按 level 排空改完后，网关日志仍**全丢**：
  ImageStart 单包能解，数据片全部 "RX unknown"，len 在 333~445 跳动（从不到 511），
  垃圾包头**周期性精确重复**（非随机射频噪声），首个缺片从序号 1 起（片 0 正常、片 1 起失步）。

### 根因（已逐条对代码核对，置信度高）
- `handle_irq()` RX_DONE 分支每收完一片都无条件 `schedule_rx()`；
  `schedule_rx()` → `ral_set_rx()`，而 `ral_lr20xx_set_rx()` 第一行就是
  `lr20xx_radio_fifo_clear_rx(context)`（`ral_lr20xx.c:462`）。
- ⇒ 连续 RX 下，每收完 1 片就把**已经流进 FIFO 的下一片前半段清掉**：
  片 0 完整、片 1 丢头 → 读指针从 JPEG 数据中间开始 → magic 变随机 → **永久错位**，
  错位是确定性的，故垃圾包头周期性重复。
- 上一轮补丁只在 `dispatch_rx_packet` 的 ImageData 分支去掉了 set_rx，
  但**外层 `handle_irq` 的统一 `schedule_rx()` 仍在跑**，注释与实际行为不符。

### 改动（严格限定 BURST 收发原语，不碰握手/重传/状态机/语音）
1. `handle_irq()` RX_DONE：进 `handle_rx_packet()` 前快照 `image_rx_pending_`；
   若在图传流中，drain 后**不再 set_rx**，只把 `mode_` 保持 `rx_pending`
   （让 `poll_once` 空闲分支也不会重挂），靠硬件 continuous RX 自动续收。
   FIFO 只由 `handle_image_start` 的 ready-ACK `schedule_rx` **对齐一次**。
2. `handle_irq()` CRC_ERROR：**不再** `handle_rx_packet()` dispatch 坏包
   （坏包长度未知，drain 无法对齐），改为直接 `schedule_rx()` 清 FIFO 重对齐，
   丢失片由 EOT/NACK 重传补回（对齐样板 on_rx_done 遇 CRC 错清 FIFO 的做法）。
3. `handle_image_data(uint16_t len)`：新增实际长度校验
   `len >= kHeaderSize + frag_len + 2`，防短读/错位时拿上一包残留字节算 CRC16 侥幸通过。

### 未采纳的一点（含理由）
- 复核建议里的"drain 首轮也拒收 <511"**未实现**：EOT/NACK/Start 是合法短单包，
  会被误杀导致握手死锁。主修复后数据片必为完整 511B（RX_DONE 只在整包到达才触发），
  首轮 <511 只可能是控制短包 → 应正常分发；错位数据片改由第 3 点的 len 校验拦截。

### 是否解决
- 代码已改完，Bash/idf.py 本机管道报错仍无法编译，**待人工 `idf.py build` + 双板实测**
  （节点+网关都要重烧最新固件；重点看数据片是否稳定解出、缺片列表是否单调收敛）。

## 2026-07-27 驱动升级 0.0.7 后 LR2021 复位初始化卡死修复（已板测验证可行）

### 背景 / 问题
- commit `5a18414`（"更新到新驱动并修复老驱动带来ERROR问题"）升级 esp_lora_driver 到 0.0.7 后，
  冷启动时串口反复刷 BUSY 卡高、每 ~5s 一次 Task WDT，最终卡在 radio 初始化阶段。
- 上一个正常 commit 是 `cbdb181`（老驱动）。

### 根因（已逐条对代码核对）
- 主工程 `radio_ping.cpp` 调用顺序：先 `ral_reset()` 再 `ral_init()`。
- 但把 NRST(GPIO38) 配成输出的 `radio_gpio_init()` 在 `lr20xx_hal_init()` 里，
  而 `lr20xx_hal_init` 要等 `ral_init()` 才执行。
- ⇒ 第一次 `ral_reset()` 操作的是一个**还没配成输出**的 GPIO38，复位脉冲根本没送到芯片。
- 老驱动 0.0.5 的 `ral_init` 不加载 PRAM，靠上电态能撑；新驱动 0.0.7 的 `ral_init`
  新增 PRAM 加载（`ral_lr20xx_load_pram`），对干净复位敏感 → 升级后暴露回归。
- `lr20xx_hal_wait_on_busy()` 纯死等无超时无让出 → CPU0 被永久占住 → 反复 Task WDT。
- 例程 `lr2021_rf_test_comp` 正常，是因为它在 app_main 以"点灯 LED_GPIO 38"名义提前把
  GPIO38 配成输出并拉高（GPIO38 == NRST），复位脉冲才真正生效。

### 改动
- `main/app_main.cpp`：在 `#if APP_RADIO_FEATURES_ENABLE` 块内、任何 radio init 之前，
  把 `CONFIG_LR2021_NRST_GPIO` 配成输出并拉高（幂等，仿例程）。保证首次复位脉冲能送达。
  改在应用层，不动第三方驱动，组件更新不会被覆盖。

### 状态
- **已板测验证可行**：冷启动不再刷 BUSY / Task WDT，radio 正常初始化。
- 暂未加固 `wait_on_busy`（超时+向上返回错误）；核心复位修好后 PRAM 正常加载，死等不触发。
  若后续需硬件异常安全网，再单独做（需改 HAL 返回签名，且超时必须中止 init、不可带病发 SPI）。

## 2026-07-27 FLRC BURST 落地（仅图传大块路径，已改完）

### 背景 / 改动
- 明确范围：**只替换图传大块数据的发送原语**，改成 FLRC BURST 流式连发；
  协议/握手/队列/重传/状态机/去重/CAD/双模式**一律不动**；PTT 实时语音不生效、**不改**。
- 关键设计确认（读驱动源码 + 样板得出）：
  - `ral_set_pkt_payload` == `fifo_write_tx`（纯写 FIFO，不含长度寄存器），
    `pld_is_fix=false` 下 `set_tx` 发送 `min(fifo_level, pld_len)` 字节 → **变长末片天然正确，无需填 0**。
  - 1024B FIFO：TX 在 `0x804000`、RX 在 `0x804400`，**地址不重叠**，
    故 `configure_flrc()` 里 TX/RX 两个 1024 都配，node/gateway 通吃，不用按相位切 FIFO。
  - CRC 多项式**保留 `0x04C11DB7`**（收发同固件，一致即可；不采用样板的 `0x0000755B`）。
- 具体改动：
  1. `app_config.h`：`APP_FLRC_MAX_PAYLOAD_BYTES` 255→**511**（联动 `tx_buf_/rx_buf_`
     扩容、分片 `APP_IMAGE_FRAGMENT_DATA_SIZE` 自动变 **495** = 511-16、RX 能收满 511B）；
     新增 `APP_FLRC_BURST_PAYLOAD_LEN 511U`。
  2. `configure_flrc()`：`ralf_setup_flrc` 成功后追加 `fifo_configure_1024_byte_tx_fifo`
     + `_rx_fifo`；`pld_len_in_bytes` 随宏自动变 511。
  3. 新增 `build_image_fragment()`（抽出三处重复构包逻辑）+ `burst_send_fragments()`
     （BURST 驱动：`FALLBACK_FS` → 清 TX FIFO → 预填 2 包 → `set_tx` → 每次 `wait_for_tx_done`
     后先 `set_tx` 发下一包再补 1 包 → 收尾恢复 `FALLBACK_STDBY_XOSC`）。
     `indices==nullptr` 走顺序 `0..N-1`（初发），否则走 nack 索引子集（重传）。
  4. `image_tx_task`：初发分片循环 @1452、重传分片循环 @1563 各替换为一行
     `burst_send_fragments(...)`。EOT/握手/ACK/NACK 仍走原 `send_single_packet`+`schedule_rx`。
- 任务是**轮询式**（`wait_for_tx_done` 自读自清中断），故 `handle_irq` 的 TX_DONE 分支**未改**。

### 问题
- Bash/idf.py 在本机环境管道报错（`Device or resource busy`），**本轮未能编译验证**，
  改动经逐符号核对（枚举名、结构体字段、头文件包含、min(fifo,pld) 模型）自洽。

### 是否解决
- 代码改动：已完成，待人工 `idf.py build` + 上板实测（满片 495B×N + 变长末片、
  重传子集、EOT 转 RX）。
- ⚠️ 更正旧提醒：CRC 多项式**不统一为 0x0000755B**，本项目保留 0x04C11DB7（收发同固件）。

## 2026-07-24 FLRC → FLRC BURST 传输路径替换（规划）

### 背景 / 改动
- 升级 `esp_lora_driver` 到 v2.0.2 后，`radio_ping.cpp` 的 FLRC 结构体字段报错，
  已迁移到新枚举式 API（`configure_flrc()` @760），**编译通过**。
- 需求：把普通 FLRC 收发改成 FLRC BURST 流式收发，**并承载现有语音/图传业务**
  （非纯测速）。样板：`lr2021_rf_test_comp\main\LiotRfCmd.c`（260~547 行）。
- 已把完整改造方案、样板代码地图、风险、落地步骤写进本文档上方
  「FLRC → FLRC BURST 传输路径替换方案」章节，重启后照做即可。

### 问题
- 启动疑似 task_wdt 崩溃 —— **已排除**，是电脑卡、串口刷屏慢造成的假象，
  正常跑不开监视即可，非固件问题。

### 是否解决
- 编译错误：已解决。
- BURST 改造：方案已定，代码尚未动手（见上方进度清单）。
- 待办首项：`app_config.h` 加 BURST 宏 + 新增 `configure_flrc_burst()`。
- ⚠️ 提醒：BURST 的 CRC 多项式是 `0x0000755B`，与现有 `0x04C11DB7` 不同，改时必须统一。

## 2026-07-23 网关 SoftAP 电脑端强制门户（captive portal）修复

### 背景 / 问题
- 网关开启 SoftAP 图库网页后，手机连上能自动弹出跳转页，但**电脑（Windows）端不行**：
  - 不弹登录页；
  - 甚至手动输入 `192.168.4.1` 也连不上；
  - 串口持续刷 `httpd_accept_conn: error in accept (128)`、`parse_block: incomplete (0/128) parser error = 16`。

### 根因
- `captive_portal.c` 里针对各操作系统探测 URL 的**显式应答 handler 之前被回退删掉了**，只剩 DNS 劫持 + 通用 404→302。
- Windows 的连通性探测（NCSI）必须对它的确切探测 URL（`connecttest.txt` / `redirect` / `ncsi.txt`）拿到**明确应答**，才会判定“这是强制门户网络”→ 弹出登录页并**停止狂开连接**。
- 拿不到明确应答时，Windows 疯狂开 TCP 连接探测，瞬间打满 httpd 的并发 socket 槽，导致 accept 报错、手动访问也抢不到 socket。手机探测很克制所以不受影响。

### 改动
1. `main/captive_portal.c`
   - 新增 `probe_redirect_handler`：对已知 OS 探测 URL 统一返回 `302 Found` → `http://192.168.4.1/gallery`。
   - 新增 `s_probe_urls[]`：覆盖 Windows（`connecttest.txt`/`redirect`/`ncsi.txt`）、Android（`generate_204`/`gen_204`）、iOS/macOS（`hotspot-detect.html`/`library/test/success.html`）、Firefox（`success.txt`）。
   - `captive_portal_start()` 中循环注册上述探测 URL 的 GET handler（保留原 DNS 劫持 + 404 兜底）。
2. `main/wifi_manager.c`
   - `start_httpd()` 的 `cfg.max_uri_handlers` 由 20 → 28（provisioning ~5 + wifi_manager 2 + image_store 6 + 探测 8 = 21，原值会溢出 HANDLERS_FULL）。

### 是否解决
- 待电脑端实测确认（预期：电脑自动弹 gallery 页，accept 错误 log 消失）。

### 备注（关联但独立的改动）
- 早前为缓解内部 RAM 压力：调小了若干 FreeRTOS 任务栈；`CONFIG_LWIP_MAX_SOCKETS` 10 → 15。
- `.gitignore` 改为**故意追踪 `sdkconfig`**（作为构建配置唯一真实来源，保证 clone 后配置可复现）。
