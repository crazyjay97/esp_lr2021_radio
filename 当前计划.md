# 当前计划

## 分支说明

| 分支 | 用途 | 状态 |
|------|------|------|
| `DEMO_V1` | 宣传视频演示用 | 当前，暂时冻结 |
| `ultra` | 主开发分支，新功能在此开发 | 下一步切回 |

---

## 下一个功能：WiFi 图片浏览器

### 概述

网关通过 WiFi 提供 HTTP 服务，用户手机/PC 扫码连接后可浏览、下载全分辨率照片。

### 功能需求

1. **WiFi 配网（SmartConfig / 二维码扫码）**
   - 首次使用：网关 LCD 显示二维码，用户扫码完成 WiFi 配网
   - 配网成功后将 SSID + 密码保存到 NVS
   - 下次开机自动连接已保存的 WiFi，无需再次扫码
   - 连接失败时回退到重新配网流程

2. **HTTP 图片服务**
   - 网关启动轻量 HTTP Server（`esp_http_server`）
   - 提供 Web 页面，展示图片列表
   - 每张图片显示：全像素预览 + 拍摄时间
   - 支持单张图片下载（原始 JPEG）

3. **图片环形缓存**
   - PSRAM 中保留最近 **20 张** JPEG
   - 第 21 张覆盖最旧的一张（环形队列）
   - 每张图片附带元数据：拍摄时间戳、文件大小、分辨率
   - Q=90 约 102KB/张，20 张 ≈ 2MB PSRAM 占用

### 技术方案

```
┌─────────┐    FLRC     ┌─────────────┐    WiFi     ┌──────────┐
│  节点   │ ──────────> │   网关      │ <────────> │ 手机/PC  │
│ (拍摄)  │   即拍即传   │ (PSRAM缓存) │   HTTP     │ (浏览器) │
└─────────┘             └─────────────┘             └──────────┘
```

- **配网方式**：ESP-IDF `wifi_provisioning` 组件 + SoftAP 模式
- **二维码内容**：SoftAP 的配网页面 URL 或 Wi-Fi Provisioning 协议链接
- **凭据存储**：NVS `wifi_ssid` / `wifi_pass`
- **HTTP 端点**：
  - `GET /` → 图片列表 HTML 页面（含缩略图、时间、下载链接）
  - `GET /photo/{id}` → 原始 JPEG 文件
  - `GET /api/list` → JSON 格式图片元数据列表（可选）
- **环形缓存结构**：

```c
#define PHOTO_RING_SIZE 20

typedef struct {
    uint8_t *jpeg;          // PSRAM 中的 JPEG 数据
    size_t   jpeg_len;      // JPEG 大小
    uint32_t timestamp;     // 拍摄时间 (epoch 或 uptime ms)
    uint16_t width;
    uint16_t height;
} photo_entry_t;

photo_entry_t photo_ring[PHOTO_RING_SIZE];
int photo_ring_head = 0;    // 下一个写入位置
int photo_ring_count = 0;   // 当前存储数量 (max 20)
```

### 资源预估

| 资源 | 用量 | 说明 |
|------|------|------|
| PSRAM | ~2.1 MB | 20 张 × ~105KB |
| WiFi 协议栈 | ~40 KB | 内部 RAM |
| HTTP Server | ~10 KB | 连接池 + 缓冲 |
| QR 码生成 | ~4 KB | qrcodegen 纯 C 库 |

### 实现步骤

1. WiFi 初始化 + NVS 凭据读取/自动连接
2. 配网流程（SoftAP + 配网页面 + 二维码显示）
3. 图片环形缓存管理
4. HTTP Server 路由注册
5. 前端 HTML 页面（内嵌，无需外部资源）
6. UI 页面集成（显示 WiFi 状态、IP 地址、二维码）

---

## 约束

- 无 SD 卡，不做本地持久化存储
- 即拍即传，图片仅在网关 PSRAM 中临时缓存
- 断电后缓存清空，保存图片由用户在浏览器端完成
