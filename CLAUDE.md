# CLAUDE.md — AM36 电子门铃工程协作约定

> 本文件每轮自动进入上下文，是强制约定。更完整的全局规范见桌面
> `C:\Users\005953\Desktop\AI_COLLAB_GENERAL_RULES_CN.md`，新任务开始前应读一遍。

## 语言（强制）

- 与用户的所有对话回复一律用**中文**：分析、总结、追问、说明都用中文，不用英文/日文。
- 不因为上一轮用了别的语言就延续；每轮都重新按中文回复。
- 唯一例外：本工程对外是**英文开源项目**，仓库内的代码、注释、commit message、文档保持**英文**（判断标准是"这段文字会不会进开源仓库"——进仓库的按英文，只在对话里给用户看的一律中文）。

## 工具（强制）

- 本机 **bash 环境不可用**：几乎每次执行都报 `cannot make pipe for command
  substitution: Device or resource busy`。**不要用 bash** 做读文件、写文件、搜索、
  列目录，一律用专用工具：Read / Write / Edit / Glob / Grep。
- 确需执行命令（编译、烧录等）时，先说明再执行；能用专用工具替代的绝不用 bash。
- 查 git 状态优先直接 Read `.git/HEAD`、`.git/refs/heads/<branch>` 等文件。

## 编码

- **本工程是 ESP-IDF 工程，源码/文档默认按 UTF-8 读写**（GB2312 只是桌面通用规范里
  针对 STM32/Keil 历史工程的默认，不适用于本工程）。
- 注意 UTF-8 BOM 和 CRLF/LF：ESP-IDF 的 Kconfig/sdkconfig 类文件不能写成 UTF-8 BOM。
- 读到乱码先怀疑读取方式/编码，不是加密。

## 改动边界与验证

- 只改当前任务必需的文件，不顺手整理无关内容，避开用户未提交的改动。
- ESP32/ESP-IDF 工程每轮改完代码或构建配置后，可自行 `idf.py build` 到零错误
  （构建为默认授权）；但**烧录、串口监视、git push 都需用户对当次任务明确指令**，
  历史授权不沿用。
- 区分三类结论：代码已改 / 本地构建已验证 / 硬件板测已验证。不把推测写成已验证；
  当前环境无法验证的要明说。
- `git add`/`commit` 仅在用户明确要求时执行；禁止破坏性 git 命令。

## 工程日志

- 维护根目录 `工程日志.md`（中文）：记录日期/阶段、新功能、改动内容、遇到的问题、
  是否解决。追加订正，不 silently 覆盖旧结论。

## 冲突处理

- 规范冲突、需求多解、方案有取舍、意图不确定时，停下来让用户拍板，不擅自决定。
