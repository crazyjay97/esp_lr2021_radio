# 图传流程优化记录

## 已完成的改动 (2026-06-29)

### 1. 包间延迟：编译常量 → 运行时可调

- `APP_IMAGE_TX_INTER_PACKET_MS` 改为 `APP_IMAGE_TX_INTER_PACKET_US`（微秒精度）
- 使用 `esp_rom_delay_us()` busy-wait 替代 `vTaskDelay`
- 新增 `image_tx_inter_packet_us_` 运行时变量 + `set_inter_packet_us()` setter
- 当前默认值：350µs

### 2. 远程配置 Pkt Delay

- 新增 `APP_CFG_KEY_INTER_PACKET (0x02)` 配置键
- 网关 Config 页新增第 6 行 "Pkt Delay"，显示当前 µs 值
- 按键交互：选中后 K4=+10µs, K5=-10µs, K3=+1µs, K6=发送并退出
- 节点收到后应用新值并在 LCD 显示 "XXXus"
- 走现有 `send_config` / `on_config_received` 通道

### 3. 页面预切换

- 按 K3 触发拍照时立刻 `show_page(UI_PAGE_RX)`，不等 ImageStart 到达
- `ui_gw_rx_begin()` 中 `show_page` 被跳过（已在 RX 页），radio task 不再被 UI 阻塞
- **效果**：丢包从 0~70 号全丢 → 降到 0~21 号丢

### 4. 图传统计显示

- Link Details 页面显示：有效速率、丢包率、重传率、上次耗时
- 去掉了 SNR 行（FLRC 无 SNR）
- 新增 `ui_gw_rx_eot_nack` 回调，radio 层每次 EOT 通知 UI 层统计数据
- 丢包率 = 首次 EOT missing / total
- 重传率 = 累计重传请求 / total

### 5. 调试日志

- 首次 EOT 时打印所有丢失包的序号（每行 16 个），用于定位丢包模式

---

## 明天要执行的改动

### P0：解决开头连续丢包（1~21号）

**根因**：网关 `handle_image_start()` 发完 ACK 后节点立刻开始 burst，但网关还在执行 `schedule_rx()` + `progress_cb()`，未就绪。

**方案**：节点端收到 ready ACK 后加 10-20ms 延迟再开始 burst。改动位置：`radio_ping.cpp` `image_tx_task()` 中 `r_ready = true` 后加 `vTaskDelay(pdMS_TO_TICKS(15))`。

### P1：解决中段连续丢包（45~51号）

**根因**：radio task（优先级 4, core 0）被 core 0 上其他更高优先级任务抢占约 2-3ms。

**方案选项**：
- A: 图传接收期间临时提升 radio task 优先级到 5+
- B: 将 radio task 绑定到 core 1（但 core 1 有 LVGL/image task）
- C: 在 image_rx_pending 期间屏蔽非关键中断处理

### P2：解决尾部零星丢包（68, 79, 94号）

**根因**：每收一个包都要 `schedule_rx()`（fifo_clear + set_rx = 3次 SPI），偶尔耗时超过 350µs。

**方案选项**：
- A: 用 `ral_set_rx_no_clear` 替代 `ral_set_rx`，省掉 fifo_clear 的 SPI 调用
- B: 确认 LR2021 continuous RX 模式下收完包是否自动 fallback 到 standby；如果设置 fallback 为 FS 模式可以省掉整个 schedule_rx 调用
- C: 增大包间延迟到 500-600µs（牺牲速率换可靠性）

### P3：节点端 LCD 延迟刷新（缩短整体耗时）

**根因**：节点拍完照后先执行 `bsp_lcd_reinit_after_camera()`（重初始化 SPI + LCD 初始化序列）和 `bsp_lcd_set_camera_status()`，再才开始传输。LCD reinit 是重量级操作，白白增加几十~上百 ms 延迟。

**当前流程**：
```
capture_frame() → bsp_lcd_reinit_after_camera() → bsp_audio_resume()
→ JPEG encode → Opus encode → bsp_lcd_set_camera_status() → send_image()
```

**优化方案**：把 LCD reinit 和 status 更新推迟到传输完成之后：
```
capture_frame() → JPEG encode → Opus encode → send_image()
→ [传输完成] → bsp_lcd_reinit_after_camera() → bsp_lcd_set_camera_status()
```

图传期间节点 LCD 保持未初始化状态（反正用户看的是网关屏幕）。

### P4：分段 burst + mini-ACK

如果 P0~P2 仍不能将丢包率降到 <5%，考虑把初始 burst 分段：每发 30 个包暂停等网关确认，类似重传逻辑（已验证 0% 丢包）。

---

## 当前丢包分布特征

| 区间 | 模式 | 原因 |
|------|------|------|
| 0~21 | 连续全丢 | handle_image_start 处理时间 |
| 22~44 | 正常接收 | — |
| 45~51 | 连续丢 7 个 | 被其他任务抢占 ~2.4ms |
| 52~180 | 偶尔丢 1 个 | schedule_rx SPI 耗时偶尔 >350µs |
