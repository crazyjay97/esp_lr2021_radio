# 图传+语音融合实现计划

> 中文工作文档，仅供开发记录。进仓库的代码/注释/commit 保持英文。
> 目标：通话中「单向图传 node→GW + 双向语音」融合，不再互斥。

## 背景决策（2026-08-27）

- 分期：先做扎实纯语音全双工（已完成，板测良好），再叠加图传。
- I2S 通道方案：**方案 A**（保持浅环 6desc 全双工，不拆时钟）。
  - 原因：分两次 simplex 建 TX/RX 在 S3 上两个 master 会静默覆盖时钟引脚+MCLK 源，
    对 ES8311（ADC/DAC 共用一组时钟）位滑移/数据损坏且不报错；干净时钟耦合
    (`sig_loopback`) 只在全双工开。方案 B（TX master+RX slave）非驱动预期用法，风险高。
  - 方案 A 的风险（浅环 RX 在相机+JPEG 争用下偶发丢样）是弹性可容忍的
    （语音小 glitch + 图传本就重传），而 TX 深环 360ms 延迟毁通话是刚性的。
  - 故：先用方案 A 跑通，板测暴露真丢样再考虑 B。原「拆 I2S 通道」步骤取消。

## 关键时序预算（已从代码核实）

- 20ms 超帧：GW master TX → node RX → 2ms guard → node voice reply(<1ms 空口) → 拼图传片。
- node 收 master 后剩约 16-17ms 到下个 master；每片约 2.9ms（含 FIFO load+turnaround）。
- **每个上行 slot 只能拼约 4-5 片 → grant 必须个位数**，这是「语音时序不破坏」的硬约束。
- 空口理论融合 FPS 约 13-15，卡接收端 LCD ~13FPS 天花板。

## Fused voice 包格式

现有 voice 包 parser 按 `frame_count`(byte12) 逐帧读 Opus，读完即停，尾部字节被忽略 →
干净扩展点。byte5 版本字节从 `2` 升 `4` 标记 fused，Opus 帧之后追加定长 trailer，
Opus 偏移不动。旧 subtype=2 包与非通话图传路径完全不碰，零回归。

- Master 下行 trailer（GW→node）：`[grant:1][ack_session:2][ack_frame_done:1][need_frag_base:2]`
- Node reply 上行 trailer（node→GW）：`[has_image:1][session:2][total_frags:2][next_idx:2]`

## 四阶段（每阶段可编译/烧录/看串口验证）

### 阶段 1 — 时序探针（最小风险，先回答方案A是否可行）
- node `send_intercom_slot` 的 voice reply 后、`schedule_rx` 前，用编译期常量
  `APP_INTERCOM_IMAGE_GRANT_MAX` 拼发 N 个填充测试片（GW 识别并丢弃）。
- 改 `dispatch_rx_packet`(radio_ping.cpp:1443-1446) 只放行该测试类型。
- 验证：串口看 `intercom_missed_slots` 是否随 N 上涨、语音是否 glitch、浅环 RX 是否受影响。
  实测方案 A 可行性 + N 安全上限。不碰真图传，极易回退。
- 状态：进行中。

### 阶段 2 — Node 上行拼接真图传
- fused 包格式落地（build/parse voice packet 加 trailer）。
- 解 node 侧互斥：`trigger_image_capture`(1764)、`send_image`(1980)、
  `set_intercom→abort_image_rx`(387)。
- node 按 grant 复用 `burst_send_fragments` 发 N 片。
- 验证：node 串口确认拼片发出、语音时序稳。

### 阶段 3 — GW 接收重组 + grant/ack 生成
- 改 `dispatch_rx_packet`(1443) 放行 `kPacketTypeImageData` → 喂 `image_xfer_.rx_fragment`。
- GW 构建 master 包时由 `rx_get_missing`/`rx_complete` 算 grant/ack trailer。
- 解 app_main 互斥：`on_gw_capture`(1214)、`on_image_capture_request`(785)、
  `gateway_image_task`(919)、loop skip(595)。
- 完成一帧 `image_rx_complete_cb_` → 送 LCD/解码。
- 验证：GW 通话中收全一帧图并上屏，语音不断。

### 阶段 4 — 缺片重传收尾 + 连续帧流
- 帧末经 master ack/nack 让 node 补发缺片（CRC32 校验），完成进下一帧，
  取消原独立 EOT/NACK 阻塞握手。
- 单帧跑通后扩成连续帧流（边说边看）。
- 验证：实测融合 FPS、丢包补发、长通话稳定性。

## 风险与回退

- 每阶段独立可烧录；阶段1 纯探针，时序不达标可当场判定方案A不可行、退回方案B。
- 全程不动 subtype=2 旧包与非通话图传，现有功能零回归。
- fused trailer 定长，parser 按 version 严格分支，无变长解析风险。
